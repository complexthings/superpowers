#!/bin/sh
// 2>/dev/null; exec "$(command -v bun 2>/dev/null || echo node)" "$0" "$@"
import{relative as EX,dirname as GX,join as OY}from"path";import{existsSync as RQ}from"fs";import{homedir as rQ,platform as ZY}from"os";import{join as O,parse as $Y,dirname as sQ}from"path";import{existsSync as u}from"fs";import{fileURLToPath as qY}from"url";var WY=qY(import.meta.url),BQ=sQ(WY),zY=()=>{let Q=rQ();switch(ZY()){case"darwin":return O(Q,"Library","Application Support","Code","User");case"win32":return O(Q,"AppData","Roaming","Code","User");case"linux":return O(Q,".config","Code","User");default:return O(Q,".config","Code","User")}},HY=()=>{let Q=process.cwd(),X=$Y(Q).root;while(Q!==X){if(u(O(Q,".agents")))return Q;Q=sQ(Q)}return process.cwd()},H={home:rQ(),vscodeUserDir:zY(),projectRoot:HY(),get projectClaudeSkills(){return O(this.projectRoot,".claude","skills")},get projectAgentsSkills(){return O(this.projectRoot,".agents","skills")},get projectCopilotSkills(){return O(this.projectRoot,".copilot","skills")},get projectOpencodeSkills(){return O(this.projectRoot,".opencode","skill")},get projectSkillsDir(){return O(this.projectRoot,"skills")},get homePersonalSkills(){return O(this.home,".agents","skills")},get homeClaudeSkills(){return O(this.home,".claude","skills")},get homeCopilotSkills(){return O(this.home,".copilot","skills")},get claudeSettings(){return O(this.home,".claude","settings.json")},get copilotHooksDir(){return process.env.COPILOT_HOME?O(process.env.COPILOT_HOME,"hooks"):O(this.home,".copilot","hooks")},get homeOpencodeSkills(){return O(this.home,".config","opencode","skill")},get bootstrap(){return O(this.projectRoot,".agents","superpowers-bootstrap.md")},get superpowersRepo(){let Q=O(BQ,"..","..","..");if(u(O(Q,"skills"))&&u(O(Q,".agents","superpowers-agent")))return Q;let X=O(BQ,"..");if(u(O(X,"skills"))&&u(O(X,".agents","superpowers-agent")))return X;return O(this.home,".agents","superpowers")},get homeSuperpowersSkills(){return O(this.superpowersRepo,"skills")},get isSuperpowersRepo(){let Q=O(BQ,"..","..","..");if(u(O(Q,"skills"))&&u(O(Q,".agents","superpowers-agent")))return!0;let X=O(BQ,"..");return u(O(X,"skills"))&&u(O(X,".agents","superpowers-agent"))}};import{readFileSync as tQ}from"fs";var ZQ=(Q)=>{try{let Y=tQ(Q,"utf8").split(`
`),$={name:"",description:"",whenToUse:""},Z=!1;for(let q of Y){if(q.trim()==="---"){if(Z)break;Z=!0;continue}if(Z){let z=q.match(/^(\w+):\s*(.*)$/);if(z){let[,W,K]=z,B={name:"name",description:"description",when_to_use:"whenToUse"};if(B[W])$[B[W]]=K.trim()}}}return $}catch{return{name:"",description:"",whenToUse:""}}},OQ=(Q)=>{try{let X=tQ(Q,"utf8"),Y=ZQ(Q),$=X.split(`
`),Z=!1,q=!1,z=[];for(let W of $){if(W.trim()==="---"){if(Z){q=!0;continue}Z=!0;continue}if(q||!Z)z.push(W)}return{content:z.join(`
`).trim(),frontmatter:Y}}catch(X){throw Error(`Error reading skill file: ${X.message}`)}};import{existsSync as TQ,readdirSync as XX,statSync as YX}from"fs";import{join as AQ,relative as nZ}from"path";import{readFileSync as BY,existsSync as KY}from"fs";import{join as eQ}from"path";var $Q=(Q)=>{let X=eQ(Q,"skill.json");if(!KY(X))return null;try{let Y=BY(X,"utf8");return JSON.parse(Y)}catch(Y){return null}};var MQ=(Q)=>{if(Q.startsWith("superpowers:"))return{type:"superpowers",path:Q.substring(12).replace(/^skills\//,"")};if(Q.startsWith("claude:"))return{type:"claude",path:Q.substring(7).replace(/^skills\//,"")};if(Q.startsWith("copilot:"))return{type:"copilot",path:Q.substring(8).replace(/^skills\//,"")};if(Q.startsWith("opencode:"))return{type:"opencode",path:Q.substring(9).replace(/^skills\//,"")};return{type:null,path:Q.replace(/^skills\//,"")}},QX=(Q,X)=>{let Y=$Q(Q);if(!Y||!Y.helpers||!Array.isArray(Y.helpers))return null;let $=Y.helpers,Z=X.toLowerCase(),q=null,z=0;for(let W of $){let K=W.toLowerCase();if(K===Z){q=W;break}if(K.includes(Z)){let V=Z.length/K.length;if(V>z)z=V,q=W}let B=W.split("/").pop().split(".")[0].toLowerCase();if(Z.includes(B)){let V=B.length/Z.length;if(V>z)z=V,q=W}}return q?eQ(Q,q):null},r={project:{dir:"projectAgentsSkills",prefix:""},claude:{dir:"projectClaudeSkills",prefix:"claude:"},copilot:{dir:"projectCopilotSkills",prefix:"copilot:"},opencode:{dir:"projectOpencodeSkills",prefix:"opencode:"},personal:{dir:"homePersonalSkills",prefix:""},personalClaude:{dir:"homeClaudeSkills",prefix:"claude:"},personalCopilot:{dir:"homeCopilotSkills",prefix:"copilot:"},personalOpencode:{dir:"homeOpencodeSkills",prefix:"opencode:"},superpowers:{dir:"homeSuperpowersSkills",prefix:"superpowers:"}};var KQ=(Q,X,Y=null)=>{let $=[];if(!TQ(Q))return $;let Z=(q,z)=>{if(Y!==null&&z>Y)return;try{let W=XX(q,{withFileTypes:!0});for(let K of W){if(K.name==="node_modules"||K.name===".git"||K.name.startsWith("."))continue;let B=AQ(q,K.name),V=K.isDirectory();if(K.isSymbolicLink())try{V=YX(B).isDirectory()}catch{continue}if(!V)continue;let E=AQ(B,"SKILL.md");if(TQ(E))$.push(B);if(Y===null||z<Y)Z(B,z+1)}}catch{}};return Z(Q,0),$};var ZX=(Q,X)=>{if(!TQ(Q))return[];let Y=[],$=X.toLowerCase().replace(/\\/g,"/"),Z=(q,z="")=>{try{let W=XX(q,{withFileTypes:!0});for(let K of W){let B=AQ(q,K.name),V=z?`${z}/${K.name}`:K.name,E=K.isDirectory();if(K.isSymbolicLink())try{E=YX(B).isDirectory()}catch{continue}if(E&&(K.name==="node_modules"||K.name===".git"||K.name.startsWith(".")))continue;if(E)Z(B,V);else if(K.name==="SKILL.md"){let G=z.toLowerCase().replace(/\\/g,"/");if(G===$||G.endsWith("/"+$)||G.endsWith($))Y.push({file:B,path:z})}}}catch(W){}};return Z(Q),Y},$X=(Q,X,Y)=>{let $={project:"project skills (.agents/skills/)",claude:"claude skills (.claude/skills/)",copilot:"copilot skills (.copilot/skills/)",opencode:"opencode skills (.opencode/skill/)",personal:"personal skills (~/.agents/skills/)",personalClaude:"personal claude skills (~/.claude/skills/)",personalCopilot:"personal copilot skills (~/.copilot/skills/)",personalOpencode:"personal opencode skills (~/.config/opencode/skill/)",superpowers:"superpowers skills (~/.agents/superpowers/skills/)"}[Y];console.log(`Error: Multiple skills match "${Q}" in ${$}:
`);for(let Z of X){let z=r[Y].prefix+Z.path;try{let W=ZQ(Z.file),K=W.description||W.whenToUse||"(no description)";console.log(`  ${z}`),console.log(`    ${K}
`)}catch(W){console.log(`  ${z}`),console.log(`    (unable to read description)
`)}}console.log("Please be more specific. Examples:");for(let Z of X.slice(0,2)){let q=r[Y].prefix;console.log(`  superpowers-agent execute ${q}${Z.path}`)}};import{existsSync as vQ}from"fs";import{join as VY,relative as EY}from"path";var GY=(Q)=>{let{type:X,path:Y}=MQ(Q),$={superpowers:[H.homeSuperpowersSkills],claude:[H.projectClaudeSkills,H.homeClaudeSkills],copilot:[H.projectCopilotSkills,H.homeCopilotSkills],opencode:[H.projectOpencodeSkills,H.homeOpencodeSkills],project:[H.projectAgentsSkills],personal:[H.homePersonalSkills]},Z;if(X)Z=($[X]||[]).map((z)=>({type:X,dir:z}));else if(H.isSuperpowersRepo&&vQ(H.projectSkillsDir))Z=[{type:"project",dir:H.projectSkillsDir},{type:"project",dir:H.projectAgentsSkills},{type:"claude",dir:H.projectClaudeSkills},{type:"copilot",dir:H.projectCopilotSkills},{type:"opencode",dir:H.projectOpencodeSkills},{type:"personal",dir:H.homePersonalSkills},{type:"personalClaude",dir:H.homeClaudeSkills},{type:"personalCopilot",dir:H.homeCopilotSkills},{type:"personalOpencode",dir:H.homeOpencodeSkills},{type:"superpowers",dir:H.homeSuperpowersSkills}];else Z=[{type:"project",dir:H.projectAgentsSkills},{type:"claude",dir:H.projectClaudeSkills},{type:"copilot",dir:H.projectCopilotSkills},{type:"opencode",dir:H.projectOpencodeSkills},{type:"personal",dir:H.homePersonalSkills},{type:"personalClaude",dir:H.homeClaudeSkills},{type:"personalCopilot",dir:H.homeCopilotSkills},{type:"personalOpencode",dir:H.homeOpencodeSkills},{type:"superpowers",dir:H.homeSuperpowersSkills}];for(let{type:q,dir:z}of Z){let W=ZX(z,Y);if(W.length===1)return{skillFile:W[0].file,sourceType:q,actualSkillPath:W[0].path};if(W.length>1)$X(Y,W,q)}return null},m=(Q)=>{let X=GY(Q);if(X)return X;let{type:Y,path:$}=MQ(Q),Z={superpowers:[H.homeSuperpowersSkills],claude:[H.projectClaudeSkills,H.homeClaudeSkills],copilot:[H.projectCopilotSkills,H.homeCopilotSkills],opencode:[H.projectOpencodeSkills,H.homeOpencodeSkills],project:[H.projectAgentsSkills],personal:[H.homePersonalSkills]},q;if(Y)q=(Z[Y]||[]).map((W)=>({type:Y,dir:W}));else if(H.isSuperpowersRepo&&vQ(H.projectSkillsDir))q=[{type:"project",dir:H.projectSkillsDir},{type:"project",dir:H.projectAgentsSkills},{type:"claude",dir:H.projectClaudeSkills},{type:"copilot",dir:H.projectCopilotSkills},{type:"opencode",dir:H.projectOpencodeSkills},{type:"personal",dir:H.homePersonalSkills},{type:"personalClaude",dir:H.homeClaudeSkills},{type:"personalCopilot",dir:H.homeCopilotSkills},{type:"personalOpencode",dir:H.homeOpencodeSkills},{type:"superpowers",dir:H.homeSuperpowersSkills}];else q=[{type:"project",dir:H.projectAgentsSkills},{type:"claude",dir:H.projectClaudeSkills},{type:"copilot",dir:H.projectCopilotSkills},{type:"opencode",dir:H.projectOpencodeSkills},{type:"personal",dir:H.homePersonalSkills},{type:"personalClaude",dir:H.homeClaudeSkills},{type:"personalCopilot",dir:H.homeCopilotSkills},{type:"personalOpencode",dir:H.homeOpencodeSkills},{type:"superpowers",dir:H.homeSuperpowersSkills}];for(let{type:z,dir:W}of q){if(!vQ(W))continue;let K=KQ(W,z,null);for(let B of K){let V=$Q(B);if(V&&V.aliases&&Array.isArray(V.aliases)){let E=$.toLowerCase();for(let G of V.aliases)if(G.toLowerCase()===E){let U=VY(B,"SKILL.md"),L=EY(W,B);return{skillFile:U,sourceType:z,actualSkillPath:L}}}}}return null};import{readFileSync as qX,writeFileSync as WX,existsSync as bQ}from"fs";import{join as i}from"path";import{execSync as _Y}from"child_process";var CQ=()=>({auto_update:!0,last_update_check:null,last_updated_commit:null}),qQ=()=>{let Q=i(H.superpowersRepo,".config.json");try{if(!bQ(Q))return CQ();let X=qX(Q,"utf8");return{...CQ(),...JSON.parse(X)}}catch{return CQ()}},zX=(Q)=>{let X=i(H.superpowersRepo,".config.json"),$={...qQ(),...Q};try{WX(X,JSON.stringify($,null,2))}catch(Z){console.log(`Warning: couldn't save config: ${Z.message}`)}},y=(Q)=>{let X=Q?i(H.home,".agents","config.json"):i(process.cwd(),".agents","config.json");if(!bQ(X))return{};try{let Y=qX(X,"utf8");return JSON.parse(Y)}catch(Y){return{}}},d=(Q,X)=>{let Y=X?i(H.home,".agents"):i(process.cwd(),".agents"),$=i(Y,"config.json");try{if(!bQ(Y))_Y(`mkdir -p "${Y}"`,{stdio:"pipe"});return WX($,JSON.stringify(Q,null,2),"utf8"),!0}catch(Z){throw Error(`Failed to write config: ${Z.message}`)}},g=(Q=!1)=>{return y(Q).repositories||{}},HX=(Q,X,Y)=>{let $=y(Y);if(!$.repositories)$.repositories={};$.repositories[Q]=X,d($,Y)};function BX(Q=!0){return y(Q).installedSkills||{}}function NQ(Q,X=!0){let Y=y(X);if(Y.installedSkills)delete Y.installedSkills[Q];d(Y,X)}function KX(){return y(!0).installedAgents||{}}import{readFileSync as UY}from"fs";import{join as FQ,dirname as IY}from"path";import{fileURLToPath as LY}from"url";import{existsSync as VX}from"fs";import{execSync as JY}from"child_process";var wY=LY(import.meta.url),jQ=IY(wY),S=()=>{try{let Q=FQ(jQ,"package.json");if(!VX(Q))Q=FQ(jQ,"..","package.json");if(!VX(Q))Q=FQ(jQ,"..","..","package.json");return JSON.parse(UY(Q,"utf8")).version||"0.0.0"}catch{return"0.0.0"}},k=()=>{let Q=S();console.log(`^^SAV:${Q}^^`)},VQ=async()=>{try{let X=JY('curl -sS "https://registry.npmjs.org/@complexthings/superpowers-agent/latest"',{encoding:"utf8",timeout:1e4}),Y=JSON.parse(X);if(!Y.version)return S();return Y.version}catch(Q){throw Error(`Failed to fetch remote version: ${Q.message}`)}},EQ=(Q,X)=>{let Y=Q.split(".").map(Number),$=X.split(".").map(Number);for(let Z=0;Z<3;Z++){let q=Y[Z]||0,z=$[Z]||0;if(q>z)return!0;if(q<z)return!1}return!1};var MY={project:{dir:"projectAgentsSkills",prefix:""},claude:{dir:"projectClaudeSkills",prefix:"claude:"},copilot:{dir:"projectCopilotSkills",prefix:"copilot:"},opencode:{dir:"projectOpencodeSkills",prefix:"opencode:"},personal:{dir:"homePersonalSkills",prefix:""},personalClaude:{dir:"homeClaudeSkills",prefix:"claude:"},personalCopilot:{dir:"homeCopilotSkills",prefix:"copilot:"},personalOpencode:{dir:"homeOpencodeSkills",prefix:"opencode:"},superpowers:{dir:"homeSuperpowersSkills",prefix:"superpowers:"}},TY=(Q,X)=>{let Y=OY(Q,"SKILL.md"),{dir:$,prefix:Z}=MY[X],q=EX(H[$],Q).replace(/\\/g,"/");console.log(`${Z}${q}`);let{description:z,whenToUse:W}=ZQ(Y);if(z)console.log(`  ${z}`);if(W)console.log(`  When to use: ${W}`);console.log("")},s=()=>{k();let Q=new Set,X=[{type:"project",dir:H.projectAgentsSkills,maxDepth:null},{type:"claude",dir:H.projectClaudeSkills,maxDepth:null},{type:"copilot",dir:H.projectCopilotSkills,maxDepth:null},{type:"opencode",dir:H.projectOpencodeSkills,maxDepth:null},{type:"personal",dir:H.homePersonalSkills,maxDepth:null},{type:"personalClaude",dir:H.homeClaudeSkills,maxDepth:null},{type:"personalCopilot",dir:H.homeCopilotSkills,maxDepth:null},{type:"personalOpencode",dir:H.homeOpencodeSkills,maxDepth:null},{type:"superpowers",dir:H.homeSuperpowersSkills,maxDepth:null}];for(let{type:Y,dir:$,maxDepth:Z}of X){let q=KQ($,Y,Z);for(let z of q){let W=EX($,z);if(!Q.has(W))Q.add(W),TY(z,Y)}}console.log(`Usage:
  superpowers-agent execute <skill-name> # Load a specific skill`)},_X=()=>{k();let Q=process.argv[3];if(!Q){console.log(`Usage: superpowers-agent execute <skill-name-or-alias>

Examples:
  superpowers-agent execute brainstorming
  superpowers-agent execute superpowers:collaboration/brainstorming
  superpowers-agent execute aem/block-collection-and-party
  superpowers-agent execute block-collection  # Using alias

Description:
  Instructs the agent to execute the specified skill.
  Supports skill aliases defined in skill.json files.`);return}let X=m(Q);if(!X){console.log(`Error: Skill not found: ${Q}

Available skills:`),s();return}let{skillFile:Y,sourceType:$}=X,Z=process.cwd(),q=Y.startsWith(Z),z=RQ(H.projectAgentsSkills)||RQ(H.projectClaudeSkills);if(q)console.log(`Open this file with your Read Tool: ${Y}

<IMPORTANT>
USE YOUR Read Tool to process this file. DO NOT use \`cat\` or print commands.
</IMPORTANT>`);else if(z&&$!=="project")console.log(`Open this file with your Read Tool: ${Y}

<IMPORTANT>
1. Try using your Read Tool first
2. If your Read tool fails (file outside workspace), use: cat "${Y}"
3. Consider using workspace-local skills for better tool compatibility
   Run: superpowers-agent setup-skills
</IMPORTANT>`);else console.log(`Open this file with your Read Tool: ${Y}

<IMPORTANT>
1. Try using your Read Tool first
2. If your Read tool fails (file outside workspace), use: cat "${Y}"
3. NEVER skip loading the skill content
</IMPORTANT>`)},UX=()=>{let Q=process.argv[3];if(!Q){console.log(`Usage: superpowers-agent path <skill-name-or-alias>

Examples:
  superpowers-agent path brainstorming
  superpowers-agent path superpowers:collaboration/brainstorming
  superpowers-agent path aem/block-collection-and-party
  superpowers-agent path block-collection  # Using alias

Description:
  Outputs the file system path of the specified SKILL.md file.
  Supports skill aliases defined in skill.json files.`);return}let X=m(Q);if(!X){console.log(`Error: Skill not found: ${Q}

Available skills:`),s();return}let{skillFile:Y}=X;console.log(Y)},IX=()=>{let Q=process.argv[3];if(!Q){console.log(`Usage: superpowers-agent dir <skill-name>

Examples:
  superpowers-agent dir brainstorming
  superpowers-agent dir aem/block-collection-and-party
  superpowers-agent dir block-collection  # Using alias
  superpowers-agent dir superpowers:testing/test-driven-development

Description:
  Returns the directory path where the specified skill is located.
  Supports skill aliases defined in skill.json files.`);return}let X=m(Q);if(!X){console.log(`Error: Skill not found: ${Q}

Available skills:`),s();return}let{skillFile:Y}=X,$=GX(Y);console.log($)},LX=()=>{k();let Q=process.argv[3],X=process.argv[4];if(!Q||!X){console.log(`Usage: superpowers-agent get-helpers <skill-name> <helper-search-term>

Examples:
  superpowers-agent get-helpers block-collection search-block
  superpowers-agent get-helpers aem/block-collection-and-party get-block-structure
  superpowers-agent get-helpers superpowers:testing/test-driven-development example

Description:
  Searches for helper files within a skill's skill.json configuration.
  Returns the full path to the best matching helper file.
  
  The command uses the skill's aliases if available, so you can use
  short names like "block-collection" instead of the full path.`);return}let Y=m(Q);if(!Y){console.log(`Error: Skill not found: ${Q}

Available skills:`),s();return}let{skillFile:$}=Y,Z=GX($),q=$Q(Z);if(!q){console.log(`Error: No skill.json found for skill: ${Q}
  Location: ${Z}`);return}if(!q.helpers||!Array.isArray(q.helpers)||q.helpers.length===0){console.log(`Error: No helpers defined in skill.json for skill: ${Q}
  Location: ${Z}`);return}let z=QX(Z,X);if(!z){console.log(`Error: No helper found matching "${X}" in skill: ${Q}`),console.log(`
Available helpers:`);for(let W of q.helpers)console.log(`  - ${W}`);return}if(!RQ(z)){console.log(`Error: Helper file not found: ${z}
  Defined in skill.json but missing from filesystem`);return}console.log(z)},JX=()=>{let Q=qQ();console.log("Current configuration:"),console.log(JSON.stringify(Q,null,2))},wX=()=>{let Q=process.argv[3],X=process.argv[4];if(!Q||X===void 0){console.log(`Usage: .agents/superpowers-agent config-set <key> <value>

Available keys:
  auto_update (true/false) - Enable/disable automatic updates during bootstrap

Examples:
  .agents/superpowers-agent config-set auto_update false
  .agents/superpowers-agent config-set auto_update true`);return}let Y=X;if(X==="true")Y=!0;else if(X==="false")Y=!1;let $={};$[Q]=Y,zX($),console.log(`✓ Set ${Q} = ${Y}`)},OX=()=>{k();let Q=g(!0),X=g(!1),Y=[];for(let[q,z]of Object.entries(Q))Y.push({alias:q,url:z,source:"global"});for(let[q,z]of Object.entries(X)){let W=Y.findIndex((K)=>K.alias===q);if(W>=0)Y[W]={alias:q,url:z,source:"project"};else Y.push({alias:q,url:z,source:"project"})}if(Y.length===0){console.log(`No repository aliases configured.

Add a repository using:
  superpowers-agent add-repository <git-url> [--as=@alias]`);return}let $=Math.max(5,...Y.map((q)=>q.alias.length)),Z=Math.max(3,...Y.map((q)=>q.url.length));console.log(`Repositories:
`),console.log(`${"Alias".padEnd($)}  ${"URL".padEnd(Z)}  Source`);for(let q of Y)console.log(`${q.alias.padEnd($)}  ${q.url.padEnd(Z)}  (${q.source})`);console.log(`
Total: ${Y.length} repository alias(es)`)};var GQ=async()=>{try{let Q=S(),X=await VQ();return{hasUpdates:EQ(X,Q),localVersion:Q,remoteVersion:X}}catch{return{hasUpdates:!1,localVersion:S(),remoteVersion:"",error:!0}}};import{execSync as MX}from"child_process";var xQ=async({skipReinstall:Q=!1}={})=>{console.log(`# Checking for Superpowers updates...
`);let X=await GQ();if(X.error){console.log("⚠️  Could not check for updates (network issue)");return}if(!X.hasUpdates){console.log(`✓ Already up to date (v${X.localVersion})`);return}if(console.log(`\uD83D\uDCE6 Update available: v${X.localVersion} → v${X.remoteVersion}
`),Q){console.log("   Skipping install (--no-reinstall flag set)"),console.log(`   Run manually: npm install -g @complexthings/superpowers-agent
`);return}console.log(`   Installing update...
`);try{MX("npm install -g @complexthings/superpowers-agent",{stdio:"inherit"}),console.log(`
✓ Update complete!`)}catch(Y){console.log(`
✗ Update failed: ${Y.message}`),console.log("   Try running manually: npm install -g @complexthings/superpowers-agent"),process.exit(1)}console.log(`
   Running bootstrap to complete setup...
`);try{MX("superpowers-agent bootstrap --no-update",{stdio:"inherit"}),console.log(`
✓ Bootstrap complete!`)}catch(Y){console.log(`
⚠️  Bootstrap step failed: ${Y.message}`),console.log("   The update installed successfully. Run manually to finish setup:"),console.log("   superpowers-agent bootstrap")}},TX=async()=>{k();let Q=S();console.log(`Current version: ${Q}`);try{let X=await VQ();if(console.log(`Latest version: ${X}`),EQ(X,Q))console.log("Update available: Yes"),process.exit(1);else console.log("You are up to date"),process.exit(0)}catch(X){console.log(`Error checking for updates: ${X.message}`),process.exit(1)}},AX=()=>{let Q=S();console.log(Q)};import{existsSync as T,readFileSync as a,writeFileSync as QQ,rmSync as uX,lstatSync as WZ}from"fs";import{join as v,dirname as mX,parse as e}from"path";import{execSync as D}from"child_process";import{platform as zZ}from"os";import{execSync as AY}from"child_process";var WQ=(Q)=>{try{return AY(`which ${Q}`,{stdio:"pipe",timeout:2000}),!0}catch{return!1}};var x={opencode:{check:()=>WQ("opencode"),cli:!0,name:"OpenCode",installUrl:"https://opencode.ai/docs/installation",bootstrapCommand:"install-opencode-commands"},claude:{check:()=>WQ("claude"),cli:!0,name:"Claude Code",installUrl:"https://code.claude.com/docs/en/installation",bootstrapCommand:"install-claude-commands"},copilot:{check:()=>!0,cli:!1,name:"GitHub Copilot",bootstrapCommand:"install-copilot-prompts"}},vX=()=>{let Q=[];if(x.copilot.check())Q.push("github-copilot");if(x.claude.check())Q.push("claude-code");if(x.opencode.check())Q.push("opencode");return Q};import{existsSync as DQ,readdirSync as y0,lstatSync as vY,symlinkSync as CX,unlinkSync as CY,mkdirSync as bY,readlinkSync as NY}from"fs";import{join as fQ,dirname as P0}from"path";import{platform as FY}from"os";var yQ=(Q)=>{try{return vY(Q).isSymbolicLink()}catch{return!1}},jY=(Q,X)=>{try{if(!yQ(Q))return!1;return NY(Q)===X}catch{return!1}},bX=()=>{let Q=fQ(H.superpowersRepo,".opencode","plugins","superpowers-agent.js"),X=fQ(H.home,".config","opencode","plugins"),Y=fQ(X,"superpowers-agent.js");if(console.log("Installing OpenCode plugin symlink..."),!DQ(Q))return console.log("⚠️  Source plugin not found, skipping symlink creation"),console.log(`   Expected at: ${Q}`),{created:!1,error:"Source plugin not found"};if(jY(Y,Q))return console.log("✓ Plugin symlink already exists and is correct"),{created:!1,existed:!0};if(DQ(Y)&&!yQ(Y))return console.log("⚠️  Warning: A file already exists at the destination path"),console.log(`   Path: ${Y}`),console.log("   Skipping symlink creation to avoid overwriting existing file"),console.log("   To use superpowers-agent plugin, manually remove or rename the existing file"),{created:!1,error:"File already exists at destination"};if(!DQ(X))try{bY(X,{recursive:!0}),console.log(`✓ Created ${X.replace(H.home,"~")}`)}catch(Z){return console.log(`⚠️  Failed to create plugins directory: ${Z.message}`),{created:!1,error:`Failed to create directory: ${Z.message}`}}if(yQ(Y))try{CY(Y)}catch(Z){return console.log(`⚠️  Failed to remove existing symlink: ${Z.message}`),{created:!1,error:`Failed to remove existing symlink: ${Z.message}`}}let $=FY();try{if($==="win32")CX(Q,Y,"file");else CX(Q,Y);let Z=Q.replace(H.home,"~"),q=Y.replace(H.home,"~");return console.log(`✓ Created symlink: ${q}`),console.log(`  -> ${Z}`),{created:!0}}catch(Z){if($==="win32"&&Z.code==="EPERM")return console.log("⚠️  Windows requires Developer Mode or admin privileges for symlinks"),console.log("   Enable Developer Mode: Settings > Update & Security > For developers"),{created:!1,error:"Windows symlink permission denied"};return console.log(`⚠️  Failed to create symlink: ${Z.message}`),{created:!1,error:`Failed to create symlink: ${Z.message}`}}};import{existsSync as NX,readdirSync as d0,readFileSync as RY,writeFileSync as xY,mkdirSync as DY,copyFileSync as fY}from"fs";import{join as p0,dirname as yY}from"path";var SY="superpowers-agent session-context",PY="superpowers-agent session-context --format=claude 2>/dev/null || true",gY="startup|resume|clear|compact",FX=(Q=H.claudeSettings)=>{let X={},Y=!1,$;if(NX(Q)){Y=!0;let V;try{V=RY(Q,"utf8")}catch(G){return{error:!0,message:`Failed to read settings.json: ${G.message}`}}try{X=V.trim()?JSON.parse(V):{}}catch(G){return{error:!0,message:`settings.json is not valid JSON, leaving it untouched: ${G.message}`}}let E=new Date().toISOString().split("T")[0];$=`${Q}.backup-${E}`;try{fY(Q,$)}catch(G){return{error:!0,message:`Failed to back up settings.json: ${G.message}`}}}if(typeof X!=="object"||X===null||Array.isArray(X))return{error:!0,message:"settings.json root is not an object"};X.hooks=X.hooks||{};let Z=Array.isArray(X.hooks.SessionStart)?X.hooks.SessionStart:[],q=(V)=>{try{return JSON.stringify(V).includes(SY)}catch{return!1}},z=Z.some(q),W=Z.filter((V)=>!q(V)),K={matcher:gY,hooks:[{type:"command",command:PY}]};X.hooks.SessionStart=[...W,K];let B=yY(Q);if(!NX(B))try{DY(B,{recursive:!0})}catch(V){return{error:!0,message:`Failed to create ${B}: ${V.message}`}}try{xY(Q,JSON.stringify(X,null,2)+`
`,"utf8")}catch(V){return{error:!0,message:`Failed to write settings.json: ${V.message}`}}if(!Y)return{created:!0};if(z)return{existed:!0,updated:!0,backup:$};return{updated:!0,backup:$}};import{existsSync as SQ,readdirSync as i0,readFileSync as kY,writeFileSync as uY,mkdirSync as mY}from"fs";import{join as dY}from"path";var hY="superpowers.json",pY=()=>({version:1,hooks:{sessionStart:[{type:"command",bash:"superpowers-agent session-context --format=copilot 2>/dev/null || true",powershell:"superpowers-agent session-context --format=copilot 2>$null; exit 0",timeoutSec:15}]}}),jX=(Q=H.copilotHooksDir)=>{let X=dY(Q,hY),Y=JSON.stringify(pY(),null,2)+`
`;if(SQ(X)){let Z;try{Z=kY(X,"utf8")}catch(q){return{error:!0,message:`Failed to read ${X}: ${q.message}`}}if(Z===Y)return{existed:!0,path:X}}if(!SQ(Q))try{mY(Q,{recursive:!0})}catch(Z){return{error:!0,message:`Failed to create ${Q}: ${Z.message}`}}let $=SQ(X);try{uY(X,Y,"utf8")}catch(Z){return{error:!0,message:`Failed to write ${X}: ${Z.message}`}}return $?{updated:!0,path:X}:{created:!0,path:X}};import{existsSync as l,readdirSync as cY,lstatSync as nY,symlinkSync as _Q,unlinkSync as PQ,mkdirSync as RX,readlinkSync as oY}from"fs";import{join as c,dirname as iY,basename as lY}from"path";import{homedir as n,platform as xX}from"os";var UQ=[{name:"claude",parentDir:()=>c(n(),".claude"),skillsDir:()=>c(n(),".claude","skills")},{name:"copilot",parentDir:()=>c(n(),".copilot"),skillsDir:()=>c(n(),".copilot","skills")},{name:"opencode",parentDir:()=>c(n(),".config","opencode"),skillsDir:()=>c(n(),".config","opencode","skill")}],t=(Q)=>{try{return nY(Q).isSymbolicLink()}catch{return!1}},DX=(Q,X)=>{try{if(!t(Q))return!1;return oY(Q)===X}catch{return!1}},fX=(Q,X)=>{if(!l(Q))return{created:!1,existed:!1,error:`Source does not exist: ${Q}`};if(DX(X,Q))return{created:!1,existed:!0};if(l(X)||t(X))if(t(X))try{PQ(X)}catch(Z){return{created:!1,existed:!1,error:`Failed to remove existing symlink: ${Z.message}`}}else return{created:!1,existed:!1,error:`Target exists and is not a symlink: ${X}`};let Y=iY(X);if(!l(Y))try{RX(Y,{recursive:!0})}catch(Z){return{created:!1,existed:!1,error:`Failed to create parent directory: ${Z.message}`}}let $=xX();try{if($==="win32")_Q(Q,X,"junction");else _Q(Q,X,"dir");return{created:!0,existed:!1}}catch(Z){if($==="win32"&&Z.code==="EPERM")return{created:!1,existed:!1,error:"Windows requires Developer Mode or admin privileges for symlinks. Enable Developer Mode: Settings > Update & Security > For developers"};return{created:!1,existed:!1,error:`Failed to create symlink: ${Z.message}`}}},gQ=(Q)=>{if(!l(Q)&&!t(Q))return{removed:!1,error:"Symlink does not exist"};if(!t(Q))return{removed:!1,error:"Target is not a symlink"};try{return PQ(Q),{removed:!0}}catch(X){return{removed:!1,error:`Failed to remove symlink: ${X.message}`}}};var yX=(Q,X,Y="skills")=>{let $=y(!0);if(!$.symlinks||!$.symlinks[Q])return;if(Y==="superpowers")$.symlinks[Q].superpowers=null;else{let Z=$.symlinks[Q].skills||[];$.symlinks[Q].skills=Z.filter((q)=>q!==X)}d($,!0)};var SX=(Q,X=[])=>{let Y;try{Y=cY(Q,{withFileTypes:!0})}catch{return X}let $=!1,Z=[];for(let q of Y){if(!q.isDirectory()&&q.name==="SKILL.md")$=!0;if(q.isDirectory()&&!q.name.startsWith("."))Z.push(c(Q,q.name))}if($)X.push(Q);for(let q of Z)SX(q,X);return X},PX=()=>{let Q=H.homeSuperpowersSkills,X=H.homePersonalSkills;if(!l(Q))return{created:0,existed:0,updated:0,errors:[`Skills directory not found: ${Q}`]};if(!l(X))try{RX(X,{recursive:!0})}catch(Z){return{created:0,existed:0,updated:0,errors:[`Failed to create ${X}: ${Z.message}`]}}let Y=SX(Q),$={created:0,existed:0,updated:0,errors:[]};for(let Z of Y){let q=lY(Z),z=c(X,q);if(DX(z,Z)){$.existed++;continue}if(t(z))try{PQ(z),$.updated++}catch(K){$.errors.push(`Failed to remove stale symlink ${z}: ${K.message}`);continue}else if(l(z)){$.errors.push(`Skipped ${z}: path exists and is not a symlink`);continue}let W=xX();try{if(W==="win32")_Q(Z,z,"junction");else _Q(Z,z,"dir");let K=z.replace(n(),"~"),B=Z.replace(n(),"~");console.log(`  ✓ ${K} -> ${B}`),$.created++}catch(K){$.errors.push(`Failed to create symlink ${z}: ${K.message}`)}}return $};import{existsSync as aY,lstatSync as rY,readdirSync as sY,readlinkSync as tY,unlinkSync as eY}from"fs";import{basename as QZ,join as XZ,resolve as gX,sep as YZ}from"path";var ZZ=new Set(["preserving-productive-tensions","dispatching-parallel-agents","executing-plans","finishing-a-development-branch","receiving-code-review","requesting-code-review","subagent-driven-development","using-git-worktrees","writing-plans","defense-in-depth","root-cause-tracing","systematic-debugging","verification-before-completion","finding-skills","create-agents-md","creating-prompts","using-superpowers","writing-prompts","collision-zone-thinking","inversion-exercise","meta-pattern-recognition","scale-game","simplification-cascades","when-stuck","tracing-knowledge-lineages","condition-based-waiting","test-driven-development","testing-anti-patterns","using-a-skill"]),$Z=(Q,X)=>Q===X||Q.startsWith(`${X}${YZ}`),qZ=({skillDir:Q,bundledSkillsDir:X,retiredSkillNames:Y=ZZ})=>{let $=[],Z=0,q=gX(X);if(!aY(Q))return{removed:$,skipped:Z};let z;try{z=sY(Q)}catch{return{removed:$,skipped:Z}}for(let W of z){let K=XZ(Q,W);try{if(!rY(K).isSymbolicLink())continue;let B=gX(Q,tY(K));if(!$Z(B,q)||!Y.has(QZ(B))){Z++;continue}eY(K),$.push(K)}catch{Z++}}return{removed:$,skipped:Z}},kX=()=>qZ({skillDir:H.homePersonalSkills,bundledSkillsDir:H.homeSuperpowersSkills});var IQ=(Q,X,Y,$=!0)=>{let Z=T(Q);if(!Z&&!$)return{updated:!1,created:!1,skipped:!0};let q=X,z=new Date().toISOString().split("T")[0];q=q.replace(/\{\{DATE\}\}/g,z),q=q.replace(/\{\{SUPERPOWERS_PATH\}\}/g,H.superpowersRepo);let W=`<!-- SUPERPOWERS_SKILLS_START -->
${q}
<!-- SUPERPOWERS_SKILLS_END -->`;if(Z){let K=new Date().toISOString().split("T")[0],B=`${Q}.backup-${K}`;try{D(`cp "${Q}" "${B}"`,{stdio:"pipe"})}catch(L){return{updated:!1,error:!0,message:`Failed to backup: ${L.message}`}}let V;try{V=a(Q,"utf8")}catch(L){return{updated:!1,error:!0,message:`Failed to read: ${L.message}`}}let E="<!-- SUPERPOWERS_SKILLS_START -->",G="<!-- SUPERPOWERS_SKILLS_END -->",U;if(V.includes(E)&&V.includes(G)){let L=new RegExp(`${E}[\\s\\S]*?${G}`,"g");U=V.replace(L,W)}else{let L=V.match(/^#\s+.+$/m);if(L){let I=V.indexOf(`
`,L.index)+1;U=V.slice(0,I)+`
`+W+`
`+V.slice(I)}else U=W+`

`+V}try{return QQ(Q,U,"utf8"),{updated:!0,created:!1,backup:B}}catch(L){return{updated:!1,error:!0,message:`Failed to write: ${L.message}`}}}else try{let K=mX(Q);if(!T(K))D(`mkdir -p "${K}"`,{stdio:"pipe"});let E=`# ${e(Q).base.toUpperCase()}

`+W;return QQ(Q,E,"utf8"),{updated:!1,created:!0}}catch(K){return{updated:!1,created:!1,error:!0,message:`Failed to create: ${K.message}`}}},HZ=()=>{console.log("Installing Unix aliases...");let Q=v(H.superpowersRepo,".agents","superpowers-agent"),X=v(H.home,".local","bin"),Y=v(X,"superpowers-agent"),$=v(X,"superpowers");if(!T(Q)){console.log("⚠️  superpowers-agent executable not found");return}if(!T(X))try{D(`mkdir -p "${X}"`,{stdio:"pipe"}),console.log(`✓ Created ${X}`)}catch(q){console.log(`⚠️  Failed to create ${X}: ${q.message}`);return}try{if(T(Y))D(`rm "${Y}"`,{stdio:"pipe"});D(`ln -s "${Q}" "${Y}"`,{stdio:"pipe"}),console.log(`✓ Created symlink: superpowers-agent -> ${Q}`)}catch(q){console.log(`⚠️  Failed to create superpowers-agent symlink: ${q.message}`)}try{if(T($))D(`rm "${$}"`,{stdio:"pipe"});D(`ln -s "${Q}" "${$}"`,{stdio:"pipe"}),console.log(`✓ Created symlink: superpowers -> ${Q}`)}catch(q){console.log(`⚠️  Failed to create superpowers symlink: ${q.message}`)}if(!(process.env.PATH||"").includes(X))console.log(`
⚠️  Warning: ${X} is not in your PATH`),console.log("   Add this to your shell profile (~/.bashrc, ~/.zshrc, etc.):"),console.log(`   export PATH="$HOME/.local/bin:$PATH"
`)},BZ=()=>{console.log("Installing Windows aliases..."),console.log("✓ Windows alias installation (implementation pending)")},kQ=()=>{let Q=zZ();if(console.log(`## Installing Universal Aliases
`),Q==="win32")return BZ();else return HZ()},KZ=(Q)=>{let X=v(H.superpowersRepo,".github","copilot-instructions.md"),Y=v(H.superpowersRepo,"skills","meta","using-superpowers","SKILL.md"),$=v(Q,".github","copilot-instructions.md"),Z="<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->",q="<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->";if(!T(X))return{error:!0,message:"Source template not found"};if(!T(Y))return{error:!0,message:"using-superpowers SKILL.md not found"};let z,W;try{z=a(X,"utf8"),W=a(Y,"utf8")}catch(V){return{error:!0,message:`Failed to read source files: ${V.message}`}}let K=z.replace("${content}",W);if(!K.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->")||!K.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->"))return{error:!0,message:"Template is missing required markers"};let B=mX($);try{if(!T(B))D(`mkdir -p "${B}"`,{stdio:"pipe"})}catch(V){return{error:!0,message:`Failed to create .github directory: ${V.message}`}}if(T($)){let V;try{V=a($,"utf8")}catch(U){return{error:!0,message:`Failed to read existing file: ${U.message}`}}let E=new Date().toISOString().replace(/[:.]/g,"-"),G=`${$}.backup-${E}`;try{D(`cp "${$}" "${G}"`,{stdio:"pipe"})}catch(U){return{error:!0,message:`Failed to backup: ${U.message}`}}if(V.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->")&&V.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->")){let U=new RegExp("<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->[\\s\\S]*?<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->","g"),L=V.replace(U,K.trim());try{return QQ($,L,"utf8"),{updated:!0,backup:G}}catch(I){return{error:!0,message:`Failed to update: ${I.message}`}}}else{let U=V.trimEnd()+`

`+K.trim()+`
`;try{return QQ($,U,"utf8"),{updated:!0,backup:G}}catch(L){return{error:!0,message:`Failed to append: ${L.message}`}}}}else try{return QQ($,K,"utf8"),{created:!0}}catch(V){return{error:!0,message:`Failed to create: ${V.message}`}}},dX=()=>{k(),console.log(`# Setting up Superpowers skills for this project
`);let Q=process.cwd(),X=v(Q,".agents"),Y=v(X,"skills"),$=v(Q,"AGENTS.md"),Z=v(X,"AGENTS.md"),q=T($)?$:Z,z=v(H.superpowersRepo,".agents","templates","AGENTS.md.template");if(!T(z)){console.log(`✗ Error: AGENTS.md template not found
  Expected at: ${z}
  Please update your Superpowers installation`);return}if(!T(X))try{D(`mkdir -p "${X}"`,{stdio:"pipe"}),console.log("✓ Created .agents/ directory")}catch(w){console.log(`✗ Failed to create .agents/ directory: ${w.message}`);return}else console.log("✓ .agents/ directory exists");if(!T(Y))try{D(`mkdir -p "${Y}"`,{stdio:"pipe"}),D(`touch "${v(Y,".gitkeep")}"`,{stdio:"pipe"}),console.log("✓ Created .agents/skills/ directory")}catch(w){console.log(`✗ Failed to create skills directory: ${w.message}`);return}else console.log("✓ .agents/skills/ directory exists");let W=v(X,"docs"),K=v(W,"SUPERPOWERS.md"),B=v(H.superpowersRepo,".agents","templates","SUPERPOWERS.md.template");if(!T(W))try{D(`mkdir -p "${W}"`,{stdio:"pipe"}),console.log("✓ Created .agents/docs/ directory")}catch(w){console.log(`⚠️  Failed to create docs directory: ${w.message}`)}else console.log("✓ .agents/docs/ directory exists");let V=T(q);if(V){let w=new Date().toISOString().split("T")[0],p=`${q}.backup-${w}`;try{D(`cp "${q}" "${p}"`,{stdio:"pipe"}),console.log(`✓ Backed up existing AGENTS.md to ${e(p).base}`)}catch(HQ){console.log(`✗ Failed to backup AGENTS.md: ${HQ.message}`);return}}let E;try{E=a(z,"utf8")}catch(w){console.log(`✗ Failed to read template: ${w.message}`);return}let G=S();if(E=E.replace(/\{\{VERSION\}\}/g,G),T(B))try{let w=a(B,"utf8"),p=new Date().toISOString().split("T")[0];w=w.replace(/\{\{VERSION\}\}/g,G),w=w.replace(/\{\{DATE\}\}/g,p),QQ(K,w,"utf8"),console.log("✓ Created .agents/docs/SUPERPOWERS.md")}catch(w){console.log(`⚠️  Failed to create SUPERPOWERS.md: ${w.message}`)}else console.log("⚠️  SUPERPOWERS.md.template not found");let U=[],L=v(Q,".github","copilot-instructions.md"),I=v(H.home,".github","copilot-instructions.md");if(T(L)||T(I)||x.copilot.check())U.push("github-copilot");let M=v(Q,"CLAUDE.md"),N=v(X,"CLAUDE.md");if(T(M)||T(N)||x.claude.check())U.push("claude-code");if(x.opencode.check())U.push("opencode");console.log(`
Detected platforms for project: ${U.join(", ")||"none"}
`);let f=U.filter((w)=>["github-copilot","opencode"].includes(w)),C=IQ(q,E,f,!V);if(C.created)console.log(`✓ Created AGENTS.md with platform tool mappings (${q===$?"root":".agents/"})`);else if(C.updated)console.log(`✓ Updated AGENTS.md with platform tool mappings (${q===$?"root":".agents/"})`);else if(C.error)console.log("⚠️  Failed to update AGENTS.md");let o=T(M)?M:N,_=IQ(o,E,["claude-code"],!1);if(_.created)console.log(`✓ Created CLAUDE.md with Claude Code tool mappings (${o===M?"root":".agents/"})`);else if(_.updated){if(console.log(`✓ Updated CLAUDE.md with Claude Code tool mappings (${o===M?"root":".agents/"})`),_.backup)console.log(`  Backed up to ${e(_.backup).base}`)}else if(_.skipped)console.log("ℹ️  Skipped CLAUDE.md (does not exist)");else if(_.error)console.log("⚠️  Failed to update CLAUDE.md");let J=v(Q,".github","copilot-instructions.md"),F={skipped:!0};if(U.includes("github-copilot")){if(F=KZ(Q),F.created)console.log("✓ Created .github/copilot-instructions.md with Superpowers instructions");else if(F.updated){if(console.log("✓ Updated .github/copilot-instructions.md with Superpowers instructions"),F.backup)console.log(`  Backed up to ${e(F.backup).base}`)}else if(F.error)console.log(`⚠️  Failed to update .github/copilot-instructions.md: ${F.message||""}`)}else console.log("ℹ️  Skipped .github/copilot-instructions.md (GitHub Copilot not detected)");let R=!T($)&&!T(Z);if(T(J)&&R){let w=IQ(J,E,["github-copilot"],!1);if(w.updated){if(console.log("✓ Updated .github/copilot-instructions.md with GitHub Copilot tool mappings"),w.backup)console.log(`  Backed up to ${e(w.backup).base}`)}else if(w.error)console.log("⚠️  Failed to update .github/copilot-instructions.md with tool mappings")}else if(T(J)&&!R)console.log("ℹ️  Skipped .github/copilot-instructions.md tool mappings (AGENTS.md exists, using that instead)");else if(!T(J)&&!U.includes("github-copilot"))console.log("ℹ️  Skipped .github/copilot-instructions.md tool mappings (does not exist)");let P=`
# Setup complete!

Your project now has:
  - .agents/ directory structure`;if(C.updated||C.created)P+=`
  - AGENTS.md with universal skills instructions`;if(_.updated||_.created)P+=`
  - CLAUDE.md with Claude Code skills instructions`;if(F.updated||F.created)P+=`
  - .github/copilot-instructions.md with Superpowers instructions`;P+=`
  - .agents/skills/ ready for project-specific skills`,P+=`
  - .agents/docs/SUPERPOWERS.md for detailed reference
`,console.log(P)},VZ=()=>{let Q=[...["brainstorming.prompt.md","execute-plan.prompt.md","write-plan.prompt.md","setup-skills.prompt.md","create-meta-prompt.md","finding-skills.prompt.md","skills.prompt.md","use-skill.prompt.md","using-a-skill.prompt.md"].map((Y)=>v(H.vscodeUserDir,"prompts",Y)),...["brainstorm.md","create-meta-prompt.md","execute-plan.md","finding-skills.md","skills.md","use-skill.md","using-a-skill.md","write-plan.md","setup-skills.md"].map((Y)=>v(H.home,".claude","commands",Y)),...["brainstorm.md","create-meta-prompt.md","execute-plan.md","finding-skills.md","skills.md","use-skill.md","using-a-skill.md","write-plan.md","setup-skills.md"].map((Y)=>v(H.home,".config","opencode","command",Y))],X=0;for(let Y of Q)if(T(Y))try{uX(Y),X++}catch($){console.log(`  ⚠️  Could not remove ${Y}: ${$.message}`)}if(X>0)console.log(`✓ Removed ${X} legacy prompt/command file${X!==1?"s":""}`);else console.log("✓ No legacy prompt/command files found")},EZ=()=>{let Q=0;for(let X of UQ){let Y=X.skillsDir(),$=v(Y,"superpowers");try{if(WZ($).isSymbolicLink())uX($),console.log(`✓ Removed legacy superpowers symlink from ${Y.replace(H.home,"~")}`),Q++}catch{}}if(Q===0)console.log("✓ No legacy superpowers directory symlinks found")},hX=async()=>{k(),console.log(`# Superpowers Bootstrap for Agents
# ==================================
`);let Q=process.argv.includes("--no-update"),Y=new Set(["copilot","claude","opencode"].filter((z)=>process.argv.includes(`--force-${z}`))),$=Y.size>0;if(!Q){let z=qQ(),W=await GQ();if(W.error)console.log(`## Update Check

⚠️  Could not check for updates (network issue)

---
`);else if(W.hasUpdates)console.log(`## Update Available
`),console.log(`⚠️  Update available: v${W.localVersion} → v${W.remoteVersion}
    To update, run: \`npm install -g @complexthings/superpowers-agent\``),console.log(`
---
`)}if(!$)kQ(),console.log(`---
`);if(!$)console.log(`## Cleaning Up Legacy Files
`),VZ(),console.log(`
---
`);if(!$||Y.has("copilot")){if(console.log(`## GitHub Copilot Integration
`),console.log("✓ Skill symlinks handled in sync step below"),WQ("copilot")||T(v(H.home,".copilot"))||Y.has("copilot")){let W=jX();if(W.created)console.log(`✓ Installed Copilot sessionStart hook -> ${W.path.replace(H.home,"~")}`);else if(W.updated)console.log(`✓ Updated Copilot sessionStart hook -> ${W.path.replace(H.home,"~")}`);else if(W.existed)console.log("✓ Copilot sessionStart hook already up to date");else if(W.error)console.log(`⚠️  Failed to install Copilot sessionStart hook: ${W.message}`)}else console.log("ℹ️  Skipped Copilot sessionStart hook (Copilot CLI not detected; run with --force-copilot to install anyway)");console.log(`
---
`)}if(!$||Y.has("claude")){if(console.log(`## Claude Code Integration
`),!x.claude.check()&&!Y.has("claude"))console.log(`⚠️  Skipped (${x.claude.name} CLI not detected)
\uD83D\uDCA1 To enable Claude Code integration:
   1. Install Claude Code: ${x.claude.installUrl}
   2. Run: superpowers-agent bootstrap --force-claude`);else{console.log("✓ Skill symlinks handled in sync step below");let W=FX();if(W.created)console.log(`✓ Installed SessionStart hook -> ${H.claudeSettings.replace(H.home,"~")}`);else if(W.updated){if(console.log(`✓ ${W.existed?"Refreshed":"Added"} SessionStart hook in ${H.claudeSettings.replace(H.home,"~")}`),W.backup)console.log(`  Backed up to ${e(W.backup).base}`)}else if(W.error)console.log(`⚠️  Failed to install SessionStart hook: ${W.message}`)}console.log(`
---
`)}if(!$||Y.has("opencode")){if(console.log(`## OpenCode Integration
`),x.opencode.check())bX();else console.log(`⚠️  Skipped (${x.opencode.name} CLI not detected)
\uD83D\uDCA1 To enable OpenCode integration:
   1. Install OpenCode: ${x.opencode.installUrl}
   2. Run: superpowers-agent ${x.opencode.bootstrapCommand}`);console.log(`
---
`)}if(!$){console.log(`## Generating Platform-Specific Files
`);let z=vX();console.log(`Detected platforms: ${z.join(", ")||"none"}
`);let W=v(H.superpowersRepo,".agents","templates","AGENTS.md.template"),K="";try{K=a(W,"utf8")}catch(B){console.log(`⚠️  Could not read AGENTS.md template: ${B.message}
`)}if(K){let B=S();K=K.replace(/\{\{VERSION\}\}/g,B);let V=v(H.home,".agents","AGENTS.md"),E=IQ(V,K,z,!0);if(E.created)console.log(`✓ Created ${V}`);else if(E.updated)console.log(`✓ Updated ${V}`)}console.log(`
---
`)}console.log(`## Cleaning Up Legacy Superpowers Dir Symlinks
`),EZ(),console.log(`
---
`),console.log(`## Syncing Repo Skills -> ~/.agents/skills/
`);let Z=PX();if(Z.created>0||Z.updated>0)console.log(`  ✓ ${Z.created} created, ${Z.updated} updated, ${Z.existed} already current`);else if(Z.errors.length>0)for(let z of Z.errors)console.log(`  ⚠️  ${z}`);else console.log(`  ✓ ${Z.existed} skill symlinks already up to date`);console.log(`
---
`),console.log(`## Reconciling Retired Repo-Managed Skill Symlinks
`);let q=kX();if(q.removed.length>0){console.log(`  ✓ Removed ${q.removed.length} retired skill symlink(s):`);for(let z of q.removed)console.log(`    - ${z}`)}else console.log("  ✓ No retired repo-managed skill symlinks found");console.log(`
---
`),console.log(`# Bootstrap Complete!
`),console.log("✓ All integrations installed"),console.log("✓ Skills system ready"),console.log(`✓ Skill symlinks synced
`),console.log("Next steps:"),console.log("  - Run `superpowers-agent find-skills` to see available skills"),console.log("  - Run `superpowers-agent setup-skills` in your project directory")};import{existsSync as b,readFileSync as JQ,mkdirSync as MZ,rmSync as nQ,cpSync as TZ,lstatSync as AZ}from"fs";import{join as A,dirname as wQ,parse as zQ,sep as vZ}from"path";import{execSync as j}from"child_process";import{homedir as h}from"os";import{createInterface as CZ}from"readline";import{readFileSync as GZ,existsSync as hQ,unlinkSync as _Z,lstatSync as UZ,mkdirSync as IZ}from"fs";import{join as LZ,dirname as JZ}from"path";import{join as XQ}from"path";import{homedir as uQ}from"os";var mQ={github:{name:"github",sourceDir:".github/agents",sourceExt:".agent.md",getDestDir:()=>XQ(H.vscodeUserDir,"prompts"),getExtraDestDirs:()=>[XQ(uQ(),".copilot","agents")],destExt:".agent.md"},opencode:{name:"opencode",sourceDir:".opencode/agents",sourceExt:".md",getDestDir:()=>XQ(uQ(),".config","opencode","agents"),destExt:".md"},claude:{name:"claude",sourceDir:".claude/agents",sourceExt:".md",getDestDir:()=>XQ(uQ(),".claude","agents"),destExt:".md"}};function pX(Q,X,Y){let $=mQ[X];if(!$)return null;return XQ(Q,$.sourceDir,`${Y}${$.sourceExt}`)}function cX(Q,X){let Y=mQ[Q];if(!Y)return[];return[Y.getDestDir(),...Y.getExtraDestDirs?Y.getExtraDestDirs():[]].map((Z)=>XQ(Z,`${X}${Y.destExt}`))}function dQ(){return Object.keys(mQ)}function wZ(Q){if(!Q||typeof Q!=="object")return{valid:!1,error:"agents.json must be a JSON object"};if(!Q.version||typeof Q.version!=="string")return{valid:!1,error:'agents.json must have a "version" string field'};if(!Q.agents||typeof Q.agents!=="object")return{valid:!1,error:'agents.json must have an "agents" object field'};let X=dQ(),Y=[];for(let[$,Z]of Object.entries(Q.agents)){if(!X.includes($)){Y.push($);continue}if(!Array.isArray(Z))return{valid:!1,error:`agents.${$} must be an array of agent names`};for(let q of Z)if(typeof q!=="string")return{valid:!1,error:`agents.${$} must contain only string agent names`}}return{valid:!0,data:Q,skippedPlatforms:Y}}function OZ(Q,X){if(!hQ(Q))return{installed:!1,error:`Source file not found: ${Q}`};if(hQ(X))try{if(!UZ(X).isSymbolicLink())_Z(X)}catch($){}IZ(JZ(X),{recursive:!0});let Y=fX(Q,X);if(Y.error)return{installed:!1,error:Y.error};return{installed:!0}}function pQ(Q,X={}){let Y=LZ(Q,"agents.json");if(!hQ(Y))return;let $;try{$=JSON.parse(GZ(Y,"utf8"))}catch(U){console.log(`
  ⚠ Could not parse agents.json: ${U.message}`);return}let Z=wZ($);if(!Z.valid){console.log(`
  ⚠ Invalid agents.json: ${Z.error}`);return}for(let U of Z.skippedPlatforms)console.log(`  ⚠ Skipping unknown platform: ${U}`);let q=$.repository||"unknown",z=$.version,W=X.isUpdate?"Updating":"Installing",K=X.isUpdate?"Updated":"Installed";console.log(`
  \uD83D\uDCE6 Found agents.json (${q} v${z})`);let B=dQ(),V=0,E=0,G=[];for(let[U,L]of Object.entries($.agents)){if(!B.includes(U))continue;console.log(`
  ${W} ${U} agents:`);for(let I of L){let M=pX(Q,U,I),N=cX(U,I),f=[],C=[];for(let o of N){let _=OZ(M,o);if(_.installed)f.push(o);else C.push(_.error)}if(f.length>0)console.log(`    ✓ ${I}`),G.push({platformKey:U,agentName:I,sourcePath:M,destPaths:f}),V++;else console.log(`    ✗ ${I}: ${C.join("; ")}`),E++}}if(G.length>0){let U=y(!0);if(!U.installedAgents)U.installedAgents={};if(!U.installedAgents[q])U.installedAgents[q]={version:z,agents:{}};U.installedAgents[q].version=z;for(let{platformKey:L,agentName:I,sourcePath:M,destPaths:N}of G){if(!U.installedAgents[q].agents[L])U.installedAgents[q].agents[L]={};U.installedAgents[q].agents[L][I]={source:M,destination:N[0],destinations:N,installedAt:new Date().toISOString()}}d(U,!0)}if(V>0)console.log(`
  ${K} ${V} agent(s)${E>0?` (${E} failed)`:""}`)}var nX=(Q,X)=>{if(X.type==="local"){let $=Q;for(let Z=0;Z<10;Z++){if(b(A($,"agents.json")))return $;let q=wQ($);if(q===$)break;$=q}return Q}let Y=A(".agents","tmp");if(Q.includes(Y)){let $=Q.indexOf(Y),q=Q.substring($+Y.length+1).split(vZ)[0];return A(Q.substring(0,$),".agents","tmp",q)}return Q},oX=(Q,X)=>{let Y=A(h(),".agents","repos"),$=X.replace(/^@/,"").replace(/[^a-zA-Z0-9_-]/g,"_"),Z=A(Y,$);try{if(MZ(Y,{recursive:!0}),b(Z))nQ(Z,{recursive:!0,force:!0});return TZ(Q,Z,{recursive:!0}),Z}catch(q){return console.log(`  Warning: Could not persist repository for agents: ${q.message}`),null}},oQ=(Q)=>{let X=Q.match(/^https:\/\/github\.com\/([^/]+)\/([^/]+)\/tree\/([^/]+)\/(.+)$/);if(X){let[,Z,q,z,W]=X;return{type:"git-tree",repoUrl:`https://github.com/${Z}/${q}.git`,branch:z,path:W,original:Q}}let Y=Q.match(/^git@github\.com:([^/]+)\/([^/]+?)(?:\.git)?$/);if(Y){let[,Z,q]=Y;return{type:"git-repo",repoUrl:`git@github.com:${Z}/${q}.git`,branch:null,path:null,original:Q}}let $=Q.match(/^https:\/\/github\.com\/([^/]+)\/([^/]+?)(?:\.git)?$/);if($){let[,Z,q]=$;return{type:"git-repo",repoUrl:`https://github.com/${Z}/${q}.git`,branch:null,path:null,original:Q}}if(b(Q))return{type:"local",path:Q,original:Q};return null},iQ=(Q)=>{let X=Q.includes("--global")||Q.includes("-g"),Y=Q.includes("--project")||Q.includes("-p");if(X)return A(h(),".agents","skills");if(Y)return A(process.cwd(),".agents","skills");let $=A(process.cwd(),".agents","config.json"),Z=A(h(),".agents","config.json");for(let q of[$,Z])if(b(q))try{if(JSON.parse(JQ(q,"utf8")).installLocation==="project")return A(process.cwd(),".agents","skills")}catch(z){}return A(h(),".agents","skills")},lQ=(Q,X)=>{let Y=A(h(),".agents","tmp",`skill-install-${Date.now()}`);try{if(j(`mkdir -p "${Y}"`,{stdio:"pipe"}),X)j(`git clone --branch ${X} --depth 1 "${Q}" "${Y}"`,{stdio:"pipe",timeout:30000});else j(`git clone --depth 1 "${Q}" "${Y}"`,{stdio:"pipe",timeout:30000});return Y}catch($){try{j(`rm -rf "${Y}"`,{stdio:"pipe"})}catch{}throw Error(`Failed to clone repository: ${$.message}`)}},YQ=(Q)=>{let X=A(Q,"skill.json");if(!b(X))return null;try{let Y=JQ(X,"utf8");return JSON.parse(Y)}catch(Y){throw Error(`Failed to read skill.json: ${Y.message}`)}},LQ=(Q,X,Y,$)=>{let Z=A(Q,X);if(!b(Z)){$.errors.push(`Skill directory not found: ${X}`);return}let q=YQ(Z);if(!q){$.errors.push(`No skill.json found in: ${X}`);return}let z=q.name||X,W=z.split("/").pop()||z,K=A(Y,W),B=wQ(K);try{j(`mkdir -p "${B}"`,{stdio:"pipe"})}catch(V){$.errors.push(`Failed to create directory ${B}: ${V.message}`);return}try{if(b(K))j(`rm -rf "${K}"`,{stdio:"pipe"});j(`cp -R "${Z}" "${K}"`,{stdio:"pipe"}),$.installed.push({name:W,path:K,title:q.title||q.description||W})}catch(V){$.errors.push(`Failed to install ${X}: ${V.message}`)}},iX=(Q,X,Y,$,Z=null)=>{let q=y($);if(!q.installedSkills)q.installedSkills={};let z={source:Q,installBase:X,installedAt:new Date().toISOString(),skills:Y.installed.map((W)=>({name:W.name,path:W.path}))};if(q.installedSkills[Q]=z,Z&&Z!==Q)q.installedSkills[Z]=z;d(q,$)},lX=()=>{let Q=process.argv.slice(3),X=Q.find((E)=>!E.startsWith("-")),Y=Q.filter((E)=>E.startsWith("-"));if(!X){console.log(`Usage: superpowers-agent add <url-or-path|@alias> [skill-path] [options]

Options:
  --global, -g   Install skills globally in ~/.agents/skills/ (default)
  --project, -p  Install skills in current project's .agents/skills/

Examples:
  superpowers-agent add https://github.com/example/skills
  superpowers-agent add https://github.com/example/repo/tree/main/skills
  superpowers-agent add ~/my-local-skills
  superpowers-agent add https://github.com/example/skills --project
  superpowers-agent add ~/my-local-skills --global
  superpowers-agent add @myrepo path/to/skill
  superpowers-agent add @myrepo path/to/skills --project

Description:
  Installs skill(s) from a Git repository, local directory, or repository alias.
  Supports repositories with single or multiple skills.
  Reads skill.json to determine skill names and installation paths.

  Repository aliases can be added using:
    superpowers-agent add-repository <git-url> [--as=@alias]`);return}console.log(`Installing skill(s)...
`);let $=!Y.includes("--project")&&!Y.includes("-p"),Z=null,q=null,z=null;if(X.startsWith("@")){let E=X;z=E,q=Q.find((I,M)=>M>0&&!I.startsWith("-")&&I!==E);let G=g(!1),U=g(!0);if(G[E])Z=G[E],console.log(`Using project repository alias: ${E}`);else if(U[E])Z=U[E],console.log(`Using global repository alias: ${E}`);else{console.log(`Error: Repository alias not found: ${E}`),console.log(`
Available aliases:`);let I={...U,...G};if(Object.keys(I).length===0)console.log("  (none)"),console.log(`
Add a repository using:`),console.log("  superpowers-agent add-repository <git-url>");else for(let[M,N]of Object.entries(I))console.log(`  ${M} -> ${N}`);return}let L=b(Z);if(q)if(L)X=A(Z,q),console.log(`Repository path: ${Z}`),console.log(`Skill path: ${q}
`);else X=Z,console.log(`Repository URL: ${Z}`),console.log(`Skill path: ${q}
`);else if(X=Z,L)console.log(`Repository path: ${Z}
`);else console.log(`Repository URL: ${Z}
`)}let W=oQ(X);if(!W){console.log(`Error: Invalid URL or path not found: ${X}`);return}if(q&&W.type==="git-repo"&&!W.path)W.path=q;let K=iQ(Y);console.log(`Install location: ${K}
`);let B,V=!1;try{if(W.type==="git-repo"||W.type==="git-tree"){if(console.log(`Cloning repository: ${W.repoUrl}`),W.branch)console.log(`Branch: ${W.branch}`);if(B=lQ(W.repoUrl,W.branch),V=!0,W.path){if(B=A(B,W.path),!b(B))throw Error(`Path not found in repository: ${W.path}`)}console.log("")}else B=W.path;let E=YQ(B);if(!E)throw Error("No skill.json found in source directory");let G={installed:[],errors:[],source:W.original};if(E.skills&&Array.isArray(E.skills)){console.log(`Found ${E.skills.length} skill(s) to install
`);for(let I of E.skills)LQ(B,I,K,G)}else{let I=E.name||zQ(B).base;LQ(wQ(B),zQ(B).base,K,G)}let U=nX(B,W),L=A(U,"agents.json");if(b(L)){let I=U;if(V){let M;try{M=JSON.parse(JQ(L,"utf8"))}catch{}let N=M?.repository||W.original,f=oX(U,N);if(f)I=f}pQ(I,{isUpdate:!1})}if(V&&B)try{let I=B.split("/.agents/tmp/")[0]+"/.agents/tmp/"+B.split("/.agents/tmp/")[1].split("/")[0];j(`rm -rf "${I}"`,{stdio:"pipe"})}catch{}if(console.log(`
**Successfully installed skills:**`),console.log(`- Source: ${G.source}`),G.installed.length>0)for(let I of G.installed)console.log(`  - Installed: ${I.name} at ${I.path}`),console.log(`    ${I.title}`);if(G.errors.length>0){console.log(`
**Errors:**`);for(let I of G.errors)console.log(`  - ${I}`)}if(G.installed.length===0&&G.errors.length===0)console.log("  No skills were installed");if(G.installed.length>0)iX(W.original,K,G,$,z)}catch(E){if(V&&B)try{let G=B.split("/.agents/tmp/")[0]+"/.agents/tmp/"+B.split("/.agents/tmp/")[1].split("/")[0];j(`rm -rf "${G}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${E.message}`)}},aX=()=>{let Q=process.argv.slice(3),X=Q.find((E)=>!E.startsWith("-")),Y=Q.filter((E)=>E.startsWith("-"));if(!X){console.log(`Usage: superpowers-agent pull <url-or-path|@alias> [skill-path] [options]

Options:
  --global, -g   Update skills globally in ~/.agents/skills/ (default)
  --project, -p  Update skills in current project's .agents/skills/

Examples:
  superpowers-agent pull https://github.com/example/skills
  superpowers-agent pull https://github.com/example/repo/tree/main/skills
  superpowers-agent pull ~/my-local-skills
  superpowers-agent pull https://github.com/example/skills --project
  superpowers-agent pull ~/my-local-skills --global
  superpowers-agent pull @myrepo
  superpowers-agent pull @myrepo path/to/skill
  superpowers-agent pull @myrepo path/to/skills --project

Description:
  Updates or adds skill(s) from a Git repository, local directory, or repository alias.
  If skills already exist, they will be updated (replaced).
  If skills don't exist, they will be added.
  Supports repositories with single or multiple skills.
  Reads skill.json to determine skill names and installation paths.
  
  Repository aliases can be added using:
    superpowers-agent add-repository <git-url> [--as=@alias]`);return}console.log(`Updating skill(s)...
`);let $=!Y.includes("--project")&&!Y.includes("-p"),Z=null,q=null,z=null;if(X.startsWith("@")){let E=X;z=E,q=Q.find((I,M)=>M>0&&!I.startsWith("-")&&I!==E);let G=g(!1),U=g(!0);if(G[E])Z=G[E],console.log(`Using project repository alias: ${E}`);else if(U[E])Z=U[E],console.log(`Using global repository alias: ${E}`);else{console.log(`Error: Repository alias not found: ${E}`),console.log(`
Available aliases:`);let I={...U,...G};if(Object.keys(I).length===0)console.log("  (none)"),console.log(`
Add a repository using:`),console.log("  superpowers-agent add-repository <git-url>");else for(let[M,N]of Object.entries(I))console.log(`  ${M} -> ${N}`);return}let L=b(Z);if(q)if(L)X=A(Z,q),console.log(`Repository path: ${Z}`),console.log(`Skill path: ${q}
`);else X=Z,console.log(`Repository URL: ${Z}`),console.log(`Skill path: ${q}
`);else if(X=Z,L)console.log(`Repository path: ${Z}
`);else console.log(`Repository URL: ${Z}
`)}let W=oQ(X);if(!W){console.log(`Error: Invalid URL or path not found: ${X}`);return}if(q&&W.type==="git-repo"&&!W.path)W.path=q;let K=iQ(Y);console.log(`Install location: ${K}
`);let B,V=!1;try{if(W.type==="git-repo"||W.type==="git-tree"){if(console.log(`Cloning repository: ${W.repoUrl}`),W.branch)console.log(`Branch: ${W.branch}`);if(B=lQ(W.repoUrl,W.branch),V=!0,W.path){if(B=A(B,W.path),!b(B))throw Error(`Path not found in repository: ${W.path}`)}console.log("")}else B=W.path;let E=YQ(B);if(!E)throw Error("No skill.json found in source directory");let G={installed:[],errors:[],source:W.original};if(E.skills&&Array.isArray(E.skills)){console.log(`Found ${E.skills.length} skill(s) to update
`);for(let I of E.skills)LQ(B,I,K,G)}else{let I=E.name||zQ(B).base;LQ(wQ(B),zQ(B).base,K,G)}let U=nX(B,W),L=A(U,"agents.json");if(b(L)){let I=U;if(V){let M;try{M=JSON.parse(JQ(L,"utf8"))}catch{}let N=M?.repository||W.original,f=oX(U,N);if(f)I=f}pQ(I,{isUpdate:!0})}if(V&&B)try{let I=B.split("/.agents/tmp/")[0]+"/.agents/tmp/"+B.split("/.agents/tmp/")[1].split("/")[0];j(`rm -rf "${I}"`,{stdio:"pipe"})}catch{}if(console.log(`
**Successfully updated skills:**`),console.log(`- Source: ${G.source}`),G.installed.length>0)for(let I of G.installed)console.log(`  - Updated: ${I.name} at ${I.path}`),console.log(`    ${I.title}`);if(G.errors.length>0){console.log(`
**Errors:**`);for(let I of G.errors)console.log(`  - ${I}`)}if(G.installed.length===0&&G.errors.length===0)console.log("  No skills were updated");if(G.installed.length>0)iX(W.original,K,G,$,z)}catch(E){if(V&&B)try{let G=B.split("/.agents/tmp/")[0]+"/.agents/tmp/"+B.split("/.agents/tmp/")[1].split("/")[0];j(`rm -rf "${G}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${E.message}`)}},bZ=(Q)=>new Promise((X)=>{let Y=CZ({input:process.stdin,output:process.stdout});Y.question(Q,($)=>{Y.close(),X($.trim())})}),cQ=(Q)=>{try{return AZ(Q).isSymbolicLink()}catch{return!1}},rX=async()=>{let Q=process.argv.slice(3),X=Q.find((_)=>!_.startsWith("-")),Y=Q.filter((_)=>_.startsWith("-"));if(!X){console.log(`Usage: superpowers-agent rm <url-or-path|@alias> [options]

Options:
  --global, -g   Remove globally installed skills (default)
  --project, -p  Remove project-installed skills

Examples:
  superpowers-agent rm https://github.com/example/skills
  superpowers-agent rm @myrepo
  superpowers-agent rm ~/my-local-skills`);return}let $=!Y.includes("--project")&&!Y.includes("-p"),Z=X,q=null;if(X.startsWith("@")){q=X;let _=g(!1),J=g(!0);if(_[X])Z=_[X];else if(J[X])Z=J[X];else{console.log(`Error: Repository alias not found: ${X}`);return}}let z=BX($),W=z[X]||z[Z],K=W?.installBase||iQ(Y),B=[];if(W)B=W.skills;else{console.log(`No tracking data found. Attempting to re-resolve skills from source...
`);let _=oQ(Z);if(!_){console.log(`Error: Invalid URL or path not found: ${Z}`);return}let J,F=!1;try{if(_.type==="git-repo"||_.type==="git-tree"){if(console.log(`Cloning repository: ${_.repoUrl}`),J=lQ(_.repoUrl,_.branch),F=!0,_.path){if(J=A(J,_.path),!b(J))throw Error(`Path not found in repository: ${_.path}`)}}else J=_.path;let R=YQ(J);if(!R)throw Error("No skill.json found in source");let P=R.skills&&Array.isArray(R.skills)?R.skills:[R.name||zQ(J).base];for(let w of P){let p=w;if(R.skills){let HQ=A(J,w);if(b(HQ)){let aQ=YQ(HQ);if(aQ?.name)p=aQ.name}}B.push({name:p,path:A(K,p)})}if(F)try{let w=J.split("/.agents/tmp/")[0]+"/.agents/tmp/"+J.split("/.agents/tmp/")[1].split("/")[0];j(`rm -rf "${w}"`,{stdio:"pipe"})}catch{}}catch(R){if(F&&J)try{let P=J.split("/.agents/tmp/")[0]+"/.agents/tmp/"+J.split("/.agents/tmp/")[1].split("/")[0];j(`rm -rf "${P}"`,{stdio:"pipe"})}catch{}console.log(`Error resolving source: ${R.message}`);return}}let V=B.filter((_)=>b(_.path)||cQ(_.path)),E=[];for(let _ of B)for(let J of UQ){let F=A(J.skillsDir(),_.name);if(b(F)||cQ(F))E.push({platform:J.name,path:F})}let G=KX(),U=q||Z,L=[];if(G[U])for(let[,_]of Object.entries(G[U].agents||{}))for(let[,J]of Object.entries(_)){let F=J.destinations||(J.destination?[J.destination]:[]);for(let R of F)if(R&&(b(R)||cQ(R)))L.push({path:R})}let I=U.replace(/^@/,"").replace(/[^a-zA-Z0-9_-]/g,"_"),M=A(h(),".agents","repos",I),N=b(M),f=h(),C=(_)=>_.replace(f,"~");if(V.length===0&&E.length===0&&L.length===0&&!N){console.log("Nothing to remove.");return}if(console.log("Will remove:"),V.length>0){console.log("  Skills:");for(let _ of V)console.log(`    ${C(_.path)}`)}if(E.length>0){console.log("  Platform symlinks:");for(let _ of E)console.log(`    ${C(_.path)}`)}if(L.length>0){console.log("  Agents:");for(let _ of L)console.log(`    ${C(_.path)}`)}if(N)console.log("  Persisted repo:"),console.log(`    ${C(M)}`);if((await bZ(`
Proceed? [y/N] `)).toLowerCase()!=="y"){console.log("Aborted.");return}for(let _ of E){let J=gQ(_.path);if(J.removed)console.log(`Removed symlink: ${C(_.path)}`),yX(_.platform,_.path,"skills");else if(J.error==="Symlink does not exist")console.log(`Already removed: ${C(_.path)}`);else console.log(`Warning: ${C(_.path)}: ${J.error}`)}for(let _ of L){let J=gQ(_.path);if(J.removed)console.log(`Removed agent: ${C(_.path)}`);else if(J.error==="Symlink does not exist")console.log(`Already removed: ${C(_.path)}`);else console.log(`Warning: ${C(_.path)}: ${J.error}`)}if(N)try{nQ(M,{recursive:!0,force:!0}),console.log(`Removed persisted repo: ${C(M)}`)}catch(_){console.log(`Warning: Failed to remove persisted repo: ${_.message}`)}for(let _ of V){if(!b(_.path)){console.log(`Already removed: ${C(_.path)}`);continue}try{nQ(_.path,{recursive:!0,force:!0}),console.log(`Removed skill: ${C(_.path)}`)}catch(J){console.log(`Warning: Failed to remove ${C(_.path)}: ${J.message}`)}}if(NQ(X,$),Z!==X)NQ(Z,$);if(G[U]){let _=y(!0);if(_.installedAgents)delete _.installedAgents[U],d(_,!0)}console.log(`
Done.`)},sX=()=>{let Q=process.argv.slice(3),X=Q.find((B)=>!B.startsWith("-")),Y=Q.filter((B)=>B.startsWith("-"));if(!X){console.log(`Usage: superpowers-agent add-repository <git-url> [options]

Options:
  --global, -g        Add repository globally in ~/.agents/config.json (default)
  --project, -p       Add repository in current project's .agents/config.json
  --as=<alias>        Specify custom alias for the repository

Examples:
  superpowers-agent add-repository https://github.com/example/skills.git
  superpowers-agent add-repository https://github.com/example/skills.git --as=@myskills
  superpowers-agent add-repository https://github.com/example/skills.git --project
  superpowers-agent add-repository https://github.com/example/skills.git --as=@custom --global

Description:
  Adds a skill repository alias to your configuration.
  The repository's skill.json will be read to determine the default alias.
  Use --as to specify a custom alias.
  After adding, you can install skills using: superpowers-agent add @alias path/to/skill`);return}console.log(`Adding repository...
`);let $=Y.some((B)=>B==="--global"||B==="-g"),Z=Y.some((B)=>B==="--project"||B==="-p"),q=Y.find((B)=>B.startsWith("--as=")),z=q?q.split("=")[1]:null,W=Z?!1:!0,K;try{console.log(`Cloning repository: ${X}`),K=A(h(),".agents","tmp",`repo-add-${Date.now()}`),j(`mkdir -p "${K}"`,{stdio:"pipe"}),j(`git clone --depth 1 "${X}" "${K}"`,{stdio:"pipe",timeout:30000}),console.log("");let B=YQ(K);if(!B)throw Error("No skill.json found in repository");let V;if(z)V=z;else if(B.repository)V=B.repository;else{let U=X.match(/\/([^/]+?)(?:\.git)?$/);if(U)V="@"+U[1];else throw Error("Could not determine repository alias. Use --as=@alias to specify manually.")}HX(V,X,W),j(`rm -rf "${K}"`,{stdio:"pipe"});let E=W?"globally":"in project",G=W?A(h(),".agents","config.json"):A(process.cwd(),".agents","config.json");console.log(`✓ Repository added ${E}`),console.log(`  Alias: ${V}`),console.log(`  URL: ${X}`),console.log(`  Config: ${G}`),console.log(`
You can now install skills using:`),console.log(`  superpowers-agent add ${V} <path-to-skill>`)}catch(B){if(K)try{j(`rm -rf "${K}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${B.message}`)}};import{dirname as tX}from"path";var eX=(Q,X,Y)=>{let{content:$,frontmatter:Z}=OQ(Q),q=r[X].prefix+Y,z=tX(Q),W=`# ${Z.name||q}`;if(Z.description)W+=`
# ${Z.description}`;if(Z.whenToUse)W+=`
# When to use: ${Z.whenToUse}`;return W+=`
# Supporting tools and docs are in ${z}
# ============================================

${$}`,W},QY=(Q)=>{if(!Q){console.log(`Usage: superpowers-agent use-skill <skill-name>

Examples (smart matching):
  superpowers-agent use-skill brainstorming              # Matches superpowers:collaboration/brainstorming
  superpowers-agent use-skill collaboration/brainstorming # More specific suffix
  superpowers-agent use-skill test-driven-development    # Matches superpowers:testing/test-driven-development

Examples (explicit paths):
  superpowers-agent use-skill superpowers:collaboration/brainstorming  # Full superpowers skill path
  superpowers-agent use-skill claude:email-assistant                   # Full claude skill path
  superpowers-agent use-skill my-custom-skill                          # Project skill

Smart matching:
  - Type just the skill name or any suffix of the path
  - Priority: .agents/skills/ > .claude/skills/ > ~/.agents/skills/ > ~/.agents/superpowers/skills/
  - If multiple skills match at same priority, you'll see them with descriptions`);return}let X=m(Q);if(!X){console.log(`Error: Skill not found: ${Q}

Available skills:`),console.log("Run: superpowers-agent find-skills");return}let{skillFile:Y,sourceType:$,actualSkillPath:Z}=X;try{let{content:q,frontmatter:z}=OQ(Y),W=r[$].prefix+Z,K=tX(Y),B=`# ${z.name||W}`;if(z.description)B+=`
# ${z.description}`;if(z.whenToUse)B+=`
# When to use: ${z.whenToUse}`;B+=`
# Supporting tools and docs are in ${K}
# ============================================

${q}`,console.log(B)}catch(q){console.log(q.message)}};var NZ=`You have superpowers.

**IMPORTANT: The using-superpowers skill content is included below. It is ALREADY LOADED - you are currently following it. Do NOT use the skill tool to load "using-superpowers" again - that would be redundant.**`,FZ="---\n\n**YOU MUST read and use the `leveraging-cli-tools` skill** the moment a task involves using BASH, searching code, parsing JSON/YAML, finding files, structural refactors, data wrangling, or any command that floods your context with verbose output (logs, CI logs, test runs). Load it with your skill tool BEFORE you reach for grep/find/sed or before you read large command output. It routes you to high-performance tools (rg, jq, fd, yq, ast-grep, gh, sd) and reduces output to just the signal — cutting token cost and latency 5-50x. Skipping it wastes tokens and time on work the right tool does instantly.",jZ=()=>{let Q=m("using-superpowers");if(!Q)return null;let X;try{X=eX(Q.skillFile,Q.sourceType,Q.actualSkillPath)}catch{return null}return`<EXTREMELY_IMPORTANT>
${NZ}

${X}

${FZ}
</EXTREMELY_IMPORTANT>`},RZ=(Q="raw")=>{let X=jZ();if(Q==="claude")return JSON.stringify({hookSpecificOutput:{hookEventName:"SessionStart",additionalContext:X||""}});if(Q==="copilot")return JSON.stringify({additionalContext:X||""});return X||""},XY=()=>{let Q=process.argv.find((Y)=>Y.startsWith("--format=")),X=Q?Q.split("=")[1]:"raw";process.stdout.write(RZ(X)+`
`)};var YY={bootstrap:hX,version:AX,"check-updates":()=>{TX().catch((Q)=>{console.error(Q.message),process.exit(1)})},update:()=>{let Q=process.argv.includes("--no-reinstall");xQ({skipReinstall:Q})},"config-get":JX,"config-set":wX,"setup-skills":dX,"use-skill":()=>QY(process.argv[3]),"session-context":XY,execute:_X,"find-skills":s,add:lX,"add-repository":sX,"list-repositories":OX,pull:aX,rm:rX,dir:IX,path:UX,"get-helpers":LX,"install-aliases":kQ,default:()=>{console.log(`Superpowers for Agents
Usage:
  superpowers-agent bootstrap [--no-update] [--force]               # Run complete bootstrap
  superpowers-agent version                                         # Show current version
  superpowers-agent check-updates                                   # Check for updates
  superpowers-agent update [--no-reinstall]                         # Update to latest version
  superpowers-agent config-get                                      # Show configuration
  superpowers-agent config-set <key> <value>                        # Update configuration
  superpowers-agent setup-skills                                    # Initialize project skills
  superpowers-agent use-skill <skill-name>                          # Load a skill
  superpowers-agent session-context [--format=claude|copilot|raw]   # Print session-start context (used by hooks)
  superpowers-agent find-skills                                     # List available skills
  superpowers-agent add <url-or-path|@alias> [path] [options]      # Install skills
  superpowers-agent add-repository <git-url> [--as=@alias] [opts]  # Add repository alias
  superpowers-agent list-repositories                              # List configured repository aliases
  superpowers-agent pull <url-or-path|@alias> [path] [options]     # Update skills
  superpowers-agent rm <url-or-path|@alias> [options]             # Remove skills
  superpowers-agent dir <skill-name>                                # Get skill directory
  superpowers-agent path <skill-name>                               # Get skill file path
  superpowers-agent execute <skill-name>                            # Execute a skill
  superpowers-agent get-helpers <skill> <search-term>               # Get helper file path
  superpowers-agent install-aliases                                 # Install universal aliases (superpowers, superpowers-agent)

Documentation: https://github.com/complexthings/superpowers
`)}},xZ=process.argv[2]||"default",DZ=YY[xZ]||YY.default;try{let Q=DZ();if(Q&&typeof Q.then==="function")Q.catch((X)=>{console.error(`Error: ${X.message}`),process.exit(1)})}catch(Q){console.error(`Error: ${Q.message}`),process.exit(1)}
