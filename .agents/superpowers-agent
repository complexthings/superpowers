#!/bin/sh
// 2>/dev/null; exec "$(command -v bun 2>/dev/null || echo node)" "$0" "$@"
import{readFileSync as pQ,writeFileSync as cQ,existsSync as UQ}from"fs";import{join as l}from"path";import{execSync as iX}from"child_process";import{homedir as mQ,platform as mX}from"os";import{join as w,parse as hX,dirname as hQ}from"path";import{existsSync as u}from"fs";import{fileURLToPath as pX}from"url";var cX=pX(import.meta.url),qQ=hQ(cX),nX=()=>{let Q=mQ();switch(mX()){case"darwin":return w(Q,"Library","Application Support","Code","User");case"win32":return w(Q,"AppData","Roaming","Code","User");case"linux":return w(Q,".config","Code","User");default:return w(Q,".config","Code","User")}},lX=()=>{let Q=process.cwd(),X=hX(Q).root;while(Q!==X){if(u(w(Q,".agents")))return Q;Q=hQ(Q)}return process.cwd()},K={home:mQ(),vscodeUserDir:nX(),projectRoot:lX(),get projectClaudeSkills(){return w(this.projectRoot,".claude","skills")},get projectAgentsSkills(){return w(this.projectRoot,".agents","skills")},get projectCopilotSkills(){return w(this.projectRoot,".copilot","skills")},get projectOpencodeSkills(){return w(this.projectRoot,".opencode","skill")},get projectSkillsDir(){return w(this.projectRoot,"skills")},get homePersonalSkills(){return w(this.home,".agents","skills")},get homeClaudeSkills(){return w(this.home,".claude","skills")},get homeCopilotSkills(){return w(this.home,".copilot","skills")},get claudeSettings(){return w(this.home,".claude","settings.json")},get copilotHooksDir(){return process.env.COPILOT_HOME?w(process.env.COPILOT_HOME,"hooks"):w(this.home,".copilot","hooks")},get homeOpencodeSkills(){return w(this.home,".config","opencode","skill")},get bootstrap(){return w(this.projectRoot,".agents","superpowers-bootstrap.md")},get superpowersRepo(){let Q=w(qQ,"..","..","..");if(u(w(Q,"skills"))&&u(w(Q,".agents","superpowers-agent")))return Q;let X=w(qQ,"..");if(u(w(X,"skills"))&&u(w(X,".agents","superpowers-agent")))return X;return w(this.home,".agents","superpowers")},get homeSuperpowersSkills(){return w(this.superpowersRepo,"skills")},get isSuperpowersRepo(){let Q=w(qQ,"..","..","..");if(u(w(Q,"skills"))&&u(w(Q,".agents","superpowers-agent")))return!0;let X=w(qQ,"..");return u(w(X,"skills"))&&u(w(X,".agents","superpowers-agent"))}};var IQ=()=>({auto_update:!0,last_update_check:null,last_updated_commit:null}),XQ=()=>{let Q=l(K.superpowersRepo,".config.json");try{if(!UQ(Q))return IQ();let X=pQ(Q,"utf8");return{...IQ(),...JSON.parse(X)}}catch{return IQ()}},nQ=(Q)=>{let X=l(K.superpowersRepo,".config.json"),$={...XQ(),...Q};try{cQ(X,JSON.stringify($,null,2))}catch(Z){console.log(`Warning: couldn't save config: ${Z.message}`)}},f=(Q)=>{let X=Q?l(K.home,".agents","config.json"):l(process.cwd(),".agents","config.json");if(!UQ(X))return{};try{let Y=pQ(X,"utf8");return JSON.parse(Y)}catch(Y){return{}}},d=(Q,X)=>{let Y=X?l(K.home,".agents"):l(process.cwd(),".agents"),$=l(Y,"config.json");try{if(!UQ(Y))iX(`mkdir -p "${Y}"`,{stdio:"pipe"});return cQ($,JSON.stringify(Q,null,2),"utf8"),!0}catch(Z){throw Error(`Failed to write config: ${Z.message}`)}},g=(Q=!1)=>{return f(Q).repositories||{}},lQ=(Q,X,Y)=>{let $=f(Y);if(!$.repositories)$.repositories={};$.repositories[Q]=X,d($,Y)};function iQ(Q=!0){return f(Q).installedSkills||{}}function LQ(Q,X=!0){let Y=f(X);if(Y.installedSkills)delete Y.installedSkills[Q];d(Y,X)}function oQ(){return f(!0).installedAgents||{}}import{readFileSync as oX}from"fs";import{join as OQ,dirname as aX}from"path";import{fileURLToPath as rX}from"url";import{existsSync as aQ}from"fs";import{execSync as sX}from"child_process";var tX=rX(import.meta.url),wQ=aX(tX),k=()=>{try{let Q=OQ(wQ,"package.json");if(!aQ(Q))Q=OQ(wQ,"..","package.json");if(!aQ(Q))Q=OQ(wQ,"..","..","package.json");return JSON.parse(oX(Q,"utf8")).version||"0.0.0"}catch{return"0.0.0"}},i=()=>{let Q=k();console.log(`^^SAV:${Q}^^`)},zQ=async()=>{try{let X=sX('curl -sS "https://registry.npmjs.org/@complexthings/superpowers-agent/latest"',{encoding:"utf8",timeout:1e4}),Y=JSON.parse(X);if(!Y.version)return k();return Y.version}catch(Q){throw Error(`Failed to fetch remote version: ${Q.message}`)}},WQ=(Q,X)=>{let Y=Q.split(".").map(Number),$=X.split(".").map(Number);for(let Z=0;Z<3;Z++){let q=Y[Z]||0,H=$[Z]||0;if(q>H)return!0;if(q<H)return!1}return!1};var rQ=()=>{let Q=XQ();console.log("Current configuration:"),console.log(JSON.stringify(Q,null,2))},sQ=()=>{let Q=process.argv[3],X=process.argv[4];if(!Q||X===void 0){console.log(`Usage: .agents/superpowers-agent config-set <key> <value>

Available keys:
  auto_update (true/false) - Enable/disable automatic updates during bootstrap

Examples:
  .agents/superpowers-agent config-set auto_update false
  .agents/superpowers-agent config-set auto_update true`);return}let Y=X;if(X==="true")Y=!0;else if(X==="false")Y=!1;nQ({[Q]:Y}),console.log(`✓ Set ${Q} = ${Y}`)},tQ=()=>{i();let Q=g(!0),X=g(!1),Y=[];for(let[q,H]of Object.entries(Q))Y.push({alias:q,url:H,source:"global"});for(let[q,H]of Object.entries(X)){let z=Y.findIndex((E)=>E.alias===q);if(z>=0)Y[z]={alias:q,url:H,source:"project"};else Y.push({alias:q,url:H,source:"project"})}if(Y.length===0){console.log(`No repository aliases configured.

Add a repository using:
  superpowers-agent add-repository <git-url> [--as=@alias]`);return}let $=Math.max(5,...Y.map((q)=>q.alias.length)),Z=Math.max(3,...Y.map((q)=>q.url.length));console.log(`Repositories:
`),console.log(`${"Alias".padEnd($)}  ${"URL".padEnd(Z)}  Source`);for(let q of Y)console.log(`${q.alias.padEnd($)}  ${q.url.padEnd(Z)}  (${q.source})`);console.log(`
Total: ${Y.length} repository alias(es)`)};var HQ=async()=>{try{let Q=k(),X=await zQ();return{hasUpdates:WQ(X,Q),localVersion:Q,remoteVersion:X}}catch{return{hasUpdates:!1,localVersion:k(),remoteVersion:"",error:!0}}};import{execSync as eQ}from"child_process";var TQ=async({skipReinstall:Q=!1}={})=>{console.log(`# Checking for Superpowers updates...
`);let X=await HQ();if(X.error){console.log("⚠️  Could not check for updates (network issue)");return}if(!X.hasUpdates){console.log(`✓ Already up to date (v${X.localVersion})`);return}if(console.log(`\uD83D\uDCE6 Update available: v${X.localVersion} → v${X.remoteVersion}
`),Q){console.log("   Skipping install (--no-reinstall flag set)"),console.log(`   Run manually: npm install -g @complexthings/superpowers-agent
`);return}console.log(`   Installing update...
`);try{eQ("npm install -g @complexthings/superpowers-agent",{stdio:"inherit"}),console.log(`
✓ Update complete!`)}catch(Y){console.log(`
✗ Update failed: ${Y.message}`),console.log("   Try running manually: npm install -g @complexthings/superpowers-agent"),process.exit(1)}console.log(`
   Running bootstrap to complete setup...
`);try{eQ("superpowers-agent bootstrap --no-update",{stdio:"inherit"}),console.log(`
✓ Bootstrap complete!`)}catch(Y){console.log(`
⚠️  Bootstrap step failed: ${Y.message}`),console.log("   The update installed successfully. Run manually to finish setup:"),console.log("   superpowers-agent bootstrap")}},QX=async()=>{i();let Q=k();console.log(`Current version: ${Q}`);try{let X=await zQ();if(console.log(`Latest version: ${X}`),WQ(X,Q))console.log("Update available: Yes"),process.exit(1);else console.log("You are up to date"),process.exit(0)}catch(X){console.log(`Error checking for updates: ${X.message}`),process.exit(1)}},XX=()=>{let Q=k();console.log(Q)};import{existsSync as M,readFileSync as a,writeFileSync as t,rmSync as OX,lstatSync as kY}from"fs";import{join as N,dirname as wX,parse as s}from"path";import{execSync as S}from"child_process";import{platform as PY}from"os";import{execSync as eX}from"child_process";var YQ=(Q)=>{try{return eX(`which ${Q}`,{stdio:"pipe",timeout:2000}),!0}catch{return!1}};var R={opencode:{check:()=>YQ("opencode"),cli:!0,name:"OpenCode",installUrl:"https://opencode.ai/docs/installation",bootstrapCommand:"install-opencode-commands"},claude:{check:()=>YQ("claude"),cli:!0,name:"Claude Code",installUrl:"https://code.claude.com/docs/en/installation",bootstrapCommand:"install-claude-commands"},copilot:{check:()=>!0,cli:!1,name:"GitHub Copilot",bootstrapCommand:"install-copilot-prompts"}},YX=()=>{let Q=[];if(R.copilot.check())Q.push("github-copilot");if(R.claude.check())Q.push("claude-code");if(R.opencode.check())Q.push("opencode");return Q};import{existsSync as MQ,readdirSync as nZ,lstatSync as QY,symlinkSync as ZX,unlinkSync as XY,mkdirSync as YY,readlinkSync as ZY}from"fs";import{join as AQ,dirname as iZ}from"path";import{platform as $Y}from"os";var NQ=(Q)=>{try{return QY(Q).isSymbolicLink()}catch{return!1}},qY=(Q,X)=>{try{if(!NQ(Q))return!1;return ZY(Q)===X}catch{return!1}},$X=()=>{let Q=AQ(K.superpowersRepo,".opencode","plugins","superpowers-agent.js"),X=AQ(K.home,".config","opencode","plugins"),Y=AQ(X,"superpowers-agent.js");if(console.log("Installing OpenCode plugin symlink..."),!MQ(Q))return console.log("⚠️  Source plugin not found, skipping symlink creation"),console.log(`   Expected at: ${Q}`),{created:!1,error:"Source plugin not found"};if(qY(Y,Q))return console.log("✓ Plugin symlink already exists and is correct"),{created:!1,existed:!0};if(MQ(Y)&&!NQ(Y))return console.log("⚠️  Warning: A file already exists at the destination path"),console.log(`   Path: ${Y}`),console.log("   Skipping symlink creation to avoid overwriting existing file"),console.log("   To use superpowers-agent plugin, manually remove or rename the existing file"),{created:!1,error:"File already exists at destination"};if(!MQ(X))try{YY(X,{recursive:!0}),console.log(`✓ Created ${X.replace(K.home,"~")}`)}catch(Z){return console.log(`⚠️  Failed to create plugins directory: ${Z.message}`),{created:!1,error:`Failed to create directory: ${Z.message}`}}if(NQ(Y))try{XY(Y)}catch(Z){return console.log(`⚠️  Failed to remove existing symlink: ${Z.message}`),{created:!1,error:`Failed to remove existing symlink: ${Z.message}`}}let $=$Y();try{if($==="win32")ZX(Q,Y,"file");else ZX(Q,Y);let Z=Q.replace(K.home,"~"),q=Y.replace(K.home,"~");return console.log(`✓ Created symlink: ${q}`),console.log(`  -> ${Z}`),{created:!0}}catch(Z){if($==="win32"&&Z.code==="EPERM")return console.log("⚠️  Windows requires Developer Mode or admin privileges for symlinks"),console.log("   Enable Developer Mode: Settings > Update & Security > For developers"),{created:!1,error:"Windows symlink permission denied"};return console.log(`⚠️  Failed to create symlink: ${Z.message}`),{created:!1,error:`Failed to create symlink: ${Z.message}`}}};import{existsSync as qX,readdirSync as tZ,readFileSync as zY,writeFileSync as WY,mkdirSync as HY,copyFileSync as BY}from"fs";import{join as Q0,dirname as EY}from"path";var KY="superpowers-agent session-context",VY="superpowers-agent session-context --format=claude 2>/dev/null || true",GY="startup|resume|clear|compact",zX=(Q=K.claudeSettings)=>{let X={},Y=!1,$;if(qX(Q)){Y=!0;let V;try{V=zY(Q,"utf8")}catch(_){return{error:!0,message:`Failed to read settings.json: ${_.message}`}}try{X=V.trim()?JSON.parse(V):{}}catch(_){return{error:!0,message:`settings.json is not valid JSON, leaving it untouched: ${_.message}`}}let B=new Date().toISOString().split("T")[0];$=`${Q}.backup-${B}`;try{BY(Q,$)}catch(_){return{error:!0,message:`Failed to back up settings.json: ${_.message}`}}}if(typeof X!=="object"||X===null||Array.isArray(X))return{error:!0,message:"settings.json root is not an object"};X.hooks=X.hooks||{};let Z=Array.isArray(X.hooks.SessionStart)?X.hooks.SessionStart:[],q=(V)=>{try{return JSON.stringify(V).includes(KY)}catch{return!1}},H=Z.some(q),z=Z.filter((V)=>!q(V)),E={matcher:GY,hooks:[{type:"command",command:VY}]};X.hooks.SessionStart=[...z,E];let W=EY(Q);if(!qX(W))try{HY(W,{recursive:!0})}catch(V){return{error:!0,message:`Failed to create ${W}: ${V.message}`}}try{WY(Q,JSON.stringify(X,null,2)+`
`,"utf8")}catch(V){return{error:!0,message:`Failed to write settings.json: ${V.message}`}}if(!Y)return{created:!0};if(H)return{existed:!0,updated:!0,backup:$};return{updated:!0,backup:$}};import{existsSync as vQ,readdirSync as $0,readFileSync as _Y,writeFileSync as JY,mkdirSync as IY}from"fs";import{join as UY}from"path";var LY="superpowers.json",OY=()=>({version:1,hooks:{sessionStart:[{type:"command",bash:"superpowers-agent session-context --format=copilot 2>/dev/null || true",powershell:"superpowers-agent session-context --format=copilot 2>$null; exit 0",timeoutSec:15}]}}),WX=(Q=K.copilotHooksDir)=>{let X=UY(Q,LY),Y=JSON.stringify(OY(),null,2)+`
`;if(vQ(X)){let Z;try{Z=_Y(X,"utf8")}catch(q){return{error:!0,message:`Failed to read ${X}: ${q.message}`}}if(Z===Y)return{existed:!0,path:X}}if(!vQ(Q))try{IY(Q,{recursive:!0})}catch(Z){return{error:!0,message:`Failed to create ${Q}: ${Z.message}`}}let $=vQ(X);try{JY(X,Y,"utf8")}catch(Z){return{error:!0,message:`Failed to write ${X}: ${Z.message}`}}return $?{updated:!0,path:X}:{created:!0,path:X}};import{existsSync as o,readdirSync as wY,lstatSync as TY,symlinkSync as BQ,unlinkSync as bQ,mkdirSync as HX,readlinkSync as MY}from"fs";import{join as p,dirname as AY,basename as NY}from"path";import{homedir as c,platform as BX}from"os";var EQ=[{name:"claude",parentDir:()=>p(c(),".claude"),skillsDir:()=>p(c(),".claude","skills")},{name:"copilot",parentDir:()=>p(c(),".copilot"),skillsDir:()=>p(c(),".copilot","skills")},{name:"opencode",parentDir:()=>p(c(),".config","opencode"),skillsDir:()=>p(c(),".config","opencode","skill")}],r=(Q)=>{try{return TY(Q).isSymbolicLink()}catch{return!1}},EX=(Q,X)=>{try{if(!r(Q))return!1;return MY(Q)===X}catch{return!1}},KX=(Q,X)=>{if(!o(Q))return{created:!1,existed:!1,error:`Source does not exist: ${Q}`};if(EX(X,Q))return{created:!1,existed:!0};if(o(X)||r(X))if(r(X))try{bQ(X)}catch(Z){return{created:!1,existed:!1,error:`Failed to remove existing symlink: ${Z.message}`}}else return{created:!1,existed:!1,error:`Target exists and is not a symlink: ${X}`};let Y=AY(X);if(!o(Y))try{HX(Y,{recursive:!0})}catch(Z){return{created:!1,existed:!1,error:`Failed to create parent directory: ${Z.message}`}}let $=BX();try{if($==="win32")BQ(Q,X,"junction");else BQ(Q,X,"dir");return{created:!0,existed:!1}}catch(Z){if($==="win32"&&Z.code==="EPERM")return{created:!1,existed:!1,error:"Windows requires Developer Mode or admin privileges for symlinks. Enable Developer Mode: Settings > Update & Security > For developers"};return{created:!1,existed:!1,error:`Failed to create symlink: ${Z.message}`}}},FQ=(Q)=>{if(!o(Q)&&!r(Q))return{removed:!1,error:"Symlink does not exist"};if(!r(Q))return{removed:!1,error:"Target is not a symlink"};try{return bQ(Q),{removed:!0}}catch(X){return{removed:!1,error:`Failed to remove symlink: ${X.message}`}}};var VX=(Q,X,Y="skills")=>{let $=f(!0);if(!$.symlinks||!$.symlinks[Q])return;if(Y==="superpowers")$.symlinks[Q].superpowers=null;else{let Z=$.symlinks[Q].skills||[];$.symlinks[Q].skills=Z.filter((q)=>q!==X)}d($,!0)};var GX=(Q,X=[])=>{let Y;try{Y=wY(Q,{withFileTypes:!0})}catch{return X}let $=!1,Z=[];for(let q of Y){if(!q.isDirectory()&&q.name==="SKILL.md")$=!0;if(q.isDirectory()&&!q.name.startsWith("."))Z.push(p(Q,q.name))}if($)X.push(Q);for(let q of Z)GX(q,X);return X},_X=()=>{let Q=K.homeSuperpowersSkills,X=K.homePersonalSkills;if(!o(Q))return{created:0,existed:0,updated:0,errors:[`Skills directory not found: ${Q}`]};if(!o(X))try{HX(X,{recursive:!0})}catch(Z){return{created:0,existed:0,updated:0,errors:[`Failed to create ${X}: ${Z.message}`]}}let Y=GX(Q),$={created:0,existed:0,updated:0,errors:[]};for(let Z of Y){let q=NY(Z),H=p(X,q);if(EX(H,Z)){$.existed++;continue}if(r(H))try{bQ(H),$.updated++}catch(E){$.errors.push(`Failed to remove stale symlink ${H}: ${E.message}`);continue}else if(o(H)){$.errors.push(`Skipped ${H}: path exists and is not a symlink`);continue}let z=BX();try{if(z==="win32")BQ(Z,H,"junction");else BQ(Z,H,"dir");let E=H.replace(c(),"~"),W=Z.replace(c(),"~");console.log(`  ✓ ${E} -> ${W}`),$.created++}catch(E){$.errors.push(`Failed to create symlink ${H}: ${E.message}`)}}return $};import{existsSync as JX,readdirSync as vY,lstatSync as bY,unlinkSync as FY,realpathSync as IX}from"fs";import{join as j}from"path";import{homedir as UX}from"os";var CY=(Q)=>{try{return IX(Q)}catch{return Q}},DY=(Q)=>{try{return bY(Q).isSymbolicLink()}catch{return!1}},xY=(Q)=>{try{return IX(Q)}catch{return null}},jY=(Q,X)=>{let Y=X.endsWith("/")?X:X+"/";return Q.startsWith(Y)||Q===X},RY=(Q,X)=>X.some((Y)=>jY(Q,Y)),SY=(Q)=>{try{return JX(j(Q,"skill.json"))}catch{return!1}},yY=({platformDirs:Q,managedSkillRoots:X})=>{let Y=[],$=0,Z=X.map(CY);for(let q of Q){if(!JX(q))continue;let H;try{H=vY(q)}catch{continue}for(let z of H){let E=j(q,z);if(!DY(E))continue;let W=xY(E);if(!W||!RY(W,Z)){$++;continue}if(!SY(W)){$++;continue}try{FY(E),Y.push(E)}catch{$++}}}return{removed:Y,skipped:$}},fY=(Q)=>{let X=UX(),Y=[j(X,".claude","skills"),j(X,".copilot","skills"),j(X,".config","opencode","skill"),j(X,".cursor","skills"),j(X,".gemini","skills"),j(X,".codex","skills")];if(!Q)return Y;let $=[j(Q,".claude","skills"),j(Q,".github","skills"),j(Q,".opencode","skill"),j(Q,".cursor","skills"),j(Q,".gemini","skills"),j(Q,".codex","skills")];return[...Y,...$]},LX=(Q={})=>{let X=UX(),Y=[j(X,".agents","skills"),...Q.projectRoot?[j(Q.projectRoot,".agents","skills")]:[]],$=fY(Q.projectRoot);return yY({platformDirs:$,managedSkillRoots:Y})};var KQ=(Q,X,Y,$=!0)=>{let Z=M(Q);if(!Z&&!$)return{updated:!1,created:!1,skipped:!0};let q=X,H=new Date().toISOString().split("T")[0];q=q.replace(/\{\{DATE\}\}/g,H),q=q.replace(/\{\{SUPERPOWERS_PATH\}\}/g,K.superpowersRepo);let z=`<!-- SUPERPOWERS_SKILLS_START -->
${q}
<!-- SUPERPOWERS_SKILLS_END -->`;if(Z){let E=new Date().toISOString().split("T")[0],W=`${Q}.backup-${E}`;try{S(`cp "${Q}" "${W}"`,{stdio:"pipe"})}catch(U){return{updated:!1,error:!0,message:`Failed to backup: ${U.message}`}}let V;try{V=a(Q,"utf8")}catch(U){return{updated:!1,error:!0,message:`Failed to read: ${U.message}`}}let B="<!-- SUPERPOWERS_SKILLS_START -->",_="<!-- SUPERPOWERS_SKILLS_END -->",J;if(V.includes(B)&&V.includes(_)){let U=new RegExp(`${B}[\\s\\S]*?${_}`,"g");J=V.replace(U,z)}else{let U=V.match(/^#\s+.+$/m);if(U){let I=V.indexOf(`
`,U.index)+1;J=V.slice(0,I)+`
`+z+`
`+V.slice(I)}else J=z+`

`+V}try{return t(Q,J,"utf8"),{updated:!0,created:!1,backup:W}}catch(U){return{updated:!1,error:!0,message:`Failed to write: ${U.message}`}}}else try{let E=wX(Q);if(!M(E))S(`mkdir -p "${E}"`,{stdio:"pipe"});let B=`# ${s(Q).base.toUpperCase()}

`+z;return t(Q,B,"utf8"),{updated:!1,created:!0}}catch(E){return{updated:!1,created:!1,error:!0,message:`Failed to create: ${E.message}`}}},gY=()=>{console.log("Installing Unix aliases...");let Q=N(K.superpowersRepo,".agents","superpowers-agent"),X=N(K.home,".local","bin"),Y=N(X,"superpowers-agent"),$=N(X,"superpowers");if(!M(Q)){console.log("⚠️  superpowers-agent executable not found");return}if(!M(X))try{S(`mkdir -p "${X}"`,{stdio:"pipe"}),console.log(`✓ Created ${X}`)}catch(q){console.log(`⚠️  Failed to create ${X}: ${q.message}`);return}try{if(M(Y))S(`rm "${Y}"`,{stdio:"pipe"});S(`ln -s "${Q}" "${Y}"`,{stdio:"pipe"}),console.log(`✓ Created symlink: superpowers-agent -> ${Q}`)}catch(q){console.log(`⚠️  Failed to create superpowers-agent symlink: ${q.message}`)}try{if(M($))S(`rm "${$}"`,{stdio:"pipe"});S(`ln -s "${Q}" "${$}"`,{stdio:"pipe"}),console.log(`✓ Created symlink: superpowers -> ${Q}`)}catch(q){console.log(`⚠️  Failed to create superpowers symlink: ${q.message}`)}if(!(process.env.PATH||"").includes(X))console.log(`
⚠️  Warning: ${X} is not in your PATH`),console.log("   Add this to your shell profile (~/.bashrc, ~/.zshrc, etc.):"),console.log(`   export PATH="$HOME/.local/bin:$PATH"
`)},uY=()=>{console.log("Installing Windows aliases..."),console.log("✓ Windows alias installation (implementation pending)")},CQ=()=>{let Q=PY();if(console.log(`## Installing Universal Aliases
`),Q==="win32")return uY();else return gY()},dY=(Q)=>{let X=N(K.superpowersRepo,".github","copilot-instructions.md"),Y=N(K.superpowersRepo,"skills","meta","using-superpowers","SKILL.md"),$=N(Q,".github","copilot-instructions.md"),Z="<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->",q="<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->";if(!M(X))return{error:!0,message:"Source template not found"};if(!M(Y))return{error:!0,message:"using-superpowers SKILL.md not found"};let H,z;try{H=a(X,"utf8"),z=a(Y,"utf8")}catch(V){return{error:!0,message:`Failed to read source files: ${V.message}`}}let E=H.replace("${content}",z);if(!E.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->")||!E.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->"))return{error:!0,message:"Template is missing required markers"};let W=wX($);try{if(!M(W))S(`mkdir -p "${W}"`,{stdio:"pipe"})}catch(V){return{error:!0,message:`Failed to create .github directory: ${V.message}`}}if(M($)){let V;try{V=a($,"utf8")}catch(J){return{error:!0,message:`Failed to read existing file: ${J.message}`}}let B=new Date().toISOString().replace(/[:.]/g,"-"),_=`${$}.backup-${B}`;try{S(`cp "${$}" "${_}"`,{stdio:"pipe"})}catch(J){return{error:!0,message:`Failed to backup: ${J.message}`}}if(V.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->")&&V.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->")){let J=new RegExp("<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->[\\s\\S]*?<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->","g"),U=V.replace(J,E.trim());try{return t($,U,"utf8"),{updated:!0,backup:_}}catch(I){return{error:!0,message:`Failed to update: ${I.message}`}}}else{let J=V.trimEnd()+`

`+E.trim()+`
`;try{return t($,J,"utf8"),{updated:!0,backup:_}}catch(U){return{error:!0,message:`Failed to append: ${U.message}`}}}}else try{return t($,E,"utf8"),{created:!0}}catch(V){return{error:!0,message:`Failed to create: ${V.message}`}}},TX=()=>{i(),console.log(`# Setting up Superpowers skills for this project
`);let Q=process.cwd(),X=N(Q,".agents"),Y=N(X,"skills"),$=N(Q,"AGENTS.md"),Z=N(X,"AGENTS.md"),q=M($)?$:Z,H=N(K.superpowersRepo,".agents","templates","AGENTS.md.template");if(!M(H)){console.log(`✗ Error: AGENTS.md template not found
  Expected at: ${H}
  Please update your Superpowers installation`);return}if(!M(X))try{S(`mkdir -p "${X}"`,{stdio:"pipe"}),console.log("✓ Created .agents/ directory")}catch(O){console.log(`✗ Failed to create .agents/ directory: ${O.message}`);return}else console.log("✓ .agents/ directory exists");if(!M(Y))try{S(`mkdir -p "${Y}"`,{stdio:"pipe"}),S(`touch "${N(Y,".gitkeep")}"`,{stdio:"pipe"}),console.log("✓ Created .agents/skills/ directory")}catch(O){console.log(`✗ Failed to create skills directory: ${O.message}`);return}else console.log("✓ .agents/skills/ directory exists");let z=N(X,"docs"),E=N(z,"SUPERPOWERS.md"),W=N(K.superpowersRepo,".agents","templates","SUPERPOWERS.md.template");if(!M(z))try{S(`mkdir -p "${z}"`,{stdio:"pipe"}),console.log("✓ Created .agents/docs/ directory")}catch(O){console.log(`⚠️  Failed to create docs directory: ${O.message}`)}else console.log("✓ .agents/docs/ directory exists");let V=M(q);if(V){let O=new Date().toISOString().split("T")[0],h=`${q}.backup-${O}`;try{S(`cp "${q}" "${h}"`,{stdio:"pipe"}),console.log(`✓ Backed up existing AGENTS.md to ${s(h).base}`)}catch($Q){console.log(`✗ Failed to backup AGENTS.md: ${$Q.message}`);return}}let B;try{B=a(H,"utf8")}catch(O){console.log(`✗ Failed to read template: ${O.message}`);return}let _=k();if(B=B.replace(/\{\{VERSION\}\}/g,_),M(W))try{let O=a(W,"utf8"),h=new Date().toISOString().split("T")[0];O=O.replace(/\{\{VERSION\}\}/g,_),O=O.replace(/\{\{DATE\}\}/g,h),t(E,O,"utf8"),console.log("✓ Created .agents/docs/SUPERPOWERS.md")}catch(O){console.log(`⚠️  Failed to create SUPERPOWERS.md: ${O.message}`)}else console.log("⚠️  SUPERPOWERS.md.template not found");let J=[],U=N(Q,".github","copilot-instructions.md"),I=N(K.home,".github","copilot-instructions.md");if(M(U)||M(I)||R.copilot.check())J.push("github-copilot");let T=N(Q,"CLAUDE.md"),F=N(X,"CLAUDE.md");if(M(T)||M(F)||R.claude.check())J.push("claude-code");if(R.opencode.check())J.push("opencode");console.log(`
Detected platforms for project: ${J.join(", ")||"none"}
`);let y=J.filter((O)=>["github-copilot","opencode"].includes(O)),v=KQ(q,B,y,!V);if(v.created)console.log(`✓ Created AGENTS.md with platform tool mappings (${q===$?"root":".agents/"})`);else if(v.updated)console.log(`✓ Updated AGENTS.md with platform tool mappings (${q===$?"root":".agents/"})`);else if(v.error)console.log("⚠️  Failed to update AGENTS.md");let n=M(T)?T:F,G=KQ(n,B,["claude-code"],!1);if(G.created)console.log(`✓ Created CLAUDE.md with Claude Code tool mappings (${n===T?"root":".agents/"})`);else if(G.updated){if(console.log(`✓ Updated CLAUDE.md with Claude Code tool mappings (${n===T?"root":".agents/"})`),G.backup)console.log(`  Backed up to ${s(G.backup).base}`)}else if(G.skipped)console.log("ℹ️  Skipped CLAUDE.md (does not exist)");else if(G.error)console.log("⚠️  Failed to update CLAUDE.md");let L=N(Q,".github","copilot-instructions.md"),C={skipped:!0};if(J.includes("github-copilot")){if(C=dY(Q),C.created)console.log("✓ Created .github/copilot-instructions.md with Superpowers instructions");else if(C.updated){if(console.log("✓ Updated .github/copilot-instructions.md with Superpowers instructions"),C.backup)console.log(`  Backed up to ${s(C.backup).base}`)}else if(C.error)console.log(`⚠️  Failed to update .github/copilot-instructions.md: ${C.message||""}`)}else console.log("ℹ️  Skipped .github/copilot-instructions.md (GitHub Copilot not detected)");let x=!M($)&&!M(Z);if(M(L)&&x){let O=KQ(L,B,["github-copilot"],!1);if(O.updated){if(console.log("✓ Updated .github/copilot-instructions.md with GitHub Copilot tool mappings"),O.backup)console.log(`  Backed up to ${s(O.backup).base}`)}else if(O.error)console.log("⚠️  Failed to update .github/copilot-instructions.md with tool mappings")}else if(M(L)&&!x)console.log("ℹ️  Skipped .github/copilot-instructions.md tool mappings (AGENTS.md exists, using that instead)");else if(!M(L)&&!J.includes("github-copilot"))console.log("ℹ️  Skipped .github/copilot-instructions.md tool mappings (does not exist)");let P=`
# Setup complete!

Your project now has:
  - .agents/ directory structure`;if(v.updated||v.created)P+=`
  - AGENTS.md with universal skills instructions`;if(G.updated||G.created)P+=`
  - CLAUDE.md with Claude Code skills instructions`;if(C.updated||C.created)P+=`
  - .github/copilot-instructions.md with Superpowers instructions`;P+=`
  - .agents/skills/ ready for project-specific skills`,P+=`
  - .agents/docs/SUPERPOWERS.md for detailed reference
`,console.log(P)},mY=()=>{let Q=[...["brainstorming.prompt.md","execute-plan.prompt.md","write-plan.prompt.md","setup-skills.prompt.md","create-meta-prompt.md","finding-skills.prompt.md","skills.prompt.md","use-skill.prompt.md","using-a-skill.prompt.md"].map((Y)=>N(K.vscodeUserDir,"prompts",Y)),...["brainstorm.md","create-meta-prompt.md","execute-plan.md","finding-skills.md","skills.md","use-skill.md","using-a-skill.md","write-plan.md","setup-skills.md"].map((Y)=>N(K.home,".claude","commands",Y)),...["brainstorm.md","create-meta-prompt.md","execute-plan.md","finding-skills.md","skills.md","use-skill.md","using-a-skill.md","write-plan.md","setup-skills.md"].map((Y)=>N(K.home,".config","opencode","command",Y))],X=0;for(let Y of Q)if(M(Y))try{OX(Y),X++}catch($){console.log(`  ⚠️  Could not remove ${Y}: ${$.message}`)}if(X>0)console.log(`✓ Removed ${X} legacy prompt/command file${X!==1?"s":""}`);else console.log("✓ No legacy prompt/command files found")},hY=()=>{let Q=0;for(let X of EQ){let Y=X.skillsDir(),$=N(Y,"superpowers");try{if(kY($).isSymbolicLink())OX($),console.log(`✓ Removed legacy superpowers symlink from ${Y.replace(K.home,"~")}`),Q++}catch{}}if(Q===0)console.log("✓ No legacy superpowers directory symlinks found")},MX=async()=>{i(),console.log(`# Superpowers Bootstrap for Agents
# ==================================
`);let Q=process.argv.includes("--no-update"),Y=new Set(["copilot","claude","opencode"].filter((H)=>process.argv.includes(`--force-${H}`))),$=Y.size>0;if(!Q){let H=XQ(),z=await HQ();if(z.error)console.log(`## Update Check

⚠️  Could not check for updates (network issue)

---
`);else if(z.hasUpdates)console.log(`## Update Available
`),console.log(`⚠️  Update available: v${z.localVersion} → v${z.remoteVersion}
    To update, run: \`npm install -g @complexthings/superpowers-agent\``),console.log(`
---
`)}if(!$)CQ(),console.log(`---
`);if(!$)console.log(`## Cleaning Up Legacy Files
`),mY(),console.log(`
---
`);if(!$||Y.has("copilot")){if(console.log(`## GitHub Copilot Integration
`),console.log("✓ Skill symlinks handled in sync step below"),YQ("copilot")||M(N(K.home,".copilot"))||Y.has("copilot")){let z=WX();if(z.created)console.log(`✓ Installed Copilot sessionStart hook -> ${z.path.replace(K.home,"~")}`);else if(z.updated)console.log(`✓ Updated Copilot sessionStart hook -> ${z.path.replace(K.home,"~")}`);else if(z.existed)console.log("✓ Copilot sessionStart hook already up to date");else if(z.error)console.log(`⚠️  Failed to install Copilot sessionStart hook: ${z.message}`)}else console.log("ℹ️  Skipped Copilot sessionStart hook (Copilot CLI not detected; run with --force-copilot to install anyway)");console.log(`
---
`)}if(!$||Y.has("claude")){if(console.log(`## Claude Code Integration
`),!R.claude.check()&&!Y.has("claude"))console.log(`⚠️  Skipped (${R.claude.name} CLI not detected)
\uD83D\uDCA1 To enable Claude Code integration:
   1. Install Claude Code: ${R.claude.installUrl}
   2. Run: superpowers-agent bootstrap --force-claude`);else{console.log("✓ Skill symlinks handled in sync step below");let z=zX();if(z.created)console.log(`✓ Installed SessionStart hook -> ${K.claudeSettings.replace(K.home,"~")}`);else if(z.updated){if(console.log(`✓ ${z.existed?"Refreshed":"Added"} SessionStart hook in ${K.claudeSettings.replace(K.home,"~")}`),z.backup)console.log(`  Backed up to ${s(z.backup).base}`)}else if(z.error)console.log(`⚠️  Failed to install SessionStart hook: ${z.message}`)}console.log(`
---
`)}if(!$||Y.has("opencode")){if(console.log(`## OpenCode Integration
`),R.opencode.check())$X();else console.log(`⚠️  Skipped (${R.opencode.name} CLI not detected)
\uD83D\uDCA1 To enable OpenCode integration:
   1. Install OpenCode: ${R.opencode.installUrl}
   2. Run: superpowers-agent ${R.opencode.bootstrapCommand}`);console.log(`
---
`)}if(!$){console.log(`## Generating Platform-Specific Files
`);let H=YX();console.log(`Detected platforms: ${H.join(", ")||"none"}
`);let z=N(K.superpowersRepo,".agents","templates","AGENTS.md.template"),E="";try{E=a(z,"utf8")}catch(W){console.log(`⚠️  Could not read AGENTS.md template: ${W.message}
`)}if(E){let W=k();E=E.replace(/\{\{VERSION\}\}/g,W);let V=N(K.home,".agents","AGENTS.md"),B=KQ(V,E,H,!0);if(B.created)console.log(`✓ Created ${V}`);else if(B.updated)console.log(`✓ Updated ${V}`)}console.log(`
---
`)}console.log(`## Cleaning Up Legacy Superpowers Dir Symlinks
`),hY(),console.log(`
---
`),console.log(`## Syncing Repo Skills -> ~/.agents/skills/
`);let Z=_X();if(Z.created>0||Z.updated>0)console.log(`  ✓ ${Z.created} created, ${Z.updated} updated, ${Z.existed} already current`);else if(Z.errors.length>0)for(let H of Z.errors)console.log(`  ⚠️  ${H}`);else console.log(`  ✓ ${Z.existed} skill symlinks already up to date`);console.log(`
---
`),console.log(`## Cleaning Up Stale Platform Skill Symlinks
`);let q=LX({projectRoot:process.cwd()});if(q.removed.length>0){console.log(`  ✓ Removed ${q.removed.length} stale skill symlink(s):`);for(let H of q.removed)console.log(`    - ${H}`)}else console.log("  ✓ No stale platform skill symlinks found");console.log(`
---
`),console.log(`# Bootstrap Complete!
`),console.log("✓ All integrations installed"),console.log("✓ Skills system ready"),console.log(`✓ Skill symlinks synced
`),console.log("Next steps:"),console.log("  - Use your platform's native skill tool to see available skills"),console.log("  - Run `superpowers-agent setup-skills` in your project directory")};import{existsSync as b,readFileSync as GQ,mkdirSync as sY,rmSync as fQ,cpSync as tY,lstatSync as eY}from"fs";import{join as A,dirname as _Q,parse as ZQ,sep as QZ}from"path";import{execSync as D}from"child_process";import{homedir as m}from"os";import{createInterface as XZ}from"readline";import{readFileSync as pY,existsSync as RQ,unlinkSync as cY,lstatSync as nY,mkdirSync as lY}from"fs";import{join as iY,dirname as oY}from"path";import{join as e}from"path";import{homedir as DQ}from"os";var xQ={github:{name:"github",sourceDir:".github/agents",sourceExt:".agent.md",getDestDir:()=>e(K.vscodeUserDir,"prompts"),getExtraDestDirs:()=>[e(DQ(),".copilot","agents")],destExt:".agent.md"},opencode:{name:"opencode",sourceDir:".opencode/agents",sourceExt:".md",getDestDir:()=>e(DQ(),".config","opencode","agents"),destExt:".md"},claude:{name:"claude",sourceDir:".claude/agents",sourceExt:".md",getDestDir:()=>e(DQ(),".claude","agents"),destExt:".md"}};function AX(Q,X,Y){let $=xQ[X];if(!$)return null;return e(Q,$.sourceDir,`${Y}${$.sourceExt}`)}function NX(Q,X){let Y=xQ[Q];if(!Y)return[];return[Y.getDestDir(),...Y.getExtraDestDirs?Y.getExtraDestDirs():[]].map((Z)=>e(Z,`${X}${Y.destExt}`))}function jQ(){return Object.keys(xQ)}function aY(Q){if(!Q||typeof Q!=="object")return{valid:!1,error:"agents.json must be a JSON object"};if(!Q.version||typeof Q.version!=="string")return{valid:!1,error:'agents.json must have a "version" string field'};if(!Q.agents||typeof Q.agents!=="object")return{valid:!1,error:'agents.json must have an "agents" object field'};let X=jQ(),Y=[];for(let[$,Z]of Object.entries(Q.agents)){if(!X.includes($)){Y.push($);continue}if(!Array.isArray(Z))return{valid:!1,error:`agents.${$} must be an array of agent names`};for(let q of Z)if(typeof q!=="string")return{valid:!1,error:`agents.${$} must contain only string agent names`}}return{valid:!0,data:Q,skippedPlatforms:Y}}function rY(Q,X){if(!RQ(Q))return{installed:!1,error:`Source file not found: ${Q}`};if(RQ(X))try{if(!nY(X).isSymbolicLink())cY(X)}catch($){}lY(oY(X),{recursive:!0});let Y=KX(Q,X);if(Y.error)return{installed:!1,error:Y.error};return{installed:!0}}function SQ(Q,X={}){let Y=iY(Q,"agents.json");if(!RQ(Y))return;let $;try{$=JSON.parse(pY(Y,"utf8"))}catch(J){console.log(`
  ⚠ Could not parse agents.json: ${J.message}`);return}let Z=aY($);if(!Z.valid){console.log(`
  ⚠ Invalid agents.json: ${Z.error}`);return}for(let J of Z.skippedPlatforms)console.log(`  ⚠ Skipping unknown platform: ${J}`);let q=$.repository||"unknown",H=$.version,z=X.isUpdate?"Updating":"Installing",E=X.isUpdate?"Updated":"Installed";console.log(`
  \uD83D\uDCE6 Found agents.json (${q} v${H})`);let W=jQ(),V=0,B=0,_=[];for(let[J,U]of Object.entries($.agents)){if(!W.includes(J))continue;console.log(`
  ${z} ${J} agents:`);for(let I of U){let T=AX(Q,J,I),F=NX(J,I),y=[],v=[];for(let n of F){let G=rY(T,n);if(G.installed)y.push(n);else v.push(G.error)}if(y.length>0)console.log(`    ✓ ${I}`),_.push({platformKey:J,agentName:I,sourcePath:T,destPaths:y}),V++;else console.log(`    ✗ ${I}: ${v.join("; ")}`),B++}}if(_.length>0){let J=f(!0);if(!J.installedAgents)J.installedAgents={};if(!J.installedAgents[q])J.installedAgents[q]={version:H,agents:{}};J.installedAgents[q].version=H;for(let{platformKey:U,agentName:I,sourcePath:T,destPaths:F}of _){if(!J.installedAgents[q].agents[U])J.installedAgents[q].agents[U]={};J.installedAgents[q].agents[U][I]={source:T,destination:F[0],destinations:F,installedAt:new Date().toISOString()}}d(J,!0)}if(V>0)console.log(`
  ${E} ${V} agent(s)${B>0?` (${B} failed)`:""}`)}var vX=(Q,X)=>{if(X.type==="local"){let $=Q;for(let Z=0;Z<10;Z++){if(b(A($,"agents.json")))return $;let q=_Q($);if(q===$)break;$=q}return Q}let Y=A(".agents","tmp");if(Q.includes(Y)){let $=Q.indexOf(Y),q=Q.substring($+Y.length+1).split(QZ)[0];return A(Q.substring(0,$),".agents","tmp",q)}return Q},bX=(Q,X)=>{let Y=A(m(),".agents","repos"),$=X.replace(/^@/,"").replace(/[^a-zA-Z0-9_-]/g,"_"),Z=A(Y,$);try{if(sY(Y,{recursive:!0}),b(Z))fQ(Z,{recursive:!0,force:!0});return tY(Q,Z,{recursive:!0}),Z}catch(q){return console.log(`  Warning: Could not persist repository for agents: ${q.message}`),null}},kQ=(Q)=>{let X=Q.match(/^https:\/\/github\.com\/([^/]+)\/([^/]+)\/tree\/([^/]+)\/(.+)$/);if(X){let[,Z,q,H,z]=X;return{type:"git-tree",repoUrl:`https://github.com/${Z}/${q}.git`,branch:H,path:z,original:Q}}let Y=Q.match(/^git@github\.com:([^/]+)\/([^/]+?)(?:\.git)?$/);if(Y){let[,Z,q]=Y;return{type:"git-repo",repoUrl:`git@github.com:${Z}/${q}.git`,branch:null,path:null,original:Q}}let $=Q.match(/^https:\/\/github\.com\/([^/]+)\/([^/]+?)(?:\.git)?$/);if($){let[,Z,q]=$;return{type:"git-repo",repoUrl:`https://github.com/${Z}/${q}.git`,branch:null,path:null,original:Q}}if(b(Q))return{type:"local",path:Q,original:Q};return null},PQ=(Q)=>{let X=Q.includes("--global")||Q.includes("-g"),Y=Q.includes("--project")||Q.includes("-p");if(X)return A(m(),".agents","skills");if(Y)return A(process.cwd(),".agents","skills");let $=A(process.cwd(),".agents","config.json"),Z=A(m(),".agents","config.json");for(let q of[$,Z])if(b(q))try{if(JSON.parse(GQ(q,"utf8")).installLocation==="project")return A(process.cwd(),".agents","skills")}catch(H){}return A(m(),".agents","skills")},gQ=(Q,X)=>{let Y=A(m(),".agents","tmp",`skill-install-${Date.now()}`);try{if(D(`mkdir -p "${Y}"`,{stdio:"pipe"}),X)D(`git clone --branch ${X} --depth 1 "${Q}" "${Y}"`,{stdio:"pipe",timeout:30000});else D(`git clone --depth 1 "${Q}" "${Y}"`,{stdio:"pipe",timeout:30000});return Y}catch($){try{D(`rm -rf "${Y}"`,{stdio:"pipe"})}catch{}throw Error(`Failed to clone repository: ${$.message}`)}},QQ=(Q)=>{let X=A(Q,"skill.json");if(!b(X))return null;try{let Y=GQ(X,"utf8");return JSON.parse(Y)}catch(Y){throw Error(`Failed to read skill.json: ${Y.message}`)}},VQ=(Q,X,Y,$)=>{let Z=A(Q,X);if(!b(Z)){$.errors.push(`Skill directory not found: ${X}`);return}let q=QQ(Z);if(!q){$.errors.push(`No skill.json found in: ${X}`);return}let H=q.name||X,z=H.split("/").pop()||H,E=A(Y,z),W=_Q(E);try{D(`mkdir -p "${W}"`,{stdio:"pipe"})}catch(V){$.errors.push(`Failed to create directory ${W}: ${V.message}`);return}try{if(b(E))D(`rm -rf "${E}"`,{stdio:"pipe"});D(`cp -R "${Z}" "${E}"`,{stdio:"pipe"}),$.installed.push({name:z,path:E,title:q.title||q.description||z})}catch(V){$.errors.push(`Failed to install ${X}: ${V.message}`)}},FX=(Q,X,Y,$,Z=null)=>{let q=f($);if(!q.installedSkills)q.installedSkills={};let H={source:Q,installBase:X,installedAt:new Date().toISOString(),skills:Y.installed.map((z)=>({name:z.name,path:z.path}))};if(q.installedSkills[Q]=H,Z&&Z!==Q)q.installedSkills[Z]=H;d(q,$)},CX=()=>{let Q=process.argv.slice(3),X=Q.find((B)=>!B.startsWith("-")),Y=Q.filter((B)=>B.startsWith("-"));if(!X){console.log(`Usage: superpowers-agent add <url-or-path|@alias> [skill-path] [options]

Options:
  --global, -g   Install skills globally in ~/.agents/skills/ (default)
  --project, -p  Install skills in current project's .agents/skills/

Examples:
  superpowers-agent add https://github.com/example/skills
  superpowers-agent add https://github.com/example/repo/tree/main/skills
  superpowers-agent add ~/my-local-skills
  superpowers-agent add https://github.com/example/skills --project
  superpowers-agent add ~/my-local-skills --global
  superpowers-agent add @myrepo path/to/skill
  superpowers-agent add @myrepo path/to/skills --project

Description:
  Installs skill(s) from a Git repository, local directory, or repository alias.
  Supports repositories with single or multiple skills.
  Reads skill.json to determine skill names and installation paths.

  Repository aliases can be added using:
    superpowers-agent add-repository <git-url> [--as=@alias]`);return}console.log(`Installing skill(s)...
`);let $=!Y.includes("--project")&&!Y.includes("-p"),Z=null,q=null,H=null;if(X.startsWith("@")){let B=X;H=B,q=Q.find((I,T)=>T>0&&!I.startsWith("-")&&I!==B);let _=g(!1),J=g(!0);if(_[B])Z=_[B],console.log(`Using project repository alias: ${B}`);else if(J[B])Z=J[B],console.log(`Using global repository alias: ${B}`);else{console.log(`Error: Repository alias not found: ${B}`),console.log(`
Available aliases:`);let I={...J,..._};if(Object.keys(I).length===0)console.log("  (none)"),console.log(`
Add a repository using:`),console.log("  superpowers-agent add-repository <git-url>");else for(let[T,F]of Object.entries(I))console.log(`  ${T} -> ${F}`);return}let U=b(Z);if(q)if(U)X=A(Z,q),console.log(`Repository path: ${Z}`),console.log(`Skill path: ${q}
`);else X=Z,console.log(`Repository URL: ${Z}`),console.log(`Skill path: ${q}
`);else if(X=Z,U)console.log(`Repository path: ${Z}
`);else console.log(`Repository URL: ${Z}
`)}let z=kQ(X);if(!z){console.log(`Error: Invalid URL or path not found: ${X}`);return}if(q&&z.type==="git-repo"&&!z.path)z.path=q;let E=PQ(Y);console.log(`Install location: ${E}
`);let W,V=!1;try{if(z.type==="git-repo"||z.type==="git-tree"){if(console.log(`Cloning repository: ${z.repoUrl}`),z.branch)console.log(`Branch: ${z.branch}`);if(W=gQ(z.repoUrl,z.branch),V=!0,z.path){if(W=A(W,z.path),!b(W))throw Error(`Path not found in repository: ${z.path}`)}console.log("")}else W=z.path;let B=QQ(W);if(!B)throw Error("No skill.json found in source directory");let _={installed:[],errors:[],source:z.original};if(B.skills&&Array.isArray(B.skills)){console.log(`Found ${B.skills.length} skill(s) to install
`);for(let I of B.skills)VQ(W,I,E,_)}else{let I=B.name||ZQ(W).base;VQ(_Q(W),ZQ(W).base,E,_)}let J=vX(W,z),U=A(J,"agents.json");if(b(U)){let I=J;if(V){let T;try{T=JSON.parse(GQ(U,"utf8"))}catch{}let F=T?.repository||z.original,y=bX(J,F);if(y)I=y}SQ(I,{isUpdate:!1})}if(V&&W)try{let I=W.split("/.agents/tmp/")[0]+"/.agents/tmp/"+W.split("/.agents/tmp/")[1].split("/")[0];D(`rm -rf "${I}"`,{stdio:"pipe"})}catch{}if(console.log(`
**Successfully installed skills:**`),console.log(`- Source: ${_.source}`),_.installed.length>0)for(let I of _.installed)console.log(`  - Installed: ${I.name} at ${I.path}`),console.log(`    ${I.title}`);if(_.errors.length>0){console.log(`
**Errors:**`);for(let I of _.errors)console.log(`  - ${I}`)}if(_.installed.length===0&&_.errors.length===0)console.log("  No skills were installed");if(_.installed.length>0)FX(z.original,E,_,$,H)}catch(B){if(V&&W)try{let _=W.split("/.agents/tmp/")[0]+"/.agents/tmp/"+W.split("/.agents/tmp/")[1].split("/")[0];D(`rm -rf "${_}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${B.message}`)}},DX=()=>{let Q=process.argv.slice(3),X=Q.find((B)=>!B.startsWith("-")),Y=Q.filter((B)=>B.startsWith("-"));if(!X){console.log(`Usage: superpowers-agent pull <url-or-path|@alias> [skill-path] [options]

Options:
  --global, -g   Update skills globally in ~/.agents/skills/ (default)
  --project, -p  Update skills in current project's .agents/skills/

Examples:
  superpowers-agent pull https://github.com/example/skills
  superpowers-agent pull https://github.com/example/repo/tree/main/skills
  superpowers-agent pull ~/my-local-skills
  superpowers-agent pull https://github.com/example/skills --project
  superpowers-agent pull ~/my-local-skills --global
  superpowers-agent pull @myrepo
  superpowers-agent pull @myrepo path/to/skill
  superpowers-agent pull @myrepo path/to/skills --project

Description:
  Updates or adds skill(s) from a Git repository, local directory, or repository alias.
  If skills already exist, they will be updated (replaced).
  If skills don't exist, they will be added.
  Supports repositories with single or multiple skills.
  Reads skill.json to determine skill names and installation paths.
  
  Repository aliases can be added using:
    superpowers-agent add-repository <git-url> [--as=@alias]`);return}console.log(`Updating skill(s)...
`);let $=!Y.includes("--project")&&!Y.includes("-p"),Z=null,q=null,H=null;if(X.startsWith("@")){let B=X;H=B,q=Q.find((I,T)=>T>0&&!I.startsWith("-")&&I!==B);let _=g(!1),J=g(!0);if(_[B])Z=_[B],console.log(`Using project repository alias: ${B}`);else if(J[B])Z=J[B],console.log(`Using global repository alias: ${B}`);else{console.log(`Error: Repository alias not found: ${B}`),console.log(`
Available aliases:`);let I={...J,..._};if(Object.keys(I).length===0)console.log("  (none)"),console.log(`
Add a repository using:`),console.log("  superpowers-agent add-repository <git-url>");else for(let[T,F]of Object.entries(I))console.log(`  ${T} -> ${F}`);return}let U=b(Z);if(q)if(U)X=A(Z,q),console.log(`Repository path: ${Z}`),console.log(`Skill path: ${q}
`);else X=Z,console.log(`Repository URL: ${Z}`),console.log(`Skill path: ${q}
`);else if(X=Z,U)console.log(`Repository path: ${Z}
`);else console.log(`Repository URL: ${Z}
`)}let z=kQ(X);if(!z){console.log(`Error: Invalid URL or path not found: ${X}`);return}if(q&&z.type==="git-repo"&&!z.path)z.path=q;let E=PQ(Y);console.log(`Install location: ${E}
`);let W,V=!1;try{if(z.type==="git-repo"||z.type==="git-tree"){if(console.log(`Cloning repository: ${z.repoUrl}`),z.branch)console.log(`Branch: ${z.branch}`);if(W=gQ(z.repoUrl,z.branch),V=!0,z.path){if(W=A(W,z.path),!b(W))throw Error(`Path not found in repository: ${z.path}`)}console.log("")}else W=z.path;let B=QQ(W);if(!B)throw Error("No skill.json found in source directory");let _={installed:[],errors:[],source:z.original};if(B.skills&&Array.isArray(B.skills)){console.log(`Found ${B.skills.length} skill(s) to update
`);for(let I of B.skills)VQ(W,I,E,_)}else{let I=B.name||ZQ(W).base;VQ(_Q(W),ZQ(W).base,E,_)}let J=vX(W,z),U=A(J,"agents.json");if(b(U)){let I=J;if(V){let T;try{T=JSON.parse(GQ(U,"utf8"))}catch{}let F=T?.repository||z.original,y=bX(J,F);if(y)I=y}SQ(I,{isUpdate:!0})}if(V&&W)try{let I=W.split("/.agents/tmp/")[0]+"/.agents/tmp/"+W.split("/.agents/tmp/")[1].split("/")[0];D(`rm -rf "${I}"`,{stdio:"pipe"})}catch{}if(console.log(`
**Successfully updated skills:**`),console.log(`- Source: ${_.source}`),_.installed.length>0)for(let I of _.installed)console.log(`  - Updated: ${I.name} at ${I.path}`),console.log(`    ${I.title}`);if(_.errors.length>0){console.log(`
**Errors:**`);for(let I of _.errors)console.log(`  - ${I}`)}if(_.installed.length===0&&_.errors.length===0)console.log("  No skills were updated");if(_.installed.length>0)FX(z.original,E,_,$,H)}catch(B){if(V&&W)try{let _=W.split("/.agents/tmp/")[0]+"/.agents/tmp/"+W.split("/.agents/tmp/")[1].split("/")[0];D(`rm -rf "${_}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${B.message}`)}},YZ=(Q)=>new Promise((X)=>{let Y=XZ({input:process.stdin,output:process.stdout});Y.question(Q,($)=>{Y.close(),X($.trim())})}),yQ=(Q)=>{try{return eY(Q).isSymbolicLink()}catch{return!1}},xX=async()=>{let Q=process.argv.slice(3),X=Q.find((G)=>!G.startsWith("-")),Y=Q.filter((G)=>G.startsWith("-"));if(!X){console.log(`Usage: superpowers-agent rm <url-or-path|@alias> [options]

Options:
  --global, -g   Remove globally installed skills (default)
  --project, -p  Remove project-installed skills

Examples:
  superpowers-agent rm https://github.com/example/skills
  superpowers-agent rm @myrepo
  superpowers-agent rm ~/my-local-skills`);return}let $=!Y.includes("--project")&&!Y.includes("-p"),Z=X,q=null;if(X.startsWith("@")){q=X;let G=g(!1),L=g(!0);if(G[X])Z=G[X];else if(L[X])Z=L[X];else{console.log(`Error: Repository alias not found: ${X}`);return}}let H=iQ($),z=H[X]||H[Z],E=z?.installBase||PQ(Y),W=[];if(z)W=z.skills;else{console.log(`No tracking data found. Attempting to re-resolve skills from source...
`);let G=kQ(Z);if(!G){console.log(`Error: Invalid URL or path not found: ${Z}`);return}let L,C=!1;try{if(G.type==="git-repo"||G.type==="git-tree"){if(console.log(`Cloning repository: ${G.repoUrl}`),L=gQ(G.repoUrl,G.branch),C=!0,G.path){if(L=A(L,G.path),!b(L))throw Error(`Path not found in repository: ${G.path}`)}}else L=G.path;let x=QQ(L);if(!x)throw Error("No skill.json found in source");let P=x.skills&&Array.isArray(x.skills)?x.skills:[x.name||ZQ(L).base];for(let O of P){let h=O;if(x.skills){let $Q=A(L,O);if(b($Q)){let dQ=QQ($Q);if(dQ?.name)h=dQ.name}}W.push({name:h,path:A(E,h)})}if(C)try{let O=L.split("/.agents/tmp/")[0]+"/.agents/tmp/"+L.split("/.agents/tmp/")[1].split("/")[0];D(`rm -rf "${O}"`,{stdio:"pipe"})}catch{}}catch(x){if(C&&L)try{let P=L.split("/.agents/tmp/")[0]+"/.agents/tmp/"+L.split("/.agents/tmp/")[1].split("/")[0];D(`rm -rf "${P}"`,{stdio:"pipe"})}catch{}console.log(`Error resolving source: ${x.message}`);return}}let V=W.filter((G)=>b(G.path)||yQ(G.path)),B=[];for(let G of W)for(let L of EQ){let C=A(L.skillsDir(),G.name);if(b(C)||yQ(C))B.push({platform:L.name,path:C})}let _=oQ(),J=q||Z,U=[];if(_[J])for(let[,G]of Object.entries(_[J].agents||{}))for(let[,L]of Object.entries(G)){let C=L.destinations||(L.destination?[L.destination]:[]);for(let x of C)if(x&&(b(x)||yQ(x)))U.push({path:x})}let I=J.replace(/^@/,"").replace(/[^a-zA-Z0-9_-]/g,"_"),T=A(m(),".agents","repos",I),F=b(T),y=m(),v=(G)=>G.replace(y,"~");if(V.length===0&&B.length===0&&U.length===0&&!F){console.log("Nothing to remove.");return}if(console.log("Will remove:"),V.length>0){console.log("  Skills:");for(let G of V)console.log(`    ${v(G.path)}`)}if(B.length>0){console.log("  Platform symlinks:");for(let G of B)console.log(`    ${v(G.path)}`)}if(U.length>0){console.log("  Agents:");for(let G of U)console.log(`    ${v(G.path)}`)}if(F)console.log("  Persisted repo:"),console.log(`    ${v(T)}`);if((await YZ(`
Proceed? [y/N] `)).toLowerCase()!=="y"){console.log("Aborted.");return}for(let G of B){let L=FQ(G.path);if(L.removed)console.log(`Removed symlink: ${v(G.path)}`),VX(G.platform,G.path,"skills");else if(L.error==="Symlink does not exist")console.log(`Already removed: ${v(G.path)}`);else console.log(`Warning: ${v(G.path)}: ${L.error}`)}for(let G of U){let L=FQ(G.path);if(L.removed)console.log(`Removed agent: ${v(G.path)}`);else if(L.error==="Symlink does not exist")console.log(`Already removed: ${v(G.path)}`);else console.log(`Warning: ${v(G.path)}: ${L.error}`)}if(F)try{fQ(T,{recursive:!0,force:!0}),console.log(`Removed persisted repo: ${v(T)}`)}catch(G){console.log(`Warning: Failed to remove persisted repo: ${G.message}`)}for(let G of V){if(!b(G.path)){console.log(`Already removed: ${v(G.path)}`);continue}try{fQ(G.path,{recursive:!0,force:!0}),console.log(`Removed skill: ${v(G.path)}`)}catch(L){console.log(`Warning: Failed to remove ${v(G.path)}: ${L.message}`)}}if(LQ(X,$),Z!==X)LQ(Z,$);if(_[J]){let G=f(!0);if(G.installedAgents)delete G.installedAgents[J],d(G,!0)}console.log(`
Done.`)},jX=()=>{let Q=process.argv.slice(3),X=Q.find((W)=>!W.startsWith("-")),Y=Q.filter((W)=>W.startsWith("-"));if(!X){console.log(`Usage: superpowers-agent add-repository <git-url> [options]

Options:
  --global, -g        Add repository globally in ~/.agents/config.json (default)
  --project, -p       Add repository in current project's .agents/config.json
  --as=<alias>        Specify custom alias for the repository

Examples:
  superpowers-agent add-repository https://github.com/example/skills.git
  superpowers-agent add-repository https://github.com/example/skills.git --as=@myskills
  superpowers-agent add-repository https://github.com/example/skills.git --project
  superpowers-agent add-repository https://github.com/example/skills.git --as=@custom --global

Description:
  Adds a skill repository alias to your configuration.
  The repository's skill.json will be read to determine the default alias.
  Use --as to specify a custom alias.
  After adding, you can install skills using: superpowers-agent add @alias path/to/skill`);return}console.log(`Adding repository...
`);let $=Y.some((W)=>W==="--global"||W==="-g"),Z=Y.some((W)=>W==="--project"||W==="-p"),q=Y.find((W)=>W.startsWith("--as=")),H=q?q.split("=")[1]:null,z=Z?!1:!0,E;try{console.log(`Cloning repository: ${X}`),E=A(m(),".agents","tmp",`repo-add-${Date.now()}`),D(`mkdir -p "${E}"`,{stdio:"pipe"}),D(`git clone --depth 1 "${X}" "${E}"`,{stdio:"pipe",timeout:30000}),console.log("");let W=QQ(E);if(!W)throw Error("No skill.json found in repository");let V;if(H)V=H;else if(W.repository)V=W.repository;else{let J=X.match(/\/([^/]+?)(?:\.git)?$/);if(J)V="@"+J[1];else throw Error("Could not determine repository alias. Use --as=@alias to specify manually.")}lQ(V,X,z),D(`rm -rf "${E}"`,{stdio:"pipe"});let B=z?"globally":"in project",_=z?A(m(),".agents","config.json"):A(process.cwd(),".agents","config.json");console.log(`✓ Repository added ${B}`),console.log(`  Alias: ${V}`),console.log(`  URL: ${X}`),console.log(`  Config: ${_}`),console.log(`
You can now install skills using:`),console.log(`  superpowers-agent add ${V} <path-to-skill>`)}catch(W){if(E)try{D(`rm -rf "${E}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${W.message}`)}};import{existsSync as WZ}from"fs";var RX=(Q)=>{if(Q.startsWith("superpowers:"))return{type:"superpowers",path:Q.substring(12).replace(/^skills\//,"")};if(Q.startsWith("claude:"))return{type:"claude",path:Q.substring(7).replace(/^skills\//,"")};if(Q.startsWith("copilot:"))return{type:"copilot",path:Q.substring(8).replace(/^skills\//,"")};if(Q.startsWith("opencode:"))return{type:"opencode",path:Q.substring(9).replace(/^skills\//,"")};return{type:null,path:Q.replace(/^skills\//,"")}},JQ={project:{dir:"projectAgentsSkills",prefix:""},claude:{dir:"projectClaudeSkills",prefix:"claude:"},copilot:{dir:"projectCopilotSkills",prefix:"copilot:"},opencode:{dir:"projectOpencodeSkills",prefix:"opencode:"},personal:{dir:"homePersonalSkills",prefix:""},personalClaude:{dir:"homeClaudeSkills",prefix:"claude:"},personalCopilot:{dir:"homeCopilotSkills",prefix:"copilot:"},personalOpencode:{dir:"homeOpencodeSkills",prefix:"opencode:"},superpowers:{dir:"homeSuperpowersSkills",prefix:"superpowers:"}};import{existsSync as ZZ,readdirSync as $Z,statSync as qZ}from"fs";import{join as zZ}from"path";import{readFileSync as SX}from"fs";var uQ=(Q)=>{try{let Y=SX(Q,"utf8").split(`
`),$={name:"",description:"",whenToUse:""},Z=!1;for(let q of Y){if(q.trim()==="---"){if(Z)break;Z=!0;continue}if(Z){let H=q.match(/^(\w+):\s*(.*)$/);if(H){let[,z,E]=H,W={name:"name",description:"description",when_to_use:"whenToUse"};if(W[z])$[W[z]]=E.trim()}}}return $}catch{return{name:"",description:"",whenToUse:""}}},yX=(Q)=>{try{let X=SX(Q,"utf8"),Y=uQ(Q),$=X.split(`
`),Z=!1,q=!1,H=[];for(let z of $){if(z.trim()==="---"){if(Z){q=!0;continue}Z=!0;continue}if(q||!Z)H.push(z)}return{content:H.join(`
`).trim(),frontmatter:Y}}catch(X){throw Error(`Error reading skill file: ${X.message}`)}};var fX=(Q,X)=>{if(!ZZ(Q))return[];let Y=[],$=X.toLowerCase().replace(/\\/g,"/"),Z=(q,H="")=>{try{let z=$Z(q,{withFileTypes:!0});for(let E of z){let W=zZ(q,E.name),V=H?`${H}/${E.name}`:E.name,B=E.isDirectory();if(E.isSymbolicLink())try{B=qZ(W).isDirectory()}catch{continue}if(B&&(E.name==="node_modules"||E.name===".git"||E.name.startsWith(".")))continue;if(B)Z(W,V);else if(E.name==="SKILL.md"){let _=H.toLowerCase().replace(/\\/g,"/");if(_===$||_.endsWith("/"+$)||_.endsWith($))Y.push({file:W,path:H})}}}catch{}};return Z(Q),Y},kX=(Q,X,Y)=>{let $={project:"project skills (.agents/skills/)",claude:"claude skills (.claude/skills/)",copilot:"copilot skills (.copilot/skills/)",opencode:"opencode skills (.opencode/skill/)",personal:"personal skills (~/.agents/skills/)",personalClaude:"personal claude skills (~/.claude/skills/)",personalCopilot:"personal copilot skills (~/.copilot/skills/)",personalOpencode:"personal opencode skills (~/.config/opencode/skill/)",superpowers:"superpowers skills (~/.agents/superpowers/skills/)"}[Y];console.log(`Error: Multiple skills match "${Q}" in ${$}:
`);for(let Z of X){let H=JQ[Y].prefix+Z.path;try{let z=uQ(Z.file),E=z.description||z.whenToUse||"(no description)";console.log(`  ${H}`),console.log(`    ${E}
`)}catch{console.log(`  ${H}`),console.log(`    (unable to read description)
`)}}console.log("Please choose one of these skill names in your platform's native skill tool.")};var PX=(Q)=>{let{type:X,path:Y}=RX(Q),$={superpowers:[K.homeSuperpowersSkills],claude:[K.projectClaudeSkills,K.homeClaudeSkills],copilot:[K.projectCopilotSkills,K.homeCopilotSkills],opencode:[K.projectOpencodeSkills,K.homeOpencodeSkills],project:[K.projectAgentsSkills],personal:[K.homePersonalSkills]},Z;if(X)Z=($[X]||[]).map((q)=>({type:X,dir:q}));else if(K.isSuperpowersRepo&&WZ(K.projectSkillsDir))Z=[{type:"project",dir:K.projectSkillsDir},{type:"project",dir:K.projectAgentsSkills},{type:"claude",dir:K.projectClaudeSkills},{type:"copilot",dir:K.projectCopilotSkills},{type:"opencode",dir:K.projectOpencodeSkills},{type:"personal",dir:K.homePersonalSkills},{type:"personalClaude",dir:K.homeClaudeSkills},{type:"personalCopilot",dir:K.homeCopilotSkills},{type:"personalOpencode",dir:K.homeOpencodeSkills},{type:"superpowers",dir:K.homeSuperpowersSkills}];else Z=[{type:"project",dir:K.projectAgentsSkills},{type:"claude",dir:K.projectClaudeSkills},{type:"copilot",dir:K.projectCopilotSkills},{type:"opencode",dir:K.projectOpencodeSkills},{type:"personal",dir:K.homePersonalSkills},{type:"personalClaude",dir:K.homeClaudeSkills},{type:"personalCopilot",dir:K.homeCopilotSkills},{type:"personalOpencode",dir:K.homeOpencodeSkills},{type:"superpowers",dir:K.homeSuperpowersSkills}];for(let{type:q,dir:H}of Z){let z=fX(H,Y);if(z.length===1)return{skillFile:z[0].file,sourceType:q,actualSkillPath:z[0].path};if(z.length>1)kX(Y,z,q)}return null};import{dirname as HZ}from"path";var gX=(Q,X,Y)=>{let{content:$,frontmatter:Z}=yX(Q),q=JQ[X].prefix+Y,H=HZ(Q),z=`# ${Z.name||q}`;if(Z.description)z+=`
# ${Z.description}`;if(Z.whenToUse)z+=`
# When to use: ${Z.whenToUse}`;return z+=`
# Supporting tools and docs are in ${H}
# ============================================

${$}`,z};var BZ=`You have superpowers.

**IMPORTANT: The using-superpowers skill content is included below. It is ALREADY LOADED - you are currently following it. Do NOT use the skill tool to load "using-superpowers" again - that would be redundant.**`,EZ="---\n\n**YOU MUST read and use the `leveraging-cli-tools` skill** the moment a task involves using BASH, searching code, parsing JSON/YAML, finding files, structural refactors, data wrangling, or any command that floods your context with verbose output (logs, CI logs, test runs). Load it with your skill tool BEFORE you reach for grep/find/sed or before you read large command output. It routes you to high-performance tools (rg, jq, fd, yq, ast-grep, gh, sd) and reduces output to just the signal — cutting token cost and latency 5-50x. Skipping it wastes tokens and time on work the right tool does instantly.",KZ=()=>{let Q=PX("using-superpowers");if(!Q)return null;let X;try{X=gX(Q.skillFile,Q.sourceType,Q.actualSkillPath)}catch{return null}return`<EXTREMELY_IMPORTANT>
${BZ}

${X}

${EZ}
</EXTREMELY_IMPORTANT>`},VZ=(Q="raw")=>{let X=KZ();if(Q==="claude")return JSON.stringify({hookSpecificOutput:{hookEventName:"SessionStart",additionalContext:X||""}});if(Q==="copilot")return JSON.stringify({additionalContext:X||""});return X||""},uX=()=>{let Q=process.argv.find((Y)=>Y.startsWith("--format=")),X=Q?Q.split("=")[1]:"raw";process.stdout.write(VZ(X)+`
`)};var dX={bootstrap:MX,version:XX,"check-updates":()=>{QX().catch((Q)=>{console.error(Q.message),process.exit(1)})},update:()=>{let Q=process.argv.includes("--no-reinstall");TQ({skipReinstall:Q})},"config-get":rQ,"config-set":sQ,"setup-skills":TX,"session-context":uX,add:CX,"add-repository":jX,"list-repositories":tQ,pull:DX,rm:xX,"install-aliases":CQ,default:()=>{console.log(`Superpowers for Agents
Usage:
  superpowers-agent bootstrap [--no-update] [--force]               # Run complete bootstrap
  superpowers-agent version                                         # Show current version
  superpowers-agent check-updates                                   # Check for updates
  superpowers-agent update [--no-reinstall]                         # Update to latest version
  superpowers-agent config-get                                      # Show configuration
  superpowers-agent config-set <key> <value>                        # Update configuration
  superpowers-agent setup-skills                                    # Initialize project skills
  superpowers-agent session-context [--format=claude|copilot|raw]   # Print session-start context (used by hooks)
  superpowers-agent add <url-or-path|@alias> [path] [options]      # Install skills
  superpowers-agent add-repository <git-url> [--as=@alias] [opts]  # Add repository alias
  superpowers-agent list-repositories                              # List configured repository aliases
  superpowers-agent pull <url-or-path|@alias> [path] [options]     # Update skills
  superpowers-agent rm <url-or-path|@alias> [options]               # Remove skills
  superpowers-agent install-aliases                                 # Install universal aliases (superpowers, superpowers-agent)

Documentation: https://github.com/complexthings/superpowers
`)}},GZ=process.argv[2]||"default",_Z=dX[GZ]||dX.default;try{let Q=_Z();if(Q&&typeof Q.then==="function")Q.catch((X)=>{console.error(`Error: ${X.message}`),process.exit(1)})}catch(Q){console.error(`Error: ${Q.message}`),process.exit(1)}
