#!/bin/sh
// 2>/dev/null; exec "$(command -v bun 2>/dev/null || echo node)" "$0" "$@"
import{relative as _Q,dirname as vQ,join as OX}from"path";import{existsSync as hq}from"fs";import{homedir as sq,platform as YX}from"os";import{join as J,parse as ZX,dirname as tq}from"path";import{existsSync as $q}from"fs";import{fileURLToPath as $X}from"url";var KX=$X(import.meta.url),Zq=tq(KX),WX=()=>{let q=sq();switch(YX()){case"darwin":return J(q,"Library","Application Support","Code","User");case"win32":return J(q,"AppData","Roaming","Code","User");case"linux":return J(q,".config","Code","User");default:return J(q,".config","Code","User")}},HX=()=>{let q=process.cwd(),Q=ZX(q).root;while(q!==Q){if($q(J(q,".agents")))return q;q=tq(q)}return process.cwd()},K={home:sq(),vscodeUserDir:WX(),projectRoot:HX(),get projectClaudeSkills(){return J(this.projectRoot,".claude","skills")},get projectAgentsSkills(){return J(this.projectRoot,".agents","skills")},get projectCopilotSkills(){return J(this.projectRoot,".copilot","skills")},get projectOpencodeSkills(){return J(this.projectRoot,".opencode","skill")},get projectCursorSkills(){return J(this.projectRoot,".cursor","skills")},get projectGeminiSkills(){return J(this.projectRoot,".gemini","skills")},get projectCodexSkills(){return J(this.projectRoot,".codex","skills")},get projectSkillsDir(){return J(this.projectRoot,"skills")},get homePersonalSkills(){return J(this.home,".agents","skills")},get homeClaudeSkills(){return J(this.home,".claude","skills")},get homeCopilotSkills(){return J(this.home,".copilot","skills")},get homeOpencodeSkills(){return J(this.home,".config","opencode","skill")},get homeCursorSkills(){return J(this.home,".cursor","skills")},get homeGeminiSkills(){return J(this.home,".gemini","skills")},get homeCodexSkills(){return J(this.home,".codex","skills")},get bootstrap(){return J(this.projectRoot,".agents","superpowers-bootstrap.md")},get superpowersRepo(){let q=J(Zq,"..","..","..","skills");if($q(q)&&$q(J(Zq,"..","..","..",".github","prompts")))return J(Zq,"..","..","..");return J(this.home,".agents","superpowers")},get homeSuperpowersSkills(){return J(this.superpowersRepo,"skills")},get isSuperpowersRepo(){let q=J(Zq,"..","..","..","skills");return $q(q)&&$q(J(Zq,"..","..","..",".github","prompts"))}};import{readFileSync as eq}from"fs";var Kq=(q)=>{try{let Y=eq(q,"utf8").split(`
`),Z={name:"",description:"",whenToUse:""},X=!1;for(let $ of Y){if($.trim()==="---"){if(X)break;X=!0;continue}if(X){let W=$.match(/^(\w+):\s*(.*)$/);if(W){let[,H,B]=W,z={name:"name",description:"description",when_to_use:"whenToUse"};if(z[H])Z[z[H]]=B.trim()}}}return Z}catch{return{name:"",description:"",whenToUse:""}}},qQ=(q)=>{try{let Q=eq(q,"utf8"),Y=Kq(q),Z=Q.split(`
`),X=!1,$=!1,W=[];for(let H of Z){if(H.trim()==="---"){if(X){$=!0;continue}X=!0;continue}if($||!X)W.push(H)}return{content:W.join(`
`).trim(),frontmatter:Y}}catch(Q){throw Error(`Error reading skill file: ${Q.message}`)}};import{existsSync as gq,readdirSync as YQ,statSync as ZQ}from"fs";import{join as Sq,relative as vY}from"path";import{readFileSync as zX,existsSync as EX}from"fs";import{join as QQ}from"path";var Wq=(q)=>{let Q=QQ(q,"skill.json");if(!EX(Q))return null;try{let Y=zX(Q,"utf8");return JSON.parse(Y)}catch(Y){return null}};var yq=(q)=>{if(q.startsWith("superpowers:"))return{type:"superpowers",path:q.substring(12).replace(/^skills\//,"")};if(q.startsWith("claude:"))return{type:"claude",path:q.substring(7).replace(/^skills\//,"")};if(q.startsWith("copilot:"))return{type:"copilot",path:q.substring(8).replace(/^skills\//,"")};if(q.startsWith("opencode:"))return{type:"opencode",path:q.substring(9).replace(/^skills\//,"")};if(q.startsWith("cursor:"))return{type:"cursor",path:q.substring(7).replace(/^skills\//,"")};if(q.startsWith("gemini:"))return{type:"gemini",path:q.substring(7).replace(/^skills\//,"")};return{type:null,path:q.replace(/^skills\//,"")}},XQ=(q,Q)=>{let Y=Wq(q);if(!Y||!Y.helpers||!Array.isArray(Y.helpers))return null;let Z=Y.helpers,X=Q.toLowerCase(),$=null,W=0;for(let H of Z){let B=H.toLowerCase();if(B===X){$=H;break}if(B.includes(X)){let E=X.length/B.length;if(E>W)W=E,$=H}let z=H.split("/").pop().split(".")[0].toLowerCase();if(X.includes(z)){let E=z.length/X.length;if(E>W)W=E,$=H}}return $?QQ(q,$):null},Hq={project:{dir:"projectAgentsSkills",prefix:""},claude:{dir:"projectClaudeSkills",prefix:"claude:"},copilot:{dir:"projectCopilotSkills",prefix:"copilot:"},opencode:{dir:"projectOpencodeSkills",prefix:"opencode:"},cursor:{dir:"projectCursorSkills",prefix:"cursor:"},gemini:{dir:"projectGeminiSkills",prefix:"gemini:"},personal:{dir:"homePersonalSkills",prefix:""},personalClaude:{dir:"homeClaudeSkills",prefix:"claude:"},personalCopilot:{dir:"homeCopilotSkills",prefix:"copilot:"},personalOpencode:{dir:"homeOpencodeSkills",prefix:"opencode:"},personalCursor:{dir:"homeCursorSkills",prefix:"cursor:"},personalGemini:{dir:"homeGeminiSkills",prefix:"gemini:"},superpowers:{dir:"homeSuperpowersSkills",prefix:"superpowers:"}};var Aq=(q,Q,Y=null)=>{let Z=[];if(!gq(q))return Z;let X=($,W)=>{if(Y!==null&&W>Y)return;try{let H=YQ($,{withFileTypes:!0});for(let B of H){if(B.name==="node_modules"||B.name===".git"||B.name.startsWith("."))continue;let z=Sq($,B.name),E=B.isDirectory();if(B.isSymbolicLink())try{E=ZQ(z).isDirectory()}catch{continue}if(!E)continue;let V=Sq(z,"SKILL.md");if(gq(V))Z.push(z);if(Y===null||W<Y)X(z,W+1)}}catch{}};return X(q,0),Z};var $Q=(q,Q)=>{if(!gq(q))return[];let Y=[],Z=Q.toLowerCase().replace(/\\/g,"/"),X=($,W="")=>{try{let H=YQ($,{withFileTypes:!0});for(let B of H){let z=Sq($,B.name),E=W?`${W}/${B.name}`:B.name,V=B.isDirectory();if(B.isSymbolicLink())try{V=ZQ(z).isDirectory()}catch{continue}if(V&&(B.name==="node_modules"||B.name===".git"||B.name.startsWith(".")))continue;if(V)X(z,E);else if(B.name==="SKILL.md"){let A=W.toLowerCase().replace(/\\/g,"/");if(A===Z||A.endsWith("/"+Z)||A.endsWith(Z))Y.push({file:z,path:W})}}}catch(H){}};return X(q),Y},KQ=(q,Q,Y)=>{let Z={project:"project skills (.agents/skills/)",claude:"claude skills (.claude/skills/)",copilot:"copilot skills (.copilot/skills/)",opencode:"opencode skills (.opencode/skill/)",cursor:"cursor skills (.cursor/skills/)",gemini:"gemini skills (.gemini/skills/)",personal:"personal skills (~/.agents/skills/)",personalClaude:"personal claude skills (~/.claude/skills/)",personalCopilot:"personal copilot skills (~/.copilot/skills/)",personalOpencode:"personal opencode skills (~/.config/opencode/skill/)",personalCursor:"personal cursor skills (~/.cursor/skills/)",personalGemini:"personal gemini skills (~/.gemini/skills/)",superpowers:"superpowers skills (~/.agents/superpowers/skills/)"}[Y];console.log(`Error: Multiple skills match "${q}" in ${Z}:
`);for(let X of Q){let W=Hq[Y].prefix+X.path;try{let H=Kq(X.file),B=H.description||H.whenToUse||"(no description)";console.log(`  ${W}`),console.log(`    ${B}
`)}catch(H){console.log(`  ${W}`),console.log(`    (unable to read description)
`)}}console.log("Please be more specific. Examples:");for(let X of Q.slice(0,2)){let $=Hq[Y].prefix;console.log(`  superpowers-agent execute ${$}${X.path}`)}};import{existsSync as kq}from"fs";import{join as BX,relative as VX}from"path";var GX=(q)=>{let{type:Q,path:Y}=yq(q),Z={superpowers:[K.homeSuperpowersSkills],claude:[K.projectClaudeSkills,K.homeClaudeSkills],copilot:[K.projectCopilotSkills,K.homeCopilotSkills],opencode:[K.projectOpencodeSkills,K.homeOpencodeSkills],gemini:[K.projectGeminiSkills,K.homeGeminiSkills],project:[K.projectAgentsSkills],personal:[K.homePersonalSkills]},X;if(Q)X=(Z[Q]||[]).map((W)=>({type:Q,dir:W}));else if(K.isSuperpowersRepo&&kq(K.projectSkillsDir))X=[{type:"project",dir:K.projectSkillsDir},{type:"project",dir:K.projectAgentsSkills},{type:"claude",dir:K.projectClaudeSkills},{type:"copilot",dir:K.projectCopilotSkills},{type:"opencode",dir:K.projectOpencodeSkills},{type:"gemini",dir:K.projectGeminiSkills},{type:"personal",dir:K.homePersonalSkills},{type:"personalClaude",dir:K.homeClaudeSkills},{type:"personalCopilot",dir:K.homeCopilotSkills},{type:"personalOpencode",dir:K.homeOpencodeSkills},{type:"personalGemini",dir:K.homeGeminiSkills},{type:"superpowers",dir:K.homeSuperpowersSkills}];else X=[{type:"project",dir:K.projectAgentsSkills},{type:"claude",dir:K.projectClaudeSkills},{type:"copilot",dir:K.projectCopilotSkills},{type:"opencode",dir:K.projectOpencodeSkills},{type:"gemini",dir:K.projectGeminiSkills},{type:"personal",dir:K.homePersonalSkills},{type:"personalClaude",dir:K.homeClaudeSkills},{type:"personalCopilot",dir:K.homeCopilotSkills},{type:"personalOpencode",dir:K.homeOpencodeSkills},{type:"personalGemini",dir:K.homeGeminiSkills},{type:"superpowers",dir:K.homeSuperpowersSkills}];for(let{type:$,dir:W}of X){let H=$Q(W,Y);if(H.length===1)return{skillFile:H[0].file,sourceType:$,actualSkillPath:H[0].path};if(H.length>1)KQ(Y,H,$)}return null},u=(q)=>{let Q=GX(q);if(Q)return Q;let{type:Y,path:Z}=yq(q),X={superpowers:[K.homeSuperpowersSkills],claude:[K.projectClaudeSkills,K.homeClaudeSkills],copilot:[K.projectCopilotSkills,K.homeCopilotSkills],opencode:[K.projectOpencodeSkills,K.homeOpencodeSkills],gemini:[K.projectGeminiSkills,K.homeGeminiSkills],project:[K.projectAgentsSkills],personal:[K.homePersonalSkills]},$;if(Y)$=(X[Y]||[]).map((H)=>({type:Y,dir:H}));else if(K.isSuperpowersRepo&&kq(K.projectSkillsDir))$=[{type:"project",dir:K.projectSkillsDir},{type:"project",dir:K.projectAgentsSkills},{type:"claude",dir:K.projectClaudeSkills},{type:"copilot",dir:K.projectCopilotSkills},{type:"opencode",dir:K.projectOpencodeSkills},{type:"gemini",dir:K.projectGeminiSkills},{type:"personal",dir:K.homePersonalSkills},{type:"personalClaude",dir:K.homeClaudeSkills},{type:"personalCopilot",dir:K.homeCopilotSkills},{type:"personalOpencode",dir:K.homeOpencodeSkills},{type:"personalGemini",dir:K.homeGeminiSkills},{type:"superpowers",dir:K.homeSuperpowersSkills}];else $=[{type:"project",dir:K.projectAgentsSkills},{type:"claude",dir:K.projectClaudeSkills},{type:"copilot",dir:K.projectCopilotSkills},{type:"opencode",dir:K.projectOpencodeSkills},{type:"gemini",dir:K.projectGeminiSkills},{type:"personal",dir:K.homePersonalSkills},{type:"personalClaude",dir:K.homeClaudeSkills},{type:"personalCopilot",dir:K.homeCopilotSkills},{type:"personalOpencode",dir:K.homeOpencodeSkills},{type:"personalGemini",dir:K.homeGeminiSkills},{type:"superpowers",dir:K.homeSuperpowersSkills}];for(let{type:W,dir:H}of $){if(!kq(H))continue;let B=Aq(H,W,null);for(let z of B){let E=Wq(z);if(E&&E.aliases&&Array.isArray(E.aliases)){let V=Z.toLowerCase();for(let A of E.aliases)if(A.toLowerCase()===V){let _=BX(z,"SKILL.md"),w=VX(H,z);return{skillFile:_,sourceType:W,actualSkillPath:w}}}}}return null};import{readFileSync as WQ,writeFileSync as HQ,existsSync as dq}from"fs";import{join as d}from"path";import{execSync as _X}from"child_process";var uq=()=>({auto_update:!0,last_update_check:null,last_updated_commit:null}),zq=()=>{let q=d(K.superpowersRepo,".config.json");try{if(!dq(q))return uq();let Q=WQ(q,"utf8");return{...uq(),...JSON.parse(Q)}}catch{return uq()}},Iq=(q)=>{let Q=d(K.superpowersRepo,".config.json"),Z={...zq(),...q};try{HQ(Q,JSON.stringify(Z,null,2))}catch(X){console.log(`Warning: couldn't save config: ${X.message}`)}},Eq=(q)=>{let Q=q?d(K.home,".agents","config.json"):d(process.cwd(),".agents","config.json");if(!dq(Q))return{};try{let Y=WQ(Q,"utf8");return JSON.parse(Y)}catch(Y){return{}}},Oq=(q,Q)=>{let Y=Q?d(K.home,".agents"):d(process.cwd(),".agents"),Z=d(Y,"config.json");try{if(!dq(Y))_X(`mkdir -p "${Y}"`,{stdio:"pipe"});return HQ(Z,JSON.stringify(q,null,2),"utf8"),!0}catch(X){throw Error(`Failed to write config: ${X.message}`)}},g=(q=!1)=>{return Eq(q).repositories||{}},zQ=(q,Q,Y)=>{let Z=Eq(Y);if(!Z.repositories)Z.repositories={};Z.repositories[q]=Q,Oq(Z,Y)};import{readFileSync as vX}from"fs";import{join as EQ,dirname as LX}from"path";import{fileURLToPath as UX}from"url";import{existsSync as JX}from"fs";import{execSync as AX}from"child_process";var IX=UX(import.meta.url),BQ=LX(IX),S=()=>{try{let q=EQ(BQ,"package.json");if(!JX(q))q=EQ(BQ,"..","..","package.json");return JSON.parse(vX(q,"utf8")).version||"5.4.0"}catch(q){return"5.4.0"}},C=()=>{let q=S();console.log(`^^SAV:${q}^^`)},VQ=async()=>{try{let Q=AX('curl -sS "https://raw.githubusercontent.com/complexthings/superpowers/main/.agents/package.json"',{encoding:"utf8",timeout:1e4}),Y=JSON.parse(Q);if(!Y.version)return S();return Y.version}catch(q){throw Error(`Failed to fetch remote version: ${q.message}`)}},GQ=(q,Q)=>{let Y=q.split(".").map(Number),Z=Q.split(".").map(Number);for(let X=0;X<3;X++){let $=Y[X]||0,W=Z[X]||0;if($>W)return!0;if($<W)return!1}return!1};var wX={project:{dir:"projectAgentsSkills",prefix:""},claude:{dir:"projectClaudeSkills",prefix:"claude:"},copilot:{dir:"projectCopilotSkills",prefix:"copilot:"},opencode:{dir:"projectOpencodeSkills",prefix:"opencode:"},gemini:{dir:"projectGeminiSkills",prefix:"gemini:"},personal:{dir:"homePersonalSkills",prefix:""},personalClaude:{dir:"homeClaudeSkills",prefix:"claude:"},personalCopilot:{dir:"homeCopilotSkills",prefix:"copilot:"},personalOpencode:{dir:"homeOpencodeSkills",prefix:"opencode:"},personalGemini:{dir:"homeGeminiSkills",prefix:"gemini:"},superpowers:{dir:"homeSuperpowersSkills",prefix:"superpowers:"}},TX=(q,Q)=>{let Y=OX(q,"SKILL.md"),{dir:Z,prefix:X}=wX[Q],$=_Q(K[Z],q).replace(/\\/g,"/");console.log(`${X}${$}`);let{description:W,whenToUse:H}=Kq(Y);if(W)console.log(`  ${W}`);if(H)console.log(`  When to use: ${H}`);console.log("")},c=()=>{C();let q=new Set,Q=[{type:"project",dir:K.projectAgentsSkills,maxDepth:null},{type:"claude",dir:K.projectClaudeSkills,maxDepth:null},{type:"copilot",dir:K.projectCopilotSkills,maxDepth:null},{type:"opencode",dir:K.projectOpencodeSkills,maxDepth:null},{type:"gemini",dir:K.projectGeminiSkills,maxDepth:null},{type:"personal",dir:K.homePersonalSkills,maxDepth:null},{type:"personalClaude",dir:K.homeClaudeSkills,maxDepth:null},{type:"personalCopilot",dir:K.homeCopilotSkills,maxDepth:null},{type:"personalOpencode",dir:K.homeOpencodeSkills,maxDepth:null},{type:"personalGemini",dir:K.homeGeminiSkills,maxDepth:null},{type:"superpowers",dir:K.homeSuperpowersSkills,maxDepth:null}];for(let{type:Y,dir:Z,maxDepth:X}of Q){let $=Aq(Z,Y,X);for(let W of $){let H=_Q(Z,W);if(!q.has(H))q.add(H),TX(W,Y)}}console.log(`Usage:
  superpowers-agent execute <skill-name> # Load a specific skill`)},LQ=()=>{C();let q=process.argv[3];if(!q){console.log(`Usage: superpowers-agent execute <skill-name-or-alias>

Examples:
  superpowers-agent execute brainstorming
  superpowers-agent execute superpowers:collaboration/brainstorming
  superpowers-agent execute aem/block-collection-and-party
  superpowers-agent execute block-collection  # Using alias

Description:
  Instructs the agent to execute the specified skill.
  Supports skill aliases defined in skill.json files.`);return}let Q=u(q);if(!Q){console.log(`Error: Skill not found: ${q}

Available skills:`),c();return}let{skillFile:Y,sourceType:Z}=Q,X=process.cwd(),$=Y.startsWith(X),W=hq(K.projectAgentsSkills)||hq(K.projectClaudeSkills);if($)console.log(`Open this file with your Read Tool: ${Y}

<IMPORTANT>
USE YOUR Read Tool to process this file. DO NOT use \`cat\` or print commands.
</IMPORTANT>`);else if(W&&Z!=="project")console.log(`Open this file with your Read Tool: ${Y}

<IMPORTANT>
1. Try using your Read Tool first
2. If your Read tool fails (file outside workspace), use: cat "${Y}"
3. Consider using workspace-local skills for better tool compatibility
   Run: superpowers-agent setup-skills
</IMPORTANT>`);else console.log(`Open this file with your Read Tool: ${Y}

<IMPORTANT>
1. Try using your Read Tool first
2. If your Read tool fails (file outside workspace), use: cat "${Y}"
3. NEVER skip loading the skill content
</IMPORTANT>`)},UQ=()=>{let q=process.argv[3];if(!q){console.log(`Usage: superpowers-agent path <skill-name-or-alias>

Examples:
  superpowers-agent path brainstorming
  superpowers-agent path superpowers:collaboration/brainstorming
  superpowers-agent path aem/block-collection-and-party
  superpowers-agent path block-collection  # Using alias

Description:
  Outputs the file system path of the specified SKILL.md file.
  Supports skill aliases defined in skill.json files.`);return}let Q=u(q);if(!Q){console.log(`Error: Skill not found: ${q}

Available skills:`),c();return}let{skillFile:Y}=Q;console.log(Y)},JQ=()=>{let q=process.argv[3];if(!q){console.log(`Usage: superpowers-agent dir <skill-name>

Examples:
  superpowers-agent dir brainstorming
  superpowers-agent dir aem/block-collection-and-party
  superpowers-agent dir block-collection  # Using alias
  superpowers-agent dir superpowers:testing/test-driven-development

Description:
  Returns the directory path where the specified skill is located.
  Supports skill aliases defined in skill.json files.`);return}let Q=u(q);if(!Q){console.log(`Error: Skill not found: ${q}

Available skills:`),c();return}let{skillFile:Y}=Q,Z=vQ(Y);console.log(Z)},AQ=()=>{C();let q=process.argv[3],Q=process.argv[4];if(!q||!Q){console.log(`Usage: superpowers-agent get-helpers <skill-name> <helper-search-term>

Examples:
  superpowers-agent get-helpers block-collection search-block
  superpowers-agent get-helpers aem/block-collection-and-party get-block-structure
  superpowers-agent get-helpers superpowers:testing/test-driven-development example

Description:
  Searches for helper files within a skill's skill.json configuration.
  Returns the full path to the best matching helper file.
  
  The command uses the skill's aliases if available, so you can use
  short names like "block-collection" instead of the full path.`);return}let Y=u(q);if(!Y){console.log(`Error: Skill not found: ${q}

Available skills:`),c();return}let{skillFile:Z}=Y,X=vQ(Z),$=Wq(X);if(!$){console.log(`Error: No skill.json found for skill: ${q}
  Location: ${X}`);return}if(!$.helpers||!Array.isArray($.helpers)||$.helpers.length===0){console.log(`Error: No helpers defined in skill.json for skill: ${q}
  Location: ${X}`);return}let W=XQ(X,Q);if(!W){console.log(`Error: No helper found matching "${Q}" in skill: ${q}`),console.log(`
Available helpers:`);for(let H of $.helpers)console.log(`  - ${H}`);return}if(!hq(W)){console.log(`Error: Helper file not found: ${W}
  Defined in skill.json but missing from filesystem`);return}console.log(W)},IQ=()=>{let q=zq();console.log("Current configuration:"),console.log(JSON.stringify(q,null,2))},OQ=()=>{let q=process.argv[3],Q=process.argv[4];if(!q||Q===void 0){console.log(`Usage: .agents/superpowers-agent config-set <key> <value>

Available keys:
  auto_update (true/false) - Enable/disable automatic updates during bootstrap

Examples:
  .agents/superpowers-agent config-set auto_update false
  .agents/superpowers-agent config-set auto_update true`);return}let Y=Q;if(Q==="true")Y=!0;else if(Q==="false")Y=!1;let Z={};Z[q]=Y,Iq(Z),console.log(`✓ Set ${q} = ${Y}`)},wQ=()=>{C();let q=g(!0),Q=g(!1),Y=[];for(let[$,W]of Object.entries(q))Y.push({alias:$,url:W,source:"global"});for(let[$,W]of Object.entries(Q)){let H=Y.findIndex((B)=>B.alias===$);if(H>=0)Y[H]={alias:$,url:W,source:"project"};else Y.push({alias:$,url:W,source:"project"})}if(Y.length===0){console.log(`No repository aliases configured.

Add a repository using:
  superpowers-agent add-repository <git-url> [--as=@alias]`);return}let Z=Math.max(5,...Y.map(($)=>$.alias.length)),X=Math.max(3,...Y.map(($)=>$.url.length));console.log(`Repositories:
`),console.log(`${"Alias".padEnd(Z)}  ${"URL".padEnd(X)}  Source`);for(let $ of Y)console.log(`${$.alias.padEnd(Z)}  ${$.url.padEnd(X)}  (${$.source})`);console.log(`
Total: ${Y.length} repository alias(es)`)};import{execSync as nX}from"child_process";import{execSync as h}from"child_process";var bX=()=>{try{return h("git status --porcelain --untracked-files=no",{cwd:K.superpowersRepo,encoding:"utf8",stdio:"pipe",timeout:3000}).trim().length===0}catch{return!1}},Bq=()=>{try{return h("git rev-parse --abbrev-ref HEAD",{cwd:K.superpowersRepo,encoding:"utf8",stdio:"pipe",timeout:3000}).trim()==="main"}catch{return!1}},wq=()=>{try{h("git fetch origin",{cwd:K.superpowersRepo,timeout:5000,stdio:"pipe"});let q=!bX(),Q=h("git rev-parse HEAD",{cwd:K.superpowersRepo,encoding:"utf8",stdio:"pipe"}).trim(),Y=h("git rev-parse origin/main",{cwd:K.superpowersRepo,encoding:"utf8",stdio:"pipe"}).trim();if(Q===Y)return{hasUpdates:!1,hasLocalChanges:q,currentCommit:Q,latestCommit:Y,commitsBehind:0,changedFiles:[]};let X=parseInt(h("git rev-list --count HEAD..origin/main",{cwd:K.superpowersRepo,encoding:"utf8",stdio:"pipe"}).trim(),10),W=h("git diff --name-only HEAD origin/main",{cwd:K.superpowersRepo,encoding:"utf8",stdio:"pipe"}).trim().split(`
`).filter((H)=>H.length>0);return{hasUpdates:!0,hasLocalChanges:q,currentCommit:Q,latestCommit:Y,commitsBehind:X,changedFiles:W}}catch{return{hasUpdates:!1,hasLocalChanges:!1,currentCommit:"",latestCommit:"",commitsBehind:0,changedFiles:[],error:!0}}},TQ=(q)=>{let Q={".github/prompts/":"copilot-prompts",".cursor/commands/":"cursor-commands","hooks/cursor/":"cursor-hooks",".codex/prompts/":"codex-prompts",".gemini/commands/":"gemini-commands","commands/":"claude-commands",".opencode/command/":"opencode-commands",".opencode/plugins/":"opencode-plugin"},Y=new Set;for(let Z of q)for(let[X,$]of Object.entries(Q))if(Z.startsWith(X))Y.add($);return Array.from(Y)};import{existsSync as bQ,readdirSync as MX}from"fs";import{join as Tq}from"path";import{execSync as MQ}from"child_process";var n=()=>{let q=Tq(K.superpowersRepo,".github","prompts"),Q=Tq(K.vscodeUserDir,"prompts");if(!bQ(q)){console.log("⚠️  No Copilot prompts to install (source directory not found).");return}try{if(!bQ(Q))MQ(`mkdir -p "${Q}"`,{stdio:"pipe"})}catch(X){console.log(`Error creating prompts directory: ${X.message}`);return}let Y;try{Y=MX(q).filter((X)=>X.endsWith(".prompt.md"))}catch(X){console.log(`Error reading prompts directory: ${X.message}`);return}if(Y.length===0){console.log("⚠️  No prompt files found to install.");return}console.log("Installing GitHub Copilot prompts...");let Z=0;for(let X of Y)try{let $=Tq(q,X),W=Tq(Q,X);MQ(`cp "${$}" "${W}"`,{stdio:"pipe"}),console.log(`  ✓ Installed ${X}`),Z++}catch($){console.log(`  ✗ Failed to install ${X}: ${$.message}`)}if(Z>0)console.log(`
✓ Installed ${Z} prompt(s) to ${Q}
  Use slash commands in GitHub Copilot:
    /brainstorm-with-superpowers - Refine ideas into designs
    /write-a-skill - Create new skills with TDD
    /skills - Discover available skills
    /use-skill - Load and apply a specific skill`)};import{existsSync as bq,readdirSync as NQ}from"fs";import{join as R}from"path";import{execSync as a}from"child_process";var l=()=>{let q=R(K.superpowersRepo,".cursor","commands"),Q=R(K.home,".cursor","commands");if(!bq(q)){console.log("⚠️  No Cursor commands to install (source directory not found).");return}try{if(!bq(Q))a(`mkdir -p "${Q}"`,{stdio:"pipe"})}catch(X){console.log(`Error creating Cursor commands directory: ${X.message}`);return}let Y;try{Y=NQ(q).filter((X)=>X.endsWith(".md"))}catch(X){console.log(`Error reading Cursor commands directory: ${X.message}`);return}if(Y.length===0){console.log("⚠️  No command files found to install.");return}console.log("Installing Cursor commands...");let Z=0;for(let X of Y)try{let $=R(q,X),W=R(Q,X);a(`cp "${$}" "${W}"`,{stdio:"pipe"}),console.log(`  ✓ Installed ${X}`),Z++}catch($){console.log(`  ✗ Failed to install ${X}: ${$.message}`)}if(Z>0)console.log(`
✓ Installed ${Z} command(s) to ${Q}
  Use slash commands in Cursor:
    /brainstorm-with-superpowers - Refine ideas into designs
    /write-a-skill - Create new skills with TDD
    /skills - Discover available skills
    /use-skill - Load and apply a specific skill`)},o=()=>{let q=R(K.superpowersRepo,"hooks","cursor"),Q=R(K.home,".cursor","hooks"),Y=R(q,"hooks.json"),Z=R(K.home,".cursor","hooks.json");if(!bq(q)){console.log("⚠️  No Cursor hooks to install (source directory not found).");return}try{if(!bq(Q))a(`mkdir -p "${Q}"`,{stdio:"pipe"})}catch(W){console.log(`Error creating Cursor hooks directory: ${W.message}`);return}try{a(`cp "${Y}" "${Z}"`,{stdio:"pipe"}),console.log("  ✓ Installed hooks.json")}catch(W){console.log(`  ✗ Failed to install hooks.json: ${W.message}`);return}let X;try{X=NQ(q).filter((W)=>W.endsWith(".sh"))}catch(W){console.log(`Error reading hooks directory: ${W.message}`);return}if(X.length===0){console.log("⚠️  No hook scripts found to install.");return}console.log("Installing Cursor hooks...");let $=0;for(let W of X)try{let H=R(q,W),B=R(Q,W);a(`cp "${H}" "${B}"`,{stdio:"pipe"}),a(`chmod +x "${B}"`,{stdio:"pipe"}),console.log(`  ✓ Installed ${W}`),$++}catch(H){console.log(`  ✗ Failed to install ${W}: ${H.message}`)}if($>0)console.log(`
✓ Installed ${$} hook(s) to ${Q}
  Cursor will now check for skills before each prompt submission
  Restart Cursor for hooks to take effect`)};import{existsSync as FQ,readdirSync as NX}from"fs";import{join as Mq}from"path";import{execSync as CQ}from"child_process";var i=()=>{let q=Mq(K.superpowersRepo,".codex","prompts"),Q=Mq(K.home,".codex","prompts");if(!FQ(q)){console.log("⚠️  No Codex prompts to install (source directory not found).");return}try{if(!FQ(Q))CQ(`mkdir -p "${Q}"`,{stdio:"pipe"})}catch(X){console.log(`Error creating Codex prompts directory: ${X.message}`);return}let Y;try{Y=NX(q).filter((X)=>X.endsWith(".md"))}catch(X){console.log(`Error reading Codex prompts directory: ${X.message}`);return}if(Y.length===0){console.log("⚠️  No prompt files found to install.");return}console.log("Installing OpenAI Codex prompts...");let Z=0;for(let X of Y)try{let $=Mq(q,X),W=Mq(Q,X);CQ(`cp "${$}" "${W}"`,{stdio:"pipe"}),console.log(`  ✓ Installed ${X}`),Z++}catch($){console.log(`  ✗ Failed to install ${X}: ${$.message}`)}if(Z>0)console.log(`
✓ Installed ${Z} prompt(s) to ${Q}
  Use slash commands in OpenAI Codex:
    /prompts:brainstorm - Refine ideas into designs
    /prompts:write-skill - Create new skills with TDD
    /prompts:skills - Discover available skills
    /prompts:use-skill - Load and apply a specific skill
  Note: Restart Codex or open a new session to reload prompts`)};import{existsSync as RQ,readdirSync as FX}from"fs";import{join as Nq}from"path";import{execSync as xQ}from"child_process";var r=()=>{let q=Nq(K.superpowersRepo,".gemini","commands"),Q=Nq(K.home,".gemini","commands");if(!RQ(q)){console.log("⚠️  No Gemini commands to install (source directory not found).");return}try{if(!RQ(Q))xQ(`mkdir -p "${Q}"`,{stdio:"pipe"})}catch(X){console.log(`Error creating Gemini commands directory: ${X.message}`);return}let Y;try{Y=FX(q).filter((X)=>X.endsWith(".toml"))}catch(X){console.log(`Error reading Gemini commands directory: ${X.message}`);return}if(Y.length===0){console.log("⚠️  No command files found to install.");return}console.log("Installing Gemini commands...");let Z=0;for(let X of Y)try{let $=Nq(q,X),W=Nq(Q,X);xQ(`cp "${$}" "${W}"`,{stdio:"pipe"}),console.log(`  ✓ Installed ${X}`),Z++}catch($){console.log(`  ✗ Failed to install ${X}: ${$.message}`)}if(Z>0)console.log(`
✓ Installed ${Z} command(s) to ${Q}
  Use slash commands in Gemini:
    /brainstorm-with-superpowers - Refine ideas into designs
    /write-a-skill - Create new skills with TDD
    /skills - Discover available skills
    /use-skill - Load and apply a specific skill`)};import{existsSync as DQ,readdirSync as CX}from"fs";import{join as Fq}from"path";import{execSync as jQ}from"child_process";var s=()=>{let q=Fq(K.superpowersRepo,"commands"),Q=Fq(K.home,".claude","commands");if(!DQ(q)){console.log("⚠️  No Claude commands to install (source directory not found).");return}try{if(!DQ(Q))jQ(`mkdir -p "${Q}"`,{stdio:"pipe"})}catch(X){console.log(`Error creating Claude commands directory: ${X.message}`);return}let Y;try{Y=CX(q).filter((X)=>X.endsWith(".md"))}catch(X){console.log(`Error reading Claude commands directory: ${X.message}`);return}if(Y.length===0){console.log("⚠️  No command files found to install.");return}console.log("Installing Claude Code commands...");let Z=0;for(let X of Y)try{let $=Fq(q,X),W=Fq(Q,X);jQ(`cp "${$}" "${W}"`,{stdio:"pipe"}),console.log(`  ✓ Installed ${X}`),Z++}catch($){console.log(`  ✗ Failed to install ${X}: ${$.message}`)}if(Z>0)console.log(`
✓ Installed ${Z} command(s) to ${Q}
  Use slash commands in Claude Code:
    /brainstorm - Refine ideas into designs
    /execute-plan - Execute plans in batches
    /write-plan - Create implementation plans
    /skills - Discover available skills
    /use-skill - Load and apply a specific skill`)};import{existsSync as Vq,readdirSync as RX,lstatSync as xX,symlinkSync as PQ,unlinkSync as DX,mkdirSync as jX,readlinkSync as PX}from"fs";import{join as m,dirname as TZ}from"path";import{execSync as fQ}from"child_process";import{platform as fX}from"os";var t=()=>{let q=m(K.superpowersRepo,".opencode","command"),Q=m(K.home,".config","opencode","command");if(!Vq(q)){console.log("⚠️  No opencode commands to install (source directory not found).");return}try{if(!Vq(Q))fQ(`mkdir -p "${Q}"`,{stdio:"pipe"})}catch(X){console.log(`Error creating opencode commands directory: ${X.message}`);return}let Y;try{Y=RX(q).filter((X)=>X.endsWith(".md"))}catch(X){console.log(`Error reading opencode commands directory: ${X.message}`);return}if(Y.length===0){console.log("⚠️  No command files found to install.");return}console.log("Installing opencode commands...");let Z=0;for(let X of Y)try{let $=m(q,X),W=m(Q,X);fQ(`cp "${$}" "${W}"`,{stdio:"pipe"}),console.log(`  ✓ Installed ${X}`),Z++}catch($){console.log(`  ✗ Failed to install ${X}: ${$.message}`)}if(Z>0)console.log(`
✓ Installed ${Z} command(s) to ${Q}
  Use slash commands in opencode:
    /brainstorm - Refine ideas into designs
    /execute-plan - Execute plans in batches
    /write-plan - Create implementation plans
    /write-skill - Create new skills with TDD
    /skills - Discover available skills
    /use-skill - Load and apply a specific skill`)},mq=(q)=>{try{return xX(q).isSymbolicLink()}catch{return!1}},yX=(q,Q)=>{try{if(!mq(q))return!1;return PX(q)===Q}catch{return!1}},Gq=()=>{let q=m(K.superpowersRepo,".opencode","plugins","superpowers-agent.js"),Q=m(K.home,".config","opencode","plugins"),Y=m(Q,"superpowers-agent.js");if(console.log("Installing OpenCode plugin symlink..."),!Vq(q))return console.log("⚠️  Source plugin not found, skipping symlink creation"),console.log(`   Expected at: ${q}`),{created:!1,error:"Source plugin not found"};if(yX(Y,q))return console.log("✓ Plugin symlink already exists and is correct"),{created:!1,existed:!0};if(Vq(Y)&&!mq(Y))return console.log("⚠️  Warning: A file already exists at the destination path"),console.log(`   Path: ${Y}`),console.log("   Skipping symlink creation to avoid overwriting existing file"),console.log("   To use superpowers-agent plugin, manually remove or rename the existing file"),{created:!1,error:"File already exists at destination"};if(!Vq(Q))try{jX(Q,{recursive:!0}),console.log(`✓ Created ${Q.replace(K.home,"~")}`)}catch(X){return console.log(`⚠️  Failed to create plugins directory: ${X.message}`),{created:!1,error:`Failed to create directory: ${X.message}`}}if(mq(Y))try{DX(Y)}catch(X){return console.log(`⚠️  Failed to remove existing symlink: ${X.message}`),{created:!1,error:`Failed to remove existing symlink: ${X.message}`}}let Z=fX();try{if(Z==="win32")PQ(q,Y,"file");else PQ(q,Y);let X=q.replace(K.home,"~"),$=Y.replace(K.home,"~");return console.log(`✓ Created symlink: ${$}`),console.log(`  -> ${X}`),{created:!0}}catch(X){if(Z==="win32"&&X.code==="EPERM")return console.log("⚠️  Windows requires Developer Mode or admin privileges for symlinks"),console.log("   Enable Developer Mode: Settings > Update & Security > For developers"),{created:!1,error:"Windows symlink permission denied"};return console.log(`⚠️  Failed to create symlink: ${X.message}`),{created:!1,error:`Failed to create symlink: ${X.message}`}}};import{existsSync as O,readdirSync as gX,lstatSync as SX,symlinkSync as yQ,unlinkSync as kX,mkdirSync as _q,readlinkSync as uX}from"fs";import{join as G,dirname as dX}from"path";import{homedir as b,platform as hX}from"os";var gQ=[{name:"claude",parentDir:()=>G(b(),".claude"),skillsDir:()=>G(b(),".claude","skills"),superpowersTarget:"superpowers"},{name:"copilot",parentDir:()=>G(b(),".copilot"),skillsDir:()=>G(b(),".copilot","skills"),superpowersTarget:"superpowers"},{name:"opencode",parentDir:()=>G(b(),".config","opencode"),skillsDir:()=>G(b(),".config","opencode","skill"),superpowersTarget:"superpowers"},{name:"cursor",parentDir:()=>G(b(),".cursor"),skillsDir:()=>G(b(),".cursor","skills"),superpowersTarget:"superpowers"},{name:"gemini",parentDir:()=>G(b(),".gemini"),skillsDir:()=>G(b(),".gemini","skills"),superpowersTarget:"superpowers"},{name:"codex",parentDir:()=>G(b(),".codex"),skillsDir:()=>G(b(),".codex","skills"),superpowersTarget:"superpowers"}],mX=[{name:"claude",agentDir:(q)=>G(q,".claude"),skillsDir:(q)=>G(q,".claude","skills"),detect:(q)=>O(G(q,".claude"))},{name:"copilot",agentDir:(q)=>G(q,".github"),skillsDir:(q)=>G(q,".github","skills"),detect:(q)=>{let Q=O(G(q,".github")),Y=O(G(q,"AGENTS.md"))||O(G(q,".agents","AGENTS.md"));return Q&&Y}},{name:"opencode",agentDir:(q)=>G(q,".opencode"),skillsDir:(q)=>G(q,".opencode","skill"),detect:(q)=>{let Q=O(G(q,".opencode")),Y=O(G(q,"AGENTS.md"))||O(G(q,".agents","AGENTS.md"))||O(G(q,".opencode","AGENTS.md"));return Q&&Y}},{name:"cursor",agentDir:(q)=>G(q,".cursor"),skillsDir:(q)=>G(q,".cursor","skills"),detect:(q)=>O(G(q,".cursor"))},{name:"gemini",agentDir:(q)=>G(q,".gemini"),skillsDir:(q)=>G(q,".gemini","skills"),detect:(q)=>{let Q=O(G(q,".gemini")),Y=O(G(q,"GEMINI.md"))||O(G(q,".agents","GEMINI.md"));return Q&&Y}},{name:"codex",agentDir:(q)=>G(q,".codex"),skillsDir:(q)=>G(q,".codex","skills"),detect:(q)=>{let Q=O(G(q,".codex")),Y=O(G(q,"AGENTS.md"))||O(G(q,".agents","AGENTS.md"));return Q&&Y}}],pq=(q)=>{try{return SX(q).isSymbolicLink()}catch{return!1}},pX=(q,Q)=>{try{if(!pq(q))return!1;return uX(q)===Q}catch{return!1}},cq=(q,Q)=>{if(!O(q))return{created:!1,existed:!1,error:`Source does not exist: ${q}`};if(pX(Q,q))return{created:!1,existed:!0};if(O(Q)||pq(Q))if(pq(Q))try{kX(Q)}catch(X){return{created:!1,existed:!1,error:`Failed to remove existing symlink: ${X.message}`}}else return{created:!1,existed:!1,error:`Target exists and is not a symlink: ${Q}`};let Y=dX(Q);if(!O(Y))try{_q(Y,{recursive:!0})}catch(X){return{created:!1,existed:!1,error:`Failed to create parent directory: ${X.message}`}}let Z=hX();try{if(Z==="win32")yQ(q,Q,"junction");else yQ(q,Q,"dir");return{created:!0,existed:!1}}catch(X){if(Z==="win32"&&X.code==="EPERM")return{created:!1,existed:!1,error:"Windows requires Developer Mode or admin privileges for symlinks. Enable Developer Mode: Settings > Update & Security > For developers"};return{created:!1,existed:!1,error:`Failed to create symlink: ${X.message}`}}};var SQ=(q,Q,Y="skills")=>{let Z=Eq(!0);if(!Z.symlinks)Z.symlinks={};if(!Z.symlinks[q])Z.symlinks[q]={superpowers:null,skills:[]};if(Y==="superpowers")Z.symlinks[q].superpowers=Q;else{let X=Z.symlinks[q].skills||[];if(!X.includes(Q))X.push(Q),Z.symlinks[q].skills=X}Oq(Z,!0)};var cX=(q,Q={})=>{let Y=K.homeSuperpowersSkills,Z=G(q.skillsDir(),q.superpowersTarget),X=cq(Y,Z);if(X.created){SQ(q.name,Z,"superpowers");let $=Y.replace(b(),"~"),W=Z.replace(b(),"~");return console.log(`  ✓ Created ${W} -> ${$}`),{created:!0}}else if(X.existed)return{created:!1,existed:!0};else if(X.error)return console.log(`  ⚠️  ${q.name} superpowers: ${X.error}`),{created:!1,error:X.error};return{created:!1}},kQ=(q,Q={})=>{let Y=K.homePersonalSkills;if(!O(Y))return{created:0,existed:0};let Z;try{Z=gX(Y,{withFileTypes:!0}).filter(($)=>$.isDirectory()&&!$.name.startsWith(".")).map(($)=>$.name)}catch{return{created:0,existed:0}}if(Z.length===0)return{created:0,existed:0};let X={created:0,existed:0,names:[]};for(let $ of Z){let W=G(Y,$),H=G(q.skillsDir(),$),B=cq(W,H);if(B.created)SQ(q.name,H,"skills"),X.created++,X.names.push($);else if(B.existed)X.existed++;else if(B.error)console.log(`  ⚠️  ${$}: ${B.error}`)}if(X.created>0)console.log(`  ✓ Synced ${X.created} personal skill(s): ${X.names.join(", ")}`);return X},Cq=(q={})=>{let{force:Q=!1}=q;for(let Y of gQ){let Z=Y.parentDir(),X=Y.skillsDir();if(!O(Z))if(Q)try{_q(Z,{recursive:!0}),console.log(`✓ Created ${Z.replace(b(),"~")}`)}catch($){console.log(`⚠️  Failed to create ${Z.replace(b(),"~")}: ${$.message}`);continue}else{console.log(`⚠️  Skipping ${Y.name} (${Z.replace(b(),"~")} not found)`),console.log("   Use --force to create directory");continue}if(!O(X))try{_q(X,{recursive:!0})}catch($){console.log(`⚠️  Failed to create skills directory for ${Y.name}: ${$.message}`);continue}console.log(`${Y.name}:`),cX(Y,q),kQ(Y,q)}},nq=(q={})=>{let{force:Q=!1}=q;for(let Y of gQ){let Z=Y.parentDir(),X=Y.skillsDir();if(!O(Z)){if(!Q)continue;try{_q(Z,{recursive:!0})}catch{continue}}if(!O(X))try{_q(X,{recursive:!0})}catch{continue}kQ(Y,q)}};var uQ=(q={})=>{let Q=q.projectRoot||process.cwd(),Y=G(Q,".agents","skills");if(!O(Y))return{created:0,existed:0,skipped:0,errors:[]};let Z={created:0,existed:0,skipped:0,errors:[]};for(let X of mX){if(!X.detect(Q)){Z.skipped++;continue}let $=X.skillsDir(Q),W=X.agentDir(Q);if(!O(W)){Z.skipped++;continue}let H=cq(Y,$);if(H.created){let B=$.replace(Q,".");console.log(`  ✓ Created ${B} -> .agents/skills`),Z.created++}else if(H.existed)Z.existed++;else if(H.error)console.log(`  ⚠️  ${X.name}: ${H.error}`),Z.errors.push({platform:X.name,error:H.error})}return Z};var aX=(q)=>{let Y={"copilot-prompts":n,"cursor-commands":l,"cursor-hooks":o,"codex-prompts":i,"gemini-commands":r,"claude-commands":s,"opencode-commands":t,"opencode-plugin":Gq}[q];if(Y)try{return Y(),{success:!0,integration:q}}catch(Z){return{success:!1,integration:q,error:Z.message}}return{success:!1,integration:q,error:"Unknown integration"}},lX=()=>{console.log("Installing aliases...")},Rq=(q={})=>{let Q=q.skipReinstall||!1;console.log(`# Checking for Superpowers updates...
`);let Y=wq();if(Y.error){console.log("⚠️  Could not check for updates (network issue)");return}if(!Y.hasUpdates){console.log("✓ Already up to date");return}if(Y.hasLocalChanges){console.log(`⚠️  Cannot auto-update: local changes detected
   Commit or stash your changes first, then run update again
   Or manually update: cd ${K.superpowersRepo} && git pull`);return}if(!Bq()){console.log(`⚠️  Not on main branch, skipping auto-update
   Switch to main branch first: cd ${K.superpowersRepo} && git checkout main`);return}console.log(`\uD83D\uDCE6 Updating from ${Y.currentCommit.substring(0,7)} to ${Y.latestCommit.substring(0,7)}
   (${Y.commitsBehind} new commit${Y.commitsBehind>1?"s":""})
`);try{nX("git pull origin main",{cwd:K.superpowersRepo,stdio:"pipe",timeout:1e4}),console.log("✓ Updated superpowers repository")}catch(W){console.log(`✗ Git pull failed: ${W.message}
   Please resolve manually and try again`);return}if(console.log(""),Iq({last_updated_commit:Y.latestCommit,last_update_check:new Date().toISOString()}),Q){console.log(`ℹ️  Skipping integration reinstall (--no-reinstall flag)

✓ Update complete!`);return}let Z=TQ(Y.changedFiles);if(Z.length===0){console.log(`ℹ️  No integration files changed, skipping reinstalls

✓ Update complete!`);return}console.log(`\uD83D\uDD04 Reinstalling updated integrations:
`);let X=[];for(let W of Z){let H=aX(W);if(X.push(H),H.success)console.log(`  ✓ ${W}`);else console.log(`  ✗ ${W} (${H.error})`)}console.log(""),console.log(`
---
`),lX(),console.log(`
---
`),console.log(`## Syncing Skill Symlinks
`),Cq(),console.log(`
---
`),console.log(`## OpenCode Plugin Symlink
`),Gq();let $=X.filter((W)=>!W.success);if($.length>0){console.log("⚠️  Update completed with errors:");for(let W of $){console.log(`  - ${W.integration} failed to install`);let H=`install-${W.integration}`;console.log(`    Run manually: superpowers-agent ${H}`)}}else console.log("✓ Update complete!")},dQ=async()=>{C();let q=S();console.log(`Current version: ${q}`);try{let Q=await VQ();if(console.log(`Latest version: ${Q}`),GQ(Q,q))console.log("Update available: Yes"),process.exit(1);else console.log("You are up to date"),process.exit(0)}catch(Q){console.log(`Error checking for updates: ${Q.message}`),process.exit(1)}},hQ=()=>{let q=S();console.log(q)};import{existsSync as v,readFileSync as k,writeFileSync as qq}from"fs";import{join as I,dirname as pQ,parse as e}from"path";import{execSync as F}from"child_process";import{platform as iX}from"os";import{execSync as oX}from"child_process";var vq=(q)=>{try{return oX(`which ${q}`,{stdio:"pipe",timeout:2000}),!0}catch{return!1}};var U={opencode:{check:()=>vq("opencode"),cli:!0,name:"OpenCode",installUrl:"https://opencode.ai/docs/installation",bootstrapCommand:"install-opencode-commands"},claude:{check:()=>vq("claude"),cli:!0,name:"Claude Code",installUrl:"https://code.claude.com/docs/en/installation",bootstrapCommand:"install-claude-commands"},gemini:{check:()=>vq("gemini"),cli:!0,name:"Gemini",installUrl:"https://cloud.google.com/gemini/docs/cli/install",bootstrapCommand:"install-gemini-commands"},codex:{check:()=>vq("codex"),cli:!0,name:"Codex",installUrl:"https://developers.openai.com/codex/docs/installation",bootstrapCommand:"install-codex-prompts"},cursor:{check:()=>!0,cli:!1,name:"Cursor",bootstrapCommand:"install-cursor-commands"},copilot:{check:()=>!0,cli:!1,name:"GitHub Copilot",bootstrapCommand:"install-copilot-prompts"}},mQ=()=>{let q=[];if(U.copilot.check())q.push("github-copilot");if(U.cursor.check())q.push("cursor");if(U.claude.check())q.push("claude-code");if(U.opencode.check())q.push("opencode");if(U.gemini.check())q.push("gemini");if(U.codex.check())q.push("codex");return q};var rX=()=>{let q=I(K.superpowersRepo,".agents","templates","TOOLS.md.template");if(v(q))try{return k(q,"utf8").trim()}catch(Q){return"### Using Tools\n\nUse your native skill tool. Fallback: `superpowers-agent find-skills`"}return""},Lq=(q,Q,Y,Z=!0)=>{let X=v(q);if(!X&&!Z)return{updated:!1,created:!1,skipped:!0};let $=rX(),W=Q.replace(/\{\{TOOL_MAPPINGS\}\}/g,$),H=new Date().toISOString().split("T")[0];W=W.replace(/\{\{DATE\}\}/g,H),W=W.replace(/\{\{SUPERPOWERS_PATH\}\}/g,K.superpowersRepo);let B=`<!-- SUPERPOWERS_SKILLS_START -->
${W}
<!-- SUPERPOWERS_SKILLS_END -->`;if(X){let z=new Date().toISOString().split("T")[0],E=`${q}.backup-${z}`;try{F(`cp "${q}" "${E}"`,{stdio:"pipe"})}catch(T){return{updated:!1,error:!0,message:`Failed to backup: ${T.message}`}}let V;try{V=k(q,"utf8")}catch(T){return{updated:!1,error:!0,message:`Failed to read: ${T.message}`}}let A="<!-- SUPERPOWERS_SKILLS_START -->",_="<!-- SUPERPOWERS_SKILLS_END -->",w;if(V.includes(A)&&V.includes(_)){let T=new RegExp(`${A}[\\s\\S]*?${_}`,"g");w=V.replace(T,B)}else{let T=V.match(/^#\s+.+$/m);if(T){let j=V.indexOf(`
`,T.index)+1;w=V.slice(0,j)+`
`+B+`
`+V.slice(j)}else w=B+`

`+V}try{return qq(q,w,"utf8"),{updated:!0,created:!1,backup:E}}catch(T){return{updated:!1,error:!0,message:`Failed to write: ${T.message}`}}}else try{let z=pQ(q);if(!v(z))F(`mkdir -p "${z}"`,{stdio:"pipe"});let A=`# ${e(q).base.toUpperCase()}

`+B;return qq(q,A,"utf8"),{updated:!1,created:!0}}catch(z){return{updated:!1,created:!1,error:!0,message:`Failed to create: ${z.message}`}}},sX=()=>{console.log("Installing Unix aliases...");let q=I(K.superpowersRepo,".agents","superpowers-agent"),Q=I(K.home,".local","bin"),Y=I(Q,"superpowers-agent"),Z=I(Q,"superpowers");if(!v(q)){console.log("⚠️  superpowers-agent executable not found");return}if(!v(Q))try{F(`mkdir -p "${Q}"`,{stdio:"pipe"}),console.log(`✓ Created ${Q}`)}catch($){console.log(`⚠️  Failed to create ${Q}: ${$.message}`);return}try{if(v(Y))F(`rm "${Y}"`,{stdio:"pipe"});F(`ln -s "${q}" "${Y}"`,{stdio:"pipe"}),console.log(`✓ Created symlink: superpowers-agent -> ${q}`)}catch($){console.log(`⚠️  Failed to create superpowers-agent symlink: ${$.message}`)}try{if(v(Z))F(`rm "${Z}"`,{stdio:"pipe"});F(`ln -s "${q}" "${Z}"`,{stdio:"pipe"}),console.log(`✓ Created symlink: superpowers -> ${q}`)}catch($){console.log(`⚠️  Failed to create superpowers symlink: ${$.message}`)}if(!(process.env.PATH||"").includes(Q))console.log(`
⚠️  Warning: ${Q} is not in your PATH`),console.log("   Add this to your shell profile (~/.bashrc, ~/.zshrc, etc.):"),console.log(`   export PATH="$HOME/.local/bin:$PATH"
`)},tX=()=>{console.log("Installing Windows aliases..."),console.log("✓ Windows alias installation (implementation pending)")},aq=()=>{let q=iX();if(console.log(`## Installing Universal Aliases
`),q==="win32")return tX();else return sX()},eX=(q)=>{let Q=I(K.superpowersRepo,".github","copilot-instructions.md"),Y=I(K.superpowersRepo,"skills","meta","using-superpowers","SKILL.md"),Z=I(q,".github","copilot-instructions.md"),X="<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->",$="<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->";if(!v(Q))return{error:!0,message:"Source template not found"};if(!v(Y))return{error:!0,message:"using-superpowers SKILL.md not found"};let W,H;try{W=k(Q,"utf8"),H=k(Y,"utf8")}catch(E){return{error:!0,message:`Failed to read source files: ${E.message}`}}let B=W.replace("${content}",H);if(!B.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->")||!B.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->"))return{error:!0,message:"Template is missing required markers"};let z=pQ(Z);try{if(!v(z))F(`mkdir -p "${z}"`,{stdio:"pipe"})}catch(E){return{error:!0,message:`Failed to create .github directory: ${E.message}`}}if(v(Z)){let E;try{E=k(Z,"utf8")}catch(_){return{error:!0,message:`Failed to read existing file: ${_.message}`}}let V=new Date().toISOString().replace(/[:.]/g,"-"),A=`${Z}.backup-${V}`;try{F(`cp "${Z}" "${A}"`,{stdio:"pipe"})}catch(_){return{error:!0,message:`Failed to backup: ${_.message}`}}if(E.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->")&&E.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->")){let _=new RegExp("<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->[\\s\\S]*?<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->","g"),w=E.replace(_,B.trim());try{return qq(Z,w,"utf8"),{updated:!0,backup:A}}catch(T){return{error:!0,message:`Failed to update: ${T.message}`}}}else{let _=E.trimEnd()+`

`+B.trim()+`
`;try{return qq(Z,_,"utf8"),{updated:!0,backup:A}}catch(w){return{error:!0,message:`Failed to append: ${w.message}`}}}}else try{return qq(Z,B,"utf8"),{created:!0}}catch(E){return{error:!0,message:`Failed to create: ${E.message}`}}},cQ=()=>{C(),console.log(`# Setting up Superpowers skills for this project
`);let q=process.cwd(),Q=I(q,".agents"),Y=I(Q,"skills"),Z=I(q,"AGENTS.md"),X=I(Q,"AGENTS.md"),$=v(Z)?Z:X,W=I(K.superpowersRepo,".agents","templates","AGENTS.md.template");if(!v(W)){console.log(`✗ Error: AGENTS.md template not found
  Expected at: ${W}
  Please update your Superpowers installation`);return}if(!v(Q))try{F(`mkdir -p "${Q}"`,{stdio:"pipe"}),console.log("✓ Created .agents/ directory")}catch(L){console.log(`✗ Failed to create .agents/ directory: ${L.message}`);return}else console.log("✓ .agents/ directory exists");if(!v(Y))try{F(`mkdir -p "${Y}"`,{stdio:"pipe"}),F(`touch "${I(Y,".gitkeep")}"`,{stdio:"pipe"}),console.log("✓ Created .agents/skills/ directory")}catch(L){console.log(`✗ Failed to create skills directory: ${L.message}`);return}else console.log("✓ .agents/skills/ directory exists");let H=I(Q,"docs"),B=I(H,"SUPERPOWERS.md"),z=I(K.superpowersRepo,".agents","templates","SUPERPOWERS.md.template");if(!v(H))try{F(`mkdir -p "${H}"`,{stdio:"pipe"}),console.log("✓ Created .agents/docs/ directory")}catch(L){console.log(`⚠️  Failed to create docs directory: ${L.message}`)}else console.log("✓ .agents/docs/ directory exists");let E=v($);if(E){let L=new Date().toISOString().split("T")[0],Jq=`${$}.backup-${L}`;try{F(`cp "${$}" "${Jq}"`,{stdio:"pipe"}),console.log(`✓ Backed up existing AGENTS.md to ${e(Jq).base}`)}catch(XX){console.log(`✗ Failed to backup AGENTS.md: ${XX.message}`);return}}let V;try{V=k(W,"utf8")}catch(L){console.log(`✗ Failed to read template: ${L.message}`);return}let A=S();if(V=V.replace(/\{\{VERSION\}\}/g,A),v(z))try{let L=k(z,"utf8"),Jq=new Date().toISOString().split("T")[0];L=L.replace(/\{\{VERSION\}\}/g,A),L=L.replace(/\{\{DATE\}\}/g,Jq),qq(B,L,"utf8"),console.log("✓ Created .agents/docs/SUPERPOWERS.md")}catch(L){console.log(`⚠️  Failed to create SUPERPOWERS.md: ${L.message}`)}else console.log("⚠️  SUPERPOWERS.md.template not found");let _=[],w=I(q,".github","copilot-instructions.md"),T=I(K.home,".github","copilot-instructions.md");if(v(w)||v(T)||U.copilot.check())_.push("github-copilot");let j=I(q,"CLAUDE.md"),oq=I(Q,"CLAUDE.md");if(v(j)||v(oq)||U.claude.check())_.push("claude-code");let Xq=I(q,"GEMINI.md"),iq=I(Q,"GEMINI.md");if(v(Xq)||v(iq)||U.gemini.check())_.push("gemini");if(U.cursor.check())_.push("cursor");if(U.opencode.check())_.push("opencode");if(U.codex.check())_.push("codex");console.log(`
Detected platforms for project: ${_.join(", ")||"none"}
`);let QX=_.filter((L)=>["github-copilot","cursor","opencode","codex"].includes(L)),Yq=Lq($,V,QX,!E);if(Yq.created)console.log(`✓ Created AGENTS.md with platform tool mappings (${$===Z?"root":".agents/"})`);else if(Yq.updated)console.log(`✓ Updated AGENTS.md with platform tool mappings (${$===Z?"root":".agents/"})`);else if(Yq.error)console.log("⚠️  Failed to update AGENTS.md");let Pq=v(j)?j:oq,P=Lq(Pq,V,["claude-code"],!1);if(P.created)console.log(`✓ Created CLAUDE.md with Claude Code tool mappings (${Pq===j?"root":".agents/"})`);else if(P.updated){if(console.log(`✓ Updated CLAUDE.md with Claude Code tool mappings (${Pq===j?"root":".agents/"})`),P.backup)console.log(`  Backed up to ${e(P.backup).base}`)}else if(P.skipped)console.log("ℹ️  Skipped CLAUDE.md (does not exist)");else if(P.error)console.log("⚠️  Failed to update CLAUDE.md");let fq=v(Xq)?Xq:iq,f=Lq(fq,V,["gemini"],!1);if(f.created)console.log(`✓ Created GEMINI.md with Gemini tool mappings (${fq===Xq?"root":".agents/"})`);else if(f.updated){if(console.log(`✓ Updated GEMINI.md with Gemini tool mappings (${fq===Xq?"root":".agents/"})`),f.backup)console.log(`  Backed up to ${e(f.backup).base}`)}else if(f.skipped)console.log("ℹ️  Skipped GEMINI.md (does not exist)");else if(f.error)console.log("⚠️  Failed to update GEMINI.md");let Uq=I(q,".github","copilot-instructions.md"),x={skipped:!0};if(_.includes("github-copilot")){if(x=eX(q),x.created)console.log("✓ Created .github/copilot-instructions.md with Superpowers instructions");else if(x.updated){if(console.log("✓ Updated .github/copilot-instructions.md with Superpowers instructions"),x.backup)console.log(`  Backed up to ${e(x.backup).base}`)}else if(x.error)console.log(`⚠️  Failed to update .github/copilot-instructions.md: ${x.message||""}`)}else console.log("ℹ️  Skipped .github/copilot-instructions.md (GitHub Copilot not detected)");let rq=!v(Z)&&!v(X);if(v(Uq)&&rq){let L=Lq(Uq,V,["github-copilot"],!1);if(L.updated){if(console.log("✓ Updated .github/copilot-instructions.md with GitHub Copilot tool mappings"),L.backup)console.log(`  Backed up to ${e(L.backup).base}`)}else if(L.error)console.log("⚠️  Failed to update .github/copilot-instructions.md with tool mappings")}else if(v(Uq)&&!rq)console.log("ℹ️  Skipped .github/copilot-instructions.md tool mappings (AGENTS.md exists, using that instead)");else if(!v(Uq)&&!_.includes("github-copilot"))console.log("ℹ️  Skipped .github/copilot-instructions.md tool mappings (does not exist)");console.log(`
## Syncing Project Skill Symlinks
`);let p=uQ({projectRoot:q});if(p.created>0)console.log(`
✓ Created ${p.created} project skill symlink(s)`);else if(p.existed>0)console.log("ℹ️  Project skill symlinks already exist");else if(p.errors.length>0)console.log("⚠️  Some symlinks could not be created");else console.log("ℹ️  No agent directories detected for symlinking");let y=`
# Setup complete!

Your project now has:
  - .agents/ directory structure`;if(Yq.updated||Yq.created)y+=`
  - AGENTS.md with universal skills instructions`;if(P.updated||P.created)y+=`
  - CLAUDE.md with Claude Code skills instructions`;if(f.updated||f.created)y+=`
  - GEMINI.md with Gemini skills instructions`;if(x.updated||x.created)y+=`
  - .github/copilot-instructions.md with Superpowers instructions`;if(p.created>0)y+=`
  - ${p.created} project skill symlink(s) to agent directories`;y+=`
  - .agents/skills/ ready for project-specific skills`,y+=`
  - .agents/docs/SUPERPOWERS.md for detailed reference
`,console.log(y)},nQ=()=>{if(C(),console.log(`# Superpowers Bootstrap for Agents
# ==================================
`),!process.argv.includes("--no-update")){let z=zq(),E=wq();if(E.error)console.log(`## Update Check

⚠️  Could not check for updates (network issue)

---
`);else if(E.hasUpdates)if(z.auto_update&&!E.hasLocalChanges&&Bq())console.log(`## Auto-Update
`),Rq(),console.log(`
---
`);else{if(console.log(`## Update Available
`),E.hasLocalChanges)console.log("⚠️  Your superpowers installation is behind the latest version.\n    Cannot auto-update: local changes detected\n    To update, commit/stash changes then run: `superpowers-agent update`");else if(!Bq())console.log("⚠️  Your superpowers installation is behind the latest version.\n    Cannot auto-update: not on main branch\n    To update, switch to main then run: `superpowers-agent update`");else console.log("⚠️  Your superpowers installation is behind the latest version.\n    To update, run: `superpowers-agent update`\n    Or enable auto-update: `superpowers-agent config-set auto_update true`");console.log(`
---
`)}}if(aq(),console.log(`---
`),console.log(`## GitHub Copilot Integration
`),n(),console.log(`
---
`),console.log(`## Cursor Integration
`),l(),console.log(""),o(),console.log(`
---
`),console.log(`## OpenAI Codex Integration
`),U.codex.check())i();else console.log(`⚠️  Skipped (${U.codex.name} CLI not detected)
\uD83D\uDCA1 To enable Codex integration:
   1. Install Codex: ${U.codex.installUrl}
   2. Run: superpowers-agent ${U.codex.bootstrapCommand}`);if(console.log(`
---
`),console.log(`## Gemini Integration
`),U.gemini.check())r();else console.log(`⚠️  Skipped (${U.gemini.name} CLI not detected)
\uD83D\uDCA1 To enable Gemini integration:
   1. Install Gemini: ${U.gemini.installUrl}
   2. Run: superpowers-agent ${U.gemini.bootstrapCommand}`);if(console.log(`
---
`),console.log(`## Claude Code Integration
`),U.claude.check())s();else console.log(`⚠️  Skipped (${U.claude.name} CLI not detected)
\uD83D\uDCA1 To enable Claude Code integration:
   1. Install Claude Code: ${U.claude.installUrl}
   2. Run: superpowers-agent ${U.claude.bootstrapCommand}`);if(console.log(`
---
`),console.log(`## OpenCode Integration
`),U.opencode.check())t(),console.log(""),Gq();else console.log(`⚠️  Skipped (${U.opencode.name} CLI not detected)
\uD83D\uDCA1 To enable OpenCode integration:
   1. Install OpenCode: ${U.opencode.installUrl}
   2. Run: superpowers-agent ${U.opencode.bootstrapCommand}`);console.log(`
---
`),console.log(`## Generating Platform-Specific Files
`);let $=mQ();console.log(`Detected platforms: ${$.join(", ")||"none"}
`);let W=I(K.superpowersRepo,".agents","templates","AGENTS.md.template"),H="";try{H=k(W,"utf8")}catch(z){console.log(`⚠️  Could not read AGENTS.md template: ${z.message}
`)}if(H){let z=S();H=H.replace(/\{\{VERSION\}\}/g,z);let E=I(K.home,".agents","AGENTS.md"),V=Lq(E,H,$,!0);if(V.created)console.log(`✓ Created ${E}`);else if(V.updated)console.log(`✓ Updated ${E}`)}console.log(`
---
`),console.log(`## Syncing Skill Symlinks
`);let B=process.argv.includes("--force");Cq({force:B}),console.log(`
---
`),console.log(`# Bootstrap Complete!
`),console.log("✓ All integrations installed"),console.log("✓ Skills system ready"),console.log(`✓ Skill symlinks synced
`),console.log("Next steps:"),console.log("  - Run `superpowers-agent find-skills` to see available skills"),console.log("  - Run `superpowers-agent setup-skills` in your project directory")};import{existsSync as D,readFileSync as aQ}from"fs";import{join as M,dirname as lq,parse as xq}from"path";import{execSync as N}from"child_process";import{homedir as Qq}from"os";var lQ=(q)=>{let Q=q.match(/^https:\/\/github\.com\/([^/]+)\/([^/]+)\/tree\/([^/]+)\/(.+)$/);if(Q){let[,X,$,W,H]=Q;return{type:"git-tree",repoUrl:`https://github.com/${X}/${$}.git`,branch:W,path:H,original:q}}let Y=q.match(/^git@github\.com:([^/]+)\/([^/]+?)(?:\.git)?$/);if(Y){let[,X,$]=Y;return{type:"git-repo",repoUrl:`git@github.com:${X}/${$}.git`,branch:null,path:null,original:q}}let Z=q.match(/^https:\/\/github\.com\/([^/]+)\/([^/]+)(?:\.git)?$/);if(Z){let[,X,$]=Z;return{type:"git-repo",repoUrl:`https://github.com/${X}/${$}.git`,branch:null,path:null,original:q}}if(D(q))return{type:"local",path:q,original:q};return null},oQ=(q)=>{let Q=q.includes("--global")||q.includes("-g"),Y=q.includes("--project")||q.includes("-p");if(Q)return M(Qq(),".agents","skills");if(Y)return M(process.cwd(),".agents","skills");let Z=M(process.cwd(),".agents","config.json"),X=M(Qq(),".agents","config.json");for(let $ of[Z,X])if(D($))try{if(JSON.parse(aQ($,"utf8")).installLocation==="project")return M(process.cwd(),".agents","skills")}catch(W){}return M(Qq(),".agents","skills")},iQ=(q,Q)=>{let Y=M(Qq(),".agents","tmp",`skill-install-${Date.now()}`);try{if(N(`mkdir -p "${Y}"`,{stdio:"pipe"}),Q)N(`git clone --branch ${Q} --depth 1 "${q}" "${Y}"`,{stdio:"pipe",timeout:30000});else N(`git clone --depth 1 "${q}" "${Y}"`,{stdio:"pipe",timeout:30000});return Y}catch(Z){try{N(`rm -rf "${Y}"`,{stdio:"pipe"})}catch{}throw Error(`Failed to clone repository: ${Z.message}`)}},jq=(q)=>{let Q=M(q,"skill.json");if(!D(Q))return null;try{let Y=aQ(Q,"utf8");return JSON.parse(Y)}catch(Y){throw Error(`Failed to read skill.json: ${Y.message}`)}},Dq=(q,Q,Y,Z)=>{let X=M(q,Q);if(!D(X)){Z.errors.push(`Skill directory not found: ${Q}`);return}let $=jq(X);if(!$){Z.errors.push(`No skill.json found in: ${Q}`);return}let W=$.name||Q,H=M(Y,W),B=lq(H);try{N(`mkdir -p "${B}"`,{stdio:"pipe"})}catch(z){Z.errors.push(`Failed to create directory ${B}: ${z.message}`);return}try{if(D(H))N(`rm -rf "${H}"`,{stdio:"pipe"});N(`cp -R "${X}" "${H}"`,{stdio:"pipe"}),Z.installed.push({name:W,path:H,title:$.title||$.description||W})}catch(z){Z.errors.push(`Failed to install ${Q}: ${z.message}`)}},rQ=()=>{let q=process.argv.slice(3),Q=q.find((z)=>!z.startsWith("-")),Y=q.filter((z)=>z.startsWith("-"));if(!Q){console.log(`Usage: superpowers-agent add <url-or-path|@alias> [skill-path] [options]

Options:
  --global, -g   Install skills globally in ~/.agents/skills/ (default)
  --project, -p  Install skills in current project's .agents/skills/

Examples:
  superpowers-agent add https://github.com/example/skills
  superpowers-agent add https://github.com/example/repo/tree/main/skills
  superpowers-agent add ~/my-local-skills
  superpowers-agent add https://github.com/example/skills --project
  superpowers-agent add ~/my-local-skills --global
  superpowers-agent add @myrepo path/to/skill
  superpowers-agent add @myrepo path/to/skills --project

Description:
  Installs skill(s) from a Git repository, local directory, or repository alias.
  Supports repositories with single or multiple skills.
  Reads skill.json to determine skill names and installation paths.
  
  Repository aliases can be added using:
    superpowers-agent add-repository <git-url> [--as=@alias]`);return}console.log(`Installing skill(s)...
`);let Z=null,X=null;if(Q.startsWith("@")){let z=Q;X=q.find((_,w)=>w>0&&!_.startsWith("-")&&_!==z);let E=g(!1),V=g(!0);if(E[z])Z=E[z],console.log(`Using project repository alias: ${z}`);else if(V[z])Z=V[z],console.log(`Using global repository alias: ${z}`);else{console.log(`Error: Repository alias not found: ${z}`),console.log(`
Available aliases:`);let _={...V,...E};if(Object.keys(_).length===0)console.log("  (none)"),console.log(`
Add a repository using:`),console.log("  superpowers-agent add-repository <git-url>");else for(let[w,T]of Object.entries(_))console.log(`  ${w} -> ${T}`);return}let A=D(Z);if(X)if(A)Q=M(Z,X),console.log(`Repository path: ${Z}`),console.log(`Skill path: ${X}
`);else Q=`${Z}/tree/main/${X}`,console.log(`Repository URL: ${Z}`),console.log(`Skill path: ${X}
`);else if(Q=Z,A)console.log(`Repository path: ${Z}
`);else console.log(`Repository URL: ${Z}
`)}let $=lQ(Q);if(!$){console.log(`Error: Invalid URL or path not found: ${Q}`);return}let W=oQ(Y);console.log(`Install location: ${W}
`);let H,B=!1;try{if($.type==="git-repo"||$.type==="git-tree"){if(console.log(`Cloning repository: ${$.repoUrl}`),$.branch)console.log(`Branch: ${$.branch}`);if(H=iQ($.repoUrl,$.branch),B=!0,$.path){if(H=M(H,$.path),!D(H))throw Error(`Path not found in repository: ${$.path}`)}console.log("")}else H=$.path;let z=jq(H);if(!z)throw Error("No skill.json found in source directory");let E={installed:[],errors:[],source:$.original};if(z.skills&&Array.isArray(z.skills)){console.log(`Found ${z.skills.length} skill(s) to install
`);for(let V of z.skills)Dq(H,V,W,E)}else{let V=z.name||xq(H).base;Dq(lq(H),xq(H).base,W,E)}if(B&&H)try{let V=H.split("/.agents/tmp/")[0]+"/.agents/tmp/"+H.split("/.agents/tmp/")[1].split("/")[0];N(`rm -rf "${V}"`,{stdio:"pipe"})}catch{}if(console.log(`
**Successfully installed skills:**`),console.log(`- Source: ${E.source}`),E.installed.length>0)for(let V of E.installed)console.log(`  - Installed: ${V.name} at ${V.path}`),console.log(`    ${V.title}`);if(E.errors.length>0){console.log(`
**Errors:**`);for(let V of E.errors)console.log(`  - ${V}`)}if(E.installed.length===0&&E.errors.length===0)console.log("  No skills were installed");if(E.installed.length>0)console.log(`
**Syncing skill symlinks...**`),nq()}catch(z){if(B&&H)try{let E=H.split("/.agents/tmp/")[0]+"/.agents/tmp/"+H.split("/.agents/tmp/")[1].split("/")[0];N(`rm -rf "${E}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${z.message}`)}},sQ=()=>{let q=process.argv.slice(3),Q=q.find((z)=>!z.startsWith("-")),Y=q.filter((z)=>z.startsWith("-"));if(!Q){console.log(`Usage: superpowers-agent pull <url-or-path|@alias> [skill-path] [options]

Options:
  --global, -g   Update skills globally in ~/.agents/skills/ (default)
  --project, -p  Update skills in current project's .agents/skills/

Examples:
  superpowers-agent pull https://github.com/example/skills
  superpowers-agent pull https://github.com/example/repo/tree/main/skills
  superpowers-agent pull ~/my-local-skills
  superpowers-agent pull https://github.com/example/skills --project
  superpowers-agent pull ~/my-local-skills --global
  superpowers-agent pull @myrepo
  superpowers-agent pull @myrepo path/to/skill
  superpowers-agent pull @myrepo path/to/skills --project

Description:
  Updates or adds skill(s) from a Git repository, local directory, or repository alias.
  If skills already exist, they will be updated (replaced).
  If skills don't exist, they will be added.
  Supports repositories with single or multiple skills.
  Reads skill.json to determine skill names and installation paths.
  
  Repository aliases can be added using:
    superpowers-agent add-repository <git-url> [--as=@alias]`);return}console.log(`Updating skill(s)...
`);let Z=null,X=null;if(Q.startsWith("@")){let z=Q;X=q.find((_,w)=>w>0&&!_.startsWith("-")&&_!==z);let E=g(!1),V=g(!0);if(E[z])Z=E[z],console.log(`Using project repository alias: ${z}`);else if(V[z])Z=V[z],console.log(`Using global repository alias: ${z}`);else{console.log(`Error: Repository alias not found: ${z}`),console.log(`
Available aliases:`);let _={...V,...E};if(Object.keys(_).length===0)console.log("  (none)"),console.log(`
Add a repository using:`),console.log("  superpowers-agent add-repository <git-url>");else for(let[w,T]of Object.entries(_))console.log(`  ${w} -> ${T}`);return}let A=D(Z);if(X)if(A)Q=M(Z,X),console.log(`Repository path: ${Z}`),console.log(`Skill path: ${X}
`);else Q=`${Z}/tree/main/${X}`,console.log(`Repository URL: ${Z}`),console.log(`Skill path: ${X}
`);else if(Q=Z,A)console.log(`Repository path: ${Z}
`);else console.log(`Repository URL: ${Z}
`)}let $=lQ(Q);if(!$){console.log(`Error: Invalid URL or path not found: ${Q}`);return}let W=oQ(Y);console.log(`Install location: ${W}
`);let H,B=!1;try{if($.type==="git-repo"||$.type==="git-tree"){if(console.log(`Cloning repository: ${$.repoUrl}`),$.branch)console.log(`Branch: ${$.branch}`);if(H=iQ($.repoUrl,$.branch),B=!0,$.path){if(H=M(H,$.path),!D(H))throw Error(`Path not found in repository: ${$.path}`)}console.log("")}else H=$.path;let z=jq(H);if(!z)throw Error("No skill.json found in source directory");let E={installed:[],errors:[],source:$.original};if(z.skills&&Array.isArray(z.skills)){console.log(`Found ${z.skills.length} skill(s) to update
`);for(let V of z.skills)Dq(H,V,W,E)}else{let V=z.name||xq(H).base;Dq(lq(H),xq(H).base,W,E)}if(B&&H)try{let V=H.split("/.agents/tmp/")[0]+"/.agents/tmp/"+H.split("/.agents/tmp/")[1].split("/")[0];N(`rm -rf "${V}"`,{stdio:"pipe"})}catch{}if(console.log(`
**Successfully updated skills:**`),console.log(`- Source: ${E.source}`),E.installed.length>0)for(let V of E.installed)console.log(`  - Updated: ${V.name} at ${V.path}`),console.log(`    ${V.title}`);if(E.errors.length>0){console.log(`
**Errors:**`);for(let V of E.errors)console.log(`  - ${V}`)}if(E.installed.length===0&&E.errors.length===0)console.log("  No skills were updated");if(E.installed.length>0)console.log(`
**Syncing skill symlinks...**`),nq()}catch(z){if(B&&H)try{let E=H.split("/.agents/tmp/")[0]+"/.agents/tmp/"+H.split("/.agents/tmp/")[1].split("/")[0];N(`rm -rf "${E}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${z.message}`)}},tQ=()=>{let q=process.argv.slice(3),Q=q.find((z)=>!z.startsWith("-")),Y=q.filter((z)=>z.startsWith("-"));if(!Q){console.log(`Usage: superpowers-agent add-repository <git-url> [options]

Options:
  --global, -g        Add repository globally in ~/.agents/config.json (default)
  --project, -p       Add repository in current project's .agents/config.json
  --as=<alias>        Specify custom alias for the repository

Examples:
  superpowers-agent add-repository https://github.com/example/skills.git
  superpowers-agent add-repository https://github.com/example/skills.git --as=@myskills
  superpowers-agent add-repository https://github.com/example/skills.git --project
  superpowers-agent add-repository https://github.com/example/skills.git --as=@custom --global

Description:
  Adds a skill repository alias to your configuration.
  The repository's skill.json will be read to determine the default alias.
  Use --as to specify a custom alias.
  After adding, you can install skills using: superpowers-agent add @alias path/to/skill`);return}console.log(`Adding repository...
`);let Z=Y.some((z)=>z==="--global"||z==="-g"),X=Y.some((z)=>z==="--project"||z==="-p"),$=Y.find((z)=>z.startsWith("--as=")),W=$?$.split("=")[1]:null,H=X?!1:!0,B;try{console.log(`Cloning repository: ${Q}`),B=M(Qq(),".agents","tmp",`repo-add-${Date.now()}`),N(`mkdir -p "${B}"`,{stdio:"pipe"}),N(`git clone --depth 1 "${Q}" "${B}"`,{stdio:"pipe",timeout:30000}),console.log("");let z=jq(B);if(!z)throw Error("No skill.json found in repository");let E;if(W)E=W;else if(z.repository)E=z.repository;else{let _=Q.match(/\/([^/]+?)(?:\.git)?$/);if(_)E="@"+_[1];else throw Error("Could not determine repository alias. Use --as=@alias to specify manually.")}zQ(E,Q,H),N(`rm -rf "${B}"`,{stdio:"pipe"});let V=H?"globally":"in project",A=H?M(Qq(),".agents","config.json"):M(process.cwd(),".agents","config.json");console.log(`✓ Repository added ${V}`),console.log(`  Alias: ${E}`),console.log(`  URL: ${Q}`),console.log(`  Config: ${A}`),console.log(`
You can now install skills using:`),console.log(`  superpowers-agent add ${E} <path-to-skill>`)}catch(z){if(B)try{N(`rm -rf "${B}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${z.message}`)}};import{dirname as qY}from"path";var eQ=(q)=>{if(!q){console.log(`Usage: superpowers-agent use-skill <skill-name>

Examples (smart matching):
  superpowers-agent use-skill brainstorming              # Matches superpowers:collaboration/brainstorming
  superpowers-agent use-skill collaboration/brainstorming # More specific suffix
  superpowers-agent use-skill test-driven-development    # Matches superpowers:testing/test-driven-development

Examples (explicit paths):
  superpowers-agent use-skill superpowers:collaboration/brainstorming  # Full superpowers skill path
  superpowers-agent use-skill claude:email-assistant                   # Full claude skill path
  superpowers-agent use-skill my-custom-skill                          # Project skill

Smart matching:
  - Type just the skill name or any suffix of the path
  - Priority: .agents/skills/ > .claude/skills/ > ~/.agents/skills/ > ~/.agents/superpowers/skills/
  - If multiple skills match at same priority, you'll see them with descriptions`);return}let Q=u(q);if(!Q){console.log(`Error: Skill not found: ${q}

Available skills:`),console.log("Run: superpowers-agent find-skills");return}let{skillFile:Y,sourceType:Z,actualSkillPath:X}=Q;try{let{content:$,frontmatter:W}=qQ(Y),H=Hq[Z].prefix+X,B=qY(Y),z=`# ${W.name||H}`;if(W.description)z+=`
# ${W.description}`;if(W.whenToUse)z+=`
# When to use: ${W.whenToUse}`;z+=`
# Supporting tools and docs are in ${B}
# ============================================

${$}`,console.log(z)}catch($){console.log($.message)}};var qX={bootstrap:nQ,version:hQ,"check-updates":()=>{dQ().catch((q)=>{console.error(q.message),process.exit(1)})},update:()=>{let q=process.argv.includes("--no-reinstall");Rq({skipReinstall:q})},"config-get":IQ,"config-set":OQ,"setup-skills":cQ,"use-skill":()=>eQ(process.argv[3]),execute:LQ,"find-skills":c,add:rQ,"add-repository":tQ,"list-repositories":wQ,pull:sQ,dir:JQ,path:UQ,"get-helpers":AQ,"install-copilot-prompts":n,"install-cursor-commands":l,"install-cursor-hooks":o,"install-codex-prompts":i,"install-gemini-commands":r,"install-claude-commands":s,"install-opencode-commands":t,"install-aliases":aq,default:()=>{console.log(`Superpowers for Agents
Usage:
  superpowers-agent bootstrap [--no-update] [--force]               # Run complete bootstrap
  superpowers-agent version                                         # Show current version
  superpowers-agent check-updates                                   # Check for updates
  superpowers-agent update [--no-reinstall]                         # Update to latest version
  superpowers-agent config-get                                      # Show configuration
  superpowers-agent config-set <key> <value>                        # Update configuration
  superpowers-agent setup-skills                                    # Initialize project skills
  superpowers-agent use-skill <skill-name>                          # Load a skill
  superpowers-agent find-skills                                     # List available skills
  superpowers-agent add <url-or-path|@alias> [path] [options]      # Install skills
  superpowers-agent add-repository <git-url> [--as=@alias] [opts]  # Add repository alias
  superpowers-agent list-repositories                              # List configured repository aliases
  superpowers-agent pull <url-or-path|@alias> [path] [options]     # Update skills
  superpowers-agent dir <skill-name>                                # Get skill directory
  superpowers-agent path <skill-name>                               # Get skill file path
  superpowers-agent execute <skill-name>                            # Execute a skill
  superpowers-agent get-helpers <skill> <search-term>               # Get helper file path
  superpowers-agent install-copilot-prompts                         # Install GitHub Copilot prompts
  superpowers-agent install-cursor-commands                         # Install Cursor commands
  superpowers-agent install-cursor-hooks                            # Install Cursor hooks
  superpowers-agent install-codex-prompts                           # Install Codex prompts
  superpowers-agent install-gemini-commands                         # Install Gemini commands
  superpowers-agent install-claude-commands                         # Install Claude commands
  superpowers-agent install-opencode-commands                       # Install OpenCode commands
  superpowers-agent install-aliases                                 # Install universal aliases (superpowers, superpowers-agent)

Documentation: https://github.com/complexthings/superpowers
`)}},QY=process.argv[2]||"default",XY=qX[QY]||qX.default;try{XY()}catch(q){console.error(`Error: ${q.message}`),process.exit(1)}
