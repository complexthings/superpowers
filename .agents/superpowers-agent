#!/usr/bin/env node
import{relative as eo,dirname as oo,join as os}from"path";import{existsSync as Pe}from"fs";import{homedir as Oe,platform as Oo}from"os";import{join as w,parse as No,dirname as Ne}from"path";import{existsSync as q}from"fs";import{fileURLToPath as vo}from"url";var Go=vo(import.meta.url),X=Ne(Go),Mo=()=>{let e=Oe();switch(Oo()){case"darwin":return w(e,"Library","Application Support","Code","User");case"win32":return w(e,"AppData","Roaming","Code","User");case"linux":return w(e,".config","Code","User");default:return w(e,".config","Code","User")}},Wo=()=>{let e=process.cwd(),o=No(e).root;for(;e!==o;){if(q(w(e,".agents")))return e;e=Ne(e)}return process.cwd()},c={home:Oe(),vscodeUserDir:Mo(),projectRoot:Wo(),get projectClaudeSkills(){return w(this.projectRoot,".claude","skills")},get projectAgentsSkills(){return w(this.projectRoot,".agents","skills")},get projectSkillsDir(){return w(this.projectRoot,"skills")},get homePersonalSkills(){return w(this.home,".agents","skills")},get bootstrap(){return w(this.projectRoot,".agents","superpowers-bootstrap.md")},get superpowersRepo(){let e=w(X,"..","..","..","skills");return q(e)&&q(w(X,"..","..","..",".github","prompts"))?w(X,"..","..",".."):w(this.home,".agents","superpowers")},get homeSuperpowersSkills(){return w(this.superpowersRepo,"skills")},get isSuperpowersRepo(){let e=w(X,"..","..","..","skills");return q(e)&&q(w(X,"..","..","..",".github","prompts"))}};import{readFileSync as ve}from"fs";var Q=e=>{try{let t=ve(e,"utf8").split(`
`),n={name:"",description:"",whenToUse:""},s=!1;for(let r of t){if(r.trim()==="---"){if(s)break;s=!0;continue}if(s){let l=r.match(/^(\w+):\s*(.*)$/);if(l){let[,i,p]=l,a={name:"name",description:"description",when_to_use:"whenToUse"};a[i]&&(n[a[i]]=p.trim())}}}return n}catch{return{name:"",description:"",whenToUse:""}}},Ge=e=>{try{let o=ve(e,"utf8"),t=Q(e),n=o.split(`
`),s=!1,r=!1,l=[];for(let i of n){if(i.trim()==="---"){if(s){r=!0;continue}s=!0;continue}(r||!s)&&l.push(i)}return{content:l.join(`
`).trim(),frontmatter:t}}catch(o){throw new Error(`Error reading skill file: ${o.message}`)}};import{existsSync as je,readdirSync as _e,statSync as He}from"fs";import{join as Ee,relative as Fs}from"path";import{readFileSync as _o,existsSync as Ho}from"fs";import{join as Me}from"path";var Z=e=>{let o=Me(e,"skill.json");if(!Ho(o))return null;try{let t=_o(o,"utf8");return JSON.parse(t)}catch{return null}};var Re=e=>e.startsWith("superpowers:")?{type:"superpowers",path:e.substring(12).replace(/^skills\//,"")}:e.startsWith("claude:")?{type:"claude",path:e.substring(7).replace(/^skills\//,"")}:{type:null,path:e.replace(/^skills\//,"")},We=(e,o)=>{let t=Z(e);if(!t||!t.helpers||!Array.isArray(t.helpers))return null;let n=t.helpers,s=o.toLowerCase(),r=null,l=0;for(let i of n){let p=i.toLowerCase();if(p===s){r=i;break}if(p.includes(s)){let d=s.length/p.length;d>l&&(l=d,r=i)}let a=i.split("/").pop().split(".")[0].toLowerCase();if(s.includes(a)){let d=a.length/s.length;d>l&&(l=d,r=i)}}return r?Me(e,r):null},ee={claude:{dir:"projectClaudeSkills",prefix:"claude:"},project:{dir:"projectAgentsSkills",prefix:""},personal:{dir:"homePersonalSkills",prefix:""},superpowers:{dir:"homeSuperpowersSkills",prefix:"superpowers:"}};var ie=(e,o,t=null)=>{let n=[];if(!je(e))return n;let s=(r,l)=>{if(!(t!==null&&l>t))try{let i=_e(r,{withFileTypes:!0});for(let p of i){if(p.name==="node_modules"||p.name===".git"||p.name.startsWith("."))continue;let a=Ee(r,p.name),d=p.isDirectory();if(p.isSymbolicLink())try{d=He(a).isDirectory()}catch{continue}if(!d)continue;let u=Ee(a,"SKILL.md");je(u)&&n.push(a),(t===null||l<t)&&s(a,l+1)}}catch{}};return s(e,0),n};var Je=(e,o)=>{if(!je(e))return[];let t=[],n=o.toLowerCase().replace(/\\/g,"/"),s=(r,l="")=>{try{let i=_e(r,{withFileTypes:!0});for(let p of i){let a=Ee(r,p.name),d=l?`${l}/${p.name}`:p.name,u=p.isDirectory();if(p.isSymbolicLink())try{u=He(a).isDirectory()}catch{continue}if(!(u&&(p.name==="node_modules"||p.name===".git"||p.name.startsWith(".")))){if(u)s(a,d);else if(p.name==="SKILL.md"){let h=l.toLowerCase().replace(/\\/g,"/");(h===n||h.endsWith("/"+n)||h.endsWith(n))&&t.push({file:a,path:l})}}}}catch{}};return s(e),t},Be=(e,o,t)=>{let n={project:"project skills (.agents/skills/)",claude:"claude skills (.claude/skills/)",personal:"personal skills (~/.agents/skills/)",superpowers:"superpowers skills (~/.agents/superpowers/skills/)"}[t];console.log(`Error: Multiple skills match "${e}" in ${n}:
`);for(let s of o){let l=ee[t].prefix+s.path;try{let i=Q(s.file),p=i.description||i.whenToUse||"(no description)";console.log(`  ${l}`),console.log(`    ${p}
`)}catch{console.log(`  ${l}`),console.log(`    (unable to read description)
`)}}console.log("Please be more specific. Examples:");for(let s of o.slice(0,2)){let r=ee[t].prefix;console.log(`  superpowers-agent execute ${r}${s.path}`)}};import{existsSync as Ue}from"fs";import{join as Jo,relative as Bo}from"path";var Vo=e=>{let{type:o,path:t}=Re(e),n={superpowers:c.homeSuperpowersSkills,claude:c.projectClaudeSkills,project:c.projectAgentsSkills,personal:c.homePersonalSkills},s;o?s=[{type:o,dir:n[o]}]:c.isSuperpowersRepo&&Ue(c.projectSkillsDir)?s=[{type:"project",dir:c.projectSkillsDir},{type:"project",dir:c.projectAgentsSkills},{type:"claude",dir:c.projectClaudeSkills},{type:"personal",dir:c.homePersonalSkills},{type:"superpowers",dir:c.homeSuperpowersSkills}]:s=[{type:"project",dir:c.projectAgentsSkills},{type:"claude",dir:c.projectClaudeSkills},{type:"personal",dir:c.homePersonalSkills},{type:"superpowers",dir:c.homeSuperpowersSkills}];for(let{type:r,dir:l}of s){let i=Je(l,t);if(i.length===1)return{skillFile:i[0].file,sourceType:r,actualSkillPath:i[0].path};i.length>1&&Be(t,i,r)}return null},P=e=>{let o=Vo(e);if(o)return o;let{type:t,path:n}=Re(e),s;t?s=[{type:t,dir:{superpowers:c.homeSuperpowersSkills,claude:c.projectClaudeSkills,project:c.projectAgentsSkills,personal:c.homePersonalSkills}[t]}]:c.isSuperpowersRepo&&Ue(c.projectSkillsDir)?s=[{type:"project",dir:c.projectSkillsDir},{type:"project",dir:c.projectAgentsSkills},{type:"claude",dir:c.projectClaudeSkills},{type:"personal",dir:c.homePersonalSkills},{type:"superpowers",dir:c.homeSuperpowersSkills}]:s=[{type:"project",dir:c.projectAgentsSkills},{type:"claude",dir:c.projectClaudeSkills},{type:"personal",dir:c.homePersonalSkills},{type:"superpowers",dir:c.homeSuperpowersSkills}];for(let{type:r,dir:l}of s){if(!Ue(l))continue;let i=ie(l,r,null);for(let p of i){let a=Z(p);if(a&&a.aliases&&Array.isArray(a.aliases)){let d=n.toLowerCase();for(let u of a.aliases)if(u.toLowerCase()===d){let h=Jo(p,"SKILL.md"),g=Bo(l,p);return{skillFile:h,sourceType:r,actualSkillPath:g}}}}}return null};import{readFileSync as Ve,writeFileSync as Ke,existsSync as De}from"fs";import{join as A}from"path";import{execSync as Ko}from"child_process";var Ie=()=>({auto_update:!0,last_update_check:null,last_updated_commit:null}),oe=()=>{let e=A(c.superpowersRepo,".config.json");try{if(!De(e))return Ie();let o=Ve(e,"utf8");return{...Ie(),...JSON.parse(o)}}catch{return Ie()}},ae=e=>{let o=A(c.superpowersRepo,".config.json"),n={...oe(),...e};try{Ke(o,JSON.stringify(n,null,2))}catch(s){console.log(`Warning: couldn't save config: ${s.message}`)}},Ye=e=>{let o=e?A(c.home,".agents","config.json"):A(process.cwd(),".agents","config.json");if(!De(o))return{};try{let t=Ve(o,"utf8");return JSON.parse(t)}catch{return{}}},Yo=(e,o)=>{let t=o?A(c.home,".agents"):A(process.cwd(),".agents"),n=A(t,"config.json");try{return De(t)||Ko(`mkdir -p "${t}"`,{stdio:"pipe"}),Ke(n,JSON.stringify(e,null,2),"utf8"),!0}catch(s){throw new Error(`Failed to write config: ${s.message}`)}},se=(e=!1)=>Ye(e).repositories||{},ze=(e,o,t)=>{let n=Ye(t);n.repositories||(n.repositories={}),n.repositories[e]=o,Yo(n,t)};import{readFileSync as zo}from"fs";import{join as Xe,dirname as Xo}from"path";import{fileURLToPath as qo}from"url";import{existsSync as Qo}from"fs";import{execSync as Zo}from"child_process";var es=qo(import.meta.url),qe=Xo(es),D=()=>{try{let e=Xe(qe,"package.json");return Qo(e)||(e=Xe(qe,"..","..","package.json")),JSON.parse(zo(e,"utf8")).version||"5.4.0"}catch{return"5.4.0"}},j=()=>{let e=D();console.log(`^^SAV:${e}^^`)},Qe=async()=>{try{let o=Zo('curl -sS "https://raw.githubusercontent.com/complexthings/superpowers/main/.agents/package.json"',{encoding:"utf8",timeout:1e4}),t=JSON.parse(o);return t.version?t.version:D()}catch(e){throw new Error(`Failed to fetch remote version: ${e.message}`)}},Ze=(e,o)=>{let t=e.split(".").map(Number),n=o.split(".").map(Number);for(let s=0;s<3;s++){let r=t[s]||0,l=n[s]||0;if(r>l)return!0;if(r<l)return!1}return!1};var ss={project:{dir:"projectAgentsSkills",prefix:""},claude:{dir:"projectClaudeSkills",prefix:"claude:"},personal:{dir:"homePersonalSkills",prefix:""},superpowers:{dir:"homeSuperpowersSkills",prefix:"superpowers:"}},ts=(e,o)=>{let t=os(e,"SKILL.md"),{dir:n,prefix:s}=ss[o],r=eo(c[n],e).replace(/\\/g,"/");console.log(`${s}${r}`);let{description:l,whenToUse:i}=Q(t);l&&console.log(`  ${l}`),i&&console.log(`  When to use: ${i}`),console.log("")},O=()=>{j();let e=new Set,o=[{type:"project",dir:c.projectAgentsSkills,maxDepth:null},{type:"claude",dir:c.projectClaudeSkills,maxDepth:null},{type:"personal",dir:c.homePersonalSkills,maxDepth:null},{type:"superpowers",dir:c.homeSuperpowersSkills,maxDepth:null}];for(let{type:t,dir:n,maxDepth:s}of o){let r=ie(n,t,s);for(let l of r){let i=eo(n,l);e.has(i)||(e.add(i),ts(l,t))}}console.log(`Usage:
  superpowers-agent execute <skill-name> # Load a specific skill`)},so=()=>{j();let e=process.argv[3];if(!e){console.log(`Usage: superpowers-agent execute <skill-name-or-alias>

Examples:
  superpowers-agent execute brainstorming
  superpowers-agent execute superpowers:collaboration/brainstorming
  superpowers-agent execute aem/block-collection-and-party
  superpowers-agent execute block-collection  # Using alias

Description:
  Instructs the agent to execute the specified skill.
  Supports skill aliases defined in skill.json files.`);return}let o=P(e);if(!o){console.log(`Error: Skill not found: ${e}

Available skills:`),O();return}let{skillFile:t,sourceType:n}=o,s=process.cwd(),r=t.startsWith(s),l=Pe(c.projectAgentsSkills)||Pe(c.projectClaudeSkills);console.log(r?`Open this file with your Read Tool: ${t}

<IMPORTANT>
USE YOUR Read Tool to process this file. DO NOT use \`cat\` or print commands.
</IMPORTANT>`:l&&n!=="project"?`Open this file with your Read Tool: ${t}

<IMPORTANT>
1. Try using your Read Tool first
2. If your Read tool fails (file outside workspace), use: cat "${t}"
3. Consider using workspace-local skills for better tool compatibility
   Run: superpowers-agent setup-skills
</IMPORTANT>`:`Open this file with your Read Tool: ${t}

<IMPORTANT>
1. Try using your Read Tool first
2. If your Read tool fails (file outside workspace), use: cat "${t}"
3. NEVER skip loading the skill content
</IMPORTANT>`)},to=()=>{let e=process.argv[3];if(!e){console.log(`Usage: superpowers-agent path <skill-name-or-alias>

Examples:
  superpowers-agent path brainstorming
  superpowers-agent path superpowers:collaboration/brainstorming
  superpowers-agent path aem/block-collection-and-party
  superpowers-agent path block-collection  # Using alias

Description:
  Outputs the file system path of the specified SKILL.md file.
  Supports skill aliases defined in skill.json files.`);return}let o=P(e);if(!o){console.log(`Error: Skill not found: ${e}

Available skills:`),O();return}let{skillFile:t}=o;console.log(t)},ro=()=>{let e=process.argv[3];if(!e){console.log(`Usage: superpowers-agent dir <skill-name>

Examples:
  superpowers-agent dir brainstorming
  superpowers-agent dir aem/block-collection-and-party
  superpowers-agent dir block-collection  # Using alias
  superpowers-agent dir superpowers:testing/test-driven-development

Description:
  Returns the directory path where the specified skill is located.
  Supports skill aliases defined in skill.json files.`);return}let o=P(e);if(!o){console.log(`Error: Skill not found: ${e}

Available skills:`),O();return}let{skillFile:t}=o,n=oo(t);console.log(n)},no=()=>{j();let e=process.argv[3],o=process.argv[4];if(!e||!o){console.log(`Usage: superpowers-agent get-helpers <skill-name> <helper-search-term>

Examples:
  superpowers-agent get-helpers block-collection search-block
  superpowers-agent get-helpers aem/block-collection-and-party get-block-structure
  superpowers-agent get-helpers superpowers:testing/test-driven-development example

Description:
  Searches for helper files within a skill's skill.json configuration.
  Returns the full path to the best matching helper file.
  
  The command uses the skill's aliases if available, so you can use
  short names like "block-collection" instead of the full path.`);return}let t=P(e);if(!t){console.log(`Error: Skill not found: ${e}

Available skills:`),O();return}let{skillFile:n}=t,s=oo(n),r=Z(s);if(!r){console.log(`Error: No skill.json found for skill: ${e}
  Location: ${s}`);return}if(!r.helpers||!Array.isArray(r.helpers)||r.helpers.length===0){console.log(`Error: No helpers defined in skill.json for skill: ${e}
  Location: ${s}`);return}let l=We(s,o);if(!l){console.log(`Error: No helper found matching "${o}" in skill: ${e}`),console.log(`
Available helpers:`);for(let i of r.helpers)console.log(`  - ${i}`);return}if(!Pe(l)){console.log(`Error: Helper file not found: ${l}
  Defined in skill.json but missing from filesystem`);return}console.log(l)},lo=()=>{let e=oe();console.log("Current configuration:"),console.log(JSON.stringify(e,null,2))},io=()=>{let e=process.argv[3],o=process.argv[4];if(!e||o===void 0){console.log(`Usage: .agents/superpowers-agent config-set <key> <value>

Available keys:
  auto_update (true/false) - Enable/disable automatic updates during bootstrap

Examples:
  .agents/superpowers-agent config-set auto_update false
  .agents/superpowers-agent config-set auto_update true`);return}let t=o;o==="true"?t=!0:o==="false"&&(t=!1);let n={};n[e]=t,ae(n),console.log(`\u2713 Set ${e} = ${t}`)};import{execSync as ds}from"child_process";import{execSync as T}from"child_process";var rs=()=>{try{return T("git status --porcelain --untracked-files=no",{cwd:c.superpowersRepo,encoding:"utf8",stdio:"pipe",timeout:3e3}).trim().length===0}catch{return!1}},te=()=>{try{return T("git rev-parse --abbrev-ref HEAD",{cwd:c.superpowersRepo,encoding:"utf8",stdio:"pipe",timeout:3e3}).trim()==="main"}catch{return!1}},ce=()=>{try{T("git fetch origin",{cwd:c.superpowersRepo,timeout:5e3,stdio:"pipe"});let e=!rs(),o=T("git rev-parse HEAD",{cwd:c.superpowersRepo,encoding:"utf8",stdio:"pipe"}).trim(),t=T("git rev-parse origin/main",{cwd:c.superpowersRepo,encoding:"utf8",stdio:"pipe"}).trim();if(!(o!==t))return{hasUpdates:!1,hasLocalChanges:e,currentCommit:o,latestCommit:t,commitsBehind:0,changedFiles:[]};let s=parseInt(T("git rev-list --count HEAD..origin/main",{cwd:c.superpowersRepo,encoding:"utf8",stdio:"pipe"}).trim(),10),l=T("git diff --name-only HEAD origin/main",{cwd:c.superpowersRepo,encoding:"utf8",stdio:"pipe"}).trim().split(`
`).filter(i=>i.length>0);return{hasUpdates:!0,hasLocalChanges:e,currentCommit:o,latestCommit:t,commitsBehind:s,changedFiles:l}}catch{return{hasUpdates:!1,hasLocalChanges:!1,currentCommit:"",latestCommit:"",commitsBehind:0,changedFiles:[],error:!0}}},ao=e=>{let o={".github/prompts/":"copilot-prompts",".github/copilot-instructions.md":"copilot-instructions",".cursor/commands/":"cursor-commands","hooks/cursor/":"cursor-hooks",".codex/prompts/":"codex-prompts",".gemini/commands/":"gemini-commands","commands/":"claude-commands",".opencode/command/":"opencode-commands"},t=new Set;for(let n of e)for(let[s,r]of Object.entries(o))n.startsWith(s)&&t.add(r);return Array.from(t)};import{existsSync as pe,readdirSync as ns}from"fs";import{join as N,dirname as ls}from"path";import{execSync as de}from"child_process";var v=()=>{let e=N(c.superpowersRepo,".github","prompts"),o=N(c.vscodeUserDir,"prompts");if(!pe(e)){console.log("\u26A0\uFE0F  No Copilot prompts to install (source directory not found).");return}try{pe(o)||de(`mkdir -p "${o}"`,{stdio:"pipe"})}catch(s){console.log(`Error creating prompts directory: ${s.message}`);return}let t;try{t=ns(e).filter(s=>s.endsWith(".prompt.md"))}catch(s){console.log(`Error reading prompts directory: ${s.message}`);return}if(t.length===0){console.log("\u26A0\uFE0F  No prompt files found to install.");return}console.log("Installing GitHub Copilot prompts...");let n=0;for(let s of t)try{let r=N(e,s),l=N(o,s);de(`cp "${r}" "${l}"`,{stdio:"pipe"}),console.log(`  \u2713 Installed ${s}`),n++}catch(r){console.log(`  \u2717 Failed to install ${s}: ${r.message}`)}n>0&&console.log(`
\u2713 Installed ${n} prompt(s) to ${o}
  Use slash commands in GitHub Copilot:
    /brainstorm-with-superpowers - Refine ideas into designs
    /write-a-skill - Create new skills with TDD
    /skills - Discover available skills
    /use-skill - Load and apply a specific skill`)},G=()=>{let e=N(c.superpowersRepo,".github","copilot-instructions.md"),o=N(c.home,".github","copilot-instructions.md");if(!pe(e)){console.log("\u26A0\uFE0F  No Copilot instructions to install (source file not found).");return}let t=ls(o);try{pe(t)||de(`mkdir -p "${t}"`,{stdio:"pipe"})}catch(n){console.log(`Error creating .github directory: ${n.message}`);return}try{de(`cp "${e}" "${o}"`,{stdio:"pipe"}),console.log(`\u2713 Installed GitHub Copilot universal instructions
  Location: ${o}
  GitHub Copilot will now use Superpowers skills universally in all workspaces`)}catch(n){console.log(`\u2717 Failed to install instructions: ${n.message}`)}};import{existsSync as ue,readdirSync as co}from"fs";import{join as R}from"path";import{execSync as M}from"child_process";var W=()=>{let e=R(c.superpowersRepo,".cursor","commands"),o=R(c.home,".cursor","commands");if(!ue(e)){console.log("\u26A0\uFE0F  No Cursor commands to install (source directory not found).");return}try{ue(o)||M(`mkdir -p "${o}"`,{stdio:"pipe"})}catch(s){console.log(`Error creating Cursor commands directory: ${s.message}`);return}let t;try{t=co(e).filter(s=>s.endsWith(".md"))}catch(s){console.log(`Error reading Cursor commands directory: ${s.message}`);return}if(t.length===0){console.log("\u26A0\uFE0F  No command files found to install.");return}console.log("Installing Cursor commands...");let n=0;for(let s of t)try{let r=R(e,s),l=R(o,s);M(`cp "${r}" "${l}"`,{stdio:"pipe"}),console.log(`  \u2713 Installed ${s}`),n++}catch(r){console.log(`  \u2717 Failed to install ${s}: ${r.message}`)}n>0&&console.log(`
\u2713 Installed ${n} command(s) to ${o}
  Use slash commands in Cursor:
    /brainstorm-with-superpowers - Refine ideas into designs
    /write-a-skill - Create new skills with TDD
    /skills - Discover available skills
    /use-skill - Load and apply a specific skill`)},_=()=>{let e=R(c.superpowersRepo,"hooks","cursor"),o=R(c.home,".cursor","hooks"),t=R(e,"hooks.json"),n=R(c.home,".cursor","hooks.json");if(!ue(e)){console.log("\u26A0\uFE0F  No Cursor hooks to install (source directory not found).");return}try{ue(o)||M(`mkdir -p "${o}"`,{stdio:"pipe"})}catch(l){console.log(`Error creating Cursor hooks directory: ${l.message}`);return}try{M(`cp "${t}" "${n}"`,{stdio:"pipe"}),console.log("  \u2713 Installed hooks.json")}catch(l){console.log(`  \u2717 Failed to install hooks.json: ${l.message}`);return}let s;try{s=co(e).filter(l=>l.endsWith(".sh"))}catch(l){console.log(`Error reading hooks directory: ${l.message}`);return}if(s.length===0){console.log("\u26A0\uFE0F  No hook scripts found to install.");return}console.log("Installing Cursor hooks...");let r=0;for(let l of s)try{let i=R(e,l),p=R(o,l);M(`cp "${i}" "${p}"`,{stdio:"pipe"}),M(`chmod +x "${p}"`,{stdio:"pipe"}),console.log(`  \u2713 Installed ${l}`),r++}catch(i){console.log(`  \u2717 Failed to install ${l}: ${i.message}`)}r>0&&console.log(`
\u2713 Installed ${r} hook(s) to ${o}
  Cursor will now check for skills before each prompt submission
  Restart Cursor for hooks to take effect`)};import{existsSync as po,readdirSync as is}from"fs";import{join as me}from"path";import{execSync as uo}from"child_process";var H=()=>{let e=me(c.superpowersRepo,".codex","prompts"),o=me(c.home,".codex","prompts");if(!po(e)){console.log("\u26A0\uFE0F  No Codex prompts to install (source directory not found).");return}try{po(o)||uo(`mkdir -p "${o}"`,{stdio:"pipe"})}catch(s){console.log(`Error creating Codex prompts directory: ${s.message}`);return}let t;try{t=is(e).filter(s=>s.endsWith(".md"))}catch(s){console.log(`Error reading Codex prompts directory: ${s.message}`);return}if(t.length===0){console.log("\u26A0\uFE0F  No prompt files found to install.");return}console.log("Installing OpenAI Codex prompts...");let n=0;for(let s of t)try{let r=me(e,s),l=me(o,s);uo(`cp "${r}" "${l}"`,{stdio:"pipe"}),console.log(`  \u2713 Installed ${s}`),n++}catch(r){console.log(`  \u2717 Failed to install ${s}: ${r.message}`)}n>0&&console.log(`
\u2713 Installed ${n} prompt(s) to ${o}
  Use slash commands in OpenAI Codex:
    /prompts:brainstorm - Refine ideas into designs
    /prompts:write-skill - Create new skills with TDD
    /prompts:skills - Discover available skills
    /prompts:use-skill - Load and apply a specific skill
  Note: Restart Codex or open a new session to reload prompts`)};import{existsSync as mo,readdirSync as as}from"fs";import{join as ge}from"path";import{execSync as go}from"child_process";var J=()=>{let e=ge(c.superpowersRepo,".gemini","commands"),o=ge(c.home,".gemini","commands");if(!mo(e)){console.log("\u26A0\uFE0F  No Gemini commands to install (source directory not found).");return}try{mo(o)||go(`mkdir -p "${o}"`,{stdio:"pipe"})}catch(s){console.log(`Error creating Gemini commands directory: ${s.message}`);return}let t;try{t=as(e).filter(s=>s.endsWith(".toml"))}catch(s){console.log(`Error reading Gemini commands directory: ${s.message}`);return}if(t.length===0){console.log("\u26A0\uFE0F  No command files found to install.");return}console.log("Installing Gemini commands...");let n=0;for(let s of t)try{let r=ge(e,s),l=ge(o,s);go(`cp "${r}" "${l}"`,{stdio:"pipe"}),console.log(`  \u2713 Installed ${s}`),n++}catch(r){console.log(`  \u2717 Failed to install ${s}: ${r.message}`)}n>0&&console.log(`
\u2713 Installed ${n} command(s) to ${o}
  Use slash commands in Gemini:
    /brainstorm-with-superpowers - Refine ideas into designs
    /write-a-skill - Create new skills with TDD
    /skills - Discover available skills
    /use-skill - Load and apply a specific skill`)};import{existsSync as fo,readdirSync as cs}from"fs";import{join as fe}from"path";import{execSync as ho}from"child_process";var B=()=>{let e=fe(c.superpowersRepo,"commands"),o=fe(c.home,".claude","commands");if(!fo(e)){console.log("\u26A0\uFE0F  No Claude commands to install (source directory not found).");return}try{fo(o)||ho(`mkdir -p "${o}"`,{stdio:"pipe"})}catch(s){console.log(`Error creating Claude commands directory: ${s.message}`);return}let t;try{t=cs(e).filter(s=>s.endsWith(".md"))}catch(s){console.log(`Error reading Claude commands directory: ${s.message}`);return}if(t.length===0){console.log("\u26A0\uFE0F  No command files found to install.");return}console.log("Installing Claude Code commands...");let n=0;for(let s of t)try{let r=fe(e,s),l=fe(o,s);ho(`cp "${r}" "${l}"`,{stdio:"pipe"}),console.log(`  \u2713 Installed ${s}`),n++}catch(r){console.log(`  \u2717 Failed to install ${s}: ${r.message}`)}n>0&&console.log(`
\u2713 Installed ${n} command(s) to ${o}
  Use slash commands in Claude Code:
    /brainstorm - Refine ideas into designs
    /execute-plan - Execute plans in batches
    /write-plan - Create implementation plans
    /skills - Discover available skills
    /use-skill - Load and apply a specific skill`)};import{existsSync as ko,readdirSync as ps}from"fs";import{join as he}from"path";import{execSync as yo}from"child_process";var V=()=>{let e=he(c.superpowersRepo,".opencode","command"),o=he(c.home,".config","opencode","command");if(!ko(e)){console.log("\u26A0\uFE0F  No opencode commands to install (source directory not found).");return}try{ko(o)||yo(`mkdir -p "${o}"`,{stdio:"pipe"})}catch(s){console.log(`Error creating opencode commands directory: ${s.message}`);return}let t;try{t=ps(e).filter(s=>s.endsWith(".md"))}catch(s){console.log(`Error reading opencode commands directory: ${s.message}`);return}if(t.length===0){console.log("\u26A0\uFE0F  No command files found to install.");return}console.log("Installing opencode commands...");let n=0;for(let s of t)try{let r=he(e,s),l=he(o,s);yo(`cp "${r}" "${l}"`,{stdio:"pipe"}),console.log(`  \u2713 Installed ${s}`),n++}catch(r){console.log(`  \u2717 Failed to install ${s}: ${r.message}`)}n>0&&console.log(`
\u2713 Installed ${n} command(s) to ${o}
  Use slash commands in opencode:
    /brainstorm - Refine ideas into designs
    /execute-plan - Execute plans in batches
    /write-plan - Create implementation plans
    /write-skill - Create new skills with TDD
    /skills - Discover available skills
    /use-skill - Load and apply a specific skill`)};var us=e=>{let t={"copilot-prompts":v,"copilot-instructions":G,"cursor-commands":W,"cursor-hooks":_,"codex-prompts":H,"gemini-commands":J,"claude-commands":B,"opencode-commands":V}[e];if(t)try{return t(),{success:!0,integration:e}}catch(n){return{success:!1,integration:e,error:n.message}}return{success:!1,integration:e,error:"Unknown integration"}},ms=()=>{console.log("Installing aliases...")},ke=(e={})=>{let o=e.skipReinstall||!1;console.log(`# Checking for Superpowers updates...
`);let t=ce();if(t.error){console.log("\u26A0\uFE0F  Could not check for updates (network issue)");return}if(!t.hasUpdates){console.log("\u2713 Already up to date");return}if(t.hasLocalChanges){console.log(`\u26A0\uFE0F  Cannot auto-update: local changes detected
   Commit or stash your changes first, then run update again
   Or manually update: cd ${c.superpowersRepo} && git pull`);return}if(!te()){console.log(`\u26A0\uFE0F  Not on main branch, skipping auto-update
   Switch to main branch first: cd ${c.superpowersRepo} && git checkout main`);return}console.log(`\u{1F4E6} Updating from ${t.currentCommit.substring(0,7)} to ${t.latestCommit.substring(0,7)}
   (${t.commitsBehind} new commit${t.commitsBehind>1?"s":""})
`);try{ds("git pull origin main",{cwd:c.superpowersRepo,stdio:"pipe",timeout:1e4}),console.log("\u2713 Updated superpowers repository")}catch(l){console.log(`\u2717 Git pull failed: ${l.message}
   Please resolve manually and try again`);return}if(console.log(""),ae({last_updated_commit:t.latestCommit,last_update_check:new Date().toISOString()}),o){console.log(`\u2139\uFE0F  Skipping integration reinstall (--no-reinstall flag)

\u2713 Update complete!`);return}let n=ao(t.changedFiles);if(n.length===0){console.log(`\u2139\uFE0F  No integration files changed, skipping reinstalls

\u2713 Update complete!`);return}console.log(`\u{1F504} Reinstalling updated integrations:
`);let s=[];for(let l of n){let i=us(l);s.push(i),i.success?console.log(`  \u2713 ${l}`):console.log(`  \u2717 ${l} (${i.error})`)}console.log(""),console.log(`
---
`),ms();let r=s.filter(l=>!l.success);if(r.length>0){console.log("\u26A0\uFE0F  Update completed with errors:");for(let l of r){console.log(`  - ${l.integration} failed to install`);let i=`install-${l.integration}`;console.log(`    Run manually: superpowers-agent ${i}`)}}else console.log("\u2713 Update complete!")},wo=async()=>{j();let e=D();console.log(`Current version: ${e}`);try{let o=await Qe();console.log(`Latest version: ${o}`),Ze(o,e)?(console.log("Update available: Yes"),process.exit(1)):(console.log("You are up to date"),process.exit(0))}catch(o){console.log(`Error checking for updates: ${o.message}`),process.exit(1)}},$o=()=>{let e=D();console.log(e)};import{existsSync as f,readFileSync as ye,writeFileSync as Co}from"fs";import{join as k,dirname as fs,parse as ne}from"path";import{execSync as x}from"child_process";import{platform as hs}from"os";import{execSync as gs}from"child_process";var re=e=>{try{return gs(`which ${e}`,{stdio:"pipe",timeout:2e3}),!0}catch{return!1}};var m={opencode:{check:()=>re("opencode"),cli:!0,name:"OpenCode",installUrl:"https://opencode.ai/docs/installation",bootstrapCommand:"install-opencode-commands"},claude:{check:()=>re("claude"),cli:!0,name:"Claude Code",installUrl:"https://code.claude.com/docs/en/installation",bootstrapCommand:"install-claude-commands"},gemini:{check:()=>re("gemini"),cli:!0,name:"Gemini",installUrl:"https://cloud.google.com/gemini/docs/cli/install",bootstrapCommand:"install-gemini-commands"},codex:{check:()=>re("codex"),cli:!0,name:"Codex",installUrl:"https://developers.openai.com/codex/docs/installation",bootstrapCommand:"install-codex-prompts"},cursor:{check:()=>!0,cli:!1,name:"Cursor",bootstrapCommand:"install-cursor-commands"},copilot:{check:()=>!0,cli:!1,name:"GitHub Copilot",bootstrapCommand:"install-copilot-prompts"}},So=()=>{let e=[];return m.copilot.check()&&e.push("github-copilot"),m.cursor.check()&&e.push("cursor"),m.claude.check()&&e.push("claude-code"),m.opencode.check()&&e.push("opencode"),m.gemini.check()&&e.push("gemini"),m.codex.check()&&e.push("codex"),e};var ks=e=>{let o={"github-copilot":"TOOLS-GITHUB-COPILOT.md.template",cursor:"TOOLS-CURSOR.md.template","claude-code":"TOOLS-CLAUDE-CODE.md.template",gemini:"TOOLS-GEMINI.md.template",opencode:"TOOLS-OPENCODE.md.template",codex:"TOOLS-CODEX.md.template"},t=k(c.superpowersRepo,".agents","templates"),n=[];for(let s of e){let r=o[s];if(!r)continue;let l=k(t,r);if(f(l))try{let i=ye(l,"utf8").trim();n.push(i)}catch{let p={"github-copilot":"GitHub Copilot",cursor:"Cursor","claude-code":"Claude Code",opencode:"OpenCode",gemini:"Gemini",codex:"Codex"};n.push(`**Tool Mapping for ${p[s]}:**`)}}return n.join(`
`)},le=(e,o,t,n=!0)=>{let s=f(e);if(!s&&!n)return{updated:!1,created:!1,skipped:!0};let r=ks(t),l=o.replace(/\{\{TOOL_MAPPINGS\}\}/g,r),i=new Date().toISOString().split("T")[0];l=l.replace(/\{\{DATE\}\}/g,i),l=l.replace(/\{\{SUPERPOWERS_PATH\}\}/g,c.superpowersRepo);let p=`<!-- SUPERPOWERS_SKILLS_START -->
${l}
<!-- SUPERPOWERS_SKILLS_END -->`;if(s){let a=new Date().toISOString().split("T")[0],d=`${e}.backup-${a}`;try{x(`cp "${e}" "${d}"`,{stdio:"pipe"})}catch(y){return{updated:!1,error:!0,message:`Failed to backup: ${y.message}`}}let u;try{u=ye(e,"utf8")}catch(y){return{updated:!1,error:!0,message:`Failed to read: ${y.message}`}}let h="<!-- SUPERPOWERS_SKILLS_START -->",g="<!-- SUPERPOWERS_SKILLS_END -->",S;if(u.includes(h)&&u.includes(g)){let y=new RegExp(`${h}[\\s\\S]*?${g}`,"g");S=u.replace(y,p)}else{let y=u.match(/^#\s+.+$/m);if(y){let Y=u.indexOf(`
`,y.index)+1;S=u.slice(0,Y)+`
`+p+`
`+u.slice(Y)}else S=p+`

`+u}try{return Co(e,S,"utf8"),{updated:!0,created:!1,backup:d}}catch(y){return{updated:!1,error:!0,message:`Failed to write: ${y.message}`}}}else try{let a=fs(e);f(a)||x(`mkdir -p "${a}"`,{stdio:"pipe"});let h=`# ${ne(e).base.toUpperCase()}

`+p;return Co(e,h,"utf8"),{updated:!1,created:!0}}catch(a){return{updated:!1,created:!1,error:!0,message:`Failed to create: ${a.message}`}}},ys=()=>{console.log("Installing Unix aliases...");let e=k(c.superpowersRepo,".agents","superpowers-agent"),o=k(c.home,".local","bin"),t=k(o,"superpowers-agent"),n=k(o,"superpowers");if(!f(e)){console.log("\u26A0\uFE0F  superpowers-agent executable not found");return}if(!f(o))try{x(`mkdir -p "${o}"`,{stdio:"pipe"}),console.log(`\u2713 Created ${o}`)}catch(r){console.log(`\u26A0\uFE0F  Failed to create ${o}: ${r.message}`);return}try{f(t)&&x(`rm "${t}"`,{stdio:"pipe"}),x(`ln -s "${e}" "${t}"`,{stdio:"pipe"}),console.log(`\u2713 Created symlink: superpowers-agent -> ${e}`)}catch(r){console.log(`\u26A0\uFE0F  Failed to create superpowers-agent symlink: ${r.message}`)}try{f(n)&&x(`rm "${n}"`,{stdio:"pipe"}),x(`ln -s "${e}" "${n}"`,{stdio:"pipe"}),console.log(`\u2713 Created symlink: superpowers -> ${e}`)}catch(r){console.log(`\u26A0\uFE0F  Failed to create superpowers symlink: ${r.message}`)}(process.env.PATH||"").includes(o)||(console.log(`
\u26A0\uFE0F  Warning: ${o} is not in your PATH`),console.log("   Add this to your shell profile (~/.bashrc, ~/.zshrc, etc.):"),console.log(`   export PATH="$HOME/.local/bin:$PATH"
`))},ws=()=>{console.log("Installing Windows aliases..."),console.log("\u2713 Windows alias installation (implementation pending)")},Ae=()=>{let e=hs();return console.log(`## Installing Universal Aliases
`),e==="win32"?ws():ys()},bo=()=>{j(),console.log(`# Setting up Superpowers skills for this project
`);let e=process.cwd(),o=k(e,".agents"),t=k(o,"skills"),n=k(e,"AGENTS.md"),s=k(o,"AGENTS.md"),r=f(n)?n:s,l=k(c.superpowersRepo,".agents","templates","AGENTS.md.template");if(!f(l)){console.log(`\u2717 Error: AGENTS.md template not found
  Expected at: ${l}
  Please update your Superpowers installation`);return}if(f(o))console.log("\u2713 .agents/ directory exists");else try{x(`mkdir -p "${o}"`,{stdio:"pipe"}),console.log("\u2713 Created .agents/ directory")}catch(C){console.log(`\u2717 Failed to create .agents/ directory: ${C.message}`);return}if(f(t))console.log("\u2713 .agents/skills/ directory exists");else try{x(`mkdir -p "${t}"`,{stdio:"pipe"}),x(`touch "${k(t,".gitkeep")}"`,{stdio:"pipe"}),console.log("\u2713 Created .agents/skills/ directory")}catch(C){console.log(`\u2717 Failed to create skills directory: ${C.message}`);return}let i=f(r);if(i){let C=new Date().toISOString().split("T")[0],Fe=`${r}.backup-${C}`;try{x(`cp "${r}" "${Fe}"`,{stdio:"pipe"}),console.log(`\u2713 Backed up existing AGENTS.md to ${ne(Fe).base}`)}catch(Fo){console.log(`\u2717 Failed to backup AGENTS.md: ${Fo.message}`);return}}let p;try{p=ye(l,"utf8")}catch(C){console.log(`\u2717 Failed to read template: ${C.message}`);return}let a=D();p=p.replace(/\{\{VERSION\}\}/g,a);let d=[],u=k(e,".github","copilot-instructions.md"),h=k(c.home,".github","copilot-instructions.md");(f(u)||f(h)||m.copilot.check())&&d.push("github-copilot");let g=k(e,"CLAUDE.md"),S=k(o,"CLAUDE.md");(f(g)||f(S)||m.claude.check())&&d.push("claude-code");let y=k(e,"GEMINI.md"),Y=k(o,"GEMINI.md");(f(y)||f(Y)||m.gemini.check())&&d.push("gemini"),m.cursor.check()&&d.push("cursor"),m.opencode.check()&&d.push("opencode"),m.codex.check()&&d.push("codex"),console.log(`
Detected platforms for project: ${d.join(", ")||"none"}
`);let Lo=d.filter(C=>["github-copilot","cursor","opencode","codex"].includes(C)),z=le(r,p,Lo,!i);z.created?console.log(`\u2713 Created AGENTS.md with platform tool mappings (${r===n?"root":".agents/"})`):z.updated?console.log(`\u2713 Updated AGENTS.md with platform tool mappings (${r===n?"root":".agents/"})`):z.error&&console.log("\u26A0\uFE0F  Failed to update AGENTS.md");let Ce=f(g)?g:S,U=le(Ce,p,["claude-code"],!1);U.created?console.log(`\u2713 Created CLAUDE.md with Claude Code tool mappings (${Ce===g?"root":".agents/"})`):U.updated?(console.log(`\u2713 Updated CLAUDE.md with Claude Code tool mappings (${Ce===g?"root":".agents/"})`),U.backup&&console.log(`  Backed up to ${ne(U.backup).base}`)):U.skipped?console.log("\u2139\uFE0F  Skipped CLAUDE.md (does not exist)"):U.error&&console.log("\u26A0\uFE0F  Failed to update CLAUDE.md");let be=f(y)?y:Y,I=le(be,p,["gemini"],!1);I.created?console.log(`\u2713 Created GEMINI.md with Gemini tool mappings (${be===y?"root":".agents/"})`):I.updated?(console.log(`\u2713 Updated GEMINI.md with Gemini tool mappings (${be===y?"root":".agents/"})`),I.backup&&console.log(`  Backed up to ${ne(I.backup).base}`)):I.skipped?console.log("\u2139\uFE0F  Skipped GEMINI.md (does not exist)"):I.error&&console.log("\u26A0\uFE0F  Failed to update GEMINI.md");let xe=k(e,".github","copilot-instructions.md"),Le=!f(n)&&!f(s),L={skipped:!0};f(xe)&&Le?(L=le(xe,p,["github-copilot"],!1),L.updated?(console.log("\u2713 Updated .github/copilot-instructions.md with GitHub Copilot tool mappings"),L.backup&&console.log(`  Backed up to ${ne(L.backup).base}`)):L.error&&console.log("\u26A0\uFE0F  Failed to update .github/copilot-instructions.md")):f(xe)&&!Le?console.log("\u2139\uFE0F  Skipped .github/copilot-instructions.md (AGENTS.md exists, using that instead)"):console.log("\u2139\uFE0F  Skipped .github/copilot-instructions.md (does not exist)");let F=`
# Setup complete!

Your project now has:
  - .agents/ directory structure`;(z.updated||z.created)&&(F+=`
  - AGENTS.md with universal skills instructions`),(U.updated||U.created)&&(F+=`
  - CLAUDE.md with Claude Code skills instructions`),(I.updated||I.created)&&(F+=`
  - GEMINI.md with Gemini skills instructions`),L.updated&&(F+=`
  - .github/copilot-instructions.md with GitHub Copilot skills instructions`),F+=`
  - .agents/skills/ ready for project-specific skills
`,console.log(F)},xo=()=>{if(j(),console.log(`# Superpowers Bootstrap for Agents
# ==================================
`),!process.argv.includes("--no-update")){let p=oe(),a=ce();a.error?console.log(`## Update Check

\u26A0\uFE0F  Could not check for updates (network issue)

---
`):a.hasUpdates&&(p.auto_update&&!a.hasLocalChanges&&te()?(console.log(`## Auto-Update
`),ke(),console.log(`
---
`)):(console.log(`## Update Available
`),a.hasLocalChanges?console.log("\u26A0\uFE0F  Your superpowers installation is behind the latest version.\n    Cannot auto-update: local changes detected\n    To update, commit/stash changes then run: `superpowers-agent update`"):te()?console.log("\u26A0\uFE0F  Your superpowers installation is behind the latest version.\n    To update, run: `superpowers-agent update`\n    Or enable auto-update: `superpowers-agent config-set auto_update true`"):console.log("\u26A0\uFE0F  Your superpowers installation is behind the latest version.\n    Cannot auto-update: not on main branch\n    To update, switch to main then run: `superpowers-agent update`"),console.log(`
---
`)))}Ae(),console.log(`---
`),console.log(`## GitHub Copilot Integration
`),v(),console.log(""),G(),console.log(`
---
`),console.log(`## Cursor Integration
`),W(),console.log(""),_(),console.log(`
---
`),console.log(`## OpenAI Codex Integration
`),m.codex.check()?H():console.log(`\u26A0\uFE0F  Skipped (${m.codex.name} CLI not detected)
\u{1F4A1} To enable Codex integration:
   1. Install Codex: ${m.codex.installUrl}
   2. Run: superpowers-agent ${m.codex.bootstrapCommand}`),console.log(`
---
`),console.log(`## Gemini Integration
`),m.gemini.check()?J():console.log(`\u26A0\uFE0F  Skipped (${m.gemini.name} CLI not detected)
\u{1F4A1} To enable Gemini integration:
   1. Install Gemini: ${m.gemini.installUrl}
   2. Run: superpowers-agent ${m.gemini.bootstrapCommand}`),console.log(`
---
`),console.log(`## Claude Code Integration
`),m.claude.check()?B():console.log(`\u26A0\uFE0F  Skipped (${m.claude.name} CLI not detected)
\u{1F4A1} To enable Claude Code integration:
   1. Install Claude Code: ${m.claude.installUrl}
   2. Run: superpowers-agent ${m.claude.bootstrapCommand}`),console.log(`
---
`),console.log(`## OpenCode Integration
`),m.opencode.check()?V():console.log(`\u26A0\uFE0F  Skipped (${m.opencode.name} CLI not detected)
\u{1F4A1} To enable OpenCode integration:
   1. Install OpenCode: ${m.opencode.installUrl}
   2. Run: superpowers-agent ${m.opencode.bootstrapCommand}`),console.log(`
---
`),console.log(`## Generating Platform-Specific Files
`);let r=So();console.log(`Detected platforms: ${r.join(", ")||"none"}
`);let l=k(c.superpowersRepo,".agents","templates","AGENTS.md.template"),i="";try{i=ye(l,"utf8")}catch(p){console.log(`\u26A0\uFE0F  Could not read AGENTS.md template: ${p.message}
`)}if(i){let p=D();i=i.replace(/\{\{VERSION\}\}/g,p);let a=k(c.home,".agents","AGENTS.md"),d=le(a,i,r,!0);d.created?console.log(`\u2713 Created ${a}`):d.updated&&console.log(`\u2713 Updated ${a}`)}console.log(`
---
`),console.log(`# Bootstrap Complete!
`),console.log("\u2713 All integrations installed"),console.log(`\u2713 Skills system ready
`),console.log("Next steps:"),console.log("  - Run `superpowers-agent find-skills` to see available skills"),console.log("  - Run `superpowers-agent setup-skills` in your project directory")};import{existsSync as E,readFileSync as Ro}from"fs";import{join as $,dirname as Te,parse as we}from"path";import{execSync as b}from"child_process";import{homedir as K}from"os";var jo=e=>{let o=e.match(/^https:\/\/github\.com\/([^/]+)\/([^/]+)\/tree\/([^/]+)\/(.+)$/);if(o){let[,s,r,l,i]=o;return{type:"git-tree",repoUrl:`https://github.com/${s}/${r}.git`,branch:l,path:i,original:e}}let t=e.match(/^git@github\.com:([^/]+)\/([^/]+?)(?:\.git)?$/);if(t){let[,s,r]=t;return{type:"git-repo",repoUrl:`git@github.com:${s}/${r}.git`,branch:null,path:null,original:e}}let n=e.match(/^https:\/\/github\.com\/([^/]+)\/([^/]+)(?:\.git)?$/);if(n){let[,s,r]=n;return{type:"git-repo",repoUrl:`https://github.com/${s}/${r}.git`,branch:null,path:null,original:e}}return E(e)?{type:"local",path:e,original:e}:null},Eo=e=>{let o=e.includes("--global")||e.includes("-g"),t=e.includes("--project")||e.includes("-p");if(o)return $(K(),".agents","skills");if(t)return $(process.cwd(),".agents","skills");let n=$(process.cwd(),".agents","config.json"),s=$(K(),".agents","config.json");for(let r of[n,s])if(E(r))try{if(JSON.parse(Ro(r,"utf8")).installLocation==="project")return $(process.cwd(),".agents","skills")}catch{}return $(K(),".agents","skills")},Uo=(e,o)=>{let t=$(K(),".agents","tmp",`skill-install-${Date.now()}`);try{return b(`mkdir -p "${t}"`,{stdio:"pipe"}),o?b(`git clone --branch ${o} --depth 1 "${e}" "${t}"`,{stdio:"pipe",timeout:3e4}):b(`git clone --depth 1 "${e}" "${t}"`,{stdio:"pipe",timeout:3e4}),t}catch(n){try{b(`rm -rf "${t}"`,{stdio:"pipe"})}catch{}throw new Error(`Failed to clone repository: ${n.message}`)}},Se=e=>{let o=$(e,"skill.json");if(!E(o))return null;try{let t=Ro(o,"utf8");return JSON.parse(t)}catch(t){throw new Error(`Failed to read skill.json: ${t.message}`)}},$e=(e,o,t,n)=>{let s=$(e,o);if(!E(s)){n.errors.push(`Skill directory not found: ${o}`);return}let r=Se(s);if(!r){n.errors.push(`No skill.json found in: ${o}`);return}let l=r.name||o,i=$(t,l),p=Te(i);try{b(`mkdir -p "${p}"`,{stdio:"pipe"})}catch(a){n.errors.push(`Failed to create directory ${p}: ${a.message}`);return}try{E(i)&&b(`rm -rf "${i}"`,{stdio:"pipe"}),b(`cp -R "${s}" "${i}"`,{stdio:"pipe"}),n.installed.push({name:l,path:i,title:r.title||r.description||l})}catch(a){n.errors.push(`Failed to install ${o}: ${a.message}`)}},Io=()=>{let e=process.argv.slice(3),o=e.find(a=>!a.startsWith("-")),t=e.filter(a=>a.startsWith("-"));if(!o){console.log(`Usage: superpowers-agent add <url-or-path|@alias> [skill-path] [options]

Options:
  --global, -g   Install skills globally in ~/.agents/skills/ (default)
  --project, -p  Install skills in current project's .agents/skills/

Examples:
  superpowers-agent add https://github.com/example/skills
  superpowers-agent add https://github.com/example/repo/tree/main/skills
  superpowers-agent add ~/my-local-skills
  superpowers-agent add https://github.com/example/skills --project
  superpowers-agent add ~/my-local-skills --global
  superpowers-agent add @myrepo path/to/skill
  superpowers-agent add @myrepo path/to/skills --project

Description:
  Installs skill(s) from a Git repository, local directory, or repository alias.
  Supports repositories with single or multiple skills.
  Reads skill.json to determine skill names and installation paths.
  
  Repository aliases can be added using:
    superpowers-agent add-repository <git-url> [--as=@alias]`);return}console.log(`Installing skill(s)...
`);let n=null,s=null;if(o.startsWith("@")){let a=o;s=e.find((g,S)=>S>0&&!g.startsWith("-")&&g!==a);let d=se(!1),u=se(!0);if(d[a])n=d[a],console.log(`Using project repository alias: ${a}`);else if(u[a])n=u[a],console.log(`Using global repository alias: ${a}`);else{console.log(`Error: Repository alias not found: ${a}`),console.log(`
Available aliases:`);let g={...u,...d};if(Object.keys(g).length===0)console.log("  (none)"),console.log(`
Add a repository using:`),console.log("  superpowers-agent add-repository <git-url>");else for(let[S,y]of Object.entries(g))console.log(`  ${S} -> ${y}`);return}let h=E(n);s?h?(o=$(n,s),console.log(`Repository path: ${n}`),console.log(`Skill path: ${s}
`)):(o=`${n}/tree/main/${s}`,console.log(`Repository URL: ${n}`),console.log(`Skill path: ${s}
`)):(o=n,console.log(h?`Repository path: ${n}
`:`Repository URL: ${n}
`))}let r=jo(o);if(!r){console.log(`Error: Invalid URL or path not found: ${o}`);return}let l=Eo(t);console.log(`Install location: ${l}
`);let i,p=!1;try{if(r.type==="git-repo"||r.type==="git-tree"){if(console.log(`Cloning repository: ${r.repoUrl}`),r.branch&&console.log(`Branch: ${r.branch}`),i=Uo(r.repoUrl,r.branch),p=!0,r.path&&(i=$(i,r.path),!E(i)))throw new Error(`Path not found in repository: ${r.path}`);console.log("")}else i=r.path;let a=Se(i);if(!a)throw new Error("No skill.json found in source directory");let d={installed:[],errors:[],source:r.original};if(a.skills&&Array.isArray(a.skills)){console.log(`Found ${a.skills.length} skill(s) to install
`);for(let u of a.skills)$e(i,u,l,d)}else{let u=a.name||we(i).base;$e(Te(i),we(i).base,l,d)}if(p&&i)try{let u=i.split("/.agents/tmp/")[0]+"/.agents/tmp/"+i.split("/.agents/tmp/")[1].split("/")[0];b(`rm -rf "${u}"`,{stdio:"pipe"})}catch{}if(console.log(`
**Successfully installed skills:**`),console.log(`- Source: ${d.source}`),d.installed.length>0)for(let u of d.installed)console.log(`  - Installed: ${u.name} at ${u.path}`),console.log(`    ${u.title}`);if(d.errors.length>0){console.log(`
**Errors:**`);for(let u of d.errors)console.log(`  - ${u}`)}d.installed.length===0&&d.errors.length===0&&console.log("  No skills were installed")}catch(a){if(p&&i)try{let d=i.split("/.agents/tmp/")[0]+"/.agents/tmp/"+i.split("/.agents/tmp/")[1].split("/")[0];b(`rm -rf "${d}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${a.message}`)}},Do=()=>{let e=process.argv.slice(3),o=e.find(a=>!a.startsWith("-")),t=e.filter(a=>a.startsWith("-"));if(!o){console.log(`Usage: superpowers-agent pull <url-or-path|@alias> [skill-path] [options]

Options:
  --global, -g   Update skills globally in ~/.agents/skills/ (default)
  --project, -p  Update skills in current project's .agents/skills/

Examples:
  superpowers-agent pull https://github.com/example/skills
  superpowers-agent pull https://github.com/example/repo/tree/main/skills
  superpowers-agent pull ~/my-local-skills
  superpowers-agent pull https://github.com/example/skills --project
  superpowers-agent pull ~/my-local-skills --global
  superpowers-agent pull @myrepo
  superpowers-agent pull @myrepo path/to/skill
  superpowers-agent pull @myrepo path/to/skills --project

Description:
  Updates or adds skill(s) from a Git repository, local directory, or repository alias.
  If skills already exist, they will be updated (replaced).
  If skills don't exist, they will be added.
  Supports repositories with single or multiple skills.
  Reads skill.json to determine skill names and installation paths.
  
  Repository aliases can be added using:
    superpowers-agent add-repository <git-url> [--as=@alias]`);return}console.log(`Updating skill(s)...
`);let n=null,s=null;if(o.startsWith("@")){let a=o;s=e.find((g,S)=>S>0&&!g.startsWith("-")&&g!==a);let d=se(!1),u=se(!0);if(d[a])n=d[a],console.log(`Using project repository alias: ${a}`);else if(u[a])n=u[a],console.log(`Using global repository alias: ${a}`);else{console.log(`Error: Repository alias not found: ${a}`),console.log(`
Available aliases:`);let g={...u,...d};if(Object.keys(g).length===0)console.log("  (none)"),console.log(`
Add a repository using:`),console.log("  superpowers-agent add-repository <git-url>");else for(let[S,y]of Object.entries(g))console.log(`  ${S} -> ${y}`);return}let h=E(n);s?h?(o=$(n,s),console.log(`Repository path: ${n}`),console.log(`Skill path: ${s}
`)):(o=`${n}/tree/main/${s}`,console.log(`Repository URL: ${n}`),console.log(`Skill path: ${s}
`)):(o=n,console.log(h?`Repository path: ${n}
`:`Repository URL: ${n}
`))}let r=jo(o);if(!r){console.log(`Error: Invalid URL or path not found: ${o}`);return}let l=Eo(t);console.log(`Install location: ${l}
`);let i,p=!1;try{if(r.type==="git-repo"||r.type==="git-tree"){if(console.log(`Cloning repository: ${r.repoUrl}`),r.branch&&console.log(`Branch: ${r.branch}`),i=Uo(r.repoUrl,r.branch),p=!0,r.path&&(i=$(i,r.path),!E(i)))throw new Error(`Path not found in repository: ${r.path}`);console.log("")}else i=r.path;let a=Se(i);if(!a)throw new Error("No skill.json found in source directory");let d={installed:[],errors:[],source:r.original};if(a.skills&&Array.isArray(a.skills)){console.log(`Found ${a.skills.length} skill(s) to update
`);for(let u of a.skills)$e(i,u,l,d)}else{let u=a.name||we(i).base;$e(Te(i),we(i).base,l,d)}if(p&&i)try{let u=i.split("/.agents/tmp/")[0]+"/.agents/tmp/"+i.split("/.agents/tmp/")[1].split("/")[0];b(`rm -rf "${u}"`,{stdio:"pipe"})}catch{}if(console.log(`
**Successfully updated skills:**`),console.log(`- Source: ${d.source}`),d.installed.length>0)for(let u of d.installed)console.log(`  - Updated: ${u.name} at ${u.path}`),console.log(`    ${u.title}`);if(d.errors.length>0){console.log(`
**Errors:**`);for(let u of d.errors)console.log(`  - ${u}`)}d.installed.length===0&&d.errors.length===0&&console.log("  No skills were updated")}catch(a){if(p&&i)try{let d=i.split("/.agents/tmp/")[0]+"/.agents/tmp/"+i.split("/.agents/tmp/")[1].split("/")[0];b(`rm -rf "${d}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${a.message}`)}},Po=()=>{let e=process.argv.slice(3),o=e.find(a=>!a.startsWith("-")),t=e.filter(a=>a.startsWith("-"));if(!o){console.log(`Usage: superpowers-agent add-repository <git-url> [options]

Options:
  --global, -g        Add repository globally in ~/.agents/config.json (default)
  --project, -p       Add repository in current project's .agents/config.json
  --as=<alias>        Specify custom alias for the repository

Examples:
  superpowers-agent add-repository https://github.com/example/skills.git
  superpowers-agent add-repository https://github.com/example/skills.git --as=@myskills
  superpowers-agent add-repository https://github.com/example/skills.git --project
  superpowers-agent add-repository https://github.com/example/skills.git --as=@custom --global

Description:
  Adds a skill repository alias to your configuration.
  The repository's skill.json will be read to determine the default alias.
  Use --as to specify a custom alias.
  After adding, you can install skills using: superpowers-agent add @alias path/to/skill`);return}console.log(`Adding repository...
`);let n=t.some(a=>a==="--global"||a==="-g"),s=t.some(a=>a==="--project"||a==="-p"),r=t.find(a=>a.startsWith("--as=")),l=r?r.split("=")[1]:null,i=!s,p;try{console.log(`Cloning repository: ${o}`),p=$(K(),".agents","tmp",`repo-add-${Date.now()}`),b(`mkdir -p "${p}"`,{stdio:"pipe"}),b(`git clone --depth 1 "${o}" "${p}"`,{stdio:"pipe",timeout:3e4}),console.log("");let a=Se(p);if(!a)throw new Error("No skill.json found in repository");let d;if(l)d=l;else if(a.repository)d=a.repository;else{let g=o.match(/\/([^/]+?)(?:\.git)?$/);if(g)d="@"+g[1];else throw new Error("Could not determine repository alias. Use --as=@alias to specify manually.")}ze(d,o,i),b(`rm -rf "${p}"`,{stdio:"pipe"});let u=i?"globally":"in project",h=i?$(K(),".agents","config.json"):$(process.cwd(),".agents","config.json");console.log(`\u2713 Repository added ${u}`),console.log(`  Alias: ${d}`),console.log(`  URL: ${o}`),console.log(`  Config: ${h}`),console.log(`
You can now install skills using:`),console.log(`  superpowers-agent add ${d} <path-to-skill>`)}catch(a){if(p)try{b(`rm -rf "${p}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${a.message}`)}};import{dirname as $s}from"path";var Ao=e=>{if(!e){console.log(`Usage: superpowers-agent use-skill <skill-name>

Examples (smart matching):
  superpowers-agent use-skill brainstorming              # Matches superpowers:collaboration/brainstorming
  superpowers-agent use-skill collaboration/brainstorming # More specific suffix
  superpowers-agent use-skill test-driven-development    # Matches superpowers:testing/test-driven-development

Examples (explicit paths):
  superpowers-agent use-skill superpowers:collaboration/brainstorming  # Full superpowers skill path
  superpowers-agent use-skill claude:email-assistant                   # Full claude skill path
  superpowers-agent use-skill my-custom-skill                          # Project skill

Smart matching:
  - Type just the skill name or any suffix of the path
  - Priority: .agents/skills/ > .claude/skills/ > ~/.agents/skills/ > ~/.agents/superpowers/skills/
  - If multiple skills match at same priority, you'll see them with descriptions`);return}let o=P(e);if(!o){console.log(`Error: Skill not found: ${e}

Available skills:`),console.log("Run: superpowers-agent find-skills");return}let{skillFile:t,sourceType:n,actualSkillPath:s}=o;try{let{content:r,frontmatter:l}=Ge(t),i=ee[n].prefix+s,p=$s(t),a=`# ${l.name||i}`;l.description&&(a+=`
# ${l.description}`),l.whenToUse&&(a+=`
# When to use: ${l.whenToUse}`),a+=`
# Supporting tools and docs are in ${p}
# ============================================

${r}`,console.log(a)}catch(r){console.log(r.message)}};var To={bootstrap:xo,version:$o,"check-updates":()=>{wo().catch(e=>{console.error(e.message),process.exit(1)})},update:()=>{let e=process.argv.includes("--no-reinstall");ke({skipReinstall:e})},"config-get":lo,"config-set":io,"setup-skills":bo,"use-skill":()=>Ao(process.argv[3]),execute:so,"find-skills":O,add:Io,"add-repository":Po,pull:Do,dir:ro,path:to,"get-helpers":no,"install-copilot-prompts":v,"install-copilot-instructions":G,"install-cursor-commands":W,"install-cursor-hooks":_,"install-codex-prompts":H,"install-gemini-commands":J,"install-claude-commands":B,"install-opencode-commands":V,"install-aliases":Ae,default:()=>{console.log(`Superpowers for Agents
Usage:
  superpowers-agent bootstrap [--no-update]                         # Run complete bootstrap
  superpowers-agent version                                         # Show current version
  superpowers-agent check-updates                                   # Check for updates
  superpowers-agent update [--no-reinstall]                         # Update to latest version
  superpowers-agent config-get                                      # Show configuration
  superpowers-agent config-set <key> <value>                        # Update configuration
  superpowers-agent setup-skills                                    # Initialize project skills
  superpowers-agent use-skill <skill-name>                          # Load a skill
  superpowers-agent find-skills                                     # List available skills
  superpowers-agent add <url-or-path|@alias> [path] [options]      # Install skills
  superpowers-agent add-repository <git-url> [--as=@alias] [opts]  # Add repository alias
  superpowers-agent pull <url-or-path|@alias> [path] [options]     # Update skills
  superpowers-agent dir <skill-name>                                # Get skill directory
  superpowers-agent path <skill-name>                               # Get skill file path
  superpowers-agent execute <skill-name>                            # Execute a skill
  superpowers-agent get-helpers <skill> <search-term>               # Get helper file path
  superpowers-agent install-copilot-prompts                         # Install GitHub Copilot prompts
  superpowers-agent install-copilot-instructions                    # Install universal instructions
  superpowers-agent install-cursor-commands                         # Install Cursor commands
  superpowers-agent install-cursor-hooks                            # Install Cursor hooks
  superpowers-agent install-codex-prompts                           # Install Codex prompts
  superpowers-agent install-gemini-commands                         # Install Gemini commands
  superpowers-agent install-claude-commands                         # Install Claude commands
  superpowers-agent install-opencode-commands                       # Install OpenCode commands
  superpowers-agent install-aliases                                 # Install universal aliases (superpowers, superpowers-agent)

Documentation: https://github.com/complexthings/superpowers
`)}},Ss=process.argv[2]||"default",Cs=To[Ss]||To.default;try{Cs()}catch(e){console.error(`Error: ${e.message}`),process.exit(1)}
