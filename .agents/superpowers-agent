#!/usr/bin/env node
import{relative as ao,dirname as co,join as gs}from"path";import{existsSync as Ge}from"fs";import{homedir as Be,platform as qo}from"os";import{join as g,parse as Xo,dirname as Ve}from"path";import{existsSync as oe}from"fs";import{fileURLToPath as Qo}from"url";var Zo=Qo(import.meta.url),ee=Ve(Zo),es=()=>{let e=Be();switch(qo()){case"darwin":return g(e,"Library","Application Support","Code","User");case"win32":return g(e,"AppData","Roaming","Code","User");case"linux":return g(e,".config","Code","User");default:return g(e,".config","Code","User")}},os=()=>{let e=process.cwd(),o=Xo(e).root;for(;e!==o;){if(oe(g(e,".agents")))return e;e=Ve(e)}return process.cwd()},n={home:Be(),vscodeUserDir:es(),projectRoot:os(),get projectClaudeSkills(){return g(this.projectRoot,".claude","skills")},get projectAgentsSkills(){return g(this.projectRoot,".agents","skills")},get projectCopilotSkills(){return g(this.projectRoot,".copilot","skills")},get projectOpencodeSkills(){return g(this.projectRoot,".opencode","skill")},get projectCursorSkills(){return g(this.projectRoot,".cursor","skills")},get projectGeminiSkills(){return g(this.projectRoot,".gemini","skills")},get projectSkillsDir(){return g(this.projectRoot,"skills")},get homePersonalSkills(){return g(this.home,".agents","skills")},get homeClaudeSkills(){return g(this.home,".claude","skills")},get homeCopilotSkills(){return g(this.home,".copilot","skills")},get homeOpencodeSkills(){return g(this.home,".config","opencode","skill")},get homeCursorSkills(){return g(this.home,".cursor","skills")},get homeGeminiSkills(){return g(this.home,".gemini","skills")},get bootstrap(){return g(this.projectRoot,".agents","superpowers-bootstrap.md")},get superpowersRepo(){let e=g(ee,"..","..","..","skills");return oe(e)&&oe(g(ee,"..","..","..",".github","prompts"))?g(ee,"..","..",".."):g(this.home,".agents","superpowers")},get homeSuperpowersSkills(){return g(this.superpowersRepo,"skills")},get isSuperpowersRepo(){let e=g(ee,"..","..","..","skills");return oe(e)&&oe(g(ee,"..","..","..",".github","prompts"))}};import{readFileSync as Ke}from"fs";var se=e=>{try{let t=Ke(e,"utf8").split(`
`),l={name:"",description:"",whenToUse:""},s=!1;for(let r of t){if(r.trim()==="---"){if(s)break;s=!0;continue}if(s){let i=r.match(/^(\w+):\s*(.*)$/);if(i){let[,a,d]=i,c={name:"name",description:"description",when_to_use:"whenToUse"};c[a]&&(l[c[a]]=d.trim())}}}return l}catch{return{name:"",description:"",whenToUse:""}}},Ye=e=>{try{let o=Ke(e,"utf8"),t=se(e),l=o.split(`
`),s=!1,r=!1,i=[];for(let a of l){if(a.trim()==="---"){if(s){r=!0;continue}s=!0;continue}(r||!s)&&i.push(a)}return{content:i.join(`
`).trim(),frontmatter:t}}catch(o){throw new Error(`Error reading skill file: ${o.message}`)}};import{existsSync as Pe,readdirSync as Xe,statSync as Qe}from"fs";import{join as Ae,relative as st}from"path";import{readFileSync as ss,existsSync as ts}from"fs";import{join as ze}from"path";var te=e=>{let o=ze(e,"skill.json");if(!ts(o))return null;try{let t=ss(o,"utf8");return JSON.parse(t)}catch{return null}};var Oe=e=>e.startsWith("superpowers:")?{type:"superpowers",path:e.substring(12).replace(/^skills\//,"")}:e.startsWith("claude:")?{type:"claude",path:e.substring(7).replace(/^skills\//,"")}:e.startsWith("copilot:")?{type:"copilot",path:e.substring(8).replace(/^skills\//,"")}:e.startsWith("opencode:")?{type:"opencode",path:e.substring(9).replace(/^skills\//,"")}:e.startsWith("cursor:")?{type:"cursor",path:e.substring(7).replace(/^skills\//,"")}:e.startsWith("gemini:")?{type:"gemini",path:e.substring(7).replace(/^skills\//,"")}:{type:null,path:e.replace(/^skills\//,"")},qe=(e,o)=>{let t=te(e);if(!t||!t.helpers||!Array.isArray(t.helpers))return null;let l=t.helpers,s=o.toLowerCase(),r=null,i=0;for(let a of l){let d=a.toLowerCase();if(d===s){r=a;break}if(d.includes(s)){let p=s.length/d.length;p>i&&(i=p,r=a)}let c=a.split("/").pop().split(".")[0].toLowerCase();if(s.includes(c)){let p=c.length/s.length;p>i&&(i=p,r=a)}}return r?ze(e,r):null},re={project:{dir:"projectAgentsSkills",prefix:""},claude:{dir:"projectClaudeSkills",prefix:"claude:"},copilot:{dir:"projectCopilotSkills",prefix:"copilot:"},opencode:{dir:"projectOpencodeSkills",prefix:"opencode:"},cursor:{dir:"projectCursorSkills",prefix:"cursor:"},gemini:{dir:"projectGeminiSkills",prefix:"gemini:"},personal:{dir:"homePersonalSkills",prefix:""},personalClaude:{dir:"homeClaudeSkills",prefix:"claude:"},personalCopilot:{dir:"homeCopilotSkills",prefix:"copilot:"},personalOpencode:{dir:"homeOpencodeSkills",prefix:"opencode:"},personalCursor:{dir:"homeCursorSkills",prefix:"cursor:"},personalGemini:{dir:"homeGeminiSkills",prefix:"gemini:"},superpowers:{dir:"homeSuperpowersSkills",prefix:"superpowers:"}};var de=(e,o,t=null)=>{let l=[];if(!Pe(e))return l;let s=(r,i)=>{if(!(t!==null&&i>t))try{let a=Xe(r,{withFileTypes:!0});for(let d of a){if(d.name==="node_modules"||d.name===".git"||d.name.startsWith("."))continue;let c=Ae(r,d.name),p=d.isDirectory();if(d.isSymbolicLink())try{p=Qe(c).isDirectory()}catch{continue}if(!p)continue;let u=Ae(c,"SKILL.md");Pe(u)&&l.push(c),(t===null||i<t)&&s(c,i+1)}}catch{}};return s(e,0),l};var Ze=(e,o)=>{if(!Pe(e))return[];let t=[],l=o.toLowerCase().replace(/\\/g,"/"),s=(r,i="")=>{try{let a=Xe(r,{withFileTypes:!0});for(let d of a){let c=Ae(r,d.name),p=i?`${i}/${d.name}`:d.name,u=d.isDirectory();if(d.isSymbolicLink())try{u=Qe(c).isDirectory()}catch{continue}if(!(u&&(d.name==="node_modules"||d.name===".git"||d.name.startsWith(".")))){if(u)s(c,p);else if(d.name==="SKILL.md"){let k=i.toLowerCase().replace(/\\/g,"/");(k===l||k.endsWith("/"+l)||k.endsWith(l))&&t.push({file:c,path:i})}}}}catch{}};return s(e),t},eo=(e,o,t)=>{let l={project:"project skills (.agents/skills/)",claude:"claude skills (.claude/skills/)",copilot:"copilot skills (.copilot/skills/)",opencode:"opencode skills (.opencode/skill/)",cursor:"cursor skills (.cursor/skills/)",gemini:"gemini skills (.gemini/skills/)",personal:"personal skills (~/.agents/skills/)",personalClaude:"personal claude skills (~/.claude/skills/)",personalCopilot:"personal copilot skills (~/.copilot/skills/)",personalOpencode:"personal opencode skills (~/.config/opencode/skill/)",personalCursor:"personal cursor skills (~/.cursor/skills/)",personalGemini:"personal gemini skills (~/.gemini/skills/)",superpowers:"superpowers skills (~/.agents/superpowers/skills/)"}[t];console.log(`Error: Multiple skills match "${e}" in ${l}:
`);for(let s of o){let i=re[t].prefix+s.path;try{let a=se(s.file),d=a.description||a.whenToUse||"(no description)";console.log(`  ${i}`),console.log(`    ${d}
`)}catch{console.log(`  ${i}`),console.log(`    (unable to read description)
`)}}console.log("Please be more specific. Examples:");for(let s of o.slice(0,2)){let r=re[t].prefix;console.log(`  superpowers-agent execute ${r}${s.path}`)}};import{existsSync as Te}from"fs";import{join as rs,relative as ls}from"path";var ns=e=>{let{type:o,path:t}=Oe(e),l={superpowers:[n.homeSuperpowersSkills],claude:[n.projectClaudeSkills,n.homeClaudeSkills],copilot:[n.projectCopilotSkills,n.homeCopilotSkills],opencode:[n.projectOpencodeSkills,n.homeOpencodeSkills],gemini:[n.projectGeminiSkills,n.homeGeminiSkills],project:[n.projectAgentsSkills],personal:[n.homePersonalSkills]},s;o?s=(l[o]||[]).map(i=>({type:o,dir:i})):n.isSuperpowersRepo&&Te(n.projectSkillsDir)?s=[{type:"project",dir:n.projectSkillsDir},{type:"project",dir:n.projectAgentsSkills},{type:"claude",dir:n.projectClaudeSkills},{type:"copilot",dir:n.projectCopilotSkills},{type:"opencode",dir:n.projectOpencodeSkills},{type:"gemini",dir:n.projectGeminiSkills},{type:"personal",dir:n.homePersonalSkills},{type:"personalClaude",dir:n.homeClaudeSkills},{type:"personalCopilot",dir:n.homeCopilotSkills},{type:"personalOpencode",dir:n.homeOpencodeSkills},{type:"personalGemini",dir:n.homeGeminiSkills},{type:"superpowers",dir:n.homeSuperpowersSkills}]:s=[{type:"project",dir:n.projectAgentsSkills},{type:"claude",dir:n.projectClaudeSkills},{type:"copilot",dir:n.projectCopilotSkills},{type:"opencode",dir:n.projectOpencodeSkills},{type:"gemini",dir:n.projectGeminiSkills},{type:"personal",dir:n.homePersonalSkills},{type:"personalClaude",dir:n.homeClaudeSkills},{type:"personalCopilot",dir:n.homeCopilotSkills},{type:"personalOpencode",dir:n.homeOpencodeSkills},{type:"personalGemini",dir:n.homeGeminiSkills},{type:"superpowers",dir:n.homeSuperpowersSkills}];for(let{type:r,dir:i}of s){let a=Ze(i,t);if(a.length===1)return{skillFile:a[0].file,sourceType:r,actualSkillPath:a[0].path};a.length>1&&eo(t,a,r)}return null},F=e=>{let o=ns(e);if(o)return o;let{type:t,path:l}=Oe(e),s={superpowers:[n.homeSuperpowersSkills],claude:[n.projectClaudeSkills,n.homeClaudeSkills],copilot:[n.projectCopilotSkills,n.homeCopilotSkills],opencode:[n.projectOpencodeSkills,n.homeOpencodeSkills],gemini:[n.projectGeminiSkills,n.homeGeminiSkills],project:[n.projectAgentsSkills],personal:[n.homePersonalSkills]},r;t?r=(s[t]||[]).map(a=>({type:t,dir:a})):n.isSuperpowersRepo&&Te(n.projectSkillsDir)?r=[{type:"project",dir:n.projectSkillsDir},{type:"project",dir:n.projectAgentsSkills},{type:"claude",dir:n.projectClaudeSkills},{type:"copilot",dir:n.projectCopilotSkills},{type:"opencode",dir:n.projectOpencodeSkills},{type:"gemini",dir:n.projectGeminiSkills},{type:"personal",dir:n.homePersonalSkills},{type:"personalClaude",dir:n.homeClaudeSkills},{type:"personalCopilot",dir:n.homeCopilotSkills},{type:"personalOpencode",dir:n.homeOpencodeSkills},{type:"personalGemini",dir:n.homeGeminiSkills},{type:"superpowers",dir:n.homeSuperpowersSkills}]:r=[{type:"project",dir:n.projectAgentsSkills},{type:"claude",dir:n.projectClaudeSkills},{type:"copilot",dir:n.projectCopilotSkills},{type:"opencode",dir:n.projectOpencodeSkills},{type:"gemini",dir:n.projectGeminiSkills},{type:"personal",dir:n.homePersonalSkills},{type:"personalClaude",dir:n.homeClaudeSkills},{type:"personalCopilot",dir:n.homeCopilotSkills},{type:"personalOpencode",dir:n.homeOpencodeSkills},{type:"personalGemini",dir:n.homeGeminiSkills},{type:"superpowers",dir:n.homeSuperpowersSkills}];for(let{type:i,dir:a}of r){if(!Te(a))continue;let d=de(a,i,null);for(let c of d){let p=te(c);if(p&&p.aliases&&Array.isArray(p.aliases)){let u=l.toLowerCase();for(let k of p.aliases)if(k.toLowerCase()===u){let f=rs(c,"SKILL.md"),w=ls(a,c);return{skillFile:f,sourceType:i,actualSkillPath:w}}}}}return null};import{readFileSync as oo,writeFileSync as so,existsSync as Le}from"fs";import{join as L}from"path";import{execSync as is}from"child_process";var Fe=()=>({auto_update:!0,last_update_check:null,last_updated_commit:null}),le=()=>{let e=L(n.superpowersRepo,".config.json");try{if(!Le(e))return Fe();let o=oo(e,"utf8");return{...Fe(),...JSON.parse(o)}}catch{return Fe()}},ue=e=>{let o=L(n.superpowersRepo,".config.json"),l={...le(),...e};try{so(o,JSON.stringify(l,null,2))}catch(s){console.log(`Warning: couldn't save config: ${s.message}`)}},me=e=>{let o=e?L(n.home,".agents","config.json"):L(process.cwd(),".agents","config.json");if(!Le(o))return{};try{let t=oo(o,"utf8");return JSON.parse(t)}catch{return{}}},ve=(e,o)=>{let t=o?L(n.home,".agents"):L(process.cwd(),".agents"),l=L(t,"config.json");try{return Le(t)||is(`mkdir -p "${t}"`,{stdio:"pipe"}),so(l,JSON.stringify(e,null,2),"utf8"),!0}catch(s){throw new Error(`Failed to write config: ${s.message}`)}},P=(e=!1)=>me(e).repositories||{},to=(e,o,t)=>{let l=me(t);l.repositories||(l.repositories={}),l.repositories[e]=o,ve(l,t)};import{readFileSync as as}from"fs";import{join as ro,dirname as cs}from"path";import{fileURLToPath as ps}from"url";import{existsSync as ds}from"fs";import{execSync as us}from"child_process";var ms=ps(import.meta.url),lo=cs(ms),A=()=>{try{let e=ro(lo,"package.json");return ds(e)||(e=ro(lo,"..","..","package.json")),JSON.parse(as(e,"utf8")).version||"5.4.0"}catch{return"5.4.0"}},D=()=>{let e=A();console.log(`^^SAV:${e}^^`)},no=async()=>{try{let o=us('curl -sS "https://raw.githubusercontent.com/complexthings/superpowers/main/.agents/package.json"',{encoding:"utf8",timeout:1e4}),t=JSON.parse(o);return t.version?t.version:A()}catch(e){throw new Error(`Failed to fetch remote version: ${e.message}`)}},io=(e,o)=>{let t=e.split(".").map(Number),l=o.split(".").map(Number);for(let s=0;s<3;s++){let r=t[s]||0,i=l[s]||0;if(r>i)return!0;if(r<i)return!1}return!1};var fs={project:{dir:"projectAgentsSkills",prefix:""},claude:{dir:"projectClaudeSkills",prefix:"claude:"},copilot:{dir:"projectCopilotSkills",prefix:"copilot:"},opencode:{dir:"projectOpencodeSkills",prefix:"opencode:"},gemini:{dir:"projectGeminiSkills",prefix:"gemini:"},personal:{dir:"homePersonalSkills",prefix:""},personalClaude:{dir:"homeClaudeSkills",prefix:"claude:"},personalCopilot:{dir:"homeCopilotSkills",prefix:"copilot:"},personalOpencode:{dir:"homeOpencodeSkills",prefix:"opencode:"},personalGemini:{dir:"homeGeminiSkills",prefix:"gemini:"},superpowers:{dir:"homeSuperpowersSkills",prefix:"superpowers:"}},hs=(e,o)=>{let t=gs(e,"SKILL.md"),{dir:l,prefix:s}=fs[o],r=ao(n[l],e).replace(/\\/g,"/");console.log(`${s}${r}`);let{description:i,whenToUse:a}=se(t);i&&console.log(`  ${i}`),a&&console.log(`  When to use: ${a}`),console.log("")},M=()=>{D();let e=new Set,o=[{type:"project",dir:n.projectAgentsSkills,maxDepth:null},{type:"claude",dir:n.projectClaudeSkills,maxDepth:null},{type:"copilot",dir:n.projectCopilotSkills,maxDepth:null},{type:"opencode",dir:n.projectOpencodeSkills,maxDepth:null},{type:"gemini",dir:n.projectGeminiSkills,maxDepth:null},{type:"personal",dir:n.homePersonalSkills,maxDepth:null},{type:"personalClaude",dir:n.homeClaudeSkills,maxDepth:null},{type:"personalCopilot",dir:n.homeCopilotSkills,maxDepth:null},{type:"personalOpencode",dir:n.homeOpencodeSkills,maxDepth:null},{type:"personalGemini",dir:n.homeGeminiSkills,maxDepth:null},{type:"superpowers",dir:n.homeSuperpowersSkills,maxDepth:null}];for(let{type:t,dir:l,maxDepth:s}of o){let r=de(l,t,s);for(let i of r){let a=ao(l,i);e.has(a)||(e.add(a),hs(i,t))}}console.log(`Usage:
  superpowers-agent execute <skill-name> # Load a specific skill`)},po=()=>{D();let e=process.argv[3];if(!e){console.log(`Usage: superpowers-agent execute <skill-name-or-alias>

Examples:
  superpowers-agent execute brainstorming
  superpowers-agent execute superpowers:collaboration/brainstorming
  superpowers-agent execute aem/block-collection-and-party
  superpowers-agent execute block-collection  # Using alias

Description:
  Instructs the agent to execute the specified skill.
  Supports skill aliases defined in skill.json files.`);return}let o=F(e);if(!o){console.log(`Error: Skill not found: ${e}

Available skills:`),M();return}let{skillFile:t,sourceType:l}=o,s=process.cwd(),r=t.startsWith(s),i=Ge(n.projectAgentsSkills)||Ge(n.projectClaudeSkills);console.log(r?`Open this file with your Read Tool: ${t}

<IMPORTANT>
USE YOUR Read Tool to process this file. DO NOT use \`cat\` or print commands.
</IMPORTANT>`:i&&l!=="project"?`Open this file with your Read Tool: ${t}

<IMPORTANT>
1. Try using your Read Tool first
2. If your Read tool fails (file outside workspace), use: cat "${t}"
3. Consider using workspace-local skills for better tool compatibility
   Run: superpowers-agent setup-skills
</IMPORTANT>`:`Open this file with your Read Tool: ${t}

<IMPORTANT>
1. Try using your Read Tool first
2. If your Read tool fails (file outside workspace), use: cat "${t}"
3. NEVER skip loading the skill content
</IMPORTANT>`)},uo=()=>{let e=process.argv[3];if(!e){console.log(`Usage: superpowers-agent path <skill-name-or-alias>

Examples:
  superpowers-agent path brainstorming
  superpowers-agent path superpowers:collaboration/brainstorming
  superpowers-agent path aem/block-collection-and-party
  superpowers-agent path block-collection  # Using alias

Description:
  Outputs the file system path of the specified SKILL.md file.
  Supports skill aliases defined in skill.json files.`);return}let o=F(e);if(!o){console.log(`Error: Skill not found: ${e}

Available skills:`),M();return}let{skillFile:t}=o;console.log(t)},mo=()=>{let e=process.argv[3];if(!e){console.log(`Usage: superpowers-agent dir <skill-name>

Examples:
  superpowers-agent dir brainstorming
  superpowers-agent dir aem/block-collection-and-party
  superpowers-agent dir block-collection  # Using alias
  superpowers-agent dir superpowers:testing/test-driven-development

Description:
  Returns the directory path where the specified skill is located.
  Supports skill aliases defined in skill.json files.`);return}let o=F(e);if(!o){console.log(`Error: Skill not found: ${e}

Available skills:`),M();return}let{skillFile:t}=o,l=co(t);console.log(l)},go=()=>{D();let e=process.argv[3],o=process.argv[4];if(!e||!o){console.log(`Usage: superpowers-agent get-helpers <skill-name> <helper-search-term>

Examples:
  superpowers-agent get-helpers block-collection search-block
  superpowers-agent get-helpers aem/block-collection-and-party get-block-structure
  superpowers-agent get-helpers superpowers:testing/test-driven-development example

Description:
  Searches for helper files within a skill's skill.json configuration.
  Returns the full path to the best matching helper file.
  
  The command uses the skill's aliases if available, so you can use
  short names like "block-collection" instead of the full path.`);return}let t=F(e);if(!t){console.log(`Error: Skill not found: ${e}

Available skills:`),M();return}let{skillFile:l}=t,s=co(l),r=te(s);if(!r){console.log(`Error: No skill.json found for skill: ${e}
  Location: ${s}`);return}if(!r.helpers||!Array.isArray(r.helpers)||r.helpers.length===0){console.log(`Error: No helpers defined in skill.json for skill: ${e}
  Location: ${s}`);return}let i=qe(s,o);if(!i){console.log(`Error: No helper found matching "${o}" in skill: ${e}`),console.log(`
Available helpers:`);for(let a of r.helpers)console.log(`  - ${a}`);return}if(!Ge(i)){console.log(`Error: Helper file not found: ${i}
  Defined in skill.json but missing from filesystem`);return}console.log(i)},fo=()=>{let e=le();console.log("Current configuration:"),console.log(JSON.stringify(e,null,2))},ho=()=>{let e=process.argv[3],o=process.argv[4];if(!e||o===void 0){console.log(`Usage: .agents/superpowers-agent config-set <key> <value>

Available keys:
  auto_update (true/false) - Enable/disable automatic updates during bootstrap

Examples:
  .agents/superpowers-agent config-set auto_update false
  .agents/superpowers-agent config-set auto_update true`);return}let t=o;o==="true"?t=!0:o==="false"&&(t=!1);let l={};l[e]=t,ue(l),console.log(`\u2713 Set ${e} = ${t}`)},ko=()=>{D();let e=P(!0),o=P(!1),t=[];for(let[r,i]of Object.entries(e))t.push({alias:r,url:i,source:"global"});for(let[r,i]of Object.entries(o)){let a=t.findIndex(d=>d.alias===r);a>=0?t[a]={alias:r,url:i,source:"project"}:t.push({alias:r,url:i,source:"project"})}if(t.length===0){console.log(`No repository aliases configured.

Add a repository using:
  superpowers-agent add-repository <git-url> [--as=@alias]`);return}let l=Math.max(5,...t.map(r=>r.alias.length)),s=Math.max(3,...t.map(r=>r.url.length));console.log(`Repositories:
`),console.log(`${"Alias".padEnd(l)}  ${"URL".padEnd(s)}  Source`);for(let r of t)console.log(`${r.alias.padEnd(l)}  ${r.url.padEnd(s)}  (${r.source})`);console.log(`
Total: ${t.length} repository alias(es)`)};import{execSync as Ps}from"child_process";import{execSync as v}from"child_process";var ks=()=>{try{return v("git status --porcelain --untracked-files=no",{cwd:n.superpowersRepo,encoding:"utf8",stdio:"pipe",timeout:3e3}).trim().length===0}catch{return!1}},ne=()=>{try{return v("git rev-parse --abbrev-ref HEAD",{cwd:n.superpowersRepo,encoding:"utf8",stdio:"pipe",timeout:3e3}).trim()==="main"}catch{return!1}},ge=()=>{try{v("git fetch origin",{cwd:n.superpowersRepo,timeout:5e3,stdio:"pipe"});let e=!ks(),o=v("git rev-parse HEAD",{cwd:n.superpowersRepo,encoding:"utf8",stdio:"pipe"}).trim(),t=v("git rev-parse origin/main",{cwd:n.superpowersRepo,encoding:"utf8",stdio:"pipe"}).trim();if(!(o!==t))return{hasUpdates:!1,hasLocalChanges:e,currentCommit:o,latestCommit:t,commitsBehind:0,changedFiles:[]};let s=parseInt(v("git rev-list --count HEAD..origin/main",{cwd:n.superpowersRepo,encoding:"utf8",stdio:"pipe"}).trim(),10),i=v("git diff --name-only HEAD origin/main",{cwd:n.superpowersRepo,encoding:"utf8",stdio:"pipe"}).trim().split(`
`).filter(a=>a.length>0);return{hasUpdates:!0,hasLocalChanges:e,currentCommit:o,latestCommit:t,commitsBehind:s,changedFiles:i}}catch{return{hasUpdates:!1,hasLocalChanges:!1,currentCommit:"",latestCommit:"",commitsBehind:0,changedFiles:[],error:!0}}},yo=e=>{let o={".github/prompts/":"copilot-prompts",".github/copilot-instructions.md":"copilot-instructions",".cursor/commands/":"cursor-commands","hooks/cursor/":"cursor-hooks",".codex/prompts/":"codex-prompts",".gemini/commands/":"gemini-commands","commands/":"claude-commands",".opencode/command/":"opencode-commands"},t=new Set;for(let l of e)for(let[s,r]of Object.entries(o))l.startsWith(s)&&t.add(r);return Array.from(t)};import{existsSync as fe,readdirSync as ys}from"fs";import{join as W,dirname as Ss}from"path";import{execSync as he}from"child_process";var _=()=>{let e=W(n.superpowersRepo,".github","prompts"),o=W(n.vscodeUserDir,"prompts");if(!fe(e)){console.log("\u26A0\uFE0F  No Copilot prompts to install (source directory not found).");return}try{fe(o)||he(`mkdir -p "${o}"`,{stdio:"pipe"})}catch(s){console.log(`Error creating prompts directory: ${s.message}`);return}let t;try{t=ys(e).filter(s=>s.endsWith(".prompt.md"))}catch(s){console.log(`Error reading prompts directory: ${s.message}`);return}if(t.length===0){console.log("\u26A0\uFE0F  No prompt files found to install.");return}console.log("Installing GitHub Copilot prompts...");let l=0;for(let s of t)try{let r=W(e,s),i=W(o,s);he(`cp "${r}" "${i}"`,{stdio:"pipe"}),console.log(`  \u2713 Installed ${s}`),l++}catch(r){console.log(`  \u2717 Failed to install ${s}: ${r.message}`)}l>0&&console.log(`
\u2713 Installed ${l} prompt(s) to ${o}
  Use slash commands in GitHub Copilot:
    /brainstorm-with-superpowers - Refine ideas into designs
    /write-a-skill - Create new skills with TDD
    /skills - Discover available skills
    /use-skill - Load and apply a specific skill`)},H=()=>{let e=W(n.superpowersRepo,".github","copilot-instructions.md"),o=W(n.home,".github","copilot-instructions.md");if(!fe(e)){console.log("\u26A0\uFE0F  No Copilot instructions to install (source file not found).");return}let t=Ss(o);try{fe(t)||he(`mkdir -p "${t}"`,{stdio:"pipe"})}catch(l){console.log(`Error creating .github directory: ${l.message}`);return}try{he(`cp "${e}" "${o}"`,{stdio:"pipe"}),console.log(`\u2713 Installed GitHub Copilot universal instructions
  Location: ${o}
  GitHub Copilot will now use Superpowers skills universally in all workspaces`)}catch(l){console.log(`\u2717 Failed to install instructions: ${l.message}`)}};import{existsSync as ke,readdirSync as So}from"fs";import{join as E}from"path";import{execSync as J}from"child_process";var B=()=>{let e=E(n.superpowersRepo,".cursor","commands"),o=E(n.home,".cursor","commands");if(!ke(e)){console.log("\u26A0\uFE0F  No Cursor commands to install (source directory not found).");return}try{ke(o)||J(`mkdir -p "${o}"`,{stdio:"pipe"})}catch(s){console.log(`Error creating Cursor commands directory: ${s.message}`);return}let t;try{t=So(e).filter(s=>s.endsWith(".md"))}catch(s){console.log(`Error reading Cursor commands directory: ${s.message}`);return}if(t.length===0){console.log("\u26A0\uFE0F  No command files found to install.");return}console.log("Installing Cursor commands...");let l=0;for(let s of t)try{let r=E(e,s),i=E(o,s);J(`cp "${r}" "${i}"`,{stdio:"pipe"}),console.log(`  \u2713 Installed ${s}`),l++}catch(r){console.log(`  \u2717 Failed to install ${s}: ${r.message}`)}l>0&&console.log(`
\u2713 Installed ${l} command(s) to ${o}
  Use slash commands in Cursor:
    /brainstorm-with-superpowers - Refine ideas into designs
    /write-a-skill - Create new skills with TDD
    /skills - Discover available skills
    /use-skill - Load and apply a specific skill`)},V=()=>{let e=E(n.superpowersRepo,"hooks","cursor"),o=E(n.home,".cursor","hooks"),t=E(e,"hooks.json"),l=E(n.home,".cursor","hooks.json");if(!ke(e)){console.log("\u26A0\uFE0F  No Cursor hooks to install (source directory not found).");return}try{ke(o)||J(`mkdir -p "${o}"`,{stdio:"pipe"})}catch(i){console.log(`Error creating Cursor hooks directory: ${i.message}`);return}try{J(`cp "${t}" "${l}"`,{stdio:"pipe"}),console.log("  \u2713 Installed hooks.json")}catch(i){console.log(`  \u2717 Failed to install hooks.json: ${i.message}`);return}let s;try{s=So(e).filter(i=>i.endsWith(".sh"))}catch(i){console.log(`Error reading hooks directory: ${i.message}`);return}if(s.length===0){console.log("\u26A0\uFE0F  No hook scripts found to install.");return}console.log("Installing Cursor hooks...");let r=0;for(let i of s)try{let a=E(e,i),d=E(o,i);J(`cp "${a}" "${d}"`,{stdio:"pipe"}),J(`chmod +x "${d}"`,{stdio:"pipe"}),console.log(`  \u2713 Installed ${i}`),r++}catch(a){console.log(`  \u2717 Failed to install ${i}: ${a.message}`)}r>0&&console.log(`
\u2713 Installed ${r} hook(s) to ${o}
  Cursor will now check for skills before each prompt submission
  Restart Cursor for hooks to take effect`)};import{existsSync as wo,readdirSync as ws}from"fs";import{join as ye}from"path";import{execSync as $o}from"child_process";var K=()=>{let e=ye(n.superpowersRepo,".codex","prompts"),o=ye(n.home,".codex","prompts");if(!wo(e)){console.log("\u26A0\uFE0F  No Codex prompts to install (source directory not found).");return}try{wo(o)||$o(`mkdir -p "${o}"`,{stdio:"pipe"})}catch(s){console.log(`Error creating Codex prompts directory: ${s.message}`);return}let t;try{t=ws(e).filter(s=>s.endsWith(".md"))}catch(s){console.log(`Error reading Codex prompts directory: ${s.message}`);return}if(t.length===0){console.log("\u26A0\uFE0F  No prompt files found to install.");return}console.log("Installing OpenAI Codex prompts...");let l=0;for(let s of t)try{let r=ye(e,s),i=ye(o,s);$o(`cp "${r}" "${i}"`,{stdio:"pipe"}),console.log(`  \u2713 Installed ${s}`),l++}catch(r){console.log(`  \u2717 Failed to install ${s}: ${r.message}`)}l>0&&console.log(`
\u2713 Installed ${l} prompt(s) to ${o}
  Use slash commands in OpenAI Codex:
    /prompts:brainstorm - Refine ideas into designs
    /prompts:write-skill - Create new skills with TDD
    /prompts:skills - Discover available skills
    /prompts:use-skill - Load and apply a specific skill
  Note: Restart Codex or open a new session to reload prompts`)};import{existsSync as Co,readdirSync as $s}from"fs";import{join as Se}from"path";import{execSync as xo}from"child_process";var Y=()=>{let e=Se(n.superpowersRepo,".gemini","commands"),o=Se(n.home,".gemini","commands");if(!Co(e)){console.log("\u26A0\uFE0F  No Gemini commands to install (source directory not found).");return}try{Co(o)||xo(`mkdir -p "${o}"`,{stdio:"pipe"})}catch(s){console.log(`Error creating Gemini commands directory: ${s.message}`);return}let t;try{t=$s(e).filter(s=>s.endsWith(".toml"))}catch(s){console.log(`Error reading Gemini commands directory: ${s.message}`);return}if(t.length===0){console.log("\u26A0\uFE0F  No command files found to install.");return}console.log("Installing Gemini commands...");let l=0;for(let s of t)try{let r=Se(e,s),i=Se(o,s);xo(`cp "${r}" "${i}"`,{stdio:"pipe"}),console.log(`  \u2713 Installed ${s}`),l++}catch(r){console.log(`  \u2717 Failed to install ${s}: ${r.message}`)}l>0&&console.log(`
\u2713 Installed ${l} command(s) to ${o}
  Use slash commands in Gemini:
    /brainstorm-with-superpowers - Refine ideas into designs
    /write-a-skill - Create new skills with TDD
    /skills - Discover available skills
    /use-skill - Load and apply a specific skill`)};import{existsSync as bo,readdirSync as Cs}from"fs";import{join as we}from"path";import{execSync as jo}from"child_process";var z=()=>{let e=we(n.superpowersRepo,"commands"),o=we(n.home,".claude","commands");if(!bo(e)){console.log("\u26A0\uFE0F  No Claude commands to install (source directory not found).");return}try{bo(o)||jo(`mkdir -p "${o}"`,{stdio:"pipe"})}catch(s){console.log(`Error creating Claude commands directory: ${s.message}`);return}let t;try{t=Cs(e).filter(s=>s.endsWith(".md"))}catch(s){console.log(`Error reading Claude commands directory: ${s.message}`);return}if(t.length===0){console.log("\u26A0\uFE0F  No command files found to install.");return}console.log("Installing Claude Code commands...");let l=0;for(let s of t)try{let r=we(e,s),i=we(o,s);jo(`cp "${r}" "${i}"`,{stdio:"pipe"}),console.log(`  \u2713 Installed ${s}`),l++}catch(r){console.log(`  \u2717 Failed to install ${s}: ${r.message}`)}l>0&&console.log(`
\u2713 Installed ${l} command(s) to ${o}
  Use slash commands in Claude Code:
    /brainstorm - Refine ideas into designs
    /execute-plan - Execute plans in batches
    /write-plan - Create implementation plans
    /skills - Discover available skills
    /use-skill - Load and apply a specific skill`)};import{existsSync as Ro,readdirSync as xs}from"fs";import{join as $e}from"path";import{execSync as Do}from"child_process";var q=()=>{let e=$e(n.superpowersRepo,".opencode","command"),o=$e(n.home,".config","opencode","command");if(!Ro(e)){console.log("\u26A0\uFE0F  No opencode commands to install (source directory not found).");return}try{Ro(o)||Do(`mkdir -p "${o}"`,{stdio:"pipe"})}catch(s){console.log(`Error creating opencode commands directory: ${s.message}`);return}let t;try{t=xs(e).filter(s=>s.endsWith(".md"))}catch(s){console.log(`Error reading opencode commands directory: ${s.message}`);return}if(t.length===0){console.log("\u26A0\uFE0F  No command files found to install.");return}console.log("Installing opencode commands...");let l=0;for(let s of t)try{let r=$e(e,s),i=$e(o,s);Do(`cp "${r}" "${i}"`,{stdio:"pipe"}),console.log(`  \u2713 Installed ${s}`),l++}catch(r){console.log(`  \u2717 Failed to install ${s}: ${r.message}`)}l>0&&console.log(`
\u2713 Installed ${l} command(s) to ${o}
  Use slash commands in opencode:
    /brainstorm - Refine ideas into designs
    /execute-plan - Execute plans in batches
    /write-plan - Create implementation plans
    /write-skill - Create new skills with TDD
    /skills - Discover available skills
    /use-skill - Load and apply a specific skill`)};import{existsSync as T,readdirSync as bs,lstatSync as js,symlinkSync as Eo,unlinkSync as Rs,mkdirSync as ie,readlinkSync as Ds}from"fs";import{join as j,dirname as Es}from"path";import{homedir as x,platform as Us}from"os";var Uo=[{name:"claude",parentDir:()=>j(x(),".claude"),skillsDir:()=>j(x(),".claude","skills"),superpowersTarget:"superpowers"},{name:"copilot",parentDir:()=>j(x(),".copilot"),skillsDir:()=>j(x(),".copilot","skills"),superpowersTarget:"superpowers"},{name:"opencode",parentDir:()=>j(x(),".config","opencode"),skillsDir:()=>j(x(),".config","opencode","skill"),superpowersTarget:"superpowers"},{name:"cursor",parentDir:()=>j(x(),".cursor"),skillsDir:()=>j(x(),".cursor","skills"),superpowersTarget:"superpowers"},{name:"gemini",parentDir:()=>j(x(),".gemini"),skillsDir:()=>j(x(),".gemini","skills"),superpowersTarget:"superpowers"}],Ne=e=>{try{return js(e).isSymbolicLink()}catch{return!1}},Is=(e,o)=>{try{return Ne(e)?Ds(e)===o:!1}catch{return!1}},Io=(e,o)=>{if(!T(e))return{created:!1,existed:!1,error:`Source does not exist: ${e}`};if(Is(o,e))return{created:!1,existed:!0};if(T(o)||Ne(o))if(Ne(o))try{Rs(o)}catch(s){return{created:!1,existed:!1,error:`Failed to remove existing symlink: ${s.message}`}}else return{created:!1,existed:!1,error:`Target exists and is not a symlink: ${o}`};let t=Es(o);if(!T(t))try{ie(t,{recursive:!0})}catch(s){return{created:!1,existed:!1,error:`Failed to create parent directory: ${s.message}`}}let l=Us();try{return l==="win32"?Eo(e,o,"junction"):Eo(e,o,"dir"),{created:!0,existed:!1}}catch(s){return l==="win32"&&s.code==="EPERM"?{created:!1,existed:!1,error:"Windows requires Developer Mode or admin privileges for symlinks. Enable Developer Mode: Settings > Update & Security > For developers"}:{created:!1,existed:!1,error:`Failed to create symlink: ${s.message}`}}};var Oo=(e,o,t="skills")=>{let l=me(!0);if(l.symlinks||(l.symlinks={}),l.symlinks[e]||(l.symlinks[e]={superpowers:null,skills:[]}),t==="superpowers")l.symlinks[e].superpowers=o;else{let s=l.symlinks[e].skills||[];s.includes(o)||(s.push(o),l.symlinks[e].skills=s)}ve(l,!0)};var Os=(e,o={})=>{let t=n.homeSuperpowersSkills,l=j(e.skillsDir(),e.superpowersTarget),s=Io(t,l);if(s.created){Oo(e.name,l,"superpowers");let r=t.replace(x(),"~"),i=l.replace(x(),"~");return console.log(`  \u2713 Created ${i} -> ${r}`),{created:!0}}else{if(s.existed)return{created:!1,existed:!0};if(s.error)return console.log(`  \u26A0\uFE0F  ${e.name} superpowers: ${s.error}`),{created:!1,error:s.error}}return{created:!1}},Po=(e,o={})=>{let t=n.homePersonalSkills;if(!T(t))return{created:0,existed:0};let l;try{l=bs(t,{withFileTypes:!0}).filter(r=>r.isDirectory()&&!r.name.startsWith(".")).map(r=>r.name)}catch{return{created:0,existed:0}}if(l.length===0)return{created:0,existed:0};let s={created:0,existed:0,names:[]};for(let r of l){let i=j(t,r),a=j(e.skillsDir(),r),d=Io(i,a);d.created?(Oo(e.name,a,"skills"),s.created++,s.names.push(r)):d.existed?s.existed++:d.error&&console.log(`  \u26A0\uFE0F  ${r}: ${d.error}`)}return s.created>0&&console.log(`  \u2713 Synced ${s.created} personal skill(s): ${s.names.join(", ")}`),s},Ce=(e={})=>{let{force:o=!1}=e;for(let t of Uo){let l=t.parentDir(),s=t.skillsDir();if(!T(l))if(o)try{ie(l,{recursive:!0}),console.log(`\u2713 Created ${l.replace(x(),"~")}`)}catch(r){console.log(`\u26A0\uFE0F  Failed to create ${l.replace(x(),"~")}: ${r.message}`);continue}else{console.log(`\u26A0\uFE0F  Skipping ${t.name} (${l.replace(x(),"~")} not found)`),console.log("   Use --force to create directory");continue}if(!T(s))try{ie(s,{recursive:!0})}catch(r){console.log(`\u26A0\uFE0F  Failed to create skills directory for ${t.name}: ${r.message}`);continue}console.log(`${t.name}:`),Os(t,e),Po(t,e)}},Me=(e={})=>{let{force:o=!1}=e;for(let t of Uo){let l=t.parentDir(),s=t.skillsDir();if(!T(l)){if(!o)continue;try{ie(l,{recursive:!0})}catch{continue}}if(!T(s))try{ie(s,{recursive:!0})}catch{continue}Po(t,e)}};var As=e=>{let t={"copilot-prompts":_,"copilot-instructions":H,"cursor-commands":B,"cursor-hooks":V,"codex-prompts":K,"gemini-commands":Y,"claude-commands":z,"opencode-commands":q}[e];if(t)try{return t(),{success:!0,integration:e}}catch(l){return{success:!1,integration:e,error:l.message}}return{success:!1,integration:e,error:"Unknown integration"}},Ts=()=>{console.log("Installing aliases...")},xe=(e={})=>{let o=e.skipReinstall||!1;console.log(`# Checking for Superpowers updates...
`);let t=ge();if(t.error){console.log("\u26A0\uFE0F  Could not check for updates (network issue)");return}if(!t.hasUpdates){console.log("\u2713 Already up to date");return}if(t.hasLocalChanges){console.log(`\u26A0\uFE0F  Cannot auto-update: local changes detected
   Commit or stash your changes first, then run update again
   Or manually update: cd ${n.superpowersRepo} && git pull`);return}if(!ne()){console.log(`\u26A0\uFE0F  Not on main branch, skipping auto-update
   Switch to main branch first: cd ${n.superpowersRepo} && git checkout main`);return}console.log(`\u{1F4E6} Updating from ${t.currentCommit.substring(0,7)} to ${t.latestCommit.substring(0,7)}
   (${t.commitsBehind} new commit${t.commitsBehind>1?"s":""})
`);try{Ps("git pull origin main",{cwd:n.superpowersRepo,stdio:"pipe",timeout:1e4}),console.log("\u2713 Updated superpowers repository")}catch(i){console.log(`\u2717 Git pull failed: ${i.message}
   Please resolve manually and try again`);return}if(console.log(""),ue({last_updated_commit:t.latestCommit,last_update_check:new Date().toISOString()}),o){console.log(`\u2139\uFE0F  Skipping integration reinstall (--no-reinstall flag)

\u2713 Update complete!`);return}let l=yo(t.changedFiles);if(l.length===0){console.log(`\u2139\uFE0F  No integration files changed, skipping reinstalls

\u2713 Update complete!`);return}console.log(`\u{1F504} Reinstalling updated integrations:
`);let s=[];for(let i of l){let a=As(i);s.push(a),a.success?console.log(`  \u2713 ${i}`):console.log(`  \u2717 ${i} (${a.error})`)}console.log(""),console.log(`
---
`),Ts(),console.log(`
---
`),console.log(`## Syncing Skill Symlinks
`),Ce();let r=s.filter(i=>!i.success);if(r.length>0){console.log("\u26A0\uFE0F  Update completed with errors:");for(let i of r){console.log(`  - ${i.integration} failed to install`);let a=`install-${i.integration}`;console.log(`    Run manually: superpowers-agent ${a}`)}}else console.log("\u2713 Update complete!")},Ao=async()=>{D();let e=A();console.log(`Current version: ${e}`);try{let o=await no();console.log(`Latest version: ${o}`),io(o,e)?(console.log("Update available: Yes"),process.exit(1)):(console.log("You are up to date"),process.exit(0))}catch(o){console.log(`Error checking for updates: ${o.message}`),process.exit(1)}},To=()=>{let e=A();console.log(e)};import{existsSync as h,readFileSync as be,writeFileSync as Lo}from"fs";import{join as y,dirname as Ls,parse as ce}from"path";import{execSync as R}from"child_process";import{platform as vs}from"os";import{execSync as Fs}from"child_process";var ae=e=>{try{return Fs(`which ${e}`,{stdio:"pipe",timeout:2e3}),!0}catch{return!1}};var m={opencode:{check:()=>ae("opencode"),cli:!0,name:"OpenCode",installUrl:"https://opencode.ai/docs/installation",bootstrapCommand:"install-opencode-commands"},claude:{check:()=>ae("claude"),cli:!0,name:"Claude Code",installUrl:"https://code.claude.com/docs/en/installation",bootstrapCommand:"install-claude-commands"},gemini:{check:()=>ae("gemini"),cli:!0,name:"Gemini",installUrl:"https://cloud.google.com/gemini/docs/cli/install",bootstrapCommand:"install-gemini-commands"},codex:{check:()=>ae("codex"),cli:!0,name:"Codex",installUrl:"https://developers.openai.com/codex/docs/installation",bootstrapCommand:"install-codex-prompts"},cursor:{check:()=>!0,cli:!1,name:"Cursor",bootstrapCommand:"install-cursor-commands"},copilot:{check:()=>!0,cli:!1,name:"GitHub Copilot",bootstrapCommand:"install-copilot-prompts"}},Fo=()=>{let e=[];return m.copilot.check()&&e.push("github-copilot"),m.cursor.check()&&e.push("cursor"),m.claude.check()&&e.push("claude-code"),m.opencode.check()&&e.push("opencode"),m.gemini.check()&&e.push("gemini"),m.codex.check()&&e.push("codex"),e};var Gs=e=>{let o={"github-copilot":"TOOLS-GITHUB-COPILOT.md.template",cursor:"TOOLS-CURSOR.md.template","claude-code":"TOOLS-CLAUDE-CODE.md.template",gemini:"TOOLS-GEMINI.md.template",opencode:"TOOLS-OPENCODE.md.template",codex:"TOOLS-CODEX.md.template"},t=y(n.superpowersRepo,".agents","templates"),l=[];for(let s of e){let r=o[s];if(!r)continue;let i=y(t,r);if(h(i))try{let a=be(i,"utf8").trim();l.push(a)}catch{let d={"github-copilot":"GitHub Copilot",cursor:"Cursor","claude-code":"Claude Code",opencode:"OpenCode",gemini:"Gemini",codex:"Codex"};l.push(`**Tool Mapping for ${d[s]}:**`)}}return l.join(`
`)},pe=(e,o,t,l=!0)=>{let s=h(e);if(!s&&!l)return{updated:!1,created:!1,skipped:!0};let r=Gs(t),i=o.replace(/\{\{TOOL_MAPPINGS\}\}/g,r),a=new Date().toISOString().split("T")[0];i=i.replace(/\{\{DATE\}\}/g,a),i=i.replace(/\{\{SUPERPOWERS_PATH\}\}/g,n.superpowersRepo);let d=`<!-- SUPERPOWERS_SKILLS_START -->
${i}
<!-- SUPERPOWERS_SKILLS_END -->`;if(s){let c=new Date().toISOString().split("T")[0],p=`${e}.backup-${c}`;try{R(`cp "${e}" "${p}"`,{stdio:"pipe"})}catch(S){return{updated:!1,error:!0,message:`Failed to backup: ${S.message}`}}let u;try{u=be(e,"utf8")}catch(S){return{updated:!1,error:!0,message:`Failed to read: ${S.message}`}}let k="<!-- SUPERPOWERS_SKILLS_START -->",f="<!-- SUPERPOWERS_SKILLS_END -->",w;if(u.includes(k)&&u.includes(f)){let S=new RegExp(`${k}[\\s\\S]*?${f}`,"g");w=u.replace(S,d)}else{let S=u.match(/^#\s+.+$/m);if(S){let Q=u.indexOf(`
`,S.index)+1;w=u.slice(0,Q)+`
`+d+`
`+u.slice(Q)}else w=d+`

`+u}try{return Lo(e,w,"utf8"),{updated:!0,created:!1,backup:p}}catch(S){return{updated:!1,error:!0,message:`Failed to write: ${S.message}`}}}else try{let c=Ls(e);h(c)||R(`mkdir -p "${c}"`,{stdio:"pipe"});let k=`# ${ce(e).base.toUpperCase()}

`+d;return Lo(e,k,"utf8"),{updated:!1,created:!0}}catch(c){return{updated:!1,created:!1,error:!0,message:`Failed to create: ${c.message}`}}},Ns=()=>{console.log("Installing Unix aliases...");let e=y(n.superpowersRepo,".agents","superpowers-agent"),o=y(n.home,".local","bin"),t=y(o,"superpowers-agent"),l=y(o,"superpowers");if(!h(e)){console.log("\u26A0\uFE0F  superpowers-agent executable not found");return}if(!h(o))try{R(`mkdir -p "${o}"`,{stdio:"pipe"}),console.log(`\u2713 Created ${o}`)}catch(r){console.log(`\u26A0\uFE0F  Failed to create ${o}: ${r.message}`);return}try{h(t)&&R(`rm "${t}"`,{stdio:"pipe"}),R(`ln -s "${e}" "${t}"`,{stdio:"pipe"}),console.log(`\u2713 Created symlink: superpowers-agent -> ${e}`)}catch(r){console.log(`\u26A0\uFE0F  Failed to create superpowers-agent symlink: ${r.message}`)}try{h(l)&&R(`rm "${l}"`,{stdio:"pipe"}),R(`ln -s "${e}" "${l}"`,{stdio:"pipe"}),console.log(`\u2713 Created symlink: superpowers -> ${e}`)}catch(r){console.log(`\u26A0\uFE0F  Failed to create superpowers symlink: ${r.message}`)}(process.env.PATH||"").includes(o)||(console.log(`
\u26A0\uFE0F  Warning: ${o} is not in your PATH`),console.log("   Add this to your shell profile (~/.bashrc, ~/.zshrc, etc.):"),console.log(`   export PATH="$HOME/.local/bin:$PATH"
`))},Ms=()=>{console.log("Installing Windows aliases..."),console.log("\u2713 Windows alias installation (implementation pending)")},We=()=>{let e=vs();return console.log(`## Installing Universal Aliases
`),e==="win32"?Ms():Ns()},vo=()=>{D(),console.log(`# Setting up Superpowers skills for this project
`);let e=process.cwd(),o=y(e,".agents"),t=y(o,"skills"),l=y(e,"AGENTS.md"),s=y(o,"AGENTS.md"),r=h(l)?l:s,i=y(n.superpowersRepo,".agents","templates","AGENTS.md.template");if(!h(i)){console.log(`\u2717 Error: AGENTS.md template not found
  Expected at: ${i}
  Please update your Superpowers installation`);return}if(h(o))console.log("\u2713 .agents/ directory exists");else try{R(`mkdir -p "${o}"`,{stdio:"pipe"}),console.log("\u2713 Created .agents/ directory")}catch(C){console.log(`\u2717 Failed to create .agents/ directory: ${C.message}`);return}if(h(t))console.log("\u2713 .agents/skills/ directory exists");else try{R(`mkdir -p "${t}"`,{stdio:"pipe"}),R(`touch "${y(t,".gitkeep")}"`,{stdio:"pipe"}),console.log("\u2713 Created .agents/skills/ directory")}catch(C){console.log(`\u2717 Failed to create skills directory: ${C.message}`);return}let a=h(r);if(a){let C=new Date().toISOString().split("T")[0],Je=`${r}.backup-${C}`;try{R(`cp "${r}" "${Je}"`,{stdio:"pipe"}),console.log(`\u2713 Backed up existing AGENTS.md to ${ce(Je).base}`)}catch(zo){console.log(`\u2717 Failed to backup AGENTS.md: ${zo.message}`);return}}let d;try{d=be(i,"utf8")}catch(C){console.log(`\u2717 Failed to read template: ${C.message}`);return}let c=A();d=d.replace(/\{\{VERSION\}\}/g,c);let p=[],u=y(e,".github","copilot-instructions.md"),k=y(n.home,".github","copilot-instructions.md");(h(u)||h(k)||m.copilot.check())&&p.push("github-copilot");let f=y(e,"CLAUDE.md"),w=y(o,"CLAUDE.md");(h(f)||h(w)||m.claude.check())&&p.push("claude-code");let S=y(e,"GEMINI.md"),Q=y(o,"GEMINI.md");(h(S)||h(Q)||m.gemini.check())&&p.push("gemini"),m.cursor.check()&&p.push("cursor"),m.opencode.check()&&p.push("opencode"),m.codex.check()&&p.push("codex"),console.log(`
Detected platforms for project: ${p.join(", ")||"none"}
`);let Yo=p.filter(C=>["github-copilot","cursor","opencode","codex"].includes(C)),Z=pe(r,d,Yo,!a);Z.created?console.log(`\u2713 Created AGENTS.md with platform tool mappings (${r===l?"root":".agents/"})`):Z.updated?console.log(`\u2713 Updated AGENTS.md with platform tool mappings (${r===l?"root":".agents/"})`):Z.error&&console.log("\u26A0\uFE0F  Failed to update AGENTS.md");let Ee=h(f)?f:w,I=pe(Ee,d,["claude-code"],!1);I.created?console.log(`\u2713 Created CLAUDE.md with Claude Code tool mappings (${Ee===f?"root":".agents/"})`):I.updated?(console.log(`\u2713 Updated CLAUDE.md with Claude Code tool mappings (${Ee===f?"root":".agents/"})`),I.backup&&console.log(`  Backed up to ${ce(I.backup).base}`)):I.skipped?console.log("\u2139\uFE0F  Skipped CLAUDE.md (does not exist)"):I.error&&console.log("\u26A0\uFE0F  Failed to update CLAUDE.md");let Ue=h(S)?S:Q,O=pe(Ue,d,["gemini"],!1);O.created?console.log(`\u2713 Created GEMINI.md with Gemini tool mappings (${Ue===S?"root":".agents/"})`):O.updated?(console.log(`\u2713 Updated GEMINI.md with Gemini tool mappings (${Ue===S?"root":".agents/"})`),O.backup&&console.log(`  Backed up to ${ce(O.backup).base}`)):O.skipped?console.log("\u2139\uFE0F  Skipped GEMINI.md (does not exist)"):O.error&&console.log("\u26A0\uFE0F  Failed to update GEMINI.md");let Ie=y(e,".github","copilot-instructions.md"),He=!h(l)&&!h(s),G={skipped:!0};h(Ie)&&He?(G=pe(Ie,d,["github-copilot"],!1),G.updated?(console.log("\u2713 Updated .github/copilot-instructions.md with GitHub Copilot tool mappings"),G.backup&&console.log(`  Backed up to ${ce(G.backup).base}`)):G.error&&console.log("\u26A0\uFE0F  Failed to update .github/copilot-instructions.md")):h(Ie)&&!He?console.log("\u2139\uFE0F  Skipped .github/copilot-instructions.md (AGENTS.md exists, using that instead)"):console.log("\u2139\uFE0F  Skipped .github/copilot-instructions.md (does not exist)");let N=`
# Setup complete!

Your project now has:
  - .agents/ directory structure`;(Z.updated||Z.created)&&(N+=`
  - AGENTS.md with universal skills instructions`),(I.updated||I.created)&&(N+=`
  - CLAUDE.md with Claude Code skills instructions`),(O.updated||O.created)&&(N+=`
  - GEMINI.md with Gemini skills instructions`),G.updated&&(N+=`
  - .github/copilot-instructions.md with GitHub Copilot skills instructions`),N+=`
  - .agents/skills/ ready for project-specific skills
`,console.log(N)},Go=()=>{if(D(),console.log(`# Superpowers Bootstrap for Agents
# ==================================
`),!process.argv.includes("--no-update")){let c=le(),p=ge();p.error?console.log(`## Update Check

\u26A0\uFE0F  Could not check for updates (network issue)

---
`):p.hasUpdates&&(c.auto_update&&!p.hasLocalChanges&&ne()?(console.log(`## Auto-Update
`),xe(),console.log(`
---
`)):(console.log(`## Update Available
`),p.hasLocalChanges?console.log("\u26A0\uFE0F  Your superpowers installation is behind the latest version.\n    Cannot auto-update: local changes detected\n    To update, commit/stash changes then run: `superpowers-agent update`"):ne()?console.log("\u26A0\uFE0F  Your superpowers installation is behind the latest version.\n    To update, run: `superpowers-agent update`\n    Or enable auto-update: `superpowers-agent config-set auto_update true`"):console.log("\u26A0\uFE0F  Your superpowers installation is behind the latest version.\n    Cannot auto-update: not on main branch\n    To update, switch to main then run: `superpowers-agent update`"),console.log(`
---
`)))}We(),console.log(`---
`),console.log(`## GitHub Copilot Integration
`),_(),console.log(""),H(),console.log(`
---
`),console.log(`## Cursor Integration
`),B(),console.log(""),V(),console.log(`
---
`),console.log(`## OpenAI Codex Integration
`),m.codex.check()?K():console.log(`\u26A0\uFE0F  Skipped (${m.codex.name} CLI not detected)
\u{1F4A1} To enable Codex integration:
   1. Install Codex: ${m.codex.installUrl}
   2. Run: superpowers-agent ${m.codex.bootstrapCommand}`),console.log(`
---
`),console.log(`## Gemini Integration
`),m.gemini.check()?Y():console.log(`\u26A0\uFE0F  Skipped (${m.gemini.name} CLI not detected)
\u{1F4A1} To enable Gemini integration:
   1. Install Gemini: ${m.gemini.installUrl}
   2. Run: superpowers-agent ${m.gemini.bootstrapCommand}`),console.log(`
---
`),console.log(`## Claude Code Integration
`),m.claude.check()?z():console.log(`\u26A0\uFE0F  Skipped (${m.claude.name} CLI not detected)
\u{1F4A1} To enable Claude Code integration:
   1. Install Claude Code: ${m.claude.installUrl}
   2. Run: superpowers-agent ${m.claude.bootstrapCommand}`),console.log(`
---
`),console.log(`## OpenCode Integration
`),m.opencode.check()?q():console.log(`\u26A0\uFE0F  Skipped (${m.opencode.name} CLI not detected)
\u{1F4A1} To enable OpenCode integration:
   1. Install OpenCode: ${m.opencode.installUrl}
   2. Run: superpowers-agent ${m.opencode.bootstrapCommand}`),console.log(`
---
`),console.log(`## Generating Platform-Specific Files
`);let r=Fo();console.log(`Detected platforms: ${r.join(", ")||"none"}
`);let i=y(n.superpowersRepo,".agents","templates","AGENTS.md.template"),a="";try{a=be(i,"utf8")}catch(c){console.log(`\u26A0\uFE0F  Could not read AGENTS.md template: ${c.message}
`)}if(a){let c=A();a=a.replace(/\{\{VERSION\}\}/g,c);let p=y(n.home,".agents","AGENTS.md"),u=pe(p,a,r,!0);u.created?console.log(`\u2713 Created ${p}`):u.updated&&console.log(`\u2713 Updated ${p}`)}console.log(`
---
`),console.log(`## Syncing Skill Symlinks
`);let d=process.argv.includes("--force");Ce({force:d}),console.log(`
---
`),console.log(`# Bootstrap Complete!
`),console.log("\u2713 All integrations installed"),console.log("\u2713 Skills system ready"),console.log(`\u2713 Skill symlinks synced
`),console.log("Next steps:"),console.log("  - Run `superpowers-agent find-skills` to see available skills"),console.log("  - Run `superpowers-agent setup-skills` in your project directory")};import{existsSync as U,readFileSync as No}from"fs";import{join as $,dirname as _e,parse as je}from"path";import{execSync as b}from"child_process";import{homedir as X}from"os";var Mo=e=>{let o=e.match(/^https:\/\/github\.com\/([^/]+)\/([^/]+)\/tree\/([^/]+)\/(.+)$/);if(o){let[,s,r,i,a]=o;return{type:"git-tree",repoUrl:`https://github.com/${s}/${r}.git`,branch:i,path:a,original:e}}let t=e.match(/^git@github\.com:([^/]+)\/([^/]+?)(?:\.git)?$/);if(t){let[,s,r]=t;return{type:"git-repo",repoUrl:`git@github.com:${s}/${r}.git`,branch:null,path:null,original:e}}let l=e.match(/^https:\/\/github\.com\/([^/]+)\/([^/]+)(?:\.git)?$/);if(l){let[,s,r]=l;return{type:"git-repo",repoUrl:`https://github.com/${s}/${r}.git`,branch:null,path:null,original:e}}return U(e)?{type:"local",path:e,original:e}:null},Wo=e=>{let o=e.includes("--global")||e.includes("-g"),t=e.includes("--project")||e.includes("-p");if(o)return $(X(),".agents","skills");if(t)return $(process.cwd(),".agents","skills");let l=$(process.cwd(),".agents","config.json"),s=$(X(),".agents","config.json");for(let r of[l,s])if(U(r))try{if(JSON.parse(No(r,"utf8")).installLocation==="project")return $(process.cwd(),".agents","skills")}catch{}return $(X(),".agents","skills")},_o=(e,o)=>{let t=$(X(),".agents","tmp",`skill-install-${Date.now()}`);try{return b(`mkdir -p "${t}"`,{stdio:"pipe"}),o?b(`git clone --branch ${o} --depth 1 "${e}" "${t}"`,{stdio:"pipe",timeout:3e4}):b(`git clone --depth 1 "${e}" "${t}"`,{stdio:"pipe",timeout:3e4}),t}catch(l){try{b(`rm -rf "${t}"`,{stdio:"pipe"})}catch{}throw new Error(`Failed to clone repository: ${l.message}`)}},De=e=>{let o=$(e,"skill.json");if(!U(o))return null;try{let t=No(o,"utf8");return JSON.parse(t)}catch(t){throw new Error(`Failed to read skill.json: ${t.message}`)}},Re=(e,o,t,l)=>{let s=$(e,o);if(!U(s)){l.errors.push(`Skill directory not found: ${o}`);return}let r=De(s);if(!r){l.errors.push(`No skill.json found in: ${o}`);return}let i=r.name||o,a=$(t,i),d=_e(a);try{b(`mkdir -p "${d}"`,{stdio:"pipe"})}catch(c){l.errors.push(`Failed to create directory ${d}: ${c.message}`);return}try{U(a)&&b(`rm -rf "${a}"`,{stdio:"pipe"}),b(`cp -R "${s}" "${a}"`,{stdio:"pipe"}),l.installed.push({name:i,path:a,title:r.title||r.description||i})}catch(c){l.errors.push(`Failed to install ${o}: ${c.message}`)}},Ho=()=>{let e=process.argv.slice(3),o=e.find(c=>!c.startsWith("-")),t=e.filter(c=>c.startsWith("-"));if(!o){console.log(`Usage: superpowers-agent add <url-or-path|@alias> [skill-path] [options]

Options:
  --global, -g   Install skills globally in ~/.agents/skills/ (default)
  --project, -p  Install skills in current project's .agents/skills/

Examples:
  superpowers-agent add https://github.com/example/skills
  superpowers-agent add https://github.com/example/repo/tree/main/skills
  superpowers-agent add ~/my-local-skills
  superpowers-agent add https://github.com/example/skills --project
  superpowers-agent add ~/my-local-skills --global
  superpowers-agent add @myrepo path/to/skill
  superpowers-agent add @myrepo path/to/skills --project

Description:
  Installs skill(s) from a Git repository, local directory, or repository alias.
  Supports repositories with single or multiple skills.
  Reads skill.json to determine skill names and installation paths.
  
  Repository aliases can be added using:
    superpowers-agent add-repository <git-url> [--as=@alias]`);return}console.log(`Installing skill(s)...
`);let l=null,s=null;if(o.startsWith("@")){let c=o;s=e.find((f,w)=>w>0&&!f.startsWith("-")&&f!==c);let p=P(!1),u=P(!0);if(p[c])l=p[c],console.log(`Using project repository alias: ${c}`);else if(u[c])l=u[c],console.log(`Using global repository alias: ${c}`);else{console.log(`Error: Repository alias not found: ${c}`),console.log(`
Available aliases:`);let f={...u,...p};if(Object.keys(f).length===0)console.log("  (none)"),console.log(`
Add a repository using:`),console.log("  superpowers-agent add-repository <git-url>");else for(let[w,S]of Object.entries(f))console.log(`  ${w} -> ${S}`);return}let k=U(l);s?k?(o=$(l,s),console.log(`Repository path: ${l}`),console.log(`Skill path: ${s}
`)):(o=`${l}/tree/main/${s}`,console.log(`Repository URL: ${l}`),console.log(`Skill path: ${s}
`)):(o=l,console.log(k?`Repository path: ${l}
`:`Repository URL: ${l}
`))}let r=Mo(o);if(!r){console.log(`Error: Invalid URL or path not found: ${o}`);return}let i=Wo(t);console.log(`Install location: ${i}
`);let a,d=!1;try{if(r.type==="git-repo"||r.type==="git-tree"){if(console.log(`Cloning repository: ${r.repoUrl}`),r.branch&&console.log(`Branch: ${r.branch}`),a=_o(r.repoUrl,r.branch),d=!0,r.path&&(a=$(a,r.path),!U(a)))throw new Error(`Path not found in repository: ${r.path}`);console.log("")}else a=r.path;let c=De(a);if(!c)throw new Error("No skill.json found in source directory");let p={installed:[],errors:[],source:r.original};if(c.skills&&Array.isArray(c.skills)){console.log(`Found ${c.skills.length} skill(s) to install
`);for(let u of c.skills)Re(a,u,i,p)}else{let u=c.name||je(a).base;Re(_e(a),je(a).base,i,p)}if(d&&a)try{let u=a.split("/.agents/tmp/")[0]+"/.agents/tmp/"+a.split("/.agents/tmp/")[1].split("/")[0];b(`rm -rf "${u}"`,{stdio:"pipe"})}catch{}if(console.log(`
**Successfully installed skills:**`),console.log(`- Source: ${p.source}`),p.installed.length>0)for(let u of p.installed)console.log(`  - Installed: ${u.name} at ${u.path}`),console.log(`    ${u.title}`);if(p.errors.length>0){console.log(`
**Errors:**`);for(let u of p.errors)console.log(`  - ${u}`)}p.installed.length===0&&p.errors.length===0&&console.log("  No skills were installed"),p.installed.length>0&&(console.log(`
**Syncing skill symlinks...**`),Me())}catch(c){if(d&&a)try{let p=a.split("/.agents/tmp/")[0]+"/.agents/tmp/"+a.split("/.agents/tmp/")[1].split("/")[0];b(`rm -rf "${p}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${c.message}`)}},Jo=()=>{let e=process.argv.slice(3),o=e.find(c=>!c.startsWith("-")),t=e.filter(c=>c.startsWith("-"));if(!o){console.log(`Usage: superpowers-agent pull <url-or-path|@alias> [skill-path] [options]

Options:
  --global, -g   Update skills globally in ~/.agents/skills/ (default)
  --project, -p  Update skills in current project's .agents/skills/

Examples:
  superpowers-agent pull https://github.com/example/skills
  superpowers-agent pull https://github.com/example/repo/tree/main/skills
  superpowers-agent pull ~/my-local-skills
  superpowers-agent pull https://github.com/example/skills --project
  superpowers-agent pull ~/my-local-skills --global
  superpowers-agent pull @myrepo
  superpowers-agent pull @myrepo path/to/skill
  superpowers-agent pull @myrepo path/to/skills --project

Description:
  Updates or adds skill(s) from a Git repository, local directory, or repository alias.
  If skills already exist, they will be updated (replaced).
  If skills don't exist, they will be added.
  Supports repositories with single or multiple skills.
  Reads skill.json to determine skill names and installation paths.
  
  Repository aliases can be added using:
    superpowers-agent add-repository <git-url> [--as=@alias]`);return}console.log(`Updating skill(s)...
`);let l=null,s=null;if(o.startsWith("@")){let c=o;s=e.find((f,w)=>w>0&&!f.startsWith("-")&&f!==c);let p=P(!1),u=P(!0);if(p[c])l=p[c],console.log(`Using project repository alias: ${c}`);else if(u[c])l=u[c],console.log(`Using global repository alias: ${c}`);else{console.log(`Error: Repository alias not found: ${c}`),console.log(`
Available aliases:`);let f={...u,...p};if(Object.keys(f).length===0)console.log("  (none)"),console.log(`
Add a repository using:`),console.log("  superpowers-agent add-repository <git-url>");else for(let[w,S]of Object.entries(f))console.log(`  ${w} -> ${S}`);return}let k=U(l);s?k?(o=$(l,s),console.log(`Repository path: ${l}`),console.log(`Skill path: ${s}
`)):(o=`${l}/tree/main/${s}`,console.log(`Repository URL: ${l}`),console.log(`Skill path: ${s}
`)):(o=l,console.log(k?`Repository path: ${l}
`:`Repository URL: ${l}
`))}let r=Mo(o);if(!r){console.log(`Error: Invalid URL or path not found: ${o}`);return}let i=Wo(t);console.log(`Install location: ${i}
`);let a,d=!1;try{if(r.type==="git-repo"||r.type==="git-tree"){if(console.log(`Cloning repository: ${r.repoUrl}`),r.branch&&console.log(`Branch: ${r.branch}`),a=_o(r.repoUrl,r.branch),d=!0,r.path&&(a=$(a,r.path),!U(a)))throw new Error(`Path not found in repository: ${r.path}`);console.log("")}else a=r.path;let c=De(a);if(!c)throw new Error("No skill.json found in source directory");let p={installed:[],errors:[],source:r.original};if(c.skills&&Array.isArray(c.skills)){console.log(`Found ${c.skills.length} skill(s) to update
`);for(let u of c.skills)Re(a,u,i,p)}else{let u=c.name||je(a).base;Re(_e(a),je(a).base,i,p)}if(d&&a)try{let u=a.split("/.agents/tmp/")[0]+"/.agents/tmp/"+a.split("/.agents/tmp/")[1].split("/")[0];b(`rm -rf "${u}"`,{stdio:"pipe"})}catch{}if(console.log(`
**Successfully updated skills:**`),console.log(`- Source: ${p.source}`),p.installed.length>0)for(let u of p.installed)console.log(`  - Updated: ${u.name} at ${u.path}`),console.log(`    ${u.title}`);if(p.errors.length>0){console.log(`
**Errors:**`);for(let u of p.errors)console.log(`  - ${u}`)}p.installed.length===0&&p.errors.length===0&&console.log("  No skills were updated"),p.installed.length>0&&(console.log(`
**Syncing skill symlinks...**`),Me())}catch(c){if(d&&a)try{let p=a.split("/.agents/tmp/")[0]+"/.agents/tmp/"+a.split("/.agents/tmp/")[1].split("/")[0];b(`rm -rf "${p}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${c.message}`)}},Bo=()=>{let e=process.argv.slice(3),o=e.find(c=>!c.startsWith("-")),t=e.filter(c=>c.startsWith("-"));if(!o){console.log(`Usage: superpowers-agent add-repository <git-url> [options]

Options:
  --global, -g        Add repository globally in ~/.agents/config.json (default)
  --project, -p       Add repository in current project's .agents/config.json
  --as=<alias>        Specify custom alias for the repository

Examples:
  superpowers-agent add-repository https://github.com/example/skills.git
  superpowers-agent add-repository https://github.com/example/skills.git --as=@myskills
  superpowers-agent add-repository https://github.com/example/skills.git --project
  superpowers-agent add-repository https://github.com/example/skills.git --as=@custom --global

Description:
  Adds a skill repository alias to your configuration.
  The repository's skill.json will be read to determine the default alias.
  Use --as to specify a custom alias.
  After adding, you can install skills using: superpowers-agent add @alias path/to/skill`);return}console.log(`Adding repository...
`);let l=t.some(c=>c==="--global"||c==="-g"),s=t.some(c=>c==="--project"||c==="-p"),r=t.find(c=>c.startsWith("--as=")),i=r?r.split("=")[1]:null,a=!s,d;try{console.log(`Cloning repository: ${o}`),d=$(X(),".agents","tmp",`repo-add-${Date.now()}`),b(`mkdir -p "${d}"`,{stdio:"pipe"}),b(`git clone --depth 1 "${o}" "${d}"`,{stdio:"pipe",timeout:3e4}),console.log("");let c=De(d);if(!c)throw new Error("No skill.json found in repository");let p;if(i)p=i;else if(c.repository)p=c.repository;else{let f=o.match(/\/([^/]+?)(?:\.git)?$/);if(f)p="@"+f[1];else throw new Error("Could not determine repository alias. Use --as=@alias to specify manually.")}to(p,o,a),b(`rm -rf "${d}"`,{stdio:"pipe"});let u=a?"globally":"in project",k=a?$(X(),".agents","config.json"):$(process.cwd(),".agents","config.json");console.log(`\u2713 Repository added ${u}`),console.log(`  Alias: ${p}`),console.log(`  URL: ${o}`),console.log(`  Config: ${k}`),console.log(`
You can now install skills using:`),console.log(`  superpowers-agent add ${p} <path-to-skill>`)}catch(c){if(d)try{b(`rm -rf "${d}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${c.message}`)}};import{dirname as Ws}from"path";var Vo=e=>{if(!e){console.log(`Usage: superpowers-agent use-skill <skill-name>

Examples (smart matching):
  superpowers-agent use-skill brainstorming              # Matches superpowers:collaboration/brainstorming
  superpowers-agent use-skill collaboration/brainstorming # More specific suffix
  superpowers-agent use-skill test-driven-development    # Matches superpowers:testing/test-driven-development

Examples (explicit paths):
  superpowers-agent use-skill superpowers:collaboration/brainstorming  # Full superpowers skill path
  superpowers-agent use-skill claude:email-assistant                   # Full claude skill path
  superpowers-agent use-skill my-custom-skill                          # Project skill

Smart matching:
  - Type just the skill name or any suffix of the path
  - Priority: .agents/skills/ > .claude/skills/ > ~/.agents/skills/ > ~/.agents/superpowers/skills/
  - If multiple skills match at same priority, you'll see them with descriptions`);return}let o=F(e);if(!o){console.log(`Error: Skill not found: ${e}

Available skills:`),console.log("Run: superpowers-agent find-skills");return}let{skillFile:t,sourceType:l,actualSkillPath:s}=o;try{let{content:r,frontmatter:i}=Ye(t),a=re[l].prefix+s,d=Ws(t),c=`# ${i.name||a}`;i.description&&(c+=`
# ${i.description}`),i.whenToUse&&(c+=`
# When to use: ${i.whenToUse}`),c+=`
# Supporting tools and docs are in ${d}
# ============================================

${r}`,console.log(c)}catch(r){console.log(r.message)}};var Ko={bootstrap:Go,version:To,"check-updates":()=>{Ao().catch(e=>{console.error(e.message),process.exit(1)})},update:()=>{let e=process.argv.includes("--no-reinstall");xe({skipReinstall:e})},"config-get":fo,"config-set":ho,"setup-skills":vo,"use-skill":()=>Vo(process.argv[3]),execute:po,"find-skills":M,add:Ho,"add-repository":Bo,"list-repositories":ko,pull:Jo,dir:mo,path:uo,"get-helpers":go,"install-copilot-prompts":_,"install-copilot-instructions":H,"install-cursor-commands":B,"install-cursor-hooks":V,"install-codex-prompts":K,"install-gemini-commands":Y,"install-claude-commands":z,"install-opencode-commands":q,"install-aliases":We,default:()=>{console.log(`Superpowers for Agents
Usage:
  superpowers-agent bootstrap [--no-update] [--force]               # Run complete bootstrap
  superpowers-agent version                                         # Show current version
  superpowers-agent check-updates                                   # Check for updates
  superpowers-agent update [--no-reinstall]                         # Update to latest version
  superpowers-agent config-get                                      # Show configuration
  superpowers-agent config-set <key> <value>                        # Update configuration
  superpowers-agent setup-skills                                    # Initialize project skills
  superpowers-agent use-skill <skill-name>                          # Load a skill
  superpowers-agent find-skills                                     # List available skills
  superpowers-agent add <url-or-path|@alias> [path] [options]      # Install skills
  superpowers-agent add-repository <git-url> [--as=@alias] [opts]  # Add repository alias
  superpowers-agent list-repositories                              # List configured repository aliases
  superpowers-agent pull <url-or-path|@alias> [path] [options]     # Update skills
  superpowers-agent dir <skill-name>                                # Get skill directory
  superpowers-agent path <skill-name>                               # Get skill file path
  superpowers-agent execute <skill-name>                            # Execute a skill
  superpowers-agent get-helpers <skill> <search-term>               # Get helper file path
  superpowers-agent install-copilot-prompts                         # Install GitHub Copilot prompts
  superpowers-agent install-copilot-instructions                    # Install universal instructions
  superpowers-agent install-cursor-commands                         # Install Cursor commands
  superpowers-agent install-cursor-hooks                            # Install Cursor hooks
  superpowers-agent install-codex-prompts                           # Install Codex prompts
  superpowers-agent install-gemini-commands                         # Install Gemini commands
  superpowers-agent install-claude-commands                         # Install Claude commands
  superpowers-agent install-opencode-commands                       # Install OpenCode commands
  superpowers-agent install-aliases                                 # Install universal aliases (superpowers, superpowers-agent)

Documentation: https://github.com/complexthings/superpowers
`)}},_s=process.argv[2]||"default",Hs=Ko[_s]||Ko.default;try{Hs()}catch(e){console.error(`Error: ${e.message}`),process.exit(1)}
