#!/bin/sh
// 2>/dev/null; exec "$(command -v bun 2>/dev/null || echo node)" "$0" "$@"
import{relative as zq,dirname as Kq,join as KQ}from"path";import{existsSync as S$}from"fs";import{homedir as l$,platform as lq}from"os";import{join as T,parse as aq,dirname as a$}from"path";import{existsSync as $$}from"fs";import{fileURLToPath as iq}from"url";var oq=iq(import.meta.url),e=a$(oq),rq=()=>{let $=l$();switch(lq()){case"darwin":return T($,"Library","Application Support","Code","User");case"win32":return T($,"AppData","Roaming","Code","User");case"linux":return T($,".config","Code","User");default:return T($,".config","Code","User")}},sq=()=>{let $=process.cwd(),Q=aq($).root;while($!==Q){if($$(T($,".agents")))return $;$=a$($)}return process.cwd()},H={home:l$(),vscodeUserDir:rq(),projectRoot:sq(),get projectClaudeSkills(){return T(this.projectRoot,".claude","skills")},get projectAgentsSkills(){return T(this.projectRoot,".agents","skills")},get projectCopilotSkills(){return T(this.projectRoot,".copilot","skills")},get projectOpencodeSkills(){return T(this.projectRoot,".opencode","skill")},get projectCursorSkills(){return T(this.projectRoot,".cursor","skills")},get projectGeminiSkills(){return T(this.projectRoot,".gemini","skills")},get projectCodexSkills(){return T(this.projectRoot,".codex","skills")},get projectSkillsDir(){return T(this.projectRoot,"skills")},get homePersonalSkills(){return T(this.home,".agents","skills")},get homeClaudeSkills(){return T(this.home,".claude","skills")},get homeCopilotSkills(){return T(this.home,".copilot","skills")},get homeOpencodeSkills(){return T(this.home,".config","opencode","skill")},get homeCursorSkills(){return T(this.home,".cursor","skills")},get homeGeminiSkills(){return T(this.home,".gemini","skills")},get homeCodexSkills(){return T(this.home,".codex","skills")},get bootstrap(){return T(this.projectRoot,".agents","superpowers-bootstrap.md")},get superpowersRepo(){let $=T(e,"..","..","..","skills");if($$($)&&$$(T(e,"..","..","..",".github","prompts")))return T(e,"..","..","..");return T(this.home,".agents","superpowers")},get homeSuperpowersSkills(){return T(this.superpowersRepo,"skills")},get isSuperpowersRepo(){let $=T(e,"..","..","..","skills");return $$($)&&$$(T(e,"..","..","..",".github","prompts"))}};import{readFileSync as i$}from"fs";var q$=($)=>{try{let q=i$($,"utf8").split(`
`),X={name:"",description:"",whenToUse:""},Z=!1;for(let Y of q){if(Y.trim()==="---"){if(Z)break;Z=!0;continue}if(Z){let W=Y.match(/^(\w+):\s*(.*)$/);if(W){let[,E,G]=W,z={name:"name",description:"description",when_to_use:"whenToUse"};if(z[E])X[z[E]]=G.trim()}}}return X}catch{return{name:"",description:"",whenToUse:""}}},o$=($)=>{try{let Q=i$($,"utf8"),q=q$($),X=Q.split(`
`),Z=!1,Y=!1,W=[];for(let E of X){if(E.trim()==="---"){if(Z){Y=!0;continue}Z=!0;continue}if(Y||!Z)W.push(E)}return{content:W.join(`
`).trim(),frontmatter:q}}catch(Q){throw Error(`Error reading skill file: ${Q.message}`)}};import{existsSync as F$,readdirSync as t$,statSync as e$}from"fs";import{join as x$,relative as LX}from"path";import{readFileSync as tq,existsSync as eq}from"fs";import{join as r$}from"path";var Q$=($)=>{let Q=r$($,"skill.json");if(!eq(Q))return null;try{let q=tq(Q,"utf8");return JSON.parse(q)}catch(q){return null}};var C$=($)=>{if($.startsWith("superpowers:"))return{type:"superpowers",path:$.substring(12).replace(/^skills\//,"")};if($.startsWith("claude:"))return{type:"claude",path:$.substring(7).replace(/^skills\//,"")};if($.startsWith("copilot:"))return{type:"copilot",path:$.substring(8).replace(/^skills\//,"")};if($.startsWith("opencode:"))return{type:"opencode",path:$.substring(9).replace(/^skills\//,"")};if($.startsWith("cursor:"))return{type:"cursor",path:$.substring(7).replace(/^skills\//,"")};if($.startsWith("gemini:"))return{type:"gemini",path:$.substring(7).replace(/^skills\//,"")};return{type:null,path:$.replace(/^skills\//,"")}},s$=($,Q)=>{let q=Q$($);if(!q||!q.helpers||!Array.isArray(q.helpers))return null;let X=q.helpers,Z=Q.toLowerCase(),Y=null,W=0;for(let E of X){let G=E.toLowerCase();if(G===Z){Y=E;break}if(G.includes(Z)){let B=Z.length/G.length;if(B>W)W=B,Y=E}let z=E.split("/").pop().split(".")[0].toLowerCase();if(Z.includes(z)){let B=z.length/Z.length;if(B>W)W=B,Y=E}}return Y?r$($,Y):null},X$={project:{dir:"projectAgentsSkills",prefix:""},claude:{dir:"projectClaudeSkills",prefix:"claude:"},copilot:{dir:"projectCopilotSkills",prefix:"copilot:"},opencode:{dir:"projectOpencodeSkills",prefix:"opencode:"},cursor:{dir:"projectCursorSkills",prefix:"cursor:"},gemini:{dir:"projectGeminiSkills",prefix:"gemini:"},personal:{dir:"homePersonalSkills",prefix:""},personalClaude:{dir:"homeClaudeSkills",prefix:"claude:"},personalCopilot:{dir:"homeCopilotSkills",prefix:"copilot:"},personalOpencode:{dir:"homeOpencodeSkills",prefix:"opencode:"},personalCursor:{dir:"homeCursorSkills",prefix:"cursor:"},personalGemini:{dir:"homeGeminiSkills",prefix:"gemini:"},superpowers:{dir:"homeSuperpowersSkills",prefix:"superpowers:"}};var V$=($,Q,q=null)=>{let X=[];if(!F$($))return X;let Z=(Y,W)=>{if(q!==null&&W>q)return;try{let E=t$(Y,{withFileTypes:!0});for(let G of E){if(G.name==="node_modules"||G.name===".git"||G.name.startsWith("."))continue;let z=x$(Y,G.name),B=G.isDirectory();if(G.isSymbolicLink())try{B=e$(z).isDirectory()}catch{continue}if(!B)continue;let V=x$(z,"SKILL.md");if(F$(V))X.push(z);if(q===null||W<q)Z(z,W+1)}}catch{}};return Z($,0),X};var $q=($,Q)=>{if(!F$($))return[];let q=[],X=Q.toLowerCase().replace(/\\/g,"/"),Z=(Y,W="")=>{try{let E=t$(Y,{withFileTypes:!0});for(let G of E){let z=x$(Y,G.name),B=W?`${W}/${G.name}`:G.name,V=G.isDirectory();if(G.isSymbolicLink())try{V=e$(z).isDirectory()}catch{continue}if(V&&(G.name==="node_modules"||G.name===".git"||G.name.startsWith(".")))continue;if(V)Z(z,B);else if(G.name==="SKILL.md"){let L=W.toLowerCase().replace(/\\/g,"/");if(L===X||L.endsWith("/"+X)||L.endsWith(X))q.push({file:z,path:W})}}}catch(E){}};return Z($),q},qq=($,Q,q)=>{let X={project:"project skills (.agents/skills/)",claude:"claude skills (.claude/skills/)",copilot:"copilot skills (.copilot/skills/)",opencode:"opencode skills (.opencode/skill/)",cursor:"cursor skills (.cursor/skills/)",gemini:"gemini skills (.gemini/skills/)",personal:"personal skills (~/.agents/skills/)",personalClaude:"personal claude skills (~/.claude/skills/)",personalCopilot:"personal copilot skills (~/.copilot/skills/)",personalOpencode:"personal opencode skills (~/.config/opencode/skill/)",personalCursor:"personal cursor skills (~/.cursor/skills/)",personalGemini:"personal gemini skills (~/.gemini/skills/)",superpowers:"superpowers skills (~/.agents/superpowers/skills/)"}[q];console.log(`Error: Multiple skills match "${$}" in ${X}:
`);for(let Z of Q){let W=X$[q].prefix+Z.path;try{let E=q$(Z.file),G=E.description||E.whenToUse||"(no description)";console.log(`  ${W}`),console.log(`    ${G}
`)}catch(E){console.log(`  ${W}`),console.log(`    (unable to read description)
`)}}console.log("Please be more specific. Examples:");for(let Z of Q.slice(0,2)){let Y=X$[q].prefix;console.log(`  superpowers-agent execute ${Y}${Z.path}`)}};import{existsSync as R$}from"fs";import{join as $Q,relative as qQ}from"path";var QQ=($)=>{let{type:Q,path:q}=C$($),X={superpowers:[H.homeSuperpowersSkills],claude:[H.projectClaudeSkills,H.homeClaudeSkills],copilot:[H.projectCopilotSkills,H.homeCopilotSkills],opencode:[H.projectOpencodeSkills,H.homeOpencodeSkills],gemini:[H.projectGeminiSkills,H.homeGeminiSkills],project:[H.projectAgentsSkills],personal:[H.homePersonalSkills]},Z;if(Q)Z=(X[Q]||[]).map((W)=>({type:Q,dir:W}));else if(H.isSuperpowersRepo&&R$(H.projectSkillsDir))Z=[{type:"project",dir:H.projectSkillsDir},{type:"project",dir:H.projectAgentsSkills},{type:"claude",dir:H.projectClaudeSkills},{type:"copilot",dir:H.projectCopilotSkills},{type:"opencode",dir:H.projectOpencodeSkills},{type:"gemini",dir:H.projectGeminiSkills},{type:"personal",dir:H.homePersonalSkills},{type:"personalClaude",dir:H.homeClaudeSkills},{type:"personalCopilot",dir:H.homeCopilotSkills},{type:"personalOpencode",dir:H.homeOpencodeSkills},{type:"personalGemini",dir:H.homeGeminiSkills},{type:"superpowers",dir:H.homeSuperpowersSkills}];else Z=[{type:"project",dir:H.projectAgentsSkills},{type:"claude",dir:H.projectClaudeSkills},{type:"copilot",dir:H.projectCopilotSkills},{type:"opencode",dir:H.projectOpencodeSkills},{type:"gemini",dir:H.projectGeminiSkills},{type:"personal",dir:H.homePersonalSkills},{type:"personalClaude",dir:H.homeClaudeSkills},{type:"personalCopilot",dir:H.homeCopilotSkills},{type:"personalOpencode",dir:H.homeOpencodeSkills},{type:"personalGemini",dir:H.homeGeminiSkills},{type:"superpowers",dir:H.homeSuperpowersSkills}];for(let{type:Y,dir:W}of Z){let E=$q(W,q);if(E.length===1)return{skillFile:E[0].file,sourceType:Y,actualSkillPath:E[0].path};if(E.length>1)qq(q,E,Y)}return null},d=($)=>{let Q=QQ($);if(Q)return Q;let{type:q,path:X}=C$($),Z={superpowers:[H.homeSuperpowersSkills],claude:[H.projectClaudeSkills,H.homeClaudeSkills],copilot:[H.projectCopilotSkills,H.homeCopilotSkills],opencode:[H.projectOpencodeSkills,H.homeOpencodeSkills],gemini:[H.projectGeminiSkills,H.homeGeminiSkills],project:[H.projectAgentsSkills],personal:[H.homePersonalSkills]},Y;if(q)Y=(Z[q]||[]).map((E)=>({type:q,dir:E}));else if(H.isSuperpowersRepo&&R$(H.projectSkillsDir))Y=[{type:"project",dir:H.projectSkillsDir},{type:"project",dir:H.projectAgentsSkills},{type:"claude",dir:H.projectClaudeSkills},{type:"copilot",dir:H.projectCopilotSkills},{type:"opencode",dir:H.projectOpencodeSkills},{type:"gemini",dir:H.projectGeminiSkills},{type:"personal",dir:H.homePersonalSkills},{type:"personalClaude",dir:H.homeClaudeSkills},{type:"personalCopilot",dir:H.homeCopilotSkills},{type:"personalOpencode",dir:H.homeOpencodeSkills},{type:"personalGemini",dir:H.homeGeminiSkills},{type:"superpowers",dir:H.homeSuperpowersSkills}];else Y=[{type:"project",dir:H.projectAgentsSkills},{type:"claude",dir:H.projectClaudeSkills},{type:"copilot",dir:H.projectCopilotSkills},{type:"opencode",dir:H.projectOpencodeSkills},{type:"gemini",dir:H.projectGeminiSkills},{type:"personal",dir:H.homePersonalSkills},{type:"personalClaude",dir:H.homeClaudeSkills},{type:"personalCopilot",dir:H.homeCopilotSkills},{type:"personalOpencode",dir:H.homeOpencodeSkills},{type:"personalGemini",dir:H.homeGeminiSkills},{type:"superpowers",dir:H.homeSuperpowersSkills}];for(let{type:W,dir:E}of Y){if(!R$(E))continue;let G=V$(E,W,null);for(let z of G){let B=Q$(z);if(B&&B.aliases&&Array.isArray(B.aliases)){let V=X.toLowerCase();for(let L of B.aliases)if(L.toLowerCase()===V){let K=$Q(z,"SKILL.md"),O=qQ(E,z);return{skillFile:K,sourceType:W,actualSkillPath:O}}}}}return null};import{readFileSync as Qq,writeFileSync as Xq,existsSync as j$}from"fs";import{join as m}from"path";import{execSync as XQ}from"child_process";var D$=()=>({auto_update:!0,last_update_check:null,last_updated_commit:null}),Y$=()=>{let $=m(H.superpowersRepo,".config.json");try{if(!j$($))return D$();let Q=Qq($,"utf8");return{...D$(),...JSON.parse(Q)}}catch{return D$()}},_$=($)=>{let Q=m(H.superpowersRepo,".config.json"),X={...Y$(),...$};try{Xq(Q,JSON.stringify(X,null,2))}catch(Z){console.log(`Warning: couldn't save config: ${Z.message}`)}},h=($)=>{let Q=$?m(H.home,".agents","config.json"):m(process.cwd(),".agents","config.json");if(!j$(Q))return{};try{let q=Qq(Q,"utf8");return JSON.parse(q)}catch(q){return{}}},l=($,Q)=>{let q=Q?m(H.home,".agents"):m(process.cwd(),".agents"),X=m(q,"config.json");try{if(!j$(q))XQ(`mkdir -p "${q}"`,{stdio:"pipe"});return Xq(X,JSON.stringify($,null,2),"utf8"),!0}catch(Z){throw Error(`Failed to write config: ${Z.message}`)}},f=($=!1)=>{return h($).repositories||{}},Yq=($,Q,q)=>{let X=h(q);if(!X.repositories)X.repositories={};X.repositories[$]=Q,l(X,q)};import{readFileSync as YQ}from"fs";import{join as Zq,dirname as ZQ}from"path";import{fileURLToPath as HQ}from"url";import{existsSync as WQ}from"fs";import{execSync as EQ}from"child_process";var zQ=HQ(import.meta.url),Hq=ZQ(zQ),g=()=>{try{let $=Zq(Hq,"package.json");if(!WQ($))$=Zq(Hq,"..","..","package.json");return JSON.parse(YQ($,"utf8")).version||"5.4.0"}catch($){return"5.4.0"}},R=()=>{let $=g();console.log(`^^SAV:${$}^^`)},Wq=async()=>{try{let Q=EQ('curl -sS "https://raw.githubusercontent.com/complexthings/superpowers/main/.agents/package.json"',{encoding:"utf8",timeout:1e4}),q=JSON.parse(Q);if(!q.version)return g();return q.version}catch($){throw Error(`Failed to fetch remote version: ${$.message}`)}},Eq=($,Q)=>{let q=$.split(".").map(Number),X=Q.split(".").map(Number);for(let Z=0;Z<3;Z++){let Y=q[Z]||0,W=X[Z]||0;if(Y>W)return!0;if(Y<W)return!1}return!1};var BQ={project:{dir:"projectAgentsSkills",prefix:""},claude:{dir:"projectClaudeSkills",prefix:"claude:"},copilot:{dir:"projectCopilotSkills",prefix:"copilot:"},opencode:{dir:"projectOpencodeSkills",prefix:"opencode:"},gemini:{dir:"projectGeminiSkills",prefix:"gemini:"},personal:{dir:"homePersonalSkills",prefix:""},personalClaude:{dir:"homeClaudeSkills",prefix:"claude:"},personalCopilot:{dir:"homeCopilotSkills",prefix:"copilot:"},personalOpencode:{dir:"homeOpencodeSkills",prefix:"opencode:"},personalGemini:{dir:"homeGeminiSkills",prefix:"gemini:"},superpowers:{dir:"homeSuperpowersSkills",prefix:"superpowers:"}},GQ=($,Q)=>{let q=KQ($,"SKILL.md"),{dir:X,prefix:Z}=BQ[Q],Y=zq(H[X],$).replace(/\\/g,"/");console.log(`${Z}${Y}`);let{description:W,whenToUse:E}=q$(q);if(W)console.log(`  ${W}`);if(E)console.log(`  When to use: ${E}`);console.log("")},a=()=>{R();let $=new Set,Q=[{type:"project",dir:H.projectAgentsSkills,maxDepth:null},{type:"claude",dir:H.projectClaudeSkills,maxDepth:null},{type:"copilot",dir:H.projectCopilotSkills,maxDepth:null},{type:"opencode",dir:H.projectOpencodeSkills,maxDepth:null},{type:"gemini",dir:H.projectGeminiSkills,maxDepth:null},{type:"personal",dir:H.homePersonalSkills,maxDepth:null},{type:"personalClaude",dir:H.homeClaudeSkills,maxDepth:null},{type:"personalCopilot",dir:H.homeCopilotSkills,maxDepth:null},{type:"personalOpencode",dir:H.homeOpencodeSkills,maxDepth:null},{type:"personalGemini",dir:H.homeGeminiSkills,maxDepth:null},{type:"superpowers",dir:H.homeSuperpowersSkills,maxDepth:null}];for(let{type:q,dir:X,maxDepth:Z}of Q){let Y=V$(X,q,Z);for(let W of Y){let E=zq(X,W);if(!$.has(E))$.add(E),GQ(W,q)}}console.log(`Usage:
  superpowers-agent execute <skill-name> # Load a specific skill`)},Bq=()=>{R();let $=process.argv[3];if(!$){console.log(`Usage: superpowers-agent execute <skill-name-or-alias>

Examples:
  superpowers-agent execute brainstorming
  superpowers-agent execute superpowers:collaboration/brainstorming
  superpowers-agent execute aem/block-collection-and-party
  superpowers-agent execute block-collection  # Using alias

Description:
  Instructs the agent to execute the specified skill.
  Supports skill aliases defined in skill.json files.`);return}let Q=d($);if(!Q){console.log(`Error: Skill not found: ${$}

Available skills:`),a();return}let{skillFile:q,sourceType:X}=Q,Z=process.cwd(),Y=q.startsWith(Z),W=S$(H.projectAgentsSkills)||S$(H.projectClaudeSkills);if(Y)console.log(`Open this file with your Read Tool: ${q}

<IMPORTANT>
USE YOUR Read Tool to process this file. DO NOT use \`cat\` or print commands.
</IMPORTANT>`);else if(W&&X!=="project")console.log(`Open this file with your Read Tool: ${q}

<IMPORTANT>
1. Try using your Read Tool first
2. If your Read tool fails (file outside workspace), use: cat "${q}"
3. Consider using workspace-local skills for better tool compatibility
   Run: superpowers-agent setup-skills
</IMPORTANT>`);else console.log(`Open this file with your Read Tool: ${q}

<IMPORTANT>
1. Try using your Read Tool first
2. If your Read tool fails (file outside workspace), use: cat "${q}"
3. NEVER skip loading the skill content
</IMPORTANT>`)},Gq=()=>{let $=process.argv[3];if(!$){console.log(`Usage: superpowers-agent path <skill-name-or-alias>

Examples:
  superpowers-agent path brainstorming
  superpowers-agent path superpowers:collaboration/brainstorming
  superpowers-agent path aem/block-collection-and-party
  superpowers-agent path block-collection  # Using alias

Description:
  Outputs the file system path of the specified SKILL.md file.
  Supports skill aliases defined in skill.json files.`);return}let Q=d($);if(!Q){console.log(`Error: Skill not found: ${$}

Available skills:`),a();return}let{skillFile:q}=Q;console.log(q)},Vq=()=>{let $=process.argv[3];if(!$){console.log(`Usage: superpowers-agent dir <skill-name>

Examples:
  superpowers-agent dir brainstorming
  superpowers-agent dir aem/block-collection-and-party
  superpowers-agent dir block-collection  # Using alias
  superpowers-agent dir superpowers:testing/test-driven-development

Description:
  Returns the directory path where the specified skill is located.
  Supports skill aliases defined in skill.json files.`);return}let Q=d($);if(!Q){console.log(`Error: Skill not found: ${$}

Available skills:`),a();return}let{skillFile:q}=Q,X=Kq(q);console.log(X)},_q=()=>{R();let $=process.argv[3],Q=process.argv[4];if(!$||!Q){console.log(`Usage: superpowers-agent get-helpers <skill-name> <helper-search-term>

Examples:
  superpowers-agent get-helpers block-collection search-block
  superpowers-agent get-helpers aem/block-collection-and-party get-block-structure
  superpowers-agent get-helpers superpowers:testing/test-driven-development example

Description:
  Searches for helper files within a skill's skill.json configuration.
  Returns the full path to the best matching helper file.
  
  The command uses the skill's aliases if available, so you can use
  short names like "block-collection" instead of the full path.`);return}let q=d($);if(!q){console.log(`Error: Skill not found: ${$}

Available skills:`),a();return}let{skillFile:X}=q,Z=Kq(X),Y=Q$(Z);if(!Y){console.log(`Error: No skill.json found for skill: ${$}
  Location: ${Z}`);return}if(!Y.helpers||!Array.isArray(Y.helpers)||Y.helpers.length===0){console.log(`Error: No helpers defined in skill.json for skill: ${$}
  Location: ${Z}`);return}let W=s$(Z,Q);if(!W){console.log(`Error: No helper found matching "${Q}" in skill: ${$}`),console.log(`
Available helpers:`);for(let E of Y.helpers)console.log(`  - ${E}`);return}if(!S$(W)){console.log(`Error: Helper file not found: ${W}
  Defined in skill.json but missing from filesystem`);return}console.log(W)},Lq=()=>{let $=Y$();console.log("Current configuration:"),console.log(JSON.stringify($,null,2))},Oq=()=>{let $=process.argv[3],Q=process.argv[4];if(!$||Q===void 0){console.log(`Usage: .agents/superpowers-agent config-set <key> <value>

Available keys:
  auto_update (true/false) - Enable/disable automatic updates during bootstrap

Examples:
  .agents/superpowers-agent config-set auto_update false
  .agents/superpowers-agent config-set auto_update true`);return}let q=Q;if(Q==="true")q=!0;else if(Q==="false")q=!1;let X={};X[$]=q,_$(X),console.log(`✓ Set ${$} = ${q}`)},Uq=()=>{R();let $=f(!0),Q=f(!1),q=[];for(let[Y,W]of Object.entries($))q.push({alias:Y,url:W,source:"global"});for(let[Y,W]of Object.entries(Q)){let E=q.findIndex((G)=>G.alias===Y);if(E>=0)q[E]={alias:Y,url:W,source:"project"};else q.push({alias:Y,url:W,source:"project"})}if(q.length===0){console.log(`No repository aliases configured.

Add a repository using:
  superpowers-agent add-repository <git-url> [--as=@alias]`);return}let X=Math.max(5,...q.map((Y)=>Y.alias.length)),Z=Math.max(3,...q.map((Y)=>Y.url.length));console.log(`Repositories:
`),console.log(`${"Alias".padEnd(X)}  ${"URL".padEnd(Z)}  Source`);for(let Y of q)console.log(`${Y.alias.padEnd(X)}  ${Y.url.padEnd(Z)}  (${Y.source})`);console.log(`
Total: ${q.length} repository alias(es)`)};import{execSync as RQ}from"child_process";import{execSync as p}from"child_process";var VQ=()=>{try{return p("git status --porcelain --untracked-files=no",{cwd:H.superpowersRepo,encoding:"utf8",stdio:"pipe",timeout:3000}).trim().length===0}catch{return!1}},Z$=()=>{try{return p("git rev-parse --abbrev-ref HEAD",{cwd:H.superpowersRepo,encoding:"utf8",stdio:"pipe",timeout:3000}).trim()==="main"}catch{return!1}},L$=()=>{try{p("git fetch origin",{cwd:H.superpowersRepo,timeout:5000,stdio:"pipe"});let $=!VQ(),Q=p("git rev-parse HEAD",{cwd:H.superpowersRepo,encoding:"utf8",stdio:"pipe"}).trim(),q=p("git rev-parse origin/main",{cwd:H.superpowersRepo,encoding:"utf8",stdio:"pipe"}).trim();if(Q===q)return{hasUpdates:!1,hasLocalChanges:$,currentCommit:Q,latestCommit:q,commitsBehind:0,changedFiles:[]};let Z=parseInt(p("git rev-list --count HEAD..origin/main",{cwd:H.superpowersRepo,encoding:"utf8",stdio:"pipe"}).trim(),10),W=p("git diff --name-only HEAD origin/main",{cwd:H.superpowersRepo,encoding:"utf8",stdio:"pipe"}).trim().split(`
`).filter((E)=>E.length>0);return{hasUpdates:!0,hasLocalChanges:$,currentCommit:Q,latestCommit:q,commitsBehind:Z,changedFiles:W}}catch{return{hasUpdates:!1,hasLocalChanges:!1,currentCommit:"",latestCommit:"",commitsBehind:0,changedFiles:[],error:!0}}},Iq=($)=>{let Q={".github/prompts/":"copilot-prompts",".cursor/commands/":"cursor-commands","hooks/cursor/":"cursor-hooks",".codex/prompts/":"codex-prompts",".gemini/commands/":"gemini-commands","commands/":"claude-commands",".opencode/command/":"opencode-commands",".opencode/plugins/":"opencode-plugin"},q=new Set;for(let X of $)for(let[Z,Y]of Object.entries(Q))if(X.startsWith(Z))q.add(Y);return Array.from(q)};import{existsSync as vq,readdirSync as _Q}from"fs";import{join as i}from"path";import{execSync as O$}from"child_process";var o=()=>{let $=i(H.superpowersRepo,"hooks","cursor"),Q=i(H.home,".cursor","hooks"),q=i($,"hooks.json"),X=i(H.home,".cursor","hooks.json");if(!vq($)){console.log("⚠️  No Cursor hooks to install (source directory not found).");return}try{if(!vq(Q))O$(`mkdir -p "${Q}"`,{stdio:"pipe"})}catch(W){console.log(`Error creating Cursor hooks directory: ${W.message}`);return}try{O$(`cp "${q}" "${X}"`,{stdio:"pipe"}),console.log("  ✓ Installed hooks.json")}catch(W){console.log(`  ✗ Failed to install hooks.json: ${W.message}`);return}let Z;try{Z=_Q($).filter((W)=>W.endsWith(".sh"))}catch(W){console.log(`Error reading hooks directory: ${W.message}`);return}if(Z.length===0){console.log("⚠️  No hook scripts found to install.");return}console.log("Installing Cursor hooks...");let Y=0;for(let W of Z)try{let E=i($,W),G=i(Q,W);O$(`cp "${E}" "${G}"`,{stdio:"pipe"}),O$(`chmod +x "${G}"`,{stdio:"pipe"}),console.log(`  ✓ Installed ${W}`),Y++}catch(E){console.log(`  ✗ Failed to install ${W}: ${E.message}`)}if(Y>0)console.log(`
✓ Installed ${Y} hook(s) to ${Q}
  Cursor will now check for skills before each prompt submission
  Restart Cursor for hooks to take effect`)};import{existsSync as P$,readdirSync as QY,lstatSync as LQ,symlinkSync as Jq,unlinkSync as OQ,mkdirSync as UQ,readlinkSync as IQ}from"fs";import{join as y$,dirname as YY}from"path";import{platform as vQ}from"os";var f$=($)=>{try{return LQ($).isSymbolicLink()}catch{return!1}},JQ=($,Q)=>{try{if(!f$($))return!1;return IQ($)===Q}catch{return!1}},H$=()=>{let $=y$(H.superpowersRepo,".opencode","plugins","superpowers-agent.js"),Q=y$(H.home,".config","opencode","plugins"),q=y$(Q,"superpowers-agent.js");if(console.log("Installing OpenCode plugin symlink..."),!P$($))return console.log("⚠️  Source plugin not found, skipping symlink creation"),console.log(`   Expected at: ${$}`),{created:!1,error:"Source plugin not found"};if(JQ(q,$))return console.log("✓ Plugin symlink already exists and is correct"),{created:!1,existed:!0};if(P$(q)&&!f$(q))return console.log("⚠️  Warning: A file already exists at the destination path"),console.log(`   Path: ${q}`),console.log("   Skipping symlink creation to avoid overwriting existing file"),console.log("   To use superpowers-agent plugin, manually remove or rename the existing file"),{created:!1,error:"File already exists at destination"};if(!P$(Q))try{UQ(Q,{recursive:!0}),console.log(`✓ Created ${Q.replace(H.home,"~")}`)}catch(Z){return console.log(`⚠️  Failed to create plugins directory: ${Z.message}`),{created:!1,error:`Failed to create directory: ${Z.message}`}}if(f$(q))try{OQ(q)}catch(Z){return console.log(`⚠️  Failed to remove existing symlink: ${Z.message}`),{created:!1,error:`Failed to remove existing symlink: ${Z.message}`}}let X=vQ();try{if(X==="win32")Jq($,q,"file");else Jq($,q);let Z=$.replace(H.home,"~"),Y=q.replace(H.home,"~");return console.log(`✓ Created symlink: ${Y}`),console.log(`  -> ${Z}`),{created:!0}}catch(Z){if(X==="win32"&&Z.code==="EPERM")return console.log("⚠️  Windows requires Developer Mode or admin privileges for symlinks"),console.log("   Enable Developer Mode: Settings > Update & Security > For developers"),{created:!1,error:"Windows symlink permission denied"};return console.log(`⚠️  Failed to create symlink: ${Z.message}`),{created:!1,error:`Failed to create symlink: ${Z.message}`}}};import{existsSync as b,readdirSync as wQ,lstatSync as TQ,symlinkSync as wq,unlinkSync as bQ,mkdirSync as W$,readlinkSync as AQ}from"fs";import{join as _,dirname as MQ}from"path";import{homedir as N,platform as NQ}from"os";var Tq=[{name:"claude",parentDir:()=>_(N(),".claude"),skillsDir:()=>_(N(),".claude","skills"),superpowersTarget:"superpowers"},{name:"copilot",parentDir:()=>_(N(),".copilot"),skillsDir:()=>_(N(),".copilot","skills"),superpowersTarget:"superpowers"},{name:"opencode",parentDir:()=>_(N(),".config","opencode"),skillsDir:()=>_(N(),".config","opencode","skill"),superpowersTarget:"superpowers"},{name:"cursor",parentDir:()=>_(N(),".cursor"),skillsDir:()=>_(N(),".cursor","skills"),superpowersTarget:"superpowers"},{name:"gemini",parentDir:()=>_(N(),".gemini"),skillsDir:()=>_(N(),".gemini","skills"),superpowersTarget:"superpowers"},{name:"codex",parentDir:()=>_(N(),".codex"),skillsDir:()=>_(N(),".codex","skills"),superpowersTarget:"superpowers"}],CQ=[{name:"claude",agentDir:($)=>_($,".claude"),skillsDir:($)=>_($,".claude","skills"),detect:($)=>b(_($,".claude"))},{name:"copilot",agentDir:($)=>_($,".github"),skillsDir:($)=>_($,".github","skills"),detect:($)=>{let Q=b(_($,".github")),q=b(_($,"AGENTS.md"))||b(_($,".agents","AGENTS.md"));return Q&&q}},{name:"opencode",agentDir:($)=>_($,".opencode"),skillsDir:($)=>_($,".opencode","skill"),detect:($)=>{let Q=b(_($,".opencode")),q=b(_($,"AGENTS.md"))||b(_($,".agents","AGENTS.md"))||b(_($,".opencode","AGENTS.md"));return Q&&q}},{name:"cursor",agentDir:($)=>_($,".cursor"),skillsDir:($)=>_($,".cursor","skills"),detect:($)=>b(_($,".cursor"))},{name:"gemini",agentDir:($)=>_($,".gemini"),skillsDir:($)=>_($,".gemini","skills"),detect:($)=>{let Q=b(_($,".gemini")),q=b(_($,"GEMINI.md"))||b(_($,".agents","GEMINI.md"));return Q&&q}},{name:"codex",agentDir:($)=>_($,".codex"),skillsDir:($)=>_($,".codex","skills"),detect:($)=>{let Q=b(_($,".codex")),q=b(_($,"AGENTS.md"))||b(_($,".agents","AGENTS.md"));return Q&&q}}],g$=($)=>{try{return TQ($).isSymbolicLink()}catch{return!1}},FQ=($,Q)=>{try{if(!g$($))return!1;return AQ($)===Q}catch{return!1}},E$=($,Q)=>{if(!b($))return{created:!1,existed:!1,error:`Source does not exist: ${$}`};if(FQ(Q,$))return{created:!1,existed:!0};if(b(Q)||g$(Q))if(g$(Q))try{bQ(Q)}catch(Z){return{created:!1,existed:!1,error:`Failed to remove existing symlink: ${Z.message}`}}else return{created:!1,existed:!1,error:`Target exists and is not a symlink: ${Q}`};let q=MQ(Q);if(!b(q))try{W$(q,{recursive:!0})}catch(Z){return{created:!1,existed:!1,error:`Failed to create parent directory: ${Z.message}`}}let X=NQ();try{if(X==="win32")wq($,Q,"junction");else wq($,Q,"dir");return{created:!0,existed:!1}}catch(Z){if(X==="win32"&&Z.code==="EPERM")return{created:!1,existed:!1,error:"Windows requires Developer Mode or admin privileges for symlinks. Enable Developer Mode: Settings > Update & Security > For developers"};return{created:!1,existed:!1,error:`Failed to create symlink: ${Z.message}`}}};var bq=($,Q,q="skills")=>{let X=h(!0);if(!X.symlinks)X.symlinks={};if(!X.symlinks[$])X.symlinks[$]={superpowers:null,skills:[]};if(q==="superpowers")X.symlinks[$].superpowers=Q;else{let Z=X.symlinks[$].skills||[];if(!Z.includes(Q))Z.push(Q),X.symlinks[$].skills=Z}l(X,!0)};var xQ=($,Q={})=>{let q=H.homeSuperpowersSkills,X=_($.skillsDir(),$.superpowersTarget),Z=E$(q,X);if(Z.created){bq($.name,X,"superpowers");let Y=q.replace(N(),"~"),W=X.replace(N(),"~");return console.log(`  ✓ Created ${W} -> ${Y}`),{created:!0}}else if(Z.existed)return{created:!1,existed:!0};else if(Z.error)return console.log(`  ⚠️  ${$.name} superpowers: ${Z.error}`),{created:!1,error:Z.error};return{created:!1}},Aq=($,Q={})=>{let q=H.homePersonalSkills;if(!b(q))return{created:0,existed:0};let X;try{X=wQ(q,{withFileTypes:!0}).filter((Y)=>Y.isDirectory()&&!Y.name.startsWith(".")).map((Y)=>Y.name)}catch{return{created:0,existed:0}}if(X.length===0)return{created:0,existed:0};let Z={created:0,existed:0,names:[]};for(let Y of X){let W=_(q,Y),E=_($.skillsDir(),Y),G=E$(W,E);if(G.created)bq($.name,E,"skills"),Z.created++,Z.names.push(Y);else if(G.existed)Z.existed++;else if(G.error)console.log(`  ⚠️  ${Y}: ${G.error}`)}if(Z.created>0)console.log(`  ✓ Synced ${Z.created} personal skill(s): ${Z.names.join(", ")}`);return Z},U$=($={})=>{let{force:Q=!1,forceAgents:q=new Set}=$;for(let X of Tq){let Z=Q||q.has(X.name),Y=X.parentDir(),W=X.skillsDir();if(!b(Y))if(Z)try{W$(Y,{recursive:!0}),console.log(`✓ Created ${Y.replace(N(),"~")}`)}catch(E){console.log(`⚠️  Failed to create ${Y.replace(N(),"~")}: ${E.message}`);continue}else{console.log(`⚠️  Skipping ${X.name} (${Y.replace(N(),"~")} not found)`),console.log("   Use --force or --force-<agent> to create directory");continue}if(!b(W))try{W$(W,{recursive:!0})}catch(E){console.log(`⚠️  Failed to create skills directory for ${X.name}: ${E.message}`);continue}console.log(`${X.name}:`),xQ(X,$),Aq(X,$)}},k$=($={})=>{let{force:Q=!1}=$;for(let q of Tq){let X=q.parentDir(),Z=q.skillsDir();if(!b(X)){if(!Q)continue;try{W$(X,{recursive:!0})}catch{continue}}if(!b(Z))try{W$(Z,{recursive:!0})}catch{continue}Aq(q,$)}};var Mq=($={})=>{let Q=$.projectRoot||process.cwd(),q=_(Q,".agents","skills");if(!b(q))return{created:0,existed:0,skipped:0,errors:[]};let X={created:0,existed:0,skipped:0,errors:[]};for(let Z of CQ){if(!Z.detect(Q)){X.skipped++;continue}let Y=Z.skillsDir(Q),W=Z.agentDir(Q);if(!b(W)){X.skipped++;continue}let E=E$(q,Y);if(E.created){let G=Y.replace(Q,".");console.log(`  ✓ Created ${G} -> .agents/skills`),X.created++}else if(E.existed)X.existed++;else if(E.error)console.log(`  ⚠️  ${Z.name}: ${E.error}`),X.errors.push({platform:Z.name,error:E.error})}return X};var DQ=($)=>{let q={"cursor-hooks":o,"opencode-plugin":H$}[$];if(q)try{return q(),{success:!0,integration:$}}catch(X){return{success:!1,integration:$,error:X.message}}return{success:!1,integration:$,error:"Unknown integration"}},jQ=()=>{console.log("Installing aliases...")},I$=($={})=>{let Q=$.skipReinstall||!1;console.log(`# Checking for Superpowers updates...
`);let q=L$();if(q.error){console.log("⚠️  Could not check for updates (network issue)");return}if(!q.hasUpdates){console.log("✓ Already up to date");return}if(q.hasLocalChanges){console.log(`⚠️  Cannot auto-update: local changes detected
   Commit or stash your changes first, then run update again
   Or manually update: cd ${H.superpowersRepo} && git pull`);return}if(!Z$()){console.log(`⚠️  Not on main branch, skipping auto-update
   Switch to main branch first: cd ${H.superpowersRepo} && git checkout main`);return}console.log(`\uD83D\uDCE6 Updating from ${q.currentCommit.substring(0,7)} to ${q.latestCommit.substring(0,7)}
   (${q.commitsBehind} new commit${q.commitsBehind>1?"s":""})
`);try{RQ("git pull origin main",{cwd:H.superpowersRepo,stdio:"pipe",timeout:1e4}),console.log("✓ Updated superpowers repository")}catch(W){console.log(`✗ Git pull failed: ${W.message}
   Please resolve manually and try again`);return}if(console.log(""),_$({last_updated_commit:q.latestCommit,last_update_check:new Date().toISOString()}),Q){console.log(`ℹ️  Skipping integration reinstall (--no-reinstall flag)

✓ Update complete!`);return}let X=Iq(q.changedFiles);if(X.length===0){console.log(`ℹ️  No integration files changed, skipping reinstalls

✓ Update complete!`);return}console.log(`\uD83D\uDD04 Reinstalling updated integrations:
`);let Z=[];for(let W of X){let E=DQ(W);if(Z.push(E),E.success)console.log(`  ✓ ${W}`);else console.log(`  ✗ ${W} (${E.error})`)}console.log(""),console.log(`
---
`),jQ(),console.log(`
---
`),console.log(`## Syncing Skill Symlinks
`),U$(),console.log(`
---
`),console.log(`## OpenCode Plugin Symlink
`),H$();let Y=Z.filter((W)=>!W.success);if(Y.length>0){console.log("⚠️  Update completed with errors:");for(let W of Y){console.log(`  - ${W.integration} failed to install`);let E=`install-${W.integration}`;console.log(`    Run manually: superpowers-agent ${E}`)}}else console.log("✓ Update complete!")},Nq=async()=>{R();let $=g();console.log(`Current version: ${$}`);try{let Q=await Wq();if(console.log(`Latest version: ${Q}`),Eq(Q,$))console.log("Update available: Yes"),process.exit(1);else console.log("You are up to date"),process.exit(0)}catch(Q){console.log(`Error checking for updates: ${Q.message}`),process.exit(1)}},Cq=()=>{let $=g();console.log($)};import{existsSync as I,readFileSync as k,writeFileSync as s,rmSync as PQ}from"fs";import{join as v,dirname as xq,parse as r}from"path";import{execSync as F}from"child_process";import{platform as yQ}from"os";import{execSync as SQ}from"child_process";var z$=($)=>{try{return SQ(`which ${$}`,{stdio:"pipe",timeout:2000}),!0}catch{return!1}};var U={opencode:{check:()=>z$("opencode"),cli:!0,name:"OpenCode",installUrl:"https://opencode.ai/docs/installation",bootstrapCommand:"install-opencode-commands"},claude:{check:()=>z$("claude"),cli:!0,name:"Claude Code",installUrl:"https://code.claude.com/docs/en/installation",bootstrapCommand:"install-claude-commands"},gemini:{check:()=>z$("gemini"),cli:!0,name:"Gemini",installUrl:"https://cloud.google.com/gemini/docs/cli/install",bootstrapCommand:"install-gemini-commands"},codex:{check:()=>z$("codex"),cli:!0,name:"Codex",installUrl:"https://developers.openai.com/codex/docs/installation",bootstrapCommand:"install-codex-prompts"},cursor:{check:()=>!0,cli:!1,name:"Cursor",bootstrapCommand:"install-cursor-commands"},copilot:{check:()=>!0,cli:!1,name:"GitHub Copilot",bootstrapCommand:"install-copilot-prompts"}},Fq=()=>{let $=[];if(U.copilot.check())$.push("github-copilot");if(U.cursor.check())$.push("cursor");if(U.claude.check())$.push("claude-code");if(U.opencode.check())$.push("opencode");if(U.gemini.check())$.push("gemini");if(U.codex.check())$.push("codex");return $};var fQ=()=>{let $=v(H.superpowersRepo,".agents","templates","TOOLS.md.template");if(I($))try{return k($,"utf8").trim()}catch(Q){return"### Using Tools\n\nUse your native skill tool. Fallback: `superpowers-agent find-skills`"}return""},K$=($,Q,q,X=!0)=>{let Z=I($);if(!Z&&!X)return{updated:!1,created:!1,skipped:!0};let Y=fQ(),W=Q.replace(/\{\{TOOL_MAPPINGS\}\}/g,Y),E=new Date().toISOString().split("T")[0];W=W.replace(/\{\{DATE\}\}/g,E),W=W.replace(/\{\{SUPERPOWERS_PATH\}\}/g,H.superpowersRepo);let G=`<!-- SUPERPOWERS_SKILLS_START -->
${W}
<!-- SUPERPOWERS_SKILLS_END -->`;if(Z){let z=new Date().toISOString().split("T")[0],B=`${$}.backup-${z}`;try{F(`cp "${$}" "${B}"`,{stdio:"pipe"})}catch(J){return{updated:!1,error:!0,message:`Failed to backup: ${J.message}`}}let V;try{V=k($,"utf8")}catch(J){return{updated:!1,error:!0,message:`Failed to read: ${J.message}`}}let L="<!-- SUPERPOWERS_SKILLS_START -->",K="<!-- SUPERPOWERS_SKILLS_END -->",O;if(V.includes(L)&&V.includes(K)){let J=new RegExp(`${L}[\\s\\S]*?${K}`,"g");O=V.replace(J,G)}else{let J=V.match(/^#\s+.+$/m);if(J){let M=V.indexOf(`
`,J.index)+1;O=V.slice(0,M)+`
`+G+`
`+V.slice(M)}else O=G+`

`+V}try{return s($,O,"utf8"),{updated:!0,created:!1,backup:B}}catch(J){return{updated:!1,error:!0,message:`Failed to write: ${J.message}`}}}else try{let z=xq($);if(!I(z))F(`mkdir -p "${z}"`,{stdio:"pipe"});let L=`# ${r($).base.toUpperCase()}

`+G;return s($,L,"utf8"),{updated:!1,created:!0}}catch(z){return{updated:!1,created:!1,error:!0,message:`Failed to create: ${z.message}`}}},gQ=()=>{console.log("Installing Unix aliases...");let $=v(H.superpowersRepo,".agents","superpowers-agent"),Q=v(H.home,".local","bin"),q=v(Q,"superpowers-agent"),X=v(Q,"superpowers");if(!I($)){console.log("⚠️  superpowers-agent executable not found");return}if(!I(Q))try{F(`mkdir -p "${Q}"`,{stdio:"pipe"}),console.log(`✓ Created ${Q}`)}catch(Y){console.log(`⚠️  Failed to create ${Q}: ${Y.message}`);return}try{if(I(q))F(`rm "${q}"`,{stdio:"pipe"});F(`ln -s "${$}" "${q}"`,{stdio:"pipe"}),console.log(`✓ Created symlink: superpowers-agent -> ${$}`)}catch(Y){console.log(`⚠️  Failed to create superpowers-agent symlink: ${Y.message}`)}try{if(I(X))F(`rm "${X}"`,{stdio:"pipe"});F(`ln -s "${$}" "${X}"`,{stdio:"pipe"}),console.log(`✓ Created symlink: superpowers -> ${$}`)}catch(Y){console.log(`⚠️  Failed to create superpowers symlink: ${Y.message}`)}if(!(process.env.PATH||"").includes(Q))console.log(`
⚠️  Warning: ${Q} is not in your PATH`),console.log("   Add this to your shell profile (~/.bashrc, ~/.zshrc, etc.):"),console.log(`   export PATH="$HOME/.local/bin:$PATH"
`)},kQ=()=>{console.log("Installing Windows aliases..."),console.log("✓ Windows alias installation (implementation pending)")},u$=()=>{let $=yQ();if(console.log(`## Installing Universal Aliases
`),$==="win32")return kQ();else return gQ()},uQ=($)=>{let Q=v(H.superpowersRepo,".github","copilot-instructions.md"),q=v(H.superpowersRepo,"skills","meta","using-superpowers","SKILL.md"),X=v($,".github","copilot-instructions.md"),Z="<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->",Y="<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->";if(!I(Q))return{error:!0,message:"Source template not found"};if(!I(q))return{error:!0,message:"using-superpowers SKILL.md not found"};let W,E;try{W=k(Q,"utf8"),E=k(q,"utf8")}catch(B){return{error:!0,message:`Failed to read source files: ${B.message}`}}let G=W.replace("${content}",E);if(!G.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->")||!G.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->"))return{error:!0,message:"Template is missing required markers"};let z=xq(X);try{if(!I(z))F(`mkdir -p "${z}"`,{stdio:"pipe"})}catch(B){return{error:!0,message:`Failed to create .github directory: ${B.message}`}}if(I(X)){let B;try{B=k(X,"utf8")}catch(K){return{error:!0,message:`Failed to read existing file: ${K.message}`}}let V=new Date().toISOString().replace(/[:.]/g,"-"),L=`${X}.backup-${V}`;try{F(`cp "${X}" "${L}"`,{stdio:"pipe"})}catch(K){return{error:!0,message:`Failed to backup: ${K.message}`}}if(B.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->")&&B.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->")){let K=new RegExp("<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->[\\s\\S]*?<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->","g"),O=B.replace(K,G.trim());try{return s(X,O,"utf8"),{updated:!0,backup:L}}catch(J){return{error:!0,message:`Failed to update: ${J.message}`}}}else{let K=B.trimEnd()+`

`+G.trim()+`
`;try{return s(X,K,"utf8"),{updated:!0,backup:L}}catch(O){return{error:!0,message:`Failed to append: ${O.message}`}}}}else try{return s(X,G,"utf8"),{created:!0}}catch(B){return{error:!0,message:`Failed to create: ${B.message}`}}},Rq=()=>{R(),console.log(`# Setting up Superpowers skills for this project
`);let $=process.cwd(),Q=v($,".agents"),q=v(Q,"skills"),X=v($,"AGENTS.md"),Z=v(Q,"AGENTS.md"),Y=I(X)?X:Z,W=v(H.superpowersRepo,".agents","templates","AGENTS.md.template");if(!I(W)){console.log(`✗ Error: AGENTS.md template not found
  Expected at: ${W}
  Please update your Superpowers installation`);return}if(!I(Q))try{F(`mkdir -p "${Q}"`,{stdio:"pipe"}),console.log("✓ Created .agents/ directory")}catch(w){console.log(`✗ Failed to create .agents/ directory: ${w.message}`);return}else console.log("✓ .agents/ directory exists");if(!I(q))try{F(`mkdir -p "${q}"`,{stdio:"pipe"}),F(`touch "${v(q,".gitkeep")}"`,{stdio:"pipe"}),console.log("✓ Created .agents/skills/ directory")}catch(w){console.log(`✗ Failed to create skills directory: ${w.message}`);return}else console.log("✓ .agents/skills/ directory exists");let E=v(Q,"docs"),G=v(E,"SUPERPOWERS.md"),z=v(H.superpowersRepo,".agents","templates","SUPERPOWERS.md.template");if(!I(E))try{F(`mkdir -p "${E}"`,{stdio:"pipe"}),console.log("✓ Created .agents/docs/ directory")}catch(w){console.log(`⚠️  Failed to create docs directory: ${w.message}`)}else console.log("✓ .agents/docs/ directory exists");let B=I(Y);if(B){let w=new Date().toISOString().split("T")[0],G$=`${Y}.backup-${w}`;try{F(`cp "${Y}" "${G$}"`,{stdio:"pipe"}),console.log(`✓ Backed up existing AGENTS.md to ${r(G$).base}`)}catch(nq){console.log(`✗ Failed to backup AGENTS.md: ${nq.message}`);return}}let V;try{V=k(W,"utf8")}catch(w){console.log(`✗ Failed to read template: ${w.message}`);return}let L=g();if(V=V.replace(/\{\{VERSION\}\}/g,L),I(z))try{let w=k(z,"utf8"),G$=new Date().toISOString().split("T")[0];w=w.replace(/\{\{VERSION\}\}/g,L),w=w.replace(/\{\{DATE\}\}/g,G$),s(G,w,"utf8"),console.log("✓ Created .agents/docs/SUPERPOWERS.md")}catch(w){console.log(`⚠️  Failed to create SUPERPOWERS.md: ${w.message}`)}else console.log("⚠️  SUPERPOWERS.md.template not found");let K=[],O=v($,".github","copilot-instructions.md"),J=v(H.home,".github","copilot-instructions.md");if(I(O)||I(J)||U.copilot.check())K.push("github-copilot");let M=v($,"CLAUDE.md"),u=v(Q,"CLAUDE.md");if(I(M)||I(u)||U.claude.check())K.push("claude-code");let j=v($,"GEMINI.md"),c$=v(Q,"GEMINI.md");if(I(j)||I(c$)||U.gemini.check())K.push("gemini");if(U.cursor.check())K.push("cursor");if(U.opencode.check())K.push("opencode");if(U.codex.check())K.push("codex");console.log(`
Detected platforms for project: ${K.join(", ")||"none"}
`);let cq=K.filter((w)=>["github-copilot","cursor","opencode","codex"].includes(w)),t=K$(Y,V,cq,!B);if(t.created)console.log(`✓ Created AGENTS.md with platform tool mappings (${Y===X?"root":".agents/"})`);else if(t.updated)console.log(`✓ Updated AGENTS.md with platform tool mappings (${Y===X?"root":".agents/"})`);else if(t.error)console.log("⚠️  Failed to update AGENTS.md");let M$=I(M)?M:u,S=K$(M$,V,["claude-code"],!1);if(S.created)console.log(`✓ Created CLAUDE.md with Claude Code tool mappings (${M$===M?"root":".agents/"})`);else if(S.updated){if(console.log(`✓ Updated CLAUDE.md with Claude Code tool mappings (${M$===M?"root":".agents/"})`),S.backup)console.log(`  Backed up to ${r(S.backup).base}`)}else if(S.skipped)console.log("ℹ️  Skipped CLAUDE.md (does not exist)");else if(S.error)console.log("⚠️  Failed to update CLAUDE.md");let N$=I(j)?j:c$,P=K$(N$,V,["gemini"],!1);if(P.created)console.log(`✓ Created GEMINI.md with Gemini tool mappings (${N$===j?"root":".agents/"})`);else if(P.updated){if(console.log(`✓ Updated GEMINI.md with Gemini tool mappings (${N$===j?"root":".agents/"})`),P.backup)console.log(`  Backed up to ${r(P.backup).base}`)}else if(P.skipped)console.log("ℹ️  Skipped GEMINI.md (does not exist)");else if(P.error)console.log("⚠️  Failed to update GEMINI.md");let B$=v($,".github","copilot-instructions.md"),D={skipped:!0};if(K.includes("github-copilot")){if(D=uQ($),D.created)console.log("✓ Created .github/copilot-instructions.md with Superpowers instructions");else if(D.updated){if(console.log("✓ Updated .github/copilot-instructions.md with Superpowers instructions"),D.backup)console.log(`  Backed up to ${r(D.backup).base}`)}else if(D.error)console.log(`⚠️  Failed to update .github/copilot-instructions.md: ${D.message||""}`)}else console.log("ℹ️  Skipped .github/copilot-instructions.md (GitHub Copilot not detected)");let n$=!I(X)&&!I(Z);if(I(B$)&&n$){let w=K$(B$,V,["github-copilot"],!1);if(w.updated){if(console.log("✓ Updated .github/copilot-instructions.md with GitHub Copilot tool mappings"),w.backup)console.log(`  Backed up to ${r(w.backup).base}`)}else if(w.error)console.log("⚠️  Failed to update .github/copilot-instructions.md with tool mappings")}else if(I(B$)&&!n$)console.log("ℹ️  Skipped .github/copilot-instructions.md tool mappings (AGENTS.md exists, using that instead)");else if(!I(B$)&&!K.includes("github-copilot"))console.log("ℹ️  Skipped .github/copilot-instructions.md tool mappings (does not exist)");console.log(`
## Syncing Project Skill Symlinks
`);let n=Mq({projectRoot:$});if(n.created>0)console.log(`
✓ Created ${n.created} project skill symlink(s)`);else if(n.existed>0)console.log("ℹ️  Project skill symlinks already exist");else if(n.errors.length>0)console.log("⚠️  Some symlinks could not be created");else console.log("ℹ️  No agent directories detected for symlinking");let y=`
# Setup complete!

Your project now has:
  - .agents/ directory structure`;if(t.updated||t.created)y+=`
  - AGENTS.md with universal skills instructions`;if(S.updated||S.created)y+=`
  - CLAUDE.md with Claude Code skills instructions`;if(P.updated||P.created)y+=`
  - GEMINI.md with Gemini skills instructions`;if(D.updated||D.created)y+=`
  - .github/copilot-instructions.md with Superpowers instructions`;if(n.created>0)y+=`
  - ${n.created} project skill symlink(s) to agent directories`;y+=`
  - .agents/skills/ ready for project-specific skills`,y+=`
  - .agents/docs/SUPERPOWERS.md for detailed reference
`,console.log(y)},dQ=()=>{let $=[...["brainstorming.prompt.md","execute-plan.prompt.md","write-plan.prompt.md","setup-skills.prompt.md","create-meta-prompt.md","finding-skills.prompt.md","skills.prompt.md","use-skill.prompt.md","using-a-skill.prompt.md"].map((q)=>v(H.vscodeUserDir,"prompts",q)),...["brainstorming.md","create-meta-prompt.md","execute-plan.md","finding-skills.md","skills.md","use-skill.md","using-a-skill.md","write-plan.md","setup-skills.md"].map((q)=>v(H.home,".cursor","commands",q)),...["brainstorm.md","create-meta-prompt.md","execute-plan.md","finding-skills.md","skills.md","use-skill.md","using-a-skill.md","write-plan.md","setup-skills.md"].map((q)=>v(H.home,".claude","commands",q)),...["brainstorm.md","create-meta-prompt.md","execute-plan.md","finding-skills.md","skills.md","use-skill.md","using-a-skill.md","write-plan.md","setup-skills.md"].map((q)=>v(H.home,".config","opencode","command",q)),...["brainstorm.md","create-meta-prompt.md","execute-plan.md","finding-skills.md","skills.md","use-skill.md","using-a-skill.md","write-plan.md","setup-skills.md"].map((q)=>v(H.home,".codex","prompts",q)),...["brainstorm-with-superpowers.toml","create-meta-prompt.toml","execute-plan.toml","finding-skills.toml","skills.toml","use-skill.toml","using-a-skill.toml","write-plan.toml","setup-skills.toml"].map((q)=>v(H.home,".gemini","commands",q))],Q=0;for(let q of $)if(I(q))try{PQ(q),Q++}catch(X){console.log(`  ⚠️  Could not remove ${q}: ${X.message}`)}if(Q>0)console.log(`✓ Removed ${Q} legacy prompt/command file${Q!==1?"s":""}`);else console.log("✓ No legacy prompt/command files found")},Dq=()=>{R(),console.log(`# Superpowers Bootstrap for Agents
# ==================================
`);let $=process.argv.includes("--no-update"),q=new Set(["copilot","cursor","codex","gemini","claude","opencode"].filter((Y)=>process.argv.includes(`--force-${Y}`))),X=q.size>0;if(!$){let Y=Y$(),W=L$();if(W.error)console.log(`## Update Check

⚠️  Could not check for updates (network issue)

---
`);else if(W.hasUpdates)if(Y.auto_update&&!W.hasLocalChanges&&Z$())console.log(`## Auto-Update
`),I$(),console.log(`
---
`);else{if(console.log(`## Update Available
`),W.hasLocalChanges)console.log("⚠️  Your superpowers installation is behind the latest version.\n    Cannot auto-update: local changes detected\n    To update, commit/stash changes then run: `superpowers-agent update`");else if(!Z$())console.log("⚠️  Your superpowers installation is behind the latest version.\n    Cannot auto-update: not on main branch\n    To update, switch to main then run: `superpowers-agent update`");else console.log("⚠️  Your superpowers installation is behind the latest version.\n    To update, run: `superpowers-agent update`\n    Or enable auto-update: `superpowers-agent config-set auto_update true`");console.log(`
---
`)}}if(!X)u$(),console.log(`---
`);if(!X)console.log(`## Cleaning Up Legacy Files
`),dQ(),console.log(`
---
`);if(!X||q.has("copilot"))console.log(`## GitHub Copilot Integration
`),console.log("✓ Skill symlinks handled in sync step below"),console.log(`
---
`);if(!X||q.has("cursor"))console.log(`## Cursor Integration
`),o(),console.log(`
---
`);if(!X||q.has("codex")){if(console.log(`## OpenAI Codex Integration
`),!U.codex.check())console.log(`⚠️  Skipped (${U.codex.name} CLI not detected)
\uD83D\uDCA1 To enable Codex integration:
   1. Install Codex: ${U.codex.installUrl}
   2. Run: superpowers-agent ${U.codex.bootstrapCommand}`);else console.log("✓ Skill symlinks handled in sync step below");console.log(`
---
`)}if(!X||q.has("gemini")){if(console.log(`## Gemini Integration
`),!U.gemini.check())console.log(`⚠️  Skipped (${U.gemini.name} CLI not detected)
\uD83D\uDCA1 To enable Gemini integration:
   1. Install Gemini: ${U.gemini.installUrl}
   2. Run: superpowers-agent ${U.gemini.bootstrapCommand}`);else console.log("✓ Skill symlinks handled in sync step below");console.log(`
---
`)}if(!X||q.has("claude")){if(console.log(`## Claude Code Integration
`),!U.claude.check())console.log(`⚠️  Skipped (${U.claude.name} CLI not detected)
\uD83D\uDCA1 To enable Claude Code integration:
   1. Install Claude Code: ${U.claude.installUrl}
   2. Run: superpowers-agent ${U.claude.bootstrapCommand}`);else console.log("✓ Skill symlinks handled in sync step below");console.log(`
---
`)}if(!X||q.has("opencode")){if(console.log(`## OpenCode Integration
`),U.opencode.check())H$();else console.log(`⚠️  Skipped (${U.opencode.name} CLI not detected)
\uD83D\uDCA1 To enable OpenCode integration:
   1. Install OpenCode: ${U.opencode.installUrl}
   2. Run: superpowers-agent ${U.opencode.bootstrapCommand}`);console.log(`
---
`)}if(!X){console.log(`## Generating Platform-Specific Files
`);let Y=Fq();console.log(`Detected platforms: ${Y.join(", ")||"none"}
`);let W=v(H.superpowersRepo,".agents","templates","AGENTS.md.template"),E="";try{E=k(W,"utf8")}catch(G){console.log(`⚠️  Could not read AGENTS.md template: ${G.message}
`)}if(E){let G=g();E=E.replace(/\{\{VERSION\}\}/g,G);let z=v(H.home,".agents","AGENTS.md"),B=K$(z,E,Y,!0);if(B.created)console.log(`✓ Created ${z}`);else if(B.updated)console.log(`✓ Updated ${z}`)}console.log(`
---
`)}console.log(`## Syncing Skill Symlinks
`);let Z=process.argv.includes("--force");U$({force:Z,forceAgents:q}),console.log(`
---
`),console.log(`# Bootstrap Complete!
`),console.log("✓ All integrations installed"),console.log("✓ Skills system ready"),console.log(`✓ Skill symlinks synced
`),console.log("Next steps:"),console.log("  - Run `superpowers-agent find-skills` to see available skills"),console.log("  - Run `superpowers-agent setup-skills` in your project directory")};import{existsSync as x,readFileSync as T$,mkdirSync as rQ,rmSync as sQ,cpSync as tQ}from"fs";import{join as A,dirname as b$,parse as J$,sep as eQ}from"path";import{execSync as C}from"child_process";import{homedir as c}from"os";import{readFileSync as hQ,existsSync as h$,unlinkSync as pQ,lstatSync as cQ,mkdirSync as nQ}from"fs";import{join as lQ,dirname as aQ}from"path";import{join as v$}from"path";import{homedir as mQ}from"os";var d$={github:{name:"github",sourceDir:".github/agents",sourceExt:".agent.md",getDestDir:()=>v$(H.vscodeUserDir,"prompts"),destExt:".agent.md"},opencode:{name:"opencode",sourceDir:".opencode/agents",sourceExt:".md",getDestDir:()=>v$(mQ(),".config","opencode","agents"),destExt:".md"}};function jq($,Q,q){let X=d$[Q];if(!X)return null;return v$($,X.sourceDir,`${q}${X.sourceExt}`)}function Sq($,Q){let q=d$[$];if(!q)return null;return v$(q.getDestDir(),`${Q}${q.destExt}`)}function m$(){return Object.keys(d$)}function iQ($){if(!$||typeof $!=="object")return{valid:!1,error:"agents.json must be a JSON object"};if(!$.version||typeof $.version!=="string")return{valid:!1,error:'agents.json must have a "version" string field'};if(!$.agents||typeof $.agents!=="object")return{valid:!1,error:'agents.json must have an "agents" object field'};let Q=m$(),q=[];for(let[X,Z]of Object.entries($.agents)){if(!Q.includes(X)){q.push(X);continue}if(!Array.isArray(Z))return{valid:!1,error:`agents.${X} must be an array of agent names`};for(let Y of Z)if(typeof Y!=="string")return{valid:!1,error:`agents.${X} must contain only string agent names`}}return{valid:!0,data:$,skippedPlatforms:q}}function oQ($,Q){if(!h$($))return{installed:!1,error:`Source file not found: ${$}`};if(h$(Q))try{if(!cQ(Q).isSymbolicLink())pQ(Q)}catch(X){}nQ(aQ(Q),{recursive:!0});let q=E$($,Q);if(q.error)return{installed:!1,error:q.error};return{installed:!0}}function p$($,Q={}){let q=lQ($,"agents.json");if(!h$(q))return;let X;try{X=JSON.parse(hQ(q,"utf8"))}catch(K){console.log(`
  ⚠ Could not parse agents.json: ${K.message}`);return}let Z=iQ(X);if(!Z.valid){console.log(`
  ⚠ Invalid agents.json: ${Z.error}`);return}for(let K of Z.skippedPlatforms)console.log(`  ⚠ Skipping unknown platform: ${K}`);let Y=X.repository||"unknown",W=X.version,E=Q.isUpdate?"Updating":"Installing",G=Q.isUpdate?"Updated":"Installed";console.log(`
  \uD83D\uDCE6 Found agents.json (${Y} v${W})`);let z=m$(),B=0,V=0,L=[];for(let[K,O]of Object.entries(X.agents)){if(!z.includes(K))continue;console.log(`
  ${E} ${K} agents:`);for(let J of O){let M=jq($,K,J),u=Sq(K,J),j=oQ(M,u);if(j.installed)console.log(`    ✓ ${J}`),L.push({platformKey:K,agentName:J,sourcePath:M,destPath:u}),B++;else console.log(`    ✗ ${J}: ${j.error}`),V++}}if(L.length>0){let K=h(!0);if(!K.installedAgents)K.installedAgents={};if(!K.installedAgents[Y])K.installedAgents[Y]={version:W,agents:{}};K.installedAgents[Y].version=W;for(let{platformKey:O,agentName:J,sourcePath:M,destPath:u}of L){if(!K.installedAgents[Y].agents[O])K.installedAgents[Y].agents[O]={};K.installedAgents[Y].agents[O][J]={source:M,destination:u,installedAt:new Date().toISOString()}}l(K,!0)}if(B>0)console.log(`
  ${G} ${B} agent(s)${V>0?` (${V} failed)`:""}`)}var Pq=($,Q)=>{if(Q.type==="local"){let X=$;for(let Z=0;Z<10;Z++){if(x(A(X,"agents.json")))return X;let Y=b$(X);if(Y===X)break;X=Y}return $}let q=A(".agents","tmp");if($.includes(q)){let X=$.indexOf(q),Y=$.substring(X+q.length+1).split(eQ)[0];return A($.substring(0,X),".agents","tmp",Y)}return $},yq=($,Q)=>{let q=A(c(),".agents","repos"),X=Q.replace(/^@/,"").replace(/[^a-zA-Z0-9_-]/g,"_"),Z=A(q,X);try{if(rQ(q,{recursive:!0}),x(Z))sQ(Z,{recursive:!0,force:!0});return tQ($,Z,{recursive:!0}),Z}catch(Y){return console.log(`  Warning: Could not persist repository for agents: ${Y.message}`),null}},fq=($)=>{let Q=$.match(/^https:\/\/github\.com\/([^/]+)\/([^/]+)\/tree\/([^/]+)\/(.+)$/);if(Q){let[,Z,Y,W,E]=Q;return{type:"git-tree",repoUrl:`https://github.com/${Z}/${Y}.git`,branch:W,path:E,original:$}}let q=$.match(/^git@github\.com:([^/]+)\/([^/]+?)(?:\.git)?$/);if(q){let[,Z,Y]=q;return{type:"git-repo",repoUrl:`git@github.com:${Z}/${Y}.git`,branch:null,path:null,original:$}}let X=$.match(/^https:\/\/github\.com\/([^/]+)\/([^/]+)(?:\.git)?$/);if(X){let[,Z,Y]=X;return{type:"git-repo",repoUrl:`https://github.com/${Z}/${Y}.git`,branch:null,path:null,original:$}}if(x($))return{type:"local",path:$,original:$};return null},gq=($)=>{let Q=$.includes("--global")||$.includes("-g"),q=$.includes("--project")||$.includes("-p");if(Q)return A(c(),".agents","skills");if(q)return A(process.cwd(),".agents","skills");let X=A(process.cwd(),".agents","config.json"),Z=A(c(),".agents","config.json");for(let Y of[X,Z])if(x(Y))try{if(JSON.parse(T$(Y,"utf8")).installLocation==="project")return A(process.cwd(),".agents","skills")}catch(W){}return A(c(),".agents","skills")},kq=($,Q)=>{let q=A(c(),".agents","tmp",`skill-install-${Date.now()}`);try{if(C(`mkdir -p "${q}"`,{stdio:"pipe"}),Q)C(`git clone --branch ${Q} --depth 1 "${$}" "${q}"`,{stdio:"pipe",timeout:30000});else C(`git clone --depth 1 "${$}" "${q}"`,{stdio:"pipe",timeout:30000});return q}catch(X){try{C(`rm -rf "${q}"`,{stdio:"pipe"})}catch{}throw Error(`Failed to clone repository: ${X.message}`)}},A$=($)=>{let Q=A($,"skill.json");if(!x(Q))return null;try{let q=T$(Q,"utf8");return JSON.parse(q)}catch(q){throw Error(`Failed to read skill.json: ${q.message}`)}},w$=($,Q,q,X)=>{let Z=A($,Q);if(!x(Z)){X.errors.push(`Skill directory not found: ${Q}`);return}let Y=A$(Z);if(!Y){X.errors.push(`No skill.json found in: ${Q}`);return}let W=Y.name||Q,E=A(q,W),G=b$(E);try{C(`mkdir -p "${G}"`,{stdio:"pipe"})}catch(z){X.errors.push(`Failed to create directory ${G}: ${z.message}`);return}try{if(x(E))C(`rm -rf "${E}"`,{stdio:"pipe"});C(`cp -R "${Z}" "${E}"`,{stdio:"pipe"}),X.installed.push({name:W,path:E,title:Y.title||Y.description||W})}catch(z){X.errors.push(`Failed to install ${Q}: ${z.message}`)}},uq=()=>{let $=process.argv.slice(3),Q=$.find((z)=>!z.startsWith("-")),q=$.filter((z)=>z.startsWith("-"));if(!Q){console.log(`Usage: superpowers-agent add <url-or-path|@alias> [skill-path] [options]

Options:
  --global, -g   Install skills globally in ~/.agents/skills/ (default)
  --project, -p  Install skills in current project's .agents/skills/

Examples:
  superpowers-agent add https://github.com/example/skills
  superpowers-agent add https://github.com/example/repo/tree/main/skills
  superpowers-agent add ~/my-local-skills
  superpowers-agent add https://github.com/example/skills --project
  superpowers-agent add ~/my-local-skills --global
  superpowers-agent add @myrepo path/to/skill
  superpowers-agent add @myrepo path/to/skills --project

Description:
  Installs skill(s) from a Git repository, local directory, or repository alias.
  Supports repositories with single or multiple skills.
  Reads skill.json to determine skill names and installation paths.
  
  Repository aliases can be added using:
    superpowers-agent add-repository <git-url> [--as=@alias]`);return}console.log(`Installing skill(s)...
`);let X=null,Z=null;if(Q.startsWith("@")){let z=Q;Z=$.find((K,O)=>O>0&&!K.startsWith("-")&&K!==z);let B=f(!1),V=f(!0);if(B[z])X=B[z],console.log(`Using project repository alias: ${z}`);else if(V[z])X=V[z],console.log(`Using global repository alias: ${z}`);else{console.log(`Error: Repository alias not found: ${z}`),console.log(`
Available aliases:`);let K={...V,...B};if(Object.keys(K).length===0)console.log("  (none)"),console.log(`
Add a repository using:`),console.log("  superpowers-agent add-repository <git-url>");else for(let[O,J]of Object.entries(K))console.log(`  ${O} -> ${J}`);return}let L=x(X);if(Z)if(L)Q=A(X,Z),console.log(`Repository path: ${X}`),console.log(`Skill path: ${Z}
`);else Q=`${X}/tree/main/${Z}`,console.log(`Repository URL: ${X}`),console.log(`Skill path: ${Z}
`);else if(Q=X,L)console.log(`Repository path: ${X}
`);else console.log(`Repository URL: ${X}
`)}let Y=fq(Q);if(!Y){console.log(`Error: Invalid URL or path not found: ${Q}`);return}let W=gq(q);console.log(`Install location: ${W}
`);let E,G=!1;try{if(Y.type==="git-repo"||Y.type==="git-tree"){if(console.log(`Cloning repository: ${Y.repoUrl}`),Y.branch)console.log(`Branch: ${Y.branch}`);if(E=kq(Y.repoUrl,Y.branch),G=!0,Y.path){if(E=A(E,Y.path),!x(E))throw Error(`Path not found in repository: ${Y.path}`)}console.log("")}else E=Y.path;let z=A$(E);if(!z)throw Error("No skill.json found in source directory");let B={installed:[],errors:[],source:Y.original};if(z.skills&&Array.isArray(z.skills)){console.log(`Found ${z.skills.length} skill(s) to install
`);for(let K of z.skills)w$(E,K,W,B)}else{let K=z.name||J$(E).base;w$(b$(E),J$(E).base,W,B)}let V=Pq(E,Y),L=A(V,"agents.json");if(x(L)){let K=V;if(G){let O;try{O=JSON.parse(T$(L,"utf8"))}catch{}let J=O?.repository||Y.original,M=yq(V,J);if(M)K=M}p$(K,{isUpdate:!1})}if(G&&E)try{let K=E.split("/.agents/tmp/")[0]+"/.agents/tmp/"+E.split("/.agents/tmp/")[1].split("/")[0];C(`rm -rf "${K}"`,{stdio:"pipe"})}catch{}if(console.log(`
**Successfully installed skills:**`),console.log(`- Source: ${B.source}`),B.installed.length>0)for(let K of B.installed)console.log(`  - Installed: ${K.name} at ${K.path}`),console.log(`    ${K.title}`);if(B.errors.length>0){console.log(`
**Errors:**`);for(let K of B.errors)console.log(`  - ${K}`)}if(B.installed.length===0&&B.errors.length===0)console.log("  No skills were installed");if(B.installed.length>0)console.log(`
**Syncing skill symlinks...**`),k$()}catch(z){if(G&&E)try{let B=E.split("/.agents/tmp/")[0]+"/.agents/tmp/"+E.split("/.agents/tmp/")[1].split("/")[0];C(`rm -rf "${B}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${z.message}`)}},dq=()=>{let $=process.argv.slice(3),Q=$.find((z)=>!z.startsWith("-")),q=$.filter((z)=>z.startsWith("-"));if(!Q){console.log(`Usage: superpowers-agent pull <url-or-path|@alias> [skill-path] [options]

Options:
  --global, -g   Update skills globally in ~/.agents/skills/ (default)
  --project, -p  Update skills in current project's .agents/skills/

Examples:
  superpowers-agent pull https://github.com/example/skills
  superpowers-agent pull https://github.com/example/repo/tree/main/skills
  superpowers-agent pull ~/my-local-skills
  superpowers-agent pull https://github.com/example/skills --project
  superpowers-agent pull ~/my-local-skills --global
  superpowers-agent pull @myrepo
  superpowers-agent pull @myrepo path/to/skill
  superpowers-agent pull @myrepo path/to/skills --project

Description:
  Updates or adds skill(s) from a Git repository, local directory, or repository alias.
  If skills already exist, they will be updated (replaced).
  If skills don't exist, they will be added.
  Supports repositories with single or multiple skills.
  Reads skill.json to determine skill names and installation paths.
  
  Repository aliases can be added using:
    superpowers-agent add-repository <git-url> [--as=@alias]`);return}console.log(`Updating skill(s)...
`);let X=null,Z=null;if(Q.startsWith("@")){let z=Q;Z=$.find((K,O)=>O>0&&!K.startsWith("-")&&K!==z);let B=f(!1),V=f(!0);if(B[z])X=B[z],console.log(`Using project repository alias: ${z}`);else if(V[z])X=V[z],console.log(`Using global repository alias: ${z}`);else{console.log(`Error: Repository alias not found: ${z}`),console.log(`
Available aliases:`);let K={...V,...B};if(Object.keys(K).length===0)console.log("  (none)"),console.log(`
Add a repository using:`),console.log("  superpowers-agent add-repository <git-url>");else for(let[O,J]of Object.entries(K))console.log(`  ${O} -> ${J}`);return}let L=x(X);if(Z)if(L)Q=A(X,Z),console.log(`Repository path: ${X}`),console.log(`Skill path: ${Z}
`);else Q=`${X}/tree/main/${Z}`,console.log(`Repository URL: ${X}`),console.log(`Skill path: ${Z}
`);else if(Q=X,L)console.log(`Repository path: ${X}
`);else console.log(`Repository URL: ${X}
`)}let Y=fq(Q);if(!Y){console.log(`Error: Invalid URL or path not found: ${Q}`);return}let W=gq(q);console.log(`Install location: ${W}
`);let E,G=!1;try{if(Y.type==="git-repo"||Y.type==="git-tree"){if(console.log(`Cloning repository: ${Y.repoUrl}`),Y.branch)console.log(`Branch: ${Y.branch}`);if(E=kq(Y.repoUrl,Y.branch),G=!0,Y.path){if(E=A(E,Y.path),!x(E))throw Error(`Path not found in repository: ${Y.path}`)}console.log("")}else E=Y.path;let z=A$(E);if(!z)throw Error("No skill.json found in source directory");let B={installed:[],errors:[],source:Y.original};if(z.skills&&Array.isArray(z.skills)){console.log(`Found ${z.skills.length} skill(s) to update
`);for(let K of z.skills)w$(E,K,W,B)}else{let K=z.name||J$(E).base;w$(b$(E),J$(E).base,W,B)}let V=Pq(E,Y),L=A(V,"agents.json");if(x(L)){let K=V;if(G){let O;try{O=JSON.parse(T$(L,"utf8"))}catch{}let J=O?.repository||Y.original,M=yq(V,J);if(M)K=M}p$(K,{isUpdate:!0})}if(G&&E)try{let K=E.split("/.agents/tmp/")[0]+"/.agents/tmp/"+E.split("/.agents/tmp/")[1].split("/")[0];C(`rm -rf "${K}"`,{stdio:"pipe"})}catch{}if(console.log(`
**Successfully updated skills:**`),console.log(`- Source: ${B.source}`),B.installed.length>0)for(let K of B.installed)console.log(`  - Updated: ${K.name} at ${K.path}`),console.log(`    ${K.title}`);if(B.errors.length>0){console.log(`
**Errors:**`);for(let K of B.errors)console.log(`  - ${K}`)}if(B.installed.length===0&&B.errors.length===0)console.log("  No skills were updated");if(B.installed.length>0)console.log(`
**Syncing skill symlinks...**`),k$()}catch(z){if(G&&E)try{let B=E.split("/.agents/tmp/")[0]+"/.agents/tmp/"+E.split("/.agents/tmp/")[1].split("/")[0];C(`rm -rf "${B}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${z.message}`)}},mq=()=>{let $=process.argv.slice(3),Q=$.find((z)=>!z.startsWith("-")),q=$.filter((z)=>z.startsWith("-"));if(!Q){console.log(`Usage: superpowers-agent add-repository <git-url> [options]

Options:
  --global, -g        Add repository globally in ~/.agents/config.json (default)
  --project, -p       Add repository in current project's .agents/config.json
  --as=<alias>        Specify custom alias for the repository

Examples:
  superpowers-agent add-repository https://github.com/example/skills.git
  superpowers-agent add-repository https://github.com/example/skills.git --as=@myskills
  superpowers-agent add-repository https://github.com/example/skills.git --project
  superpowers-agent add-repository https://github.com/example/skills.git --as=@custom --global

Description:
  Adds a skill repository alias to your configuration.
  The repository's skill.json will be read to determine the default alias.
  Use --as to specify a custom alias.
  After adding, you can install skills using: superpowers-agent add @alias path/to/skill`);return}console.log(`Adding repository...
`);let X=q.some((z)=>z==="--global"||z==="-g"),Z=q.some((z)=>z==="--project"||z==="-p"),Y=q.find((z)=>z.startsWith("--as=")),W=Y?Y.split("=")[1]:null,E=Z?!1:!0,G;try{console.log(`Cloning repository: ${Q}`),G=A(c(),".agents","tmp",`repo-add-${Date.now()}`),C(`mkdir -p "${G}"`,{stdio:"pipe"}),C(`git clone --depth 1 "${Q}" "${G}"`,{stdio:"pipe",timeout:30000}),console.log("");let z=A$(G);if(!z)throw Error("No skill.json found in repository");let B;if(W)B=W;else if(z.repository)B=z.repository;else{let K=Q.match(/\/([^/]+?)(?:\.git)?$/);if(K)B="@"+K[1];else throw Error("Could not determine repository alias. Use --as=@alias to specify manually.")}Yq(B,Q,E),C(`rm -rf "${G}"`,{stdio:"pipe"});let V=E?"globally":"in project",L=E?A(c(),".agents","config.json"):A(process.cwd(),".agents","config.json");console.log(`✓ Repository added ${V}`),console.log(`  Alias: ${B}`),console.log(`  URL: ${Q}`),console.log(`  Config: ${L}`),console.log(`
You can now install skills using:`),console.log(`  superpowers-agent add ${B} <path-to-skill>`)}catch(z){if(G)try{C(`rm -rf "${G}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${z.message}`)}};import{dirname as $X}from"path";var hq=($)=>{if(!$){console.log(`Usage: superpowers-agent use-skill <skill-name>

Examples (smart matching):
  superpowers-agent use-skill brainstorming              # Matches superpowers:collaboration/brainstorming
  superpowers-agent use-skill collaboration/brainstorming # More specific suffix
  superpowers-agent use-skill test-driven-development    # Matches superpowers:testing/test-driven-development

Examples (explicit paths):
  superpowers-agent use-skill superpowers:collaboration/brainstorming  # Full superpowers skill path
  superpowers-agent use-skill claude:email-assistant                   # Full claude skill path
  superpowers-agent use-skill my-custom-skill                          # Project skill

Smart matching:
  - Type just the skill name or any suffix of the path
  - Priority: .agents/skills/ > .claude/skills/ > ~/.agents/skills/ > ~/.agents/superpowers/skills/
  - If multiple skills match at same priority, you'll see them with descriptions`);return}let Q=d($);if(!Q){console.log(`Error: Skill not found: ${$}

Available skills:`),console.log("Run: superpowers-agent find-skills");return}let{skillFile:q,sourceType:X,actualSkillPath:Z}=Q;try{let{content:Y,frontmatter:W}=o$(q),E=X$[X].prefix+Z,G=$X(q),z=`# ${W.name||E}`;if(W.description)z+=`
# ${W.description}`;if(W.whenToUse)z+=`
# When to use: ${W.whenToUse}`;z+=`
# Supporting tools and docs are in ${G}
# ============================================

${Y}`,console.log(z)}catch(Y){console.log(Y.message)}};var pq={bootstrap:Dq,version:Cq,"check-updates":()=>{Nq().catch(($)=>{console.error($.message),process.exit(1)})},update:()=>{let $=process.argv.includes("--no-reinstall");I$({skipReinstall:$})},"config-get":Lq,"config-set":Oq,"setup-skills":Rq,"use-skill":()=>hq(process.argv[3]),execute:Bq,"find-skills":a,add:uq,"add-repository":mq,"list-repositories":Uq,pull:dq,dir:Vq,path:Gq,"get-helpers":_q,"install-cursor-hooks":o,"install-aliases":u$,default:()=>{console.log(`Superpowers for Agents
Usage:
  superpowers-agent bootstrap [--no-update] [--force]               # Run complete bootstrap
  superpowers-agent version                                         # Show current version
  superpowers-agent check-updates                                   # Check for updates
  superpowers-agent update [--no-reinstall]                         # Update to latest version
  superpowers-agent config-get                                      # Show configuration
  superpowers-agent config-set <key> <value>                        # Update configuration
  superpowers-agent setup-skills                                    # Initialize project skills
  superpowers-agent use-skill <skill-name>                          # Load a skill
  superpowers-agent find-skills                                     # List available skills
  superpowers-agent add <url-or-path|@alias> [path] [options]      # Install skills
  superpowers-agent add-repository <git-url> [--as=@alias] [opts]  # Add repository alias
  superpowers-agent list-repositories                              # List configured repository aliases
  superpowers-agent pull <url-or-path|@alias> [path] [options]     # Update skills
  superpowers-agent dir <skill-name>                                # Get skill directory
  superpowers-agent path <skill-name>                               # Get skill file path
  superpowers-agent execute <skill-name>                            # Execute a skill
  superpowers-agent get-helpers <skill> <search-term>               # Get helper file path
  superpowers-agent install-cursor-hooks                            # Install Cursor hooks
  superpowers-agent install-aliases                                 # Install universal aliases (superpowers, superpowers-agent)

Documentation: https://github.com/complexthings/superpowers
`)}},qX=process.argv[2]||"default",QX=pq[qX]||pq.default;try{QX()}catch($){console.error(`Error: ${$.message}`),process.exit(1)}
