#!/bin/sh
// 2>/dev/null; exec "$(command -v bun 2>/dev/null || echo node)" "$0" "$@"
import{relative as UQ,dirname as JQ,join as IX}from"path";import{existsSync as pq}from"fs";import{homedir as qQ,platform as YX}from"os";import{join as L,parse as ZX,dirname as QQ}from"path";import{existsSync as Zq}from"fs";import{fileURLToPath as $X}from"url";var KX=$X(import.meta.url),Yq=QQ(KX),WX=()=>{let q=qQ();switch(YX()){case"darwin":return L(q,"Library","Application Support","Code","User");case"win32":return L(q,"AppData","Roaming","Code","User");case"linux":return L(q,".config","Code","User");default:return L(q,".config","Code","User")}},HX=()=>{let q=process.cwd(),Q=ZX(q).root;while(q!==Q){if(Zq(L(q,".agents")))return q;q=QQ(q)}return process.cwd()},K={home:qQ(),vscodeUserDir:WX(),projectRoot:HX(),get projectClaudeSkills(){return L(this.projectRoot,".claude","skills")},get projectAgentsSkills(){return L(this.projectRoot,".agents","skills")},get projectCopilotSkills(){return L(this.projectRoot,".copilot","skills")},get projectOpencodeSkills(){return L(this.projectRoot,".opencode","skill")},get projectCursorSkills(){return L(this.projectRoot,".cursor","skills")},get projectGeminiSkills(){return L(this.projectRoot,".gemini","skills")},get projectCodexSkills(){return L(this.projectRoot,".codex","skills")},get projectSkillsDir(){return L(this.projectRoot,"skills")},get homePersonalSkills(){return L(this.home,".agents","skills")},get homeClaudeSkills(){return L(this.home,".claude","skills")},get homeCopilotSkills(){return L(this.home,".copilot","skills")},get homeOpencodeSkills(){return L(this.home,".config","opencode","skill")},get homeCursorSkills(){return L(this.home,".cursor","skills")},get homeGeminiSkills(){return L(this.home,".gemini","skills")},get homeCodexSkills(){return L(this.home,".codex","skills")},get bootstrap(){return L(this.projectRoot,".agents","superpowers-bootstrap.md")},get superpowersRepo(){let q=L(Yq,"..","..","..","skills");if(Zq(q)&&Zq(L(Yq,"..","..","..",".github","prompts")))return L(Yq,"..","..","..");return L(this.home,".agents","superpowers")},get homeSuperpowersSkills(){return L(this.superpowersRepo,"skills")},get isSuperpowersRepo(){let q=L(Yq,"..","..","..","skills");return Zq(q)&&Zq(L(Yq,"..","..","..",".github","prompts"))}};import{readFileSync as XQ}from"fs";var $q=(q)=>{try{let Y=XQ(q,"utf8").split(`
`),$={name:"",description:"",whenToUse:""},X=!1;for(let Z of Y){if(Z.trim()==="---"){if(X)break;X=!0;continue}if(X){let W=Z.match(/^(\w+):\s*(.*)$/);if(W){let[,H,B]=W,z={name:"name",description:"description",when_to_use:"whenToUse"};if(z[H])$[z[H]]=B.trim()}}}return $}catch{return{name:"",description:"",whenToUse:""}}},YQ=(q)=>{try{let Q=XQ(q,"utf8"),Y=$q(q),$=Q.split(`
`),X=!1,Z=!1,W=[];for(let H of $){if(H.trim()==="---"){if(X){Z=!0;continue}X=!0;continue}if(Z||!X)W.push(H)}return{content:W.join(`
`).trim(),frontmatter:Y}}catch(Q){throw Error(`Error reading skill file: ${Q.message}`)}};import{existsSync as kq,readdirSync as KQ,statSync as WQ}from"fs";import{join as uq,relative as LY}from"path";import{readFileSync as zX,existsSync as EX}from"fs";import{join as ZQ}from"path";var Kq=(q)=>{let Q=ZQ(q,"skill.json");if(!EX(Q))return null;try{let Y=zX(Q,"utf8");return JSON.parse(Y)}catch(Y){return null}};var Sq=(q)=>{if(q.startsWith("superpowers:"))return{type:"superpowers",path:q.substring(12).replace(/^skills\//,"")};if(q.startsWith("claude:"))return{type:"claude",path:q.substring(7).replace(/^skills\//,"")};if(q.startsWith("copilot:"))return{type:"copilot",path:q.substring(8).replace(/^skills\//,"")};if(q.startsWith("opencode:"))return{type:"opencode",path:q.substring(9).replace(/^skills\//,"")};if(q.startsWith("cursor:"))return{type:"cursor",path:q.substring(7).replace(/^skills\//,"")};if(q.startsWith("gemini:"))return{type:"gemini",path:q.substring(7).replace(/^skills\//,"")};return{type:null,path:q.replace(/^skills\//,"")}},$Q=(q,Q)=>{let Y=Kq(q);if(!Y||!Y.helpers||!Array.isArray(Y.helpers))return null;let $=Y.helpers,X=Q.toLowerCase(),Z=null,W=0;for(let H of $){let B=H.toLowerCase();if(B===X){Z=H;break}if(B.includes(X)){let E=X.length/B.length;if(E>W)W=E,Z=H}let z=H.split("/").pop().split(".")[0].toLowerCase();if(X.includes(z)){let E=z.length/X.length;if(E>W)W=E,Z=H}}return Z?ZQ(q,Z):null},Wq={project:{dir:"projectAgentsSkills",prefix:""},claude:{dir:"projectClaudeSkills",prefix:"claude:"},copilot:{dir:"projectCopilotSkills",prefix:"copilot:"},opencode:{dir:"projectOpencodeSkills",prefix:"opencode:"},cursor:{dir:"projectCursorSkills",prefix:"cursor:"},gemini:{dir:"projectGeminiSkills",prefix:"gemini:"},personal:{dir:"homePersonalSkills",prefix:""},personalClaude:{dir:"homeClaudeSkills",prefix:"claude:"},personalCopilot:{dir:"homeCopilotSkills",prefix:"copilot:"},personalOpencode:{dir:"homeOpencodeSkills",prefix:"opencode:"},personalCursor:{dir:"homeCursorSkills",prefix:"cursor:"},personalGemini:{dir:"homeGeminiSkills",prefix:"gemini:"},superpowers:{dir:"homeSuperpowersSkills",prefix:"superpowers:"}};var Aq=(q,Q,Y=null)=>{let $=[];if(!kq(q))return $;let X=(Z,W)=>{if(Y!==null&&W>Y)return;try{let H=KQ(Z,{withFileTypes:!0});for(let B of H){if(B.name==="node_modules"||B.name===".git"||B.name.startsWith("."))continue;let z=uq(Z,B.name),E=B.isDirectory();if(B.isSymbolicLink())try{E=WQ(z).isDirectory()}catch{continue}if(!E)continue;let V=uq(z,"SKILL.md");if(kq(V))$.push(z);if(Y===null||W<Y)X(z,W+1)}}catch{}};return X(q,0),$};var HQ=(q,Q)=>{if(!kq(q))return[];let Y=[],$=Q.toLowerCase().replace(/\\/g,"/"),X=(Z,W="")=>{try{let H=KQ(Z,{withFileTypes:!0});for(let B of H){let z=uq(Z,B.name),E=W?`${W}/${B.name}`:B.name,V=B.isDirectory();if(B.isSymbolicLink())try{V=WQ(z).isDirectory()}catch{continue}if(V&&(B.name==="node_modules"||B.name===".git"||B.name.startsWith(".")))continue;if(V)X(z,E);else if(B.name==="SKILL.md"){let O=W.toLowerCase().replace(/\\/g,"/");if(O===$||O.endsWith("/"+$)||O.endsWith($))Y.push({file:z,path:W})}}}catch(H){}};return X(q),Y},zQ=(q,Q,Y)=>{let $={project:"project skills (.agents/skills/)",claude:"claude skills (.claude/skills/)",copilot:"copilot skills (.copilot/skills/)",opencode:"opencode skills (.opencode/skill/)",cursor:"cursor skills (.cursor/skills/)",gemini:"gemini skills (.gemini/skills/)",personal:"personal skills (~/.agents/skills/)",personalClaude:"personal claude skills (~/.claude/skills/)",personalCopilot:"personal copilot skills (~/.copilot/skills/)",personalOpencode:"personal opencode skills (~/.config/opencode/skill/)",personalCursor:"personal cursor skills (~/.cursor/skills/)",personalGemini:"personal gemini skills (~/.gemini/skills/)",superpowers:"superpowers skills (~/.agents/superpowers/skills/)"}[Y];console.log(`Error: Multiple skills match "${q}" in ${$}:
`);for(let X of Q){let W=Wq[Y].prefix+X.path;try{let H=$q(X.file),B=H.description||H.whenToUse||"(no description)";console.log(`  ${W}`),console.log(`    ${B}
`)}catch(H){console.log(`  ${W}`),console.log(`    (unable to read description)
`)}}console.log("Please be more specific. Examples:");for(let X of Q.slice(0,2)){let Z=Wq[Y].prefix;console.log(`  superpowers-agent execute ${Z}${X.path}`)}};import{existsSync as dq}from"fs";import{join as BX,relative as VX}from"path";var GX=(q)=>{let{type:Q,path:Y}=Sq(q),$={superpowers:[K.homeSuperpowersSkills],claude:[K.projectClaudeSkills,K.homeClaudeSkills],copilot:[K.projectCopilotSkills,K.homeCopilotSkills],opencode:[K.projectOpencodeSkills,K.homeOpencodeSkills],gemini:[K.projectGeminiSkills,K.homeGeminiSkills],project:[K.projectAgentsSkills],personal:[K.homePersonalSkills]},X;if(Q)X=($[Q]||[]).map((W)=>({type:Q,dir:W}));else if(K.isSuperpowersRepo&&dq(K.projectSkillsDir))X=[{type:"project",dir:K.projectSkillsDir},{type:"project",dir:K.projectAgentsSkills},{type:"claude",dir:K.projectClaudeSkills},{type:"copilot",dir:K.projectCopilotSkills},{type:"opencode",dir:K.projectOpencodeSkills},{type:"gemini",dir:K.projectGeminiSkills},{type:"personal",dir:K.homePersonalSkills},{type:"personalClaude",dir:K.homeClaudeSkills},{type:"personalCopilot",dir:K.homeCopilotSkills},{type:"personalOpencode",dir:K.homeOpencodeSkills},{type:"personalGemini",dir:K.homeGeminiSkills},{type:"superpowers",dir:K.homeSuperpowersSkills}];else X=[{type:"project",dir:K.projectAgentsSkills},{type:"claude",dir:K.projectClaudeSkills},{type:"copilot",dir:K.projectCopilotSkills},{type:"opencode",dir:K.projectOpencodeSkills},{type:"gemini",dir:K.projectGeminiSkills},{type:"personal",dir:K.homePersonalSkills},{type:"personalClaude",dir:K.homeClaudeSkills},{type:"personalCopilot",dir:K.homeCopilotSkills},{type:"personalOpencode",dir:K.homeOpencodeSkills},{type:"personalGemini",dir:K.homeGeminiSkills},{type:"superpowers",dir:K.homeSuperpowersSkills}];for(let{type:Z,dir:W}of X){let H=HQ(W,Y);if(H.length===1)return{skillFile:H[0].file,sourceType:Z,actualSkillPath:H[0].path};if(H.length>1)zQ(Y,H,Z)}return null},S=(q)=>{let Q=GX(q);if(Q)return Q;let{type:Y,path:$}=Sq(q),X={superpowers:[K.homeSuperpowersSkills],claude:[K.projectClaudeSkills,K.homeClaudeSkills],copilot:[K.projectCopilotSkills,K.homeCopilotSkills],opencode:[K.projectOpencodeSkills,K.homeOpencodeSkills],gemini:[K.projectGeminiSkills,K.homeGeminiSkills],project:[K.projectAgentsSkills],personal:[K.homePersonalSkills]},Z;if(Y)Z=(X[Y]||[]).map((H)=>({type:Y,dir:H}));else if(K.isSuperpowersRepo&&dq(K.projectSkillsDir))Z=[{type:"project",dir:K.projectSkillsDir},{type:"project",dir:K.projectAgentsSkills},{type:"claude",dir:K.projectClaudeSkills},{type:"copilot",dir:K.projectCopilotSkills},{type:"opencode",dir:K.projectOpencodeSkills},{type:"gemini",dir:K.projectGeminiSkills},{type:"personal",dir:K.homePersonalSkills},{type:"personalClaude",dir:K.homeClaudeSkills},{type:"personalCopilot",dir:K.homeCopilotSkills},{type:"personalOpencode",dir:K.homeOpencodeSkills},{type:"personalGemini",dir:K.homeGeminiSkills},{type:"superpowers",dir:K.homeSuperpowersSkills}];else Z=[{type:"project",dir:K.projectAgentsSkills},{type:"claude",dir:K.projectClaudeSkills},{type:"copilot",dir:K.projectCopilotSkills},{type:"opencode",dir:K.projectOpencodeSkills},{type:"gemini",dir:K.projectGeminiSkills},{type:"personal",dir:K.homePersonalSkills},{type:"personalClaude",dir:K.homeClaudeSkills},{type:"personalCopilot",dir:K.homeCopilotSkills},{type:"personalOpencode",dir:K.homeOpencodeSkills},{type:"personalGemini",dir:K.homeGeminiSkills},{type:"superpowers",dir:K.homeSuperpowersSkills}];for(let{type:W,dir:H}of Z){if(!dq(H))continue;let B=Aq(H,W,null);for(let z of B){let E=Kq(z);if(E&&E.aliases&&Array.isArray(E.aliases)){let V=$.toLowerCase();for(let O of E.aliases)if(O.toLowerCase()===V){let v=BX(z,"SKILL.md"),b=VX(H,z);return{skillFile:v,sourceType:W,actualSkillPath:b}}}}}return null};import{readFileSync as EQ,writeFileSync as BQ,existsSync as mq}from"fs";import{join as k}from"path";import{execSync as _X}from"child_process";var hq=()=>({auto_update:!0,last_update_check:null,last_updated_commit:null}),Hq=()=>{let q=k(K.superpowersRepo,".config.json");try{if(!mq(q))return hq();let Q=EQ(q,"utf8");return{...hq(),...JSON.parse(Q)}}catch{return hq()}},Oq=(q)=>{let Q=k(K.superpowersRepo,".config.json"),$={...Hq(),...q};try{BQ(Q,JSON.stringify($,null,2))}catch(X){console.log(`Warning: couldn't save config: ${X.message}`)}},zq=(q)=>{let Q=q?k(K.home,".agents","config.json"):k(process.cwd(),".agents","config.json");if(!mq(Q))return{};try{let Y=EQ(Q,"utf8");return JSON.parse(Y)}catch(Y){return{}}},Iq=(q,Q)=>{let Y=Q?k(K.home,".agents"):k(process.cwd(),".agents"),$=k(Y,"config.json");try{if(!mq(Y))_X(`mkdir -p "${Y}"`,{stdio:"pipe"});return BQ($,JSON.stringify(q,null,2),"utf8"),!0}catch(X){throw Error(`Failed to write config: ${X.message}`)}},y=(q=!1)=>{return zq(q).repositories||{}},VQ=(q,Q,Y)=>{let $=zq(Y);if(!$.repositories)$.repositories={};$.repositories[q]=Q,Iq($,Y)};import{readFileSync as vX}from"fs";import{join as GQ,dirname as LX}from"path";import{fileURLToPath as UX}from"url";import{existsSync as JX}from"fs";import{execSync as AX}from"child_process";var OX=UX(import.meta.url),_Q=LX(OX),g=()=>{try{let q=GQ(_Q,"package.json");if(!JX(q))q=GQ(_Q,"..","..","package.json");return JSON.parse(vX(q,"utf8")).version||"5.4.0"}catch(q){return"5.4.0"}},C=()=>{let q=g();console.log(`^^SAV:${q}^^`)},vQ=async()=>{try{let Q=AX('curl -sS "https://raw.githubusercontent.com/complexthings/superpowers/main/.agents/package.json"',{encoding:"utf8",timeout:1e4}),Y=JSON.parse(Q);if(!Y.version)return g();return Y.version}catch(q){throw Error(`Failed to fetch remote version: ${q.message}`)}},LQ=(q,Q)=>{let Y=q.split(".").map(Number),$=Q.split(".").map(Number);for(let X=0;X<3;X++){let Z=Y[X]||0,W=$[X]||0;if(Z>W)return!0;if(Z<W)return!1}return!1};var wX={project:{dir:"projectAgentsSkills",prefix:""},claude:{dir:"projectClaudeSkills",prefix:"claude:"},copilot:{dir:"projectCopilotSkills",prefix:"copilot:"},opencode:{dir:"projectOpencodeSkills",prefix:"opencode:"},gemini:{dir:"projectGeminiSkills",prefix:"gemini:"},personal:{dir:"homePersonalSkills",prefix:""},personalClaude:{dir:"homeClaudeSkills",prefix:"claude:"},personalCopilot:{dir:"homeCopilotSkills",prefix:"copilot:"},personalOpencode:{dir:"homeOpencodeSkills",prefix:"opencode:"},personalGemini:{dir:"homeGeminiSkills",prefix:"gemini:"},superpowers:{dir:"homeSuperpowersSkills",prefix:"superpowers:"}},TX=(q,Q)=>{let Y=IX(q,"SKILL.md"),{dir:$,prefix:X}=wX[Q],Z=UQ(K[$],q).replace(/\\/g,"/");console.log(`${X}${Z}`);let{description:W,whenToUse:H}=$q(Y);if(W)console.log(`  ${W}`);if(H)console.log(`  When to use: ${H}`);console.log("")},p=()=>{C();let q=new Set,Q=[{type:"project",dir:K.projectAgentsSkills,maxDepth:null},{type:"claude",dir:K.projectClaudeSkills,maxDepth:null},{type:"copilot",dir:K.projectCopilotSkills,maxDepth:null},{type:"opencode",dir:K.projectOpencodeSkills,maxDepth:null},{type:"gemini",dir:K.projectGeminiSkills,maxDepth:null},{type:"personal",dir:K.homePersonalSkills,maxDepth:null},{type:"personalClaude",dir:K.homeClaudeSkills,maxDepth:null},{type:"personalCopilot",dir:K.homeCopilotSkills,maxDepth:null},{type:"personalOpencode",dir:K.homeOpencodeSkills,maxDepth:null},{type:"personalGemini",dir:K.homeGeminiSkills,maxDepth:null},{type:"superpowers",dir:K.homeSuperpowersSkills,maxDepth:null}];for(let{type:Y,dir:$,maxDepth:X}of Q){let Z=Aq($,Y,X);for(let W of Z){let H=UQ($,W);if(!q.has(H))q.add(H),TX(W,Y)}}console.log(`Usage:
  superpowers-agent execute <skill-name> # Load a specific skill`)},AQ=()=>{C();let q=process.argv[3];if(!q){console.log(`Usage: superpowers-agent execute <skill-name-or-alias>

Examples:
  superpowers-agent execute brainstorming
  superpowers-agent execute superpowers:collaboration/brainstorming
  superpowers-agent execute aem/block-collection-and-party
  superpowers-agent execute block-collection  # Using alias

Description:
  Instructs the agent to execute the specified skill.
  Supports skill aliases defined in skill.json files.`);return}let Q=S(q);if(!Q){console.log(`Error: Skill not found: ${q}

Available skills:`),p();return}let{skillFile:Y,sourceType:$}=Q,X=process.cwd(),Z=Y.startsWith(X),W=pq(K.projectAgentsSkills)||pq(K.projectClaudeSkills);if(Z)console.log(`Open this file with your Read Tool: ${Y}

<IMPORTANT>
USE YOUR Read Tool to process this file. DO NOT use \`cat\` or print commands.
</IMPORTANT>`);else if(W&&$!=="project")console.log(`Open this file with your Read Tool: ${Y}

<IMPORTANT>
1. Try using your Read Tool first
2. If your Read tool fails (file outside workspace), use: cat "${Y}"
3. Consider using workspace-local skills for better tool compatibility
   Run: superpowers-agent setup-skills
</IMPORTANT>`);else console.log(`Open this file with your Read Tool: ${Y}

<IMPORTANT>
1. Try using your Read Tool first
2. If your Read tool fails (file outside workspace), use: cat "${Y}"
3. NEVER skip loading the skill content
</IMPORTANT>`)},OQ=()=>{let q=process.argv[3];if(!q){console.log(`Usage: superpowers-agent path <skill-name-or-alias>

Examples:
  superpowers-agent path brainstorming
  superpowers-agent path superpowers:collaboration/brainstorming
  superpowers-agent path aem/block-collection-and-party
  superpowers-agent path block-collection  # Using alias

Description:
  Outputs the file system path of the specified SKILL.md file.
  Supports skill aliases defined in skill.json files.`);return}let Q=S(q);if(!Q){console.log(`Error: Skill not found: ${q}

Available skills:`),p();return}let{skillFile:Y}=Q;console.log(Y)},IQ=()=>{let q=process.argv[3];if(!q){console.log(`Usage: superpowers-agent dir <skill-name>

Examples:
  superpowers-agent dir brainstorming
  superpowers-agent dir aem/block-collection-and-party
  superpowers-agent dir block-collection  # Using alias
  superpowers-agent dir superpowers:testing/test-driven-development

Description:
  Returns the directory path where the specified skill is located.
  Supports skill aliases defined in skill.json files.`);return}let Q=S(q);if(!Q){console.log(`Error: Skill not found: ${q}

Available skills:`),p();return}let{skillFile:Y}=Q,$=JQ(Y);console.log($)},wQ=()=>{C();let q=process.argv[3],Q=process.argv[4];if(!q||!Q){console.log(`Usage: superpowers-agent get-helpers <skill-name> <helper-search-term>

Examples:
  superpowers-agent get-helpers block-collection search-block
  superpowers-agent get-helpers aem/block-collection-and-party get-block-structure
  superpowers-agent get-helpers superpowers:testing/test-driven-development example

Description:
  Searches for helper files within a skill's skill.json configuration.
  Returns the full path to the best matching helper file.
  
  The command uses the skill's aliases if available, so you can use
  short names like "block-collection" instead of the full path.`);return}let Y=S(q);if(!Y){console.log(`Error: Skill not found: ${q}

Available skills:`),p();return}let{skillFile:$}=Y,X=JQ($),Z=Kq(X);if(!Z){console.log(`Error: No skill.json found for skill: ${q}
  Location: ${X}`);return}if(!Z.helpers||!Array.isArray(Z.helpers)||Z.helpers.length===0){console.log(`Error: No helpers defined in skill.json for skill: ${q}
  Location: ${X}`);return}let W=$Q(X,Q);if(!W){console.log(`Error: No helper found matching "${Q}" in skill: ${q}`),console.log(`
Available helpers:`);for(let H of Z.helpers)console.log(`  - ${H}`);return}if(!pq(W)){console.log(`Error: Helper file not found: ${W}
  Defined in skill.json but missing from filesystem`);return}console.log(W)},TQ=()=>{let q=Hq();console.log("Current configuration:"),console.log(JSON.stringify(q,null,2))},bQ=()=>{let q=process.argv[3],Q=process.argv[4];if(!q||Q===void 0){console.log(`Usage: .agents/superpowers-agent config-set <key> <value>

Available keys:
  auto_update (true/false) - Enable/disable automatic updates during bootstrap

Examples:
  .agents/superpowers-agent config-set auto_update false
  .agents/superpowers-agent config-set auto_update true`);return}let Y=Q;if(Q==="true")Y=!0;else if(Q==="false")Y=!1;let $={};$[q]=Y,Oq($),console.log(`✓ Set ${q} = ${Y}`)},MQ=()=>{C();let q=y(!0),Q=y(!1),Y=[];for(let[Z,W]of Object.entries(q))Y.push({alias:Z,url:W,source:"global"});for(let[Z,W]of Object.entries(Q)){let H=Y.findIndex((B)=>B.alias===Z);if(H>=0)Y[H]={alias:Z,url:W,source:"project"};else Y.push({alias:Z,url:W,source:"project"})}if(Y.length===0){console.log(`No repository aliases configured.

Add a repository using:
  superpowers-agent add-repository <git-url> [--as=@alias]`);return}let $=Math.max(5,...Y.map((Z)=>Z.alias.length)),X=Math.max(3,...Y.map((Z)=>Z.url.length));console.log(`Repositories:
`),console.log(`${"Alias".padEnd($)}  ${"URL".padEnd(X)}  Source`);for(let Z of Y)console.log(`${Z.alias.padEnd($)}  ${Z.url.padEnd(X)}  (${Z.source})`);console.log(`
Total: ${Y.length} repository alias(es)`)};import{execSync as aX}from"child_process";import{execSync as u}from"child_process";var bX=()=>{try{return u("git status --porcelain --untracked-files=no",{cwd:K.superpowersRepo,encoding:"utf8",stdio:"pipe",timeout:3000}).trim().length===0}catch{return!1}},Eq=()=>{try{return u("git rev-parse --abbrev-ref HEAD",{cwd:K.superpowersRepo,encoding:"utf8",stdio:"pipe",timeout:3000}).trim()==="main"}catch{return!1}},wq=()=>{try{u("git fetch origin",{cwd:K.superpowersRepo,timeout:5000,stdio:"pipe"});let q=!bX(),Q=u("git rev-parse HEAD",{cwd:K.superpowersRepo,encoding:"utf8",stdio:"pipe"}).trim(),Y=u("git rev-parse origin/main",{cwd:K.superpowersRepo,encoding:"utf8",stdio:"pipe"}).trim();if(Q===Y)return{hasUpdates:!1,hasLocalChanges:q,currentCommit:Q,latestCommit:Y,commitsBehind:0,changedFiles:[]};let X=parseInt(u("git rev-list --count HEAD..origin/main",{cwd:K.superpowersRepo,encoding:"utf8",stdio:"pipe"}).trim(),10),W=u("git diff --name-only HEAD origin/main",{cwd:K.superpowersRepo,encoding:"utf8",stdio:"pipe"}).trim().split(`
`).filter((H)=>H.length>0);return{hasUpdates:!0,hasLocalChanges:q,currentCommit:Q,latestCommit:Y,commitsBehind:X,changedFiles:W}}catch{return{hasUpdates:!1,hasLocalChanges:!1,currentCommit:"",latestCommit:"",commitsBehind:0,changedFiles:[],error:!0}}},NQ=(q)=>{let Q={".github/prompts/":"copilot-prompts",".github/copilot-instructions.md":"copilot-instructions",".cursor/commands/":"cursor-commands","hooks/cursor/":"cursor-hooks",".codex/prompts/":"codex-prompts",".gemini/commands/":"gemini-commands","commands/":"claude-commands",".opencode/command/":"opencode-commands",".opencode/plugins/":"opencode-plugin"},Y=new Set;for(let $ of q)for(let[X,Z]of Object.entries(Q))if($.startsWith(X))Y.add(Z);return Array.from(Y)};import{existsSync as Tq,readdirSync as MX}from"fs";import{join as c,dirname as NX}from"path";import{execSync as bq}from"child_process";var n=()=>{let q=c(K.superpowersRepo,".github","prompts"),Q=c(K.vscodeUserDir,"prompts");if(!Tq(q)){console.log("⚠️  No Copilot prompts to install (source directory not found).");return}try{if(!Tq(Q))bq(`mkdir -p "${Q}"`,{stdio:"pipe"})}catch(X){console.log(`Error creating prompts directory: ${X.message}`);return}let Y;try{Y=MX(q).filter((X)=>X.endsWith(".prompt.md"))}catch(X){console.log(`Error reading prompts directory: ${X.message}`);return}if(Y.length===0){console.log("⚠️  No prompt files found to install.");return}console.log("Installing GitHub Copilot prompts...");let $=0;for(let X of Y)try{let Z=c(q,X),W=c(Q,X);bq(`cp "${Z}" "${W}"`,{stdio:"pipe"}),console.log(`  ✓ Installed ${X}`),$++}catch(Z){console.log(`  ✗ Failed to install ${X}: ${Z.message}`)}if($>0)console.log(`
✓ Installed ${$} prompt(s) to ${Q}
  Use slash commands in GitHub Copilot:
    /brainstorm-with-superpowers - Refine ideas into designs
    /write-a-skill - Create new skills with TDD
    /skills - Discover available skills
    /use-skill - Load and apply a specific skill`)},a=()=>{let q=c(K.superpowersRepo,".github","copilot-instructions.md"),Q=c(K.home,".github","copilot-instructions.md");if(!Tq(q)){console.log("⚠️  No Copilot instructions to install (source file not found).");return}let Y=NX(Q);try{if(!Tq(Y))bq(`mkdir -p "${Y}"`,{stdio:"pipe"})}catch($){console.log(`Error creating .github directory: ${$.message}`);return}try{bq(`cp "${q}" "${Q}"`,{stdio:"pipe"}),console.log(`✓ Installed GitHub Copilot universal instructions
  Location: ${Q}
  GitHub Copilot will now use Superpowers skills universally in all workspaces`)}catch($){console.log(`✗ Failed to install instructions: ${$.message}`)}};import{existsSync as Mq,readdirSync as FQ}from"fs";import{join as R}from"path";import{execSync as l}from"child_process";var o=()=>{let q=R(K.superpowersRepo,".cursor","commands"),Q=R(K.home,".cursor","commands");if(!Mq(q)){console.log("⚠️  No Cursor commands to install (source directory not found).");return}try{if(!Mq(Q))l(`mkdir -p "${Q}"`,{stdio:"pipe"})}catch(X){console.log(`Error creating Cursor commands directory: ${X.message}`);return}let Y;try{Y=FQ(q).filter((X)=>X.endsWith(".md"))}catch(X){console.log(`Error reading Cursor commands directory: ${X.message}`);return}if(Y.length===0){console.log("⚠️  No command files found to install.");return}console.log("Installing Cursor commands...");let $=0;for(let X of Y)try{let Z=R(q,X),W=R(Q,X);l(`cp "${Z}" "${W}"`,{stdio:"pipe"}),console.log(`  ✓ Installed ${X}`),$++}catch(Z){console.log(`  ✗ Failed to install ${X}: ${Z.message}`)}if($>0)console.log(`
✓ Installed ${$} command(s) to ${Q}
  Use slash commands in Cursor:
    /brainstorm-with-superpowers - Refine ideas into designs
    /write-a-skill - Create new skills with TDD
    /skills - Discover available skills
    /use-skill - Load and apply a specific skill`)},i=()=>{let q=R(K.superpowersRepo,"hooks","cursor"),Q=R(K.home,".cursor","hooks"),Y=R(q,"hooks.json"),$=R(K.home,".cursor","hooks.json");if(!Mq(q)){console.log("⚠️  No Cursor hooks to install (source directory not found).");return}try{if(!Mq(Q))l(`mkdir -p "${Q}"`,{stdio:"pipe"})}catch(W){console.log(`Error creating Cursor hooks directory: ${W.message}`);return}try{l(`cp "${Y}" "${$}"`,{stdio:"pipe"}),console.log("  ✓ Installed hooks.json")}catch(W){console.log(`  ✗ Failed to install hooks.json: ${W.message}`);return}let X;try{X=FQ(q).filter((W)=>W.endsWith(".sh"))}catch(W){console.log(`Error reading hooks directory: ${W.message}`);return}if(X.length===0){console.log("⚠️  No hook scripts found to install.");return}console.log("Installing Cursor hooks...");let Z=0;for(let W of X)try{let H=R(q,W),B=R(Q,W);l(`cp "${H}" "${B}"`,{stdio:"pipe"}),l(`chmod +x "${B}"`,{stdio:"pipe"}),console.log(`  ✓ Installed ${W}`),Z++}catch(H){console.log(`  ✗ Failed to install ${W}: ${H.message}`)}if(Z>0)console.log(`
✓ Installed ${Z} hook(s) to ${Q}
  Cursor will now check for skills before each prompt submission
  Restart Cursor for hooks to take effect`)};import{existsSync as CQ,readdirSync as FX}from"fs";import{join as Nq}from"path";import{execSync as RQ}from"child_process";var r=()=>{let q=Nq(K.superpowersRepo,".codex","prompts"),Q=Nq(K.home,".codex","prompts");if(!CQ(q)){console.log("⚠️  No Codex prompts to install (source directory not found).");return}try{if(!CQ(Q))RQ(`mkdir -p "${Q}"`,{stdio:"pipe"})}catch(X){console.log(`Error creating Codex prompts directory: ${X.message}`);return}let Y;try{Y=FX(q).filter((X)=>X.endsWith(".md"))}catch(X){console.log(`Error reading Codex prompts directory: ${X.message}`);return}if(Y.length===0){console.log("⚠️  No prompt files found to install.");return}console.log("Installing OpenAI Codex prompts...");let $=0;for(let X of Y)try{let Z=Nq(q,X),W=Nq(Q,X);RQ(`cp "${Z}" "${W}"`,{stdio:"pipe"}),console.log(`  ✓ Installed ${X}`),$++}catch(Z){console.log(`  ✗ Failed to install ${X}: ${Z.message}`)}if($>0)console.log(`
✓ Installed ${$} prompt(s) to ${Q}
  Use slash commands in OpenAI Codex:
    /prompts:brainstorm - Refine ideas into designs
    /prompts:write-skill - Create new skills with TDD
    /prompts:skills - Discover available skills
    /prompts:use-skill - Load and apply a specific skill
  Note: Restart Codex or open a new session to reload prompts`)};import{existsSync as xQ,readdirSync as CX}from"fs";import{join as Fq}from"path";import{execSync as DQ}from"child_process";var s=()=>{let q=Fq(K.superpowersRepo,".gemini","commands"),Q=Fq(K.home,".gemini","commands");if(!xQ(q)){console.log("⚠️  No Gemini commands to install (source directory not found).");return}try{if(!xQ(Q))DQ(`mkdir -p "${Q}"`,{stdio:"pipe"})}catch(X){console.log(`Error creating Gemini commands directory: ${X.message}`);return}let Y;try{Y=CX(q).filter((X)=>X.endsWith(".toml"))}catch(X){console.log(`Error reading Gemini commands directory: ${X.message}`);return}if(Y.length===0){console.log("⚠️  No command files found to install.");return}console.log("Installing Gemini commands...");let $=0;for(let X of Y)try{let Z=Fq(q,X),W=Fq(Q,X);DQ(`cp "${Z}" "${W}"`,{stdio:"pipe"}),console.log(`  ✓ Installed ${X}`),$++}catch(Z){console.log(`  ✗ Failed to install ${X}: ${Z.message}`)}if($>0)console.log(`
✓ Installed ${$} command(s) to ${Q}
  Use slash commands in Gemini:
    /brainstorm-with-superpowers - Refine ideas into designs
    /write-a-skill - Create new skills with TDD
    /skills - Discover available skills
    /use-skill - Load and apply a specific skill`)};import{existsSync as jQ,readdirSync as RX}from"fs";import{join as Cq}from"path";import{execSync as PQ}from"child_process";var t=()=>{let q=Cq(K.superpowersRepo,"commands"),Q=Cq(K.home,".claude","commands");if(!jQ(q)){console.log("⚠️  No Claude commands to install (source directory not found).");return}try{if(!jQ(Q))PQ(`mkdir -p "${Q}"`,{stdio:"pipe"})}catch(X){console.log(`Error creating Claude commands directory: ${X.message}`);return}let Y;try{Y=RX(q).filter((X)=>X.endsWith(".md"))}catch(X){console.log(`Error reading Claude commands directory: ${X.message}`);return}if(Y.length===0){console.log("⚠️  No command files found to install.");return}console.log("Installing Claude Code commands...");let $=0;for(let X of Y)try{let Z=Cq(q,X),W=Cq(Q,X);PQ(`cp "${Z}" "${W}"`,{stdio:"pipe"}),console.log(`  ✓ Installed ${X}`),$++}catch(Z){console.log(`  ✗ Failed to install ${X}: ${Z.message}`)}if($>0)console.log(`
✓ Installed ${$} command(s) to ${Q}
  Use slash commands in Claude Code:
    /brainstorm - Refine ideas into designs
    /execute-plan - Execute plans in batches
    /write-plan - Create implementation plans
    /skills - Discover available skills
    /use-skill - Load and apply a specific skill`)};import{existsSync as Bq,readdirSync as xX,lstatSync as DX,symlinkSync as fQ,unlinkSync as jX,mkdirSync as PX,readlinkSync as fX}from"fs";import{join as d,dirname as bZ}from"path";import{execSync as yQ}from"child_process";import{platform as yX}from"os";var e=()=>{let q=d(K.superpowersRepo,".opencode","command"),Q=d(K.home,".config","opencode","command");if(!Bq(q)){console.log("⚠️  No opencode commands to install (source directory not found).");return}try{if(!Bq(Q))yQ(`mkdir -p "${Q}"`,{stdio:"pipe"})}catch(X){console.log(`Error creating opencode commands directory: ${X.message}`);return}let Y;try{Y=xX(q).filter((X)=>X.endsWith(".md"))}catch(X){console.log(`Error reading opencode commands directory: ${X.message}`);return}if(Y.length===0){console.log("⚠️  No command files found to install.");return}console.log("Installing opencode commands...");let $=0;for(let X of Y)try{let Z=d(q,X),W=d(Q,X);yQ(`cp "${Z}" "${W}"`,{stdio:"pipe"}),console.log(`  ✓ Installed ${X}`),$++}catch(Z){console.log(`  ✗ Failed to install ${X}: ${Z.message}`)}if($>0)console.log(`
✓ Installed ${$} command(s) to ${Q}
  Use slash commands in opencode:
    /brainstorm - Refine ideas into designs
    /execute-plan - Execute plans in batches
    /write-plan - Create implementation plans
    /write-skill - Create new skills with TDD
    /skills - Discover available skills
    /use-skill - Load and apply a specific skill`)},cq=(q)=>{try{return DX(q).isSymbolicLink()}catch{return!1}},gX=(q,Q)=>{try{if(!cq(q))return!1;return fX(q)===Q}catch{return!1}},Vq=()=>{let q=d(K.superpowersRepo,".opencode","plugins","superpowers-agent.js"),Q=d(K.home,".config","opencode","plugins"),Y=d(Q,"superpowers-agent.js");if(console.log("Installing OpenCode plugin symlink..."),!Bq(q))return console.log("⚠️  Source plugin not found, skipping symlink creation"),console.log(`   Expected at: ${q}`),{created:!1,error:"Source plugin not found"};if(gX(Y,q))return console.log("✓ Plugin symlink already exists and is correct"),{created:!1,existed:!0};if(Bq(Y)&&!cq(Y))return console.log("⚠️  Warning: A file already exists at the destination path"),console.log(`   Path: ${Y}`),console.log("   Skipping symlink creation to avoid overwriting existing file"),console.log("   To use superpowers-agent plugin, manually remove or rename the existing file"),{created:!1,error:"File already exists at destination"};if(!Bq(Q))try{PX(Q,{recursive:!0}),console.log(`✓ Created ${Q.replace(K.home,"~")}`)}catch(X){return console.log(`⚠️  Failed to create plugins directory: ${X.message}`),{created:!1,error:`Failed to create directory: ${X.message}`}}if(cq(Y))try{jX(Y)}catch(X){return console.log(`⚠️  Failed to remove existing symlink: ${X.message}`),{created:!1,error:`Failed to remove existing symlink: ${X.message}`}}let $=yX();try{if($==="win32")fQ(q,Y,"file");else fQ(q,Y);let X=q.replace(K.home,"~"),Z=Y.replace(K.home,"~");return console.log(`✓ Created symlink: ${Z}`),console.log(`  -> ${X}`),{created:!0}}catch(X){if($==="win32"&&X.code==="EPERM")return console.log("⚠️  Windows requires Developer Mode or admin privileges for symlinks"),console.log("   Enable Developer Mode: Settings > Update & Security > For developers"),{created:!1,error:"Windows symlink permission denied"};return console.log(`⚠️  Failed to create symlink: ${X.message}`),{created:!1,error:`Failed to create symlink: ${X.message}`}}};import{existsSync as A,readdirSync as SX,lstatSync as kX,symlinkSync as gQ,unlinkSync as uX,mkdirSync as Gq,readlinkSync as dX}from"fs";import{join as G,dirname as hX}from"path";import{homedir as w,platform as mX}from"os";var SQ=[{name:"claude",parentDir:()=>G(w(),".claude"),skillsDir:()=>G(w(),".claude","skills"),superpowersTarget:"superpowers"},{name:"copilot",parentDir:()=>G(w(),".copilot"),skillsDir:()=>G(w(),".copilot","skills"),superpowersTarget:"superpowers"},{name:"opencode",parentDir:()=>G(w(),".config","opencode"),skillsDir:()=>G(w(),".config","opencode","skill"),superpowersTarget:"superpowers"},{name:"cursor",parentDir:()=>G(w(),".cursor"),skillsDir:()=>G(w(),".cursor","skills"),superpowersTarget:"superpowers"},{name:"gemini",parentDir:()=>G(w(),".gemini"),skillsDir:()=>G(w(),".gemini","skills"),superpowersTarget:"superpowers"},{name:"codex",parentDir:()=>G(w(),".codex"),skillsDir:()=>G(w(),".codex","skills"),superpowersTarget:"superpowers"}],pX=[{name:"claude",agentDir:(q)=>G(q,".claude"),skillsDir:(q)=>G(q,".claude","skills"),detect:(q)=>A(G(q,".claude"))},{name:"copilot",agentDir:(q)=>G(q,".github"),skillsDir:(q)=>G(q,".github","skills"),detect:(q)=>{let Q=A(G(q,".github")),Y=A(G(q,"AGENTS.md"))||A(G(q,".agents","AGENTS.md"));return Q&&Y}},{name:"opencode",agentDir:(q)=>G(q,".opencode"),skillsDir:(q)=>G(q,".opencode","skill"),detect:(q)=>{let Q=A(G(q,".opencode")),Y=A(G(q,"AGENTS.md"))||A(G(q,".agents","AGENTS.md"))||A(G(q,".opencode","AGENTS.md"));return Q&&Y}},{name:"cursor",agentDir:(q)=>G(q,".cursor"),skillsDir:(q)=>G(q,".cursor","skills"),detect:(q)=>A(G(q,".cursor"))},{name:"gemini",agentDir:(q)=>G(q,".gemini"),skillsDir:(q)=>G(q,".gemini","skills"),detect:(q)=>{let Q=A(G(q,".gemini")),Y=A(G(q,"GEMINI.md"))||A(G(q,".agents","GEMINI.md"));return Q&&Y}},{name:"codex",agentDir:(q)=>G(q,".codex"),skillsDir:(q)=>G(q,".codex","skills"),detect:(q)=>{let Q=A(G(q,".codex")),Y=A(G(q,"AGENTS.md"))||A(G(q,".agents","AGENTS.md"));return Q&&Y}}],nq=(q)=>{try{return kX(q).isSymbolicLink()}catch{return!1}},cX=(q,Q)=>{try{if(!nq(q))return!1;return dX(q)===Q}catch{return!1}},aq=(q,Q)=>{if(!A(q))return{created:!1,existed:!1,error:`Source does not exist: ${q}`};if(cX(Q,q))return{created:!1,existed:!0};if(A(Q)||nq(Q))if(nq(Q))try{uX(Q)}catch(X){return{created:!1,existed:!1,error:`Failed to remove existing symlink: ${X.message}`}}else return{created:!1,existed:!1,error:`Target exists and is not a symlink: ${Q}`};let Y=hX(Q);if(!A(Y))try{Gq(Y,{recursive:!0})}catch(X){return{created:!1,existed:!1,error:`Failed to create parent directory: ${X.message}`}}let $=mX();try{if($==="win32")gQ(q,Q,"junction");else gQ(q,Q,"dir");return{created:!0,existed:!1}}catch(X){if($==="win32"&&X.code==="EPERM")return{created:!1,existed:!1,error:"Windows requires Developer Mode or admin privileges for symlinks. Enable Developer Mode: Settings > Update & Security > For developers"};return{created:!1,existed:!1,error:`Failed to create symlink: ${X.message}`}}};var kQ=(q,Q,Y="skills")=>{let $=zq(!0);if(!$.symlinks)$.symlinks={};if(!$.symlinks[q])$.symlinks[q]={superpowers:null,skills:[]};if(Y==="superpowers")$.symlinks[q].superpowers=Q;else{let X=$.symlinks[q].skills||[];if(!X.includes(Q))X.push(Q),$.symlinks[q].skills=X}Iq($,!0)};var nX=(q,Q={})=>{let Y=K.homeSuperpowersSkills,$=G(q.skillsDir(),q.superpowersTarget),X=aq(Y,$);if(X.created){kQ(q.name,$,"superpowers");let Z=Y.replace(w(),"~"),W=$.replace(w(),"~");return console.log(`  ✓ Created ${W} -> ${Z}`),{created:!0}}else if(X.existed)return{created:!1,existed:!0};else if(X.error)return console.log(`  ⚠️  ${q.name} superpowers: ${X.error}`),{created:!1,error:X.error};return{created:!1}},uQ=(q,Q={})=>{let Y=K.homePersonalSkills;if(!A(Y))return{created:0,existed:0};let $;try{$=SX(Y,{withFileTypes:!0}).filter((Z)=>Z.isDirectory()&&!Z.name.startsWith(".")).map((Z)=>Z.name)}catch{return{created:0,existed:0}}if($.length===0)return{created:0,existed:0};let X={created:0,existed:0,names:[]};for(let Z of $){let W=G(Y,Z),H=G(q.skillsDir(),Z),B=aq(W,H);if(B.created)kQ(q.name,H,"skills"),X.created++,X.names.push(Z);else if(B.existed)X.existed++;else if(B.error)console.log(`  ⚠️  ${Z}: ${B.error}`)}if(X.created>0)console.log(`  ✓ Synced ${X.created} personal skill(s): ${X.names.join(", ")}`);return X},Rq=(q={})=>{let{force:Q=!1}=q;for(let Y of SQ){let $=Y.parentDir(),X=Y.skillsDir();if(!A($))if(Q)try{Gq($,{recursive:!0}),console.log(`✓ Created ${$.replace(w(),"~")}`)}catch(Z){console.log(`⚠️  Failed to create ${$.replace(w(),"~")}: ${Z.message}`);continue}else{console.log(`⚠️  Skipping ${Y.name} (${$.replace(w(),"~")} not found)`),console.log("   Use --force to create directory");continue}if(!A(X))try{Gq(X,{recursive:!0})}catch(Z){console.log(`⚠️  Failed to create skills directory for ${Y.name}: ${Z.message}`);continue}console.log(`${Y.name}:`),nX(Y,q),uQ(Y,q)}},lq=(q={})=>{let{force:Q=!1}=q;for(let Y of SQ){let $=Y.parentDir(),X=Y.skillsDir();if(!A($)){if(!Q)continue;try{Gq($,{recursive:!0})}catch{continue}}if(!A(X))try{Gq(X,{recursive:!0})}catch{continue}uQ(Y,q)}};var dQ=(q={})=>{let Q=q.projectRoot||process.cwd(),Y=G(Q,".agents","skills");if(!A(Y))return{created:0,existed:0,skipped:0,errors:[]};let $={created:0,existed:0,skipped:0,errors:[]};for(let X of pX){if(!X.detect(Q)){$.skipped++;continue}let Z=X.skillsDir(Q),W=X.agentDir(Q);if(!A(W)){$.skipped++;continue}let H=aq(Y,Z);if(H.created){let B=Z.replace(Q,".");console.log(`  ✓ Created ${B} -> .agents/skills`),$.created++}else if(H.existed)$.existed++;else if(H.error)console.log(`  ⚠️  ${X.name}: ${H.error}`),$.errors.push({platform:X.name,error:H.error})}return $};var lX=(q)=>{let Y={"copilot-prompts":n,"copilot-instructions":a,"cursor-commands":o,"cursor-hooks":i,"codex-prompts":r,"gemini-commands":s,"claude-commands":t,"opencode-commands":e,"opencode-plugin":Vq}[q];if(Y)try{return Y(),{success:!0,integration:q}}catch($){return{success:!1,integration:q,error:$.message}}return{success:!1,integration:q,error:"Unknown integration"}},oX=()=>{console.log("Installing aliases...")},xq=(q={})=>{let Q=q.skipReinstall||!1;console.log(`# Checking for Superpowers updates...
`);let Y=wq();if(Y.error){console.log("⚠️  Could not check for updates (network issue)");return}if(!Y.hasUpdates){console.log("✓ Already up to date");return}if(Y.hasLocalChanges){console.log(`⚠️  Cannot auto-update: local changes detected
   Commit or stash your changes first, then run update again
   Or manually update: cd ${K.superpowersRepo} && git pull`);return}if(!Eq()){console.log(`⚠️  Not on main branch, skipping auto-update
   Switch to main branch first: cd ${K.superpowersRepo} && git checkout main`);return}console.log(`\uD83D\uDCE6 Updating from ${Y.currentCommit.substring(0,7)} to ${Y.latestCommit.substring(0,7)}
   (${Y.commitsBehind} new commit${Y.commitsBehind>1?"s":""})
`);try{aX("git pull origin main",{cwd:K.superpowersRepo,stdio:"pipe",timeout:1e4}),console.log("✓ Updated superpowers repository")}catch(W){console.log(`✗ Git pull failed: ${W.message}
   Please resolve manually and try again`);return}if(console.log(""),Oq({last_updated_commit:Y.latestCommit,last_update_check:new Date().toISOString()}),Q){console.log(`ℹ️  Skipping integration reinstall (--no-reinstall flag)

✓ Update complete!`);return}let $=NQ(Y.changedFiles);if($.length===0){console.log(`ℹ️  No integration files changed, skipping reinstalls

✓ Update complete!`);return}console.log(`\uD83D\uDD04 Reinstalling updated integrations:
`);let X=[];for(let W of $){let H=lX(W);if(X.push(H),H.success)console.log(`  ✓ ${W}`);else console.log(`  ✗ ${W} (${H.error})`)}console.log(""),console.log(`
---
`),oX(),console.log(`
---
`),console.log(`## Syncing Skill Symlinks
`),Rq(),console.log(`
---
`),console.log(`## OpenCode Plugin Symlink
`),Vq();let Z=X.filter((W)=>!W.success);if(Z.length>0){console.log("⚠️  Update completed with errors:");for(let W of Z){console.log(`  - ${W.integration} failed to install`);let H=`install-${W.integration}`;console.log(`    Run manually: superpowers-agent ${H}`)}}else console.log("✓ Update complete!")},hQ=async()=>{C();let q=g();console.log(`Current version: ${q}`);try{let Q=await vQ();if(console.log(`Latest version: ${Q}`),LQ(Q,q))console.log("Update available: Yes"),process.exit(1);else console.log("You are up to date"),process.exit(0)}catch(Q){console.log(`Error checking for updates: ${Q.message}`),process.exit(1)}},mQ=()=>{let q=g();console.log(q)};import{existsSync as U,readFileSync as Uq,writeFileSync as oq}from"fs";import{join as I,dirname as rX,parse as vq}from"path";import{execSync as F}from"child_process";import{platform as sX}from"os";import{execSync as iX}from"child_process";var _q=(q)=>{try{return iX(`which ${q}`,{stdio:"pipe",timeout:2000}),!0}catch{return!1}};var _={opencode:{check:()=>_q("opencode"),cli:!0,name:"OpenCode",installUrl:"https://opencode.ai/docs/installation",bootstrapCommand:"install-opencode-commands"},claude:{check:()=>_q("claude"),cli:!0,name:"Claude Code",installUrl:"https://code.claude.com/docs/en/installation",bootstrapCommand:"install-claude-commands"},gemini:{check:()=>_q("gemini"),cli:!0,name:"Gemini",installUrl:"https://cloud.google.com/gemini/docs/cli/install",bootstrapCommand:"install-gemini-commands"},codex:{check:()=>_q("codex"),cli:!0,name:"Codex",installUrl:"https://developers.openai.com/codex/docs/installation",bootstrapCommand:"install-codex-prompts"},cursor:{check:()=>!0,cli:!1,name:"Cursor",bootstrapCommand:"install-cursor-commands"},copilot:{check:()=>!0,cli:!1,name:"GitHub Copilot",bootstrapCommand:"install-copilot-prompts"}},pQ=()=>{let q=[];if(_.copilot.check())q.push("github-copilot");if(_.cursor.check())q.push("cursor");if(_.claude.check())q.push("claude-code");if(_.opencode.check())q.push("opencode");if(_.gemini.check())q.push("gemini");if(_.codex.check())q.push("codex");return q};var tX=()=>{let q=I(K.superpowersRepo,".agents","templates","TOOLS.md.template");if(U(q))try{return Uq(q,"utf8").trim()}catch(Q){return"### Using Tools\n\nUse your native skill tool. Fallback: `superpowers-agent find-skills`"}return""},Lq=(q,Q,Y,$=!0)=>{let X=U(q);if(!X&&!$)return{updated:!1,created:!1,skipped:!0};let Z=tX(),W=Q.replace(/\{\{TOOL_MAPPINGS\}\}/g,Z),H=new Date().toISOString().split("T")[0];W=W.replace(/\{\{DATE\}\}/g,H),W=W.replace(/\{\{SUPERPOWERS_PATH\}\}/g,K.superpowersRepo);let B=`<!-- SUPERPOWERS_SKILLS_START -->
${W}
<!-- SUPERPOWERS_SKILLS_END -->`;if(X){let z=new Date().toISOString().split("T")[0],E=`${q}.backup-${z}`;try{F(`cp "${q}" "${E}"`,{stdio:"pipe"})}catch(M){return{updated:!1,error:!0,message:`Failed to backup: ${M.message}`}}let V;try{V=Uq(q,"utf8")}catch(M){return{updated:!1,error:!0,message:`Failed to read: ${M.message}`}}let O="<!-- SUPERPOWERS_SKILLS_START -->",v="<!-- SUPERPOWERS_SKILLS_END -->",b;if(V.includes(O)&&V.includes(v)){let M=new RegExp(`${O}[\\s\\S]*?${v}`,"g");b=V.replace(M,B)}else{let M=V.match(/^#\s+.+$/m);if(M){let D=V.indexOf(`
`,M.index)+1;b=V.slice(0,D)+`
`+B+`
`+V.slice(D)}else b=B+`

`+V}try{return oq(q,b,"utf8"),{updated:!0,created:!1,backup:E}}catch(M){return{updated:!1,error:!0,message:`Failed to write: ${M.message}`}}}else try{let z=rX(q);if(!U(z))F(`mkdir -p "${z}"`,{stdio:"pipe"});let O=`# ${vq(q).base.toUpperCase()}

`+B;return oq(q,O,"utf8"),{updated:!1,created:!0}}catch(z){return{updated:!1,created:!1,error:!0,message:`Failed to create: ${z.message}`}}},eX=()=>{console.log("Installing Unix aliases...");let q=I(K.superpowersRepo,".agents","superpowers-agent"),Q=I(K.home,".local","bin"),Y=I(Q,"superpowers-agent"),$=I(Q,"superpowers");if(!U(q)){console.log("⚠️  superpowers-agent executable not found");return}if(!U(Q))try{F(`mkdir -p "${Q}"`,{stdio:"pipe"}),console.log(`✓ Created ${Q}`)}catch(Z){console.log(`⚠️  Failed to create ${Q}: ${Z.message}`);return}try{if(U(Y))F(`rm "${Y}"`,{stdio:"pipe"});F(`ln -s "${q}" "${Y}"`,{stdio:"pipe"}),console.log(`✓ Created symlink: superpowers-agent -> ${q}`)}catch(Z){console.log(`⚠️  Failed to create superpowers-agent symlink: ${Z.message}`)}try{if(U($))F(`rm "${$}"`,{stdio:"pipe"});F(`ln -s "${q}" "${$}"`,{stdio:"pipe"}),console.log(`✓ Created symlink: superpowers -> ${q}`)}catch(Z){console.log(`⚠️  Failed to create superpowers symlink: ${Z.message}`)}if(!(process.env.PATH||"").includes(Q))console.log(`
⚠️  Warning: ${Q} is not in your PATH`),console.log("   Add this to your shell profile (~/.bashrc, ~/.zshrc, etc.):"),console.log(`   export PATH="$HOME/.local/bin:$PATH"
`)},qY=()=>{console.log("Installing Windows aliases..."),console.log("✓ Windows alias installation (implementation pending)")},iq=()=>{let q=sX();if(console.log(`## Installing Universal Aliases
`),q==="win32")return qY();else return eX()},cQ=()=>{C(),console.log(`# Setting up Superpowers skills for this project
`);let q=process.cwd(),Q=I(q,".agents"),Y=I(Q,"skills"),$=I(q,"AGENTS.md"),X=I(Q,"AGENTS.md"),Z=U($)?$:X,W=I(K.superpowersRepo,".agents","templates","AGENTS.md.template");if(!U(W)){console.log(`✗ Error: AGENTS.md template not found
  Expected at: ${W}
  Please update your Superpowers installation`);return}if(!U(Q))try{F(`mkdir -p "${Q}"`,{stdio:"pipe"}),console.log("✓ Created .agents/ directory")}catch(J){console.log(`✗ Failed to create .agents/ directory: ${J.message}`);return}else console.log("✓ .agents/ directory exists");if(!U(Y))try{F(`mkdir -p "${Y}"`,{stdio:"pipe"}),F(`touch "${I(Y,".gitkeep")}"`,{stdio:"pipe"}),console.log("✓ Created .agents/skills/ directory")}catch(J){console.log(`✗ Failed to create skills directory: ${J.message}`);return}else console.log("✓ .agents/skills/ directory exists");let H=I(Q,"docs"),B=I(H,"SUPERPOWERS.md"),z=I(K.superpowersRepo,".agents","templates","SUPERPOWERS.md.template");if(!U(H))try{F(`mkdir -p "${H}"`,{stdio:"pipe"}),console.log("✓ Created .agents/docs/ directory")}catch(J){console.log(`⚠️  Failed to create docs directory: ${J.message}`)}else console.log("✓ .agents/docs/ directory exists");let E=U(Z);if(E){let J=new Date().toISOString().split("T")[0],Jq=`${Z}.backup-${J}`;try{F(`cp "${Z}" "${Jq}"`,{stdio:"pipe"}),console.log(`✓ Backed up existing AGENTS.md to ${vq(Jq).base}`)}catch(XX){console.log(`✗ Failed to backup AGENTS.md: ${XX.message}`);return}}let V;try{V=Uq(W,"utf8")}catch(J){console.log(`✗ Failed to read template: ${J.message}`);return}let O=g();if(V=V.replace(/\{\{VERSION\}\}/g,O),U(z))try{let J=Uq(z,"utf8"),Jq=new Date().toISOString().split("T")[0];J=J.replace(/\{\{VERSION\}\}/g,O),J=J.replace(/\{\{DATE\}\}/g,Jq),oq(B,J,"utf8"),console.log("✓ Created .agents/docs/SUPERPOWERS.md")}catch(J){console.log(`⚠️  Failed to create SUPERPOWERS.md: ${J.message}`)}else console.log("⚠️  SUPERPOWERS.md.template not found");let v=[],b=I(q,".github","copilot-instructions.md"),M=I(K.home,".github","copilot-instructions.md");if(U(b)||U(M)||_.copilot.check())v.push("github-copilot");let D=I(q,"CLAUDE.md"),sq=I(Q,"CLAUDE.md");if(U(D)||U(sq)||_.claude.check())v.push("claude-code");let Qq=I(q,"GEMINI.md"),tq=I(Q,"GEMINI.md");if(U(Qq)||U(tq)||_.gemini.check())v.push("gemini");if(_.cursor.check())v.push("cursor");if(_.opencode.check())v.push("opencode");if(_.codex.check())v.push("codex");console.log(`
Detected platforms for project: ${v.join(", ")||"none"}
`);let QX=v.filter((J)=>["github-copilot","cursor","opencode","codex"].includes(J)),Xq=Lq(Z,V,QX,!E);if(Xq.created)console.log(`✓ Created AGENTS.md with platform tool mappings (${Z===$?"root":".agents/"})`);else if(Xq.updated)console.log(`✓ Updated AGENTS.md with platform tool mappings (${Z===$?"root":".agents/"})`);else if(Xq.error)console.log("⚠️  Failed to update AGENTS.md");let fq=U(D)?D:sq,j=Lq(fq,V,["claude-code"],!1);if(j.created)console.log(`✓ Created CLAUDE.md with Claude Code tool mappings (${fq===D?"root":".agents/"})`);else if(j.updated){if(console.log(`✓ Updated CLAUDE.md with Claude Code tool mappings (${fq===D?"root":".agents/"})`),j.backup)console.log(`  Backed up to ${vq(j.backup).base}`)}else if(j.skipped)console.log("ℹ️  Skipped CLAUDE.md (does not exist)");else if(j.error)console.log("⚠️  Failed to update CLAUDE.md");let yq=U(Qq)?Qq:tq,P=Lq(yq,V,["gemini"],!1);if(P.created)console.log(`✓ Created GEMINI.md with Gemini tool mappings (${yq===Qq?"root":".agents/"})`);else if(P.updated){if(console.log(`✓ Updated GEMINI.md with Gemini tool mappings (${yq===Qq?"root":".agents/"})`),P.backup)console.log(`  Backed up to ${vq(P.backup).base}`)}else if(P.skipped)console.log("ℹ️  Skipped GEMINI.md (does not exist)");else if(P.error)console.log("⚠️  Failed to update GEMINI.md");let gq=I(q,".github","copilot-instructions.md"),eq=!U($)&&!U(X),h={skipped:!0};if(U(gq)&&eq){if(h=Lq(gq,V,["github-copilot"],!1),h.updated){if(console.log("✓ Updated .github/copilot-instructions.md with GitHub Copilot tool mappings"),h.backup)console.log(`  Backed up to ${vq(h.backup).base}`)}else if(h.error)console.log("⚠️  Failed to update .github/copilot-instructions.md")}else if(U(gq)&&!eq)console.log("ℹ️  Skipped .github/copilot-instructions.md (AGENTS.md exists, using that instead)");else console.log("ℹ️  Skipped .github/copilot-instructions.md (does not exist)");console.log(`
## Syncing Project Skill Symlinks
`);let m=dQ({projectRoot:q});if(m.created>0)console.log(`
✓ Created ${m.created} project skill symlink(s)`);else if(m.existed>0)console.log("ℹ️  Project skill symlinks already exist");else if(m.errors.length>0)console.log("⚠️  Some symlinks could not be created");else console.log("ℹ️  No agent directories detected for symlinking");let f=`
# Setup complete!

Your project now has:
  - .agents/ directory structure`;if(Xq.updated||Xq.created)f+=`
  - AGENTS.md with universal skills instructions`;if(j.updated||j.created)f+=`
  - CLAUDE.md with Claude Code skills instructions`;if(P.updated||P.created)f+=`
  - GEMINI.md with Gemini skills instructions`;if(h.updated)f+=`
  - .github/copilot-instructions.md with GitHub Copilot skills instructions`;if(m.created>0)f+=`
  - ${m.created} project skill symlink(s) to agent directories`;f+=`
  - .agents/skills/ ready for project-specific skills`,f+=`
  - .agents/docs/SUPERPOWERS.md for detailed reference
`,console.log(f)},nQ=()=>{if(C(),console.log(`# Superpowers Bootstrap for Agents
# ==================================
`),!process.argv.includes("--no-update")){let z=Hq(),E=wq();if(E.error)console.log(`## Update Check

⚠️  Could not check for updates (network issue)

---
`);else if(E.hasUpdates)if(z.auto_update&&!E.hasLocalChanges&&Eq())console.log(`## Auto-Update
`),xq(),console.log(`
---
`);else{if(console.log(`## Update Available
`),E.hasLocalChanges)console.log("⚠️  Your superpowers installation is behind the latest version.\n    Cannot auto-update: local changes detected\n    To update, commit/stash changes then run: `superpowers-agent update`");else if(!Eq())console.log("⚠️  Your superpowers installation is behind the latest version.\n    Cannot auto-update: not on main branch\n    To update, switch to main then run: `superpowers-agent update`");else console.log("⚠️  Your superpowers installation is behind the latest version.\n    To update, run: `superpowers-agent update`\n    Or enable auto-update: `superpowers-agent config-set auto_update true`");console.log(`
---
`)}}if(iq(),console.log(`---
`),console.log(`## GitHub Copilot Integration
`),n(),console.log(""),a(),console.log(`
---
`),console.log(`## Cursor Integration
`),o(),console.log(""),i(),console.log(`
---
`),console.log(`## OpenAI Codex Integration
`),_.codex.check())r();else console.log(`⚠️  Skipped (${_.codex.name} CLI not detected)
\uD83D\uDCA1 To enable Codex integration:
   1. Install Codex: ${_.codex.installUrl}
   2. Run: superpowers-agent ${_.codex.bootstrapCommand}`);if(console.log(`
---
`),console.log(`## Gemini Integration
`),_.gemini.check())s();else console.log(`⚠️  Skipped (${_.gemini.name} CLI not detected)
\uD83D\uDCA1 To enable Gemini integration:
   1. Install Gemini: ${_.gemini.installUrl}
   2. Run: superpowers-agent ${_.gemini.bootstrapCommand}`);if(console.log(`
---
`),console.log(`## Claude Code Integration
`),_.claude.check())t();else console.log(`⚠️  Skipped (${_.claude.name} CLI not detected)
\uD83D\uDCA1 To enable Claude Code integration:
   1. Install Claude Code: ${_.claude.installUrl}
   2. Run: superpowers-agent ${_.claude.bootstrapCommand}`);if(console.log(`
---
`),console.log(`## OpenCode Integration
`),_.opencode.check())e(),console.log(""),Vq();else console.log(`⚠️  Skipped (${_.opencode.name} CLI not detected)
\uD83D\uDCA1 To enable OpenCode integration:
   1. Install OpenCode: ${_.opencode.installUrl}
   2. Run: superpowers-agent ${_.opencode.bootstrapCommand}`);console.log(`
---
`),console.log(`## Generating Platform-Specific Files
`);let Z=pQ();console.log(`Detected platforms: ${Z.join(", ")||"none"}
`);let W=I(K.superpowersRepo,".agents","templates","AGENTS.md.template"),H="";try{H=Uq(W,"utf8")}catch(z){console.log(`⚠️  Could not read AGENTS.md template: ${z.message}
`)}if(H){let z=g();H=H.replace(/\{\{VERSION\}\}/g,z);let E=I(K.home,".agents","AGENTS.md"),V=Lq(E,H,Z,!0);if(V.created)console.log(`✓ Created ${E}`);else if(V.updated)console.log(`✓ Updated ${E}`)}console.log(`
---
`),console.log(`## Syncing Skill Symlinks
`);let B=process.argv.includes("--force");Rq({force:B}),console.log(`
---
`),console.log(`# Bootstrap Complete!
`),console.log("✓ All integrations installed"),console.log("✓ Skills system ready"),console.log(`✓ Skill symlinks synced
`),console.log("Next steps:"),console.log("  - Run `superpowers-agent find-skills` to see available skills"),console.log("  - Run `superpowers-agent setup-skills` in your project directory")};import{existsSync as x,readFileSync as aQ}from"fs";import{join as T,dirname as rq,parse as Dq}from"path";import{execSync as N}from"child_process";import{homedir as qq}from"os";var lQ=(q)=>{let Q=q.match(/^https:\/\/github\.com\/([^/]+)\/([^/]+)\/tree\/([^/]+)\/(.+)$/);if(Q){let[,X,Z,W,H]=Q;return{type:"git-tree",repoUrl:`https://github.com/${X}/${Z}.git`,branch:W,path:H,original:q}}let Y=q.match(/^git@github\.com:([^/]+)\/([^/]+?)(?:\.git)?$/);if(Y){let[,X,Z]=Y;return{type:"git-repo",repoUrl:`git@github.com:${X}/${Z}.git`,branch:null,path:null,original:q}}let $=q.match(/^https:\/\/github\.com\/([^/]+)\/([^/]+)(?:\.git)?$/);if($){let[,X,Z]=$;return{type:"git-repo",repoUrl:`https://github.com/${X}/${Z}.git`,branch:null,path:null,original:q}}if(x(q))return{type:"local",path:q,original:q};return null},oQ=(q)=>{let Q=q.includes("--global")||q.includes("-g"),Y=q.includes("--project")||q.includes("-p");if(Q)return T(qq(),".agents","skills");if(Y)return T(process.cwd(),".agents","skills");let $=T(process.cwd(),".agents","config.json"),X=T(qq(),".agents","config.json");for(let Z of[$,X])if(x(Z))try{if(JSON.parse(aQ(Z,"utf8")).installLocation==="project")return T(process.cwd(),".agents","skills")}catch(W){}return T(qq(),".agents","skills")},iQ=(q,Q)=>{let Y=T(qq(),".agents","tmp",`skill-install-${Date.now()}`);try{if(N(`mkdir -p "${Y}"`,{stdio:"pipe"}),Q)N(`git clone --branch ${Q} --depth 1 "${q}" "${Y}"`,{stdio:"pipe",timeout:30000});else N(`git clone --depth 1 "${q}" "${Y}"`,{stdio:"pipe",timeout:30000});return Y}catch($){try{N(`rm -rf "${Y}"`,{stdio:"pipe"})}catch{}throw Error(`Failed to clone repository: ${$.message}`)}},Pq=(q)=>{let Q=T(q,"skill.json");if(!x(Q))return null;try{let Y=aQ(Q,"utf8");return JSON.parse(Y)}catch(Y){throw Error(`Failed to read skill.json: ${Y.message}`)}},jq=(q,Q,Y,$)=>{let X=T(q,Q);if(!x(X)){$.errors.push(`Skill directory not found: ${Q}`);return}let Z=Pq(X);if(!Z){$.errors.push(`No skill.json found in: ${Q}`);return}let W=Z.name||Q,H=T(Y,W),B=rq(H);try{N(`mkdir -p "${B}"`,{stdio:"pipe"})}catch(z){$.errors.push(`Failed to create directory ${B}: ${z.message}`);return}try{if(x(H))N(`rm -rf "${H}"`,{stdio:"pipe"});N(`cp -R "${X}" "${H}"`,{stdio:"pipe"}),$.installed.push({name:W,path:H,title:Z.title||Z.description||W})}catch(z){$.errors.push(`Failed to install ${Q}: ${z.message}`)}},rQ=()=>{let q=process.argv.slice(3),Q=q.find((z)=>!z.startsWith("-")),Y=q.filter((z)=>z.startsWith("-"));if(!Q){console.log(`Usage: superpowers-agent add <url-or-path|@alias> [skill-path] [options]

Options:
  --global, -g   Install skills globally in ~/.agents/skills/ (default)
  --project, -p  Install skills in current project's .agents/skills/

Examples:
  superpowers-agent add https://github.com/example/skills
  superpowers-agent add https://github.com/example/repo/tree/main/skills
  superpowers-agent add ~/my-local-skills
  superpowers-agent add https://github.com/example/skills --project
  superpowers-agent add ~/my-local-skills --global
  superpowers-agent add @myrepo path/to/skill
  superpowers-agent add @myrepo path/to/skills --project

Description:
  Installs skill(s) from a Git repository, local directory, or repository alias.
  Supports repositories with single or multiple skills.
  Reads skill.json to determine skill names and installation paths.
  
  Repository aliases can be added using:
    superpowers-agent add-repository <git-url> [--as=@alias]`);return}console.log(`Installing skill(s)...
`);let $=null,X=null;if(Q.startsWith("@")){let z=Q;X=q.find((v,b)=>b>0&&!v.startsWith("-")&&v!==z);let E=y(!1),V=y(!0);if(E[z])$=E[z],console.log(`Using project repository alias: ${z}`);else if(V[z])$=V[z],console.log(`Using global repository alias: ${z}`);else{console.log(`Error: Repository alias not found: ${z}`),console.log(`
Available aliases:`);let v={...V,...E};if(Object.keys(v).length===0)console.log("  (none)"),console.log(`
Add a repository using:`),console.log("  superpowers-agent add-repository <git-url>");else for(let[b,M]of Object.entries(v))console.log(`  ${b} -> ${M}`);return}let O=x($);if(X)if(O)Q=T($,X),console.log(`Repository path: ${$}`),console.log(`Skill path: ${X}
`);else Q=`${$}/tree/main/${X}`,console.log(`Repository URL: ${$}`),console.log(`Skill path: ${X}
`);else if(Q=$,O)console.log(`Repository path: ${$}
`);else console.log(`Repository URL: ${$}
`)}let Z=lQ(Q);if(!Z){console.log(`Error: Invalid URL or path not found: ${Q}`);return}let W=oQ(Y);console.log(`Install location: ${W}
`);let H,B=!1;try{if(Z.type==="git-repo"||Z.type==="git-tree"){if(console.log(`Cloning repository: ${Z.repoUrl}`),Z.branch)console.log(`Branch: ${Z.branch}`);if(H=iQ(Z.repoUrl,Z.branch),B=!0,Z.path){if(H=T(H,Z.path),!x(H))throw Error(`Path not found in repository: ${Z.path}`)}console.log("")}else H=Z.path;let z=Pq(H);if(!z)throw Error("No skill.json found in source directory");let E={installed:[],errors:[],source:Z.original};if(z.skills&&Array.isArray(z.skills)){console.log(`Found ${z.skills.length} skill(s) to install
`);for(let V of z.skills)jq(H,V,W,E)}else{let V=z.name||Dq(H).base;jq(rq(H),Dq(H).base,W,E)}if(B&&H)try{let V=H.split("/.agents/tmp/")[0]+"/.agents/tmp/"+H.split("/.agents/tmp/")[1].split("/")[0];N(`rm -rf "${V}"`,{stdio:"pipe"})}catch{}if(console.log(`
**Successfully installed skills:**`),console.log(`- Source: ${E.source}`),E.installed.length>0)for(let V of E.installed)console.log(`  - Installed: ${V.name} at ${V.path}`),console.log(`    ${V.title}`);if(E.errors.length>0){console.log(`
**Errors:**`);for(let V of E.errors)console.log(`  - ${V}`)}if(E.installed.length===0&&E.errors.length===0)console.log("  No skills were installed");if(E.installed.length>0)console.log(`
**Syncing skill symlinks...**`),lq()}catch(z){if(B&&H)try{let E=H.split("/.agents/tmp/")[0]+"/.agents/tmp/"+H.split("/.agents/tmp/")[1].split("/")[0];N(`rm -rf "${E}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${z.message}`)}},sQ=()=>{let q=process.argv.slice(3),Q=q.find((z)=>!z.startsWith("-")),Y=q.filter((z)=>z.startsWith("-"));if(!Q){console.log(`Usage: superpowers-agent pull <url-or-path|@alias> [skill-path] [options]

Options:
  --global, -g   Update skills globally in ~/.agents/skills/ (default)
  --project, -p  Update skills in current project's .agents/skills/

Examples:
  superpowers-agent pull https://github.com/example/skills
  superpowers-agent pull https://github.com/example/repo/tree/main/skills
  superpowers-agent pull ~/my-local-skills
  superpowers-agent pull https://github.com/example/skills --project
  superpowers-agent pull ~/my-local-skills --global
  superpowers-agent pull @myrepo
  superpowers-agent pull @myrepo path/to/skill
  superpowers-agent pull @myrepo path/to/skills --project

Description:
  Updates or adds skill(s) from a Git repository, local directory, or repository alias.
  If skills already exist, they will be updated (replaced).
  If skills don't exist, they will be added.
  Supports repositories with single or multiple skills.
  Reads skill.json to determine skill names and installation paths.
  
  Repository aliases can be added using:
    superpowers-agent add-repository <git-url> [--as=@alias]`);return}console.log(`Updating skill(s)...
`);let $=null,X=null;if(Q.startsWith("@")){let z=Q;X=q.find((v,b)=>b>0&&!v.startsWith("-")&&v!==z);let E=y(!1),V=y(!0);if(E[z])$=E[z],console.log(`Using project repository alias: ${z}`);else if(V[z])$=V[z],console.log(`Using global repository alias: ${z}`);else{console.log(`Error: Repository alias not found: ${z}`),console.log(`
Available aliases:`);let v={...V,...E};if(Object.keys(v).length===0)console.log("  (none)"),console.log(`
Add a repository using:`),console.log("  superpowers-agent add-repository <git-url>");else for(let[b,M]of Object.entries(v))console.log(`  ${b} -> ${M}`);return}let O=x($);if(X)if(O)Q=T($,X),console.log(`Repository path: ${$}`),console.log(`Skill path: ${X}
`);else Q=`${$}/tree/main/${X}`,console.log(`Repository URL: ${$}`),console.log(`Skill path: ${X}
`);else if(Q=$,O)console.log(`Repository path: ${$}
`);else console.log(`Repository URL: ${$}
`)}let Z=lQ(Q);if(!Z){console.log(`Error: Invalid URL or path not found: ${Q}`);return}let W=oQ(Y);console.log(`Install location: ${W}
`);let H,B=!1;try{if(Z.type==="git-repo"||Z.type==="git-tree"){if(console.log(`Cloning repository: ${Z.repoUrl}`),Z.branch)console.log(`Branch: ${Z.branch}`);if(H=iQ(Z.repoUrl,Z.branch),B=!0,Z.path){if(H=T(H,Z.path),!x(H))throw Error(`Path not found in repository: ${Z.path}`)}console.log("")}else H=Z.path;let z=Pq(H);if(!z)throw Error("No skill.json found in source directory");let E={installed:[],errors:[],source:Z.original};if(z.skills&&Array.isArray(z.skills)){console.log(`Found ${z.skills.length} skill(s) to update
`);for(let V of z.skills)jq(H,V,W,E)}else{let V=z.name||Dq(H).base;jq(rq(H),Dq(H).base,W,E)}if(B&&H)try{let V=H.split("/.agents/tmp/")[0]+"/.agents/tmp/"+H.split("/.agents/tmp/")[1].split("/")[0];N(`rm -rf "${V}"`,{stdio:"pipe"})}catch{}if(console.log(`
**Successfully updated skills:**`),console.log(`- Source: ${E.source}`),E.installed.length>0)for(let V of E.installed)console.log(`  - Updated: ${V.name} at ${V.path}`),console.log(`    ${V.title}`);if(E.errors.length>0){console.log(`
**Errors:**`);for(let V of E.errors)console.log(`  - ${V}`)}if(E.installed.length===0&&E.errors.length===0)console.log("  No skills were updated");if(E.installed.length>0)console.log(`
**Syncing skill symlinks...**`),lq()}catch(z){if(B&&H)try{let E=H.split("/.agents/tmp/")[0]+"/.agents/tmp/"+H.split("/.agents/tmp/")[1].split("/")[0];N(`rm -rf "${E}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${z.message}`)}},tQ=()=>{let q=process.argv.slice(3),Q=q.find((z)=>!z.startsWith("-")),Y=q.filter((z)=>z.startsWith("-"));if(!Q){console.log(`Usage: superpowers-agent add-repository <git-url> [options]

Options:
  --global, -g        Add repository globally in ~/.agents/config.json (default)
  --project, -p       Add repository in current project's .agents/config.json
  --as=<alias>        Specify custom alias for the repository

Examples:
  superpowers-agent add-repository https://github.com/example/skills.git
  superpowers-agent add-repository https://github.com/example/skills.git --as=@myskills
  superpowers-agent add-repository https://github.com/example/skills.git --project
  superpowers-agent add-repository https://github.com/example/skills.git --as=@custom --global

Description:
  Adds a skill repository alias to your configuration.
  The repository's skill.json will be read to determine the default alias.
  Use --as to specify a custom alias.
  After adding, you can install skills using: superpowers-agent add @alias path/to/skill`);return}console.log(`Adding repository...
`);let $=Y.some((z)=>z==="--global"||z==="-g"),X=Y.some((z)=>z==="--project"||z==="-p"),Z=Y.find((z)=>z.startsWith("--as=")),W=Z?Z.split("=")[1]:null,H=X?!1:!0,B;try{console.log(`Cloning repository: ${Q}`),B=T(qq(),".agents","tmp",`repo-add-${Date.now()}`),N(`mkdir -p "${B}"`,{stdio:"pipe"}),N(`git clone --depth 1 "${Q}" "${B}"`,{stdio:"pipe",timeout:30000}),console.log("");let z=Pq(B);if(!z)throw Error("No skill.json found in repository");let E;if(W)E=W;else if(z.repository)E=z.repository;else{let v=Q.match(/\/([^/]+?)(?:\.git)?$/);if(v)E="@"+v[1];else throw Error("Could not determine repository alias. Use --as=@alias to specify manually.")}VQ(E,Q,H),N(`rm -rf "${B}"`,{stdio:"pipe"});let V=H?"globally":"in project",O=H?T(qq(),".agents","config.json"):T(process.cwd(),".agents","config.json");console.log(`✓ Repository added ${V}`),console.log(`  Alias: ${E}`),console.log(`  URL: ${Q}`),console.log(`  Config: ${O}`),console.log(`
You can now install skills using:`),console.log(`  superpowers-agent add ${E} <path-to-skill>`)}catch(z){if(B)try{N(`rm -rf "${B}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${z.message}`)}};import{dirname as QY}from"path";var eQ=(q)=>{if(!q){console.log(`Usage: superpowers-agent use-skill <skill-name>

Examples (smart matching):
  superpowers-agent use-skill brainstorming              # Matches superpowers:collaboration/brainstorming
  superpowers-agent use-skill collaboration/brainstorming # More specific suffix
  superpowers-agent use-skill test-driven-development    # Matches superpowers:testing/test-driven-development

Examples (explicit paths):
  superpowers-agent use-skill superpowers:collaboration/brainstorming  # Full superpowers skill path
  superpowers-agent use-skill claude:email-assistant                   # Full claude skill path
  superpowers-agent use-skill my-custom-skill                          # Project skill

Smart matching:
  - Type just the skill name or any suffix of the path
  - Priority: .agents/skills/ > .claude/skills/ > ~/.agents/skills/ > ~/.agents/superpowers/skills/
  - If multiple skills match at same priority, you'll see them with descriptions`);return}let Q=S(q);if(!Q){console.log(`Error: Skill not found: ${q}

Available skills:`),console.log("Run: superpowers-agent find-skills");return}let{skillFile:Y,sourceType:$,actualSkillPath:X}=Q;try{let{content:Z,frontmatter:W}=YQ(Y),H=Wq[$].prefix+X,B=QY(Y),z=`# ${W.name||H}`;if(W.description)z+=`
# ${W.description}`;if(W.whenToUse)z+=`
# When to use: ${W.whenToUse}`;z+=`
# Supporting tools and docs are in ${B}
# ============================================

${Z}`,console.log(z)}catch(Z){console.log(Z.message)}};var qX={bootstrap:nQ,version:mQ,"check-updates":()=>{hQ().catch((q)=>{console.error(q.message),process.exit(1)})},update:()=>{let q=process.argv.includes("--no-reinstall");xq({skipReinstall:q})},"config-get":TQ,"config-set":bQ,"setup-skills":cQ,"use-skill":()=>eQ(process.argv[3]),execute:AQ,"find-skills":p,add:rQ,"add-repository":tQ,"list-repositories":MQ,pull:sQ,dir:IQ,path:OQ,"get-helpers":wQ,"install-copilot-prompts":n,"install-copilot-instructions":a,"install-cursor-commands":o,"install-cursor-hooks":i,"install-codex-prompts":r,"install-gemini-commands":s,"install-claude-commands":t,"install-opencode-commands":e,"install-aliases":iq,default:()=>{console.log(`Superpowers for Agents
Usage:
  superpowers-agent bootstrap [--no-update] [--force]               # Run complete bootstrap
  superpowers-agent version                                         # Show current version
  superpowers-agent check-updates                                   # Check for updates
  superpowers-agent update [--no-reinstall]                         # Update to latest version
  superpowers-agent config-get                                      # Show configuration
  superpowers-agent config-set <key> <value>                        # Update configuration
  superpowers-agent setup-skills                                    # Initialize project skills
  superpowers-agent use-skill <skill-name>                          # Load a skill
  superpowers-agent find-skills                                     # List available skills
  superpowers-agent add <url-or-path|@alias> [path] [options]      # Install skills
  superpowers-agent add-repository <git-url> [--as=@alias] [opts]  # Add repository alias
  superpowers-agent list-repositories                              # List configured repository aliases
  superpowers-agent pull <url-or-path|@alias> [path] [options]     # Update skills
  superpowers-agent dir <skill-name>                                # Get skill directory
  superpowers-agent path <skill-name>                               # Get skill file path
  superpowers-agent execute <skill-name>                            # Execute a skill
  superpowers-agent get-helpers <skill> <search-term>               # Get helper file path
  superpowers-agent install-copilot-prompts                         # Install GitHub Copilot prompts
  superpowers-agent install-copilot-instructions                    # Install universal instructions
  superpowers-agent install-cursor-commands                         # Install Cursor commands
  superpowers-agent install-cursor-hooks                            # Install Cursor hooks
  superpowers-agent install-codex-prompts                           # Install Codex prompts
  superpowers-agent install-gemini-commands                         # Install Gemini commands
  superpowers-agent install-claude-commands                         # Install Claude commands
  superpowers-agent install-opencode-commands                       # Install OpenCode commands
  superpowers-agent install-aliases                                 # Install universal aliases (superpowers, superpowers-agent)

Documentation: https://github.com/complexthings/superpowers
`)}},XY=process.argv[2]||"default",YY=qX[XY]||qX.default;try{YY()}catch(q){console.error(`Error: ${q.message}`),process.exit(1)}
