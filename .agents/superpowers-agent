#!/bin/sh
// 2>/dev/null; exec "$(command -v bun 2>/dev/null || echo node)" "$0" "$@"
import{readFileSync as uz,writeFileSync as hz,existsSync as _z}from"fs";import{join as n}from"path";import{execSync as fQ}from"child_process";import{homedir as gz,platform as jQ}from"os";import{join as T,parse as DQ,dirname as mz}from"path";import{existsSync as g}from"fs";import{fileURLToPath as xQ}from"url";var RQ=xQ(import.meta.url),Zz=mz(RQ),yQ=()=>{let z=gz();switch(jQ()){case"darwin":return T(z,"Library","Application Support","Code","User");case"win32":return T(z,"AppData","Roaming","Code","User");case"linux":return T(z,".config","Code","User");default:return T(z,".config","Code","User")}},SQ=()=>{let z=process.cwd(),Q=DQ(z).root;while(z!==Q){if(g(T(z,".agents")))return z;z=mz(z)}return process.cwd()},I={home:gz(),vscodeUserDir:yQ(),projectRoot:SQ(),get projectClaudeSkills(){return T(this.projectRoot,".claude","skills")},get projectAgentsSkills(){return T(this.projectRoot,".agents","skills")},get projectCopilotSkills(){return T(this.projectRoot,".copilot","skills")},get projectOpencodeSkills(){return T(this.projectRoot,".opencode","skill")},get projectSkillsDir(){return T(this.projectRoot,"skills")},get homePersonalSkills(){return T(this.home,".agents","skills")},get homeClaudeSkills(){return T(this.home,".claude","skills")},get homeCopilotSkills(){return T(this.home,".copilot","skills")},get claudeSettings(){return T(this.home,".claude","settings.json")},get copilotHooksDir(){return process.env.COPILOT_HOME?T(process.env.COPILOT_HOME,"hooks"):T(this.home,".copilot","hooks")},get homeOpencodeSkills(){return T(this.home,".config","opencode","skill")},get bootstrap(){return T(this.projectRoot,".agents","superpowers-bootstrap.md")},get superpowersRepo(){let z=T(Zz,"..","..","..");if(g(T(z,"skills"))&&g(T(z,".agents","superpowers-agent")))return z;let Q=T(Zz,"..");if(g(T(Q,"skills"))&&g(T(Q,".agents","superpowers-agent")))return Q;return T(this.home,".agents","superpowers")},get homeSuperpowersSkills(){return T(this.superpowersRepo,"skills")},get isSuperpowersRepo(){let z=T(Zz,"..","..","..");if(g(T(z,"skills"))&&g(T(z,".agents","superpowers-agent")))return!0;let Q=T(Zz,"..");return g(T(Q,"skills"))&&g(T(Q,".agents","superpowers-agent"))}};var Jz=()=>({auto_update:!0,last_update_check:null,last_updated_commit:null}),zz=()=>{let z=n(I.superpowersRepo,".config.json");try{if(!_z(z))return Jz();let Q=uz(z,"utf8");return{...Jz(),...JSON.parse(Q)}}catch{return Jz()}},dz=(z)=>{let Q=n(I.superpowersRepo,".config.json"),Z={...zz(),...z};try{hz(Q,JSON.stringify(Z,null,2))}catch(Y){console.log(`Warning: couldn't save config: ${Y.message}`)}},S=(z)=>{let Q=z?n(I.home,".agents","config.json"):n(process.cwd(),".agents","config.json");if(!_z(Q))return{};try{let X=uz(Q,"utf8");return JSON.parse(X)}catch(X){return{}}},m=(z,Q)=>{let X=Q?n(I.home,".agents"):n(process.cwd(),".agents"),Z=n(X,"config.json");try{if(!_z(X))fQ(`mkdir -p "${X}"`,{stdio:"pipe"});return hz(Z,JSON.stringify(z,null,2),"utf8"),!0}catch(Y){throw Error(`Failed to write config: ${Y.message}`)}},P=(z=!1)=>{return S(z).repositories||{}},pz=(z,Q,X)=>{let Z=S(X);if(!Z.repositories)Z.repositories={};Z.repositories[z]=Q,m(Z,X)};function cz(z=!0){return S(z).installedSkills||{}}function Iz(z,Q=!0){let X=S(Q);if(X.installedSkills)delete X.installedSkills[z];m(X,Q)}function nz(){return S(!0).installedAgents||{}}import{readFileSync as kQ}from"fs";import{join as Lz,dirname as PQ}from"path";import{fileURLToPath as gQ}from"url";import{existsSync as lz}from"fs";import{execSync as mQ}from"child_process";var uQ=gQ(import.meta.url),Uz=PQ(uQ),f=()=>{try{let z=Lz(Uz,"package.json");if(!lz(z))z=Lz(Uz,"..","package.json");if(!lz(z))z=Lz(Uz,"..","..","package.json");return JSON.parse(kQ(z,"utf8")).version||"0.0.0"}catch{return"0.0.0"}},l=()=>{let z=f();console.log(`^^SAV:${z}^^`)},$z=async()=>{try{let Q=mQ('curl -sS "https://registry.npmjs.org/@complexthings/superpowers-agent/latest"',{encoding:"utf8",timeout:1e4}),X=JSON.parse(Q);if(!X.version)return f();return X.version}catch(z){throw Error(`Failed to fetch remote version: ${z.message}`)}},qz=(z,Q)=>{let X=z.split(".").map(Number),Z=Q.split(".").map(Number);for(let Y=0;Y<3;Y++){let $=X[Y]||0,B=Z[Y]||0;if($>B)return!0;if($<B)return!1}return!1};var iz=()=>{let z=zz();console.log("Current configuration:"),console.log(JSON.stringify(z,null,2))},az=()=>{let z=process.argv[3],Q=process.argv[4];if(!z||Q===void 0){console.log(`Usage: .agents/superpowers-agent config-set <key> <value>

Available keys:
  auto_update (true/false) - Enable/disable automatic updates during bootstrap

Examples:
  .agents/superpowers-agent config-set auto_update false
  .agents/superpowers-agent config-set auto_update true`);return}let X=Q;if(Q==="true")X=!0;else if(Q==="false")X=!1;dz({[z]:X}),console.log(`✓ Set ${z} = ${X}`)},oz=()=>{l();let z=P(!0),Q=P(!1),X=[];for(let[$,B]of Object.entries(z))X.push({alias:$,url:B,source:"global"});for(let[$,B]of Object.entries(Q)){let q=X.findIndex((V)=>V.alias===$);if(q>=0)X[q]={alias:$,url:B,source:"project"};else X.push({alias:$,url:B,source:"project"})}if(X.length===0){console.log(`No repository aliases configured.

Add a repository using:
  superpowers-agent add-repository <git-url> [--as=@alias]`);return}let Z=Math.max(5,...X.map(($)=>$.alias.length)),Y=Math.max(3,...X.map(($)=>$.url.length));console.log(`Repositories:
`),console.log(`${"Alias".padEnd(Z)}  ${"URL".padEnd(Y)}  Source`);for(let $ of X)console.log(`${$.alias.padEnd(Z)}  ${$.url.padEnd(Y)}  (${$.source})`);console.log(`
Total: ${X.length} repository alias(es)`)};var Wz=async()=>{try{let z=f(),Q=await $z();return{hasUpdates:qz(Q,z),localVersion:z,remoteVersion:Q}}catch{return{hasUpdates:!1,localVersion:f(),remoteVersion:"",error:!0}}};import{execSync as rz}from"child_process";var Oz=async({skipReinstall:z=!1}={})=>{console.log(`# Checking for Superpowers updates...
`);let Q=await Wz();if(Q.error){console.log("⚠️  Could not check for updates (network issue)");return}if(!Q.hasUpdates){console.log(`✓ Already up to date (v${Q.localVersion})`);return}if(console.log(`\uD83D\uDCE6 Update available: v${Q.localVersion} → v${Q.remoteVersion}
`),z){console.log("   Skipping install (--no-reinstall flag set)"),console.log(`   Run manually: npm install -g @complexthings/superpowers-agent
`);return}console.log(`   Installing update...
`);try{rz("npm install -g @complexthings/superpowers-agent",{stdio:"inherit"}),console.log(`
✓ Update complete!`)}catch(X){console.log(`
✗ Update failed: ${X.message}`),console.log("   Try running manually: npm install -g @complexthings/superpowers-agent"),process.exit(1)}console.log(`
   Running bootstrap to complete setup...
`);try{rz("superpowers-agent bootstrap --no-update",{stdio:"inherit"}),console.log(`
✓ Bootstrap complete!`)}catch(X){console.log(`
⚠️  Bootstrap step failed: ${X.message}`),console.log("   The update installed successfully. Run manually to finish setup:"),console.log("   superpowers-agent bootstrap")}},sz=async()=>{l();let z=f();console.log(`Current version: ${z}`);try{let Q=await $z();if(console.log(`Latest version: ${Q}`),qz(Q,z))console.log("Update available: Yes"),process.exit(1);else console.log("You are up to date"),process.exit(0)}catch(Q){console.log(`Error checking for updates: ${Q.message}`),process.exit(1)}},tz=()=>{let z=f();console.log(z)};import{existsSync as N,readFileSync as s,writeFileSync as r,rmSync as JQ,lstatSync as vX}from"fs";import{join as v,dirname as _Q,parse as o}from"path";import{execSync as R}from"child_process";import{platform as AX}from"os";import{execSync as hQ}from"child_process";var Qz=(z)=>{try{return hQ(`which ${z}`,{stdio:"pipe",timeout:2000}),!0}catch{return!1}};var x={opencode:{check:()=>Qz("opencode"),cli:!0,name:"OpenCode",installUrl:"https://opencode.ai/docs/installation",bootstrapCommand:"install-opencode-commands"},claude:{check:()=>Qz("claude"),cli:!0,name:"Claude Code",installUrl:"https://code.claude.com/docs/en/installation",bootstrapCommand:"install-claude-commands"},copilot:{check:()=>!0,cli:!1,name:"GitHub Copilot",bootstrapCommand:"install-copilot-prompts"}},ez=()=>{let z=[];if(x.copilot.check())z.push("github-copilot");if(x.claude.check())z.push("claude-code");if(x.opencode.check())z.push("opencode");return z};import{existsSync as Tz,readdirSync as AY,lstatSync as dQ,symlinkSync as zQ,unlinkSync as pQ,mkdirSync as cQ,readlinkSync as nQ}from"fs";import{join as wz,dirname as bY}from"path";import{platform as lQ}from"os";var Mz=(z)=>{try{return dQ(z).isSymbolicLink()}catch{return!1}},iQ=(z,Q)=>{try{if(!Mz(z))return!1;return nQ(z)===Q}catch{return!1}},QQ=()=>{let z=wz(I.superpowersRepo,".opencode","plugins","superpowers-agent.js"),Q=wz(I.home,".config","opencode","plugins"),X=wz(Q,"superpowers-agent.js");if(console.log("Installing OpenCode plugin symlink..."),!Tz(z))return console.log("⚠️  Source plugin not found, skipping symlink creation"),console.log(`   Expected at: ${z}`),{created:!1,error:"Source plugin not found"};if(iQ(X,z))return console.log("✓ Plugin symlink already exists and is correct"),{created:!1,existed:!0};if(Tz(X)&&!Mz(X))return console.log("⚠️  Warning: A file already exists at the destination path"),console.log(`   Path: ${X}`),console.log("   Skipping symlink creation to avoid overwriting existing file"),console.log("   To use superpowers-agent plugin, manually remove or rename the existing file"),{created:!1,error:"File already exists at destination"};if(!Tz(Q))try{cQ(Q,{recursive:!0}),console.log(`✓ Created ${Q.replace(I.home,"~")}`)}catch(Y){return console.log(`⚠️  Failed to create plugins directory: ${Y.message}`),{created:!1,error:`Failed to create directory: ${Y.message}`}}if(Mz(X))try{pQ(X)}catch(Y){return console.log(`⚠️  Failed to remove existing symlink: ${Y.message}`),{created:!1,error:`Failed to remove existing symlink: ${Y.message}`}}let Z=lQ();try{if(Z==="win32")zQ(z,X,"file");else zQ(z,X);let Y=z.replace(I.home,"~"),$=X.replace(I.home,"~");return console.log(`✓ Created symlink: ${$}`),console.log(`  -> ${Y}`),{created:!0}}catch(Y){if(Z==="win32"&&Y.code==="EPERM")return console.log("⚠️  Windows requires Developer Mode or admin privileges for symlinks"),console.log("   Enable Developer Mode: Settings > Update & Security > For developers"),{created:!1,error:"Windows symlink permission denied"};return console.log(`⚠️  Failed to create symlink: ${Y.message}`),{created:!1,error:`Failed to create symlink: ${Y.message}`}}};import{existsSync as XQ,readdirSync as RY,readFileSync as aQ,writeFileSync as oQ,mkdirSync as rQ,copyFileSync as sQ}from"fs";import{join as SY,dirname as tQ}from"path";var eQ="superpowers-agent session-context",zX="superpowers-agent session-context --format=claude 2>/dev/null || true",QX="startup|resume|clear|compact",YQ=(z=I.claudeSettings)=>{let Q={},X=!1,Z;if(XQ(z)){X=!0;let K;try{K=aQ(z,"utf8")}catch(G){return{error:!0,message:`Failed to read settings.json: ${G.message}`}}try{Q=K.trim()?JSON.parse(K):{}}catch(G){return{error:!0,message:`settings.json is not valid JSON, leaving it untouched: ${G.message}`}}let H=new Date().toISOString().split("T")[0];Z=`${z}.backup-${H}`;try{sQ(z,Z)}catch(G){return{error:!0,message:`Failed to back up settings.json: ${G.message}`}}}if(typeof Q!=="object"||Q===null||Array.isArray(Q))return{error:!0,message:"settings.json root is not an object"};Q.hooks=Q.hooks||{};let Y=Array.isArray(Q.hooks.SessionStart)?Q.hooks.SessionStart:[],$=(K)=>{try{return JSON.stringify(K).includes(eQ)}catch{return!1}},B=Y.some($),q=Y.filter((K)=>!$(K)),V={matcher:QX,hooks:[{type:"command",command:zX}]};Q.hooks.SessionStart=[...q,V];let W=tQ(z);if(!XQ(W))try{rQ(W,{recursive:!0})}catch(K){return{error:!0,message:`Failed to create ${W}: ${K.message}`}}try{oQ(z,JSON.stringify(Q,null,2)+`
`,"utf8")}catch(K){return{error:!0,message:`Failed to write settings.json: ${K.message}`}}if(!X)return{created:!0};if(B)return{existed:!0,updated:!0,backup:Z};return{updated:!0,backup:Z}};import{existsSync as Nz,readdirSync as gY,readFileSync as XX,writeFileSync as YX,mkdirSync as ZX}from"fs";import{join as $X}from"path";var qX="superpowers.json",WX=()=>({version:1,hooks:{sessionStart:[{type:"command",bash:"superpowers-agent session-context --format=copilot 2>/dev/null || true",powershell:"superpowers-agent session-context --format=copilot 2>$null; exit 0",timeoutSec:15}]}}),ZQ=(z=I.copilotHooksDir)=>{let Q=$X(z,qX),X=JSON.stringify(WX(),null,2)+`
`;if(Nz(Q)){let Y;try{Y=XX(Q,"utf8")}catch($){return{error:!0,message:`Failed to read ${Q}: ${$.message}`}}if(Y===X)return{existed:!0,path:Q}}if(!Nz(z))try{ZX(z,{recursive:!0})}catch(Y){return{error:!0,message:`Failed to create ${z}: ${Y.message}`}}let Z=Nz(Q);try{YX(Q,X,"utf8")}catch(Y){return{error:!0,message:`Failed to write ${Q}: ${Y.message}`}}return Z?{updated:!0,path:Q}:{created:!0,path:Q}};import{existsSync as i,readdirSync as HX,lstatSync as BX,symlinkSync as Hz,unlinkSync as vz,mkdirSync as $Q,readlinkSync as EX}from"fs";import{join as d,dirname as KX,basename as VX}from"path";import{homedir as p,platform as qQ}from"os";var Bz=[{name:"claude",parentDir:()=>d(p(),".claude"),skillsDir:()=>d(p(),".claude","skills")},{name:"copilot",parentDir:()=>d(p(),".copilot"),skillsDir:()=>d(p(),".copilot","skills")},{name:"opencode",parentDir:()=>d(p(),".config","opencode"),skillsDir:()=>d(p(),".config","opencode","skill")}],a=(z)=>{try{return BX(z).isSymbolicLink()}catch{return!1}},WQ=(z,Q)=>{try{if(!a(z))return!1;return EX(z)===Q}catch{return!1}},HQ=(z,Q)=>{if(!i(z))return{created:!1,existed:!1,error:`Source does not exist: ${z}`};if(WQ(Q,z))return{created:!1,existed:!0};if(i(Q)||a(Q))if(a(Q))try{vz(Q)}catch(Y){return{created:!1,existed:!1,error:`Failed to remove existing symlink: ${Y.message}`}}else return{created:!1,existed:!1,error:`Target exists and is not a symlink: ${Q}`};let X=KX(Q);if(!i(X))try{$Q(X,{recursive:!0})}catch(Y){return{created:!1,existed:!1,error:`Failed to create parent directory: ${Y.message}`}}let Z=qQ();try{if(Z==="win32")Hz(z,Q,"junction");else Hz(z,Q,"dir");return{created:!0,existed:!1}}catch(Y){if(Z==="win32"&&Y.code==="EPERM")return{created:!1,existed:!1,error:"Windows requires Developer Mode or admin privileges for symlinks. Enable Developer Mode: Settings > Update & Security > For developers"};return{created:!1,existed:!1,error:`Failed to create symlink: ${Y.message}`}}},Az=(z)=>{if(!i(z)&&!a(z))return{removed:!1,error:"Symlink does not exist"};if(!a(z))return{removed:!1,error:"Target is not a symlink"};try{return vz(z),{removed:!0}}catch(Q){return{removed:!1,error:`Failed to remove symlink: ${Q.message}`}}};var BQ=(z,Q,X="skills")=>{let Z=S(!0);if(!Z.symlinks||!Z.symlinks[z])return;if(X==="superpowers")Z.symlinks[z].superpowers=null;else{let Y=Z.symlinks[z].skills||[];Z.symlinks[z].skills=Y.filter(($)=>$!==Q)}m(Z,!0)};var EQ=(z,Q=[])=>{let X;try{X=HX(z,{withFileTypes:!0})}catch{return Q}let Z=!1,Y=[];for(let $ of X){if(!$.isDirectory()&&$.name==="SKILL.md")Z=!0;if($.isDirectory()&&!$.name.startsWith("."))Y.push(d(z,$.name))}if(Z)Q.push(z);for(let $ of Y)EQ($,Q);return Q},KQ=()=>{let z=I.homeSuperpowersSkills,Q=I.homePersonalSkills;if(!i(z))return{created:0,existed:0,updated:0,errors:[`Skills directory not found: ${z}`]};if(!i(Q))try{$Q(Q,{recursive:!0})}catch(Y){return{created:0,existed:0,updated:0,errors:[`Failed to create ${Q}: ${Y.message}`]}}let X=EQ(z),Z={created:0,existed:0,updated:0,errors:[]};for(let Y of X){let $=VX(Y),B=d(Q,$);if(WQ(B,Y)){Z.existed++;continue}if(a(B))try{vz(B),Z.updated++}catch(V){Z.errors.push(`Failed to remove stale symlink ${B}: ${V.message}`);continue}else if(i(B)){Z.errors.push(`Skipped ${B}: path exists and is not a symlink`);continue}let q=qQ();try{if(q==="win32")Hz(Y,B,"junction");else Hz(Y,B,"dir");let V=B.replace(p(),"~"),W=Y.replace(p(),"~");console.log(`  ✓ ${V} -> ${W}`),Z.created++}catch(V){Z.errors.push(`Failed to create symlink ${B}: ${V.message}`)}}return Z};import{existsSync as GX,lstatSync as JX,readdirSync as _X,readlinkSync as IX,unlinkSync as LX}from"fs";import{basename as UX,join as OX,resolve as VQ,sep as TX}from"path";var wX=new Set(["preserving-productive-tensions","dispatching-parallel-agents","executing-plans","finishing-a-development-branch","receiving-code-review","requesting-code-review","subagent-driven-development","using-git-worktrees","writing-plans","defense-in-depth","root-cause-tracing","systematic-debugging","verification-before-completion","finding-skills","create-agents-md","creating-prompts","using-superpowers","writing-prompts","collision-zone-thinking","inversion-exercise","meta-pattern-recognition","scale-game","simplification-cascades","when-stuck","tracing-knowledge-lineages","condition-based-waiting","test-driven-development","testing-anti-patterns","using-a-skill"]),MX=(z,Q)=>z===Q||z.startsWith(`${Q}${TX}`),NX=({skillDir:z,bundledSkillsDir:Q,retiredSkillNames:X=wX})=>{let Z=[],Y=0,$=VQ(Q);if(!GX(z))return{removed:Z,skipped:Y};let B;try{B=_X(z)}catch{return{removed:Z,skipped:Y}}for(let q of B){let V=OX(z,q);try{if(!JX(V).isSymbolicLink())continue;let W=VQ(z,IX(V));if(!MX(W,$)||!X.has(UX(W))){Y++;continue}LX(V),Z.push(V)}catch{Y++}}return{removed:Z,skipped:Y}},GQ=()=>NX({skillDir:I.homePersonalSkills,bundledSkillsDir:I.homeSuperpowersSkills});var Ez=(z,Q,X,Z=!0)=>{let Y=N(z);if(!Y&&!Z)return{updated:!1,created:!1,skipped:!0};let $=Q,B=new Date().toISOString().split("T")[0];$=$.replace(/\{\{DATE\}\}/g,B),$=$.replace(/\{\{SUPERPOWERS_PATH\}\}/g,I.superpowersRepo);let q=`<!-- SUPERPOWERS_SKILLS_START -->
${$}
<!-- SUPERPOWERS_SKILLS_END -->`;if(Y){let V=new Date().toISOString().split("T")[0],W=`${z}.backup-${V}`;try{R(`cp "${z}" "${W}"`,{stdio:"pipe"})}catch(U){return{updated:!1,error:!0,message:`Failed to backup: ${U.message}`}}let K;try{K=s(z,"utf8")}catch(U){return{updated:!1,error:!0,message:`Failed to read: ${U.message}`}}let H="<!-- SUPERPOWERS_SKILLS_START -->",G="<!-- SUPERPOWERS_SKILLS_END -->",J;if(K.includes(H)&&K.includes(G)){let U=new RegExp(`${H}[\\s\\S]*?${G}`,"g");J=K.replace(U,q)}else{let U=K.match(/^#\s+.+$/m);if(U){let _=K.indexOf(`
`,U.index)+1;J=K.slice(0,_)+`
`+q+`
`+K.slice(_)}else J=q+`

`+K}try{return r(z,J,"utf8"),{updated:!0,created:!1,backup:W}}catch(U){return{updated:!1,error:!0,message:`Failed to write: ${U.message}`}}}else try{let V=_Q(z);if(!N(V))R(`mkdir -p "${V}"`,{stdio:"pipe"});let H=`# ${o(z).base.toUpperCase()}

`+q;return r(z,H,"utf8"),{updated:!1,created:!0}}catch(V){return{updated:!1,created:!1,error:!0,message:`Failed to create: ${V.message}`}}},FX=()=>{console.log("Installing Unix aliases...");let z=v(I.superpowersRepo,".agents","superpowers-agent"),Q=v(I.home,".local","bin"),X=v(Q,"superpowers-agent"),Z=v(Q,"superpowers");if(!N(z)){console.log("⚠️  superpowers-agent executable not found");return}if(!N(Q))try{R(`mkdir -p "${Q}"`,{stdio:"pipe"}),console.log(`✓ Created ${Q}`)}catch($){console.log(`⚠️  Failed to create ${Q}: ${$.message}`);return}try{if(N(X))R(`rm "${X}"`,{stdio:"pipe"});R(`ln -s "${z}" "${X}"`,{stdio:"pipe"}),console.log(`✓ Created symlink: superpowers-agent -> ${z}`)}catch($){console.log(`⚠️  Failed to create superpowers-agent symlink: ${$.message}`)}try{if(N(Z))R(`rm "${Z}"`,{stdio:"pipe"});R(`ln -s "${z}" "${Z}"`,{stdio:"pipe"}),console.log(`✓ Created symlink: superpowers -> ${z}`)}catch($){console.log(`⚠️  Failed to create superpowers symlink: ${$.message}`)}if(!(process.env.PATH||"").includes(Q))console.log(`
⚠️  Warning: ${Q} is not in your PATH`),console.log("   Add this to your shell profile (~/.bashrc, ~/.zshrc, etc.):"),console.log(`   export PATH="$HOME/.local/bin:$PATH"
`)},bX=()=>{console.log("Installing Windows aliases..."),console.log("✓ Windows alias installation (implementation pending)")},Fz=()=>{let z=AX();if(console.log(`## Installing Universal Aliases
`),z==="win32")return bX();else return FX()},CX=(z)=>{let Q=v(I.superpowersRepo,".github","copilot-instructions.md"),X=v(z,".github","copilot-instructions.md"),Z="<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->",Y="<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->";if(!N(Q))return{error:!0,message:"Source template not found"};let $;try{$=s(Q,"utf8")}catch(q){return{error:!0,message:`Failed to read source template: ${q.message}`}}if(!$.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->")||!$.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->"))return{error:!0,message:"Template is missing required markers"};let B=_Q(X);try{if(!N(B))R(`mkdir -p "${B}"`,{stdio:"pipe"})}catch(q){return{error:!0,message:`Failed to create .github directory: ${q.message}`}}if(N(X)){let q;try{q=s(X,"utf8")}catch(K){return{error:!0,message:`Failed to read existing file: ${K.message}`}}let V=new Date().toISOString().replace(/[:.]/g,"-"),W=`${X}.backup-${V}`;try{R(`cp "${X}" "${W}"`,{stdio:"pipe"})}catch(K){return{error:!0,message:`Failed to backup: ${K.message}`}}if(q.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->")&&q.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->")){let K=new RegExp("<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->[\\s\\S]*?<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->","g"),H=q.replace(K,$.trim());try{return r(X,H,"utf8"),{updated:!0,backup:W}}catch(G){return{error:!0,message:`Failed to update: ${G.message}`}}}else{let K=q.trimEnd()+`

`+$.trim()+`
`;try{return r(X,K,"utf8"),{updated:!0,backup:W}}catch(H){return{error:!0,message:`Failed to append: ${H.message}`}}}}else try{return r(X,$,"utf8"),{created:!0}}catch(q){return{error:!0,message:`Failed to create: ${q.message}`}}},IQ=()=>{l(),console.log(`# Setting up Superpowers skills for this project
`);let z=process.cwd(),Q=v(z,".agents"),X=v(Q,"skills"),Z=v(z,"AGENTS.md"),Y=v(Q,"AGENTS.md"),$=N(Z)?Z:Y,B=v(I.superpowersRepo,".agents","templates","AGENTS.md.template");if(!N(B)){console.log(`✗ Error: AGENTS.md template not found
  Expected at: ${B}
  Please update your Superpowers installation`);return}if(!N(Q))try{R(`mkdir -p "${Q}"`,{stdio:"pipe"}),console.log("✓ Created .agents/ directory")}catch(O){console.log(`✗ Failed to create .agents/ directory: ${O.message}`);return}else console.log("✓ .agents/ directory exists");if(!N(X))try{R(`mkdir -p "${X}"`,{stdio:"pipe"}),R(`touch "${v(X,".gitkeep")}"`,{stdio:"pipe"}),console.log("✓ Created .agents/skills/ directory")}catch(O){console.log(`✗ Failed to create skills directory: ${O.message}`);return}else console.log("✓ .agents/skills/ directory exists");let q=v(Q,"docs"),V=v(q,"SUPERPOWERS.md"),W=v(I.superpowersRepo,".agents","templates","SUPERPOWERS.md.template");if(!N(q))try{R(`mkdir -p "${q}"`,{stdio:"pipe"}),console.log("✓ Created .agents/docs/ directory")}catch(O){console.log(`⚠️  Failed to create docs directory: ${O.message}`)}else console.log("✓ .agents/docs/ directory exists");let K=N($);if(K){let O=new Date().toISOString().split("T")[0],h=`${$}.backup-${O}`;try{R(`cp "${$}" "${h}"`,{stdio:"pipe"}),console.log(`✓ Backed up existing AGENTS.md to ${o(h).base}`)}catch(Yz){console.log(`✗ Failed to backup AGENTS.md: ${Yz.message}`);return}}let H;try{H=s(B,"utf8")}catch(O){console.log(`✗ Failed to read template: ${O.message}`);return}let G=f();if(H=H.replace(/\{\{VERSION\}\}/g,G),N(W))try{let O=s(W,"utf8"),h=new Date().toISOString().split("T")[0];O=O.replace(/\{\{VERSION\}\}/g,G),O=O.replace(/\{\{DATE\}\}/g,h),r(V,O,"utf8"),console.log("✓ Created .agents/docs/SUPERPOWERS.md")}catch(O){console.log(`⚠️  Failed to create SUPERPOWERS.md: ${O.message}`)}else console.log("⚠️  SUPERPOWERS.md.template not found");let J=[],U=v(z,".github","copilot-instructions.md"),_=v(I.home,".github","copilot-instructions.md");if(N(U)||N(_)||x.copilot.check())J.push("github-copilot");let w=v(z,"CLAUDE.md"),b=v(Q,"CLAUDE.md");if(N(w)||N(b)||x.claude.check())J.push("claude-code");if(x.opencode.check())J.push("opencode");console.log(`
Detected platforms for project: ${J.join(", ")||"none"}
`);let y=J.filter((O)=>["github-copilot","opencode"].includes(O)),A=Ez($,H,y,!K);if(A.created)console.log(`✓ Created AGENTS.md with platform tool mappings (${$===Z?"root":".agents/"})`);else if(A.updated)console.log(`✓ Updated AGENTS.md with platform tool mappings (${$===Z?"root":".agents/"})`);else if(A.error)console.log("⚠️  Failed to update AGENTS.md");let c=N(w)?w:b,E=Ez(c,H,["claude-code"],!1);if(E.created)console.log(`✓ Created CLAUDE.md with Claude Code tool mappings (${c===w?"root":".agents/"})`);else if(E.updated){if(console.log(`✓ Updated CLAUDE.md with Claude Code tool mappings (${c===w?"root":".agents/"})`),E.backup)console.log(`  Backed up to ${o(E.backup).base}`)}else if(E.skipped)console.log("ℹ️  Skipped CLAUDE.md (does not exist)");else if(E.error)console.log("⚠️  Failed to update CLAUDE.md");let L=v(z,".github","copilot-instructions.md"),C={skipped:!0};if(J.includes("github-copilot")){if(C=CX(z),C.created)console.log("✓ Created .github/copilot-instructions.md with Superpowers instructions");else if(C.updated){if(console.log("✓ Updated .github/copilot-instructions.md with Superpowers instructions"),C.backup)console.log(`  Backed up to ${o(C.backup).base}`)}else if(C.error)console.log(`⚠️  Failed to update .github/copilot-instructions.md: ${C.message||""}`)}else console.log("ℹ️  Skipped .github/copilot-instructions.md (GitHub Copilot not detected)");let D=!N(Z)&&!N(Y);if(N(L)&&D){let O=Ez(L,H,["github-copilot"],!1);if(O.updated){if(console.log("✓ Updated .github/copilot-instructions.md with GitHub Copilot tool mappings"),O.backup)console.log(`  Backed up to ${o(O.backup).base}`)}else if(O.error)console.log("⚠️  Failed to update .github/copilot-instructions.md with tool mappings")}else if(N(L)&&!D)console.log("ℹ️  Skipped .github/copilot-instructions.md tool mappings (AGENTS.md exists, using that instead)");else if(!N(L)&&!J.includes("github-copilot"))console.log("ℹ️  Skipped .github/copilot-instructions.md tool mappings (does not exist)");let k=`
# Setup complete!

Your project now has:
  - .agents/ directory structure`;if(A.updated||A.created)k+=`
  - AGENTS.md with universal skills instructions`;if(E.updated||E.created)k+=`
  - CLAUDE.md with Claude Code skills instructions`;if(C.updated||C.created)k+=`
  - .github/copilot-instructions.md with Superpowers instructions`;k+=`
  - .agents/skills/ ready for project-specific skills`,k+=`
  - .agents/docs/SUPERPOWERS.md for detailed reference
`,console.log(k)},jX=()=>{let z=[...["brainstorming.prompt.md","execute-plan.prompt.md","write-plan.prompt.md","setup-skills.prompt.md","create-meta-prompt.md","finding-skills.prompt.md","skills.prompt.md","use-skill.prompt.md","using-a-skill.prompt.md"].map((X)=>v(I.vscodeUserDir,"prompts",X)),...["brainstorm.md","create-meta-prompt.md","execute-plan.md","finding-skills.md","skills.md","use-skill.md","using-a-skill.md","write-plan.md","setup-skills.md"].map((X)=>v(I.home,".claude","commands",X)),...["brainstorm.md","create-meta-prompt.md","execute-plan.md","finding-skills.md","skills.md","use-skill.md","using-a-skill.md","write-plan.md","setup-skills.md"].map((X)=>v(I.home,".config","opencode","command",X))],Q=0;for(let X of z)if(N(X))try{JQ(X),Q++}catch(Z){console.log(`  ⚠️  Could not remove ${X}: ${Z.message}`)}if(Q>0)console.log(`✓ Removed ${Q} legacy prompt/command file${Q!==1?"s":""}`);else console.log("✓ No legacy prompt/command files found")},DX=()=>{let z=0;for(let Q of Bz){let X=Q.skillsDir(),Z=v(X,"superpowers");try{if(vX(Z).isSymbolicLink())JQ(Z),console.log(`✓ Removed legacy superpowers symlink from ${X.replace(I.home,"~")}`),z++}catch{}}if(z===0)console.log("✓ No legacy superpowers directory symlinks found")},LQ=async()=>{l(),console.log(`# Superpowers Bootstrap for Agents
# ==================================
`);let z=process.argv.includes("--no-update"),X=new Set(["copilot","claude","opencode"].filter((B)=>process.argv.includes(`--force-${B}`))),Z=X.size>0;if(!z){let B=zz(),q=await Wz();if(q.error)console.log(`## Update Check

⚠️  Could not check for updates (network issue)

---
`);else if(q.hasUpdates)console.log(`## Update Available
`),console.log(`⚠️  Update available: v${q.localVersion} → v${q.remoteVersion}
    To update, run: \`npm install -g @complexthings/superpowers-agent\``),console.log(`
---
`)}if(!Z)Fz(),console.log(`---
`);if(!Z)console.log(`## Cleaning Up Legacy Files
`),jX(),console.log(`
---
`);if(!Z||X.has("copilot")){if(console.log(`## GitHub Copilot Integration
`),console.log("✓ Skill symlinks handled in sync step below"),Qz("copilot")||N(v(I.home,".copilot"))||X.has("copilot")){let q=ZQ();if(q.created)console.log(`✓ Installed Copilot sessionStart hook -> ${q.path.replace(I.home,"~")}`);else if(q.updated)console.log(`✓ Updated Copilot sessionStart hook -> ${q.path.replace(I.home,"~")}`);else if(q.existed)console.log("✓ Copilot sessionStart hook already up to date");else if(q.error)console.log(`⚠️  Failed to install Copilot sessionStart hook: ${q.message}`)}else console.log("ℹ️  Skipped Copilot sessionStart hook (Copilot CLI not detected; run with --force-copilot to install anyway)");console.log(`
---
`)}if(!Z||X.has("claude")){if(console.log(`## Claude Code Integration
`),!x.claude.check()&&!X.has("claude"))console.log(`⚠️  Skipped (${x.claude.name} CLI not detected)
\uD83D\uDCA1 To enable Claude Code integration:
   1. Install Claude Code: ${x.claude.installUrl}
   2. Run: superpowers-agent bootstrap --force-claude`);else{console.log("✓ Skill symlinks handled in sync step below");let q=YQ();if(q.created)console.log(`✓ Installed SessionStart hook -> ${I.claudeSettings.replace(I.home,"~")}`);else if(q.updated){if(console.log(`✓ ${q.existed?"Refreshed":"Added"} SessionStart hook in ${I.claudeSettings.replace(I.home,"~")}`),q.backup)console.log(`  Backed up to ${o(q.backup).base}`)}else if(q.error)console.log(`⚠️  Failed to install SessionStart hook: ${q.message}`)}console.log(`
---
`)}if(!Z||X.has("opencode")){if(console.log(`## OpenCode Integration
`),x.opencode.check())QQ();else console.log(`⚠️  Skipped (${x.opencode.name} CLI not detected)
\uD83D\uDCA1 To enable OpenCode integration:
   1. Install OpenCode: ${x.opencode.installUrl}
   2. Run: superpowers-agent ${x.opencode.bootstrapCommand}`);console.log(`
---
`)}if(!Z){console.log(`## Generating Platform-Specific Files
`);let B=ez();console.log(`Detected platforms: ${B.join(", ")||"none"}
`);let q=v(I.superpowersRepo,".agents","templates","AGENTS.md.template"),V="";try{V=s(q,"utf8")}catch(W){console.log(`⚠️  Could not read AGENTS.md template: ${W.message}
`)}if(V){let W=f();V=V.replace(/\{\{VERSION\}\}/g,W);let K=v(I.home,".agents","AGENTS.md"),H=Ez(K,V,B,!0);if(H.created)console.log(`✓ Created ${K}`);else if(H.updated)console.log(`✓ Updated ${K}`)}console.log(`
---
`)}console.log(`## Cleaning Up Legacy Superpowers Dir Symlinks
`),DX(),console.log(`
---
`),console.log(`## Syncing Repo Skills -> ~/.agents/skills/
`);let Y=KQ();if(Y.created>0||Y.updated>0)console.log(`  ✓ ${Y.created} created, ${Y.updated} updated, ${Y.existed} already current`);else if(Y.errors.length>0)for(let B of Y.errors)console.log(`  ⚠️  ${B}`);else console.log(`  ✓ ${Y.existed} skill symlinks already up to date`);console.log(`
---
`),console.log(`## Reconciling Retired Repo-Managed Skill Symlinks
`);let $=GQ();if($.removed.length>0){console.log(`  ✓ Removed ${$.removed.length} retired skill symlink(s):`);for(let B of $.removed)console.log(`    - ${B}`)}else console.log("  ✓ No retired repo-managed skill symlinks found");console.log(`
---
`),console.log(`# Bootstrap Complete!
`),console.log("✓ All integrations installed"),console.log("✓ Skills system ready"),console.log(`✓ Skill symlinks synced
`),console.log("Next steps:"),console.log("  - Use your platform's native skill tool to see available skills"),console.log("  - Run `superpowers-agent setup-skills` in your project directory")};import{existsSync as F,readFileSync as Vz,mkdirSync as mX,rmSync as yz,cpSync as uX,lstatSync as hX}from"fs";import{join as M,dirname as Gz,parse as Xz,sep as dX}from"path";import{execSync as j}from"child_process";import{homedir as u}from"os";import{createInterface as pX}from"readline";import{readFileSync as xX,existsSync as Dz,unlinkSync as RX,lstatSync as yX,mkdirSync as SX}from"fs";import{join as fX,dirname as kX}from"path";import{join as t}from"path";import{homedir as bz}from"os";var Cz={github:{name:"github",sourceDir:".github/agents",sourceExt:".agent.md",getDestDir:()=>t(I.vscodeUserDir,"prompts"),getExtraDestDirs:()=>[t(bz(),".copilot","agents")],destExt:".agent.md"},opencode:{name:"opencode",sourceDir:".opencode/agents",sourceExt:".md",getDestDir:()=>t(bz(),".config","opencode","agents"),destExt:".md"},claude:{name:"claude",sourceDir:".claude/agents",sourceExt:".md",getDestDir:()=>t(bz(),".claude","agents"),destExt:".md"}};function UQ(z,Q,X){let Z=Cz[Q];if(!Z)return null;return t(z,Z.sourceDir,`${X}${Z.sourceExt}`)}function OQ(z,Q){let X=Cz[z];if(!X)return[];return[X.getDestDir(),...X.getExtraDestDirs?X.getExtraDestDirs():[]].map((Y)=>t(Y,`${Q}${X.destExt}`))}function jz(){return Object.keys(Cz)}function PX(z){if(!z||typeof z!=="object")return{valid:!1,error:"agents.json must be a JSON object"};if(!z.version||typeof z.version!=="string")return{valid:!1,error:'agents.json must have a "version" string field'};if(!z.agents||typeof z.agents!=="object")return{valid:!1,error:'agents.json must have an "agents" object field'};let Q=jz(),X=[];for(let[Z,Y]of Object.entries(z.agents)){if(!Q.includes(Z)){X.push(Z);continue}if(!Array.isArray(Y))return{valid:!1,error:`agents.${Z} must be an array of agent names`};for(let $ of Y)if(typeof $!=="string")return{valid:!1,error:`agents.${Z} must contain only string agent names`}}return{valid:!0,data:z,skippedPlatforms:X}}function gX(z,Q){if(!Dz(z))return{installed:!1,error:`Source file not found: ${z}`};if(Dz(Q))try{if(!yX(Q).isSymbolicLink())RX(Q)}catch(Z){}SX(kX(Q),{recursive:!0});let X=HQ(z,Q);if(X.error)return{installed:!1,error:X.error};return{installed:!0}}function xz(z,Q={}){let X=fX(z,"agents.json");if(!Dz(X))return;let Z;try{Z=JSON.parse(xX(X,"utf8"))}catch(J){console.log(`
  ⚠ Could not parse agents.json: ${J.message}`);return}let Y=PX(Z);if(!Y.valid){console.log(`
  ⚠ Invalid agents.json: ${Y.error}`);return}for(let J of Y.skippedPlatforms)console.log(`  ⚠ Skipping unknown platform: ${J}`);let $=Z.repository||"unknown",B=Z.version,q=Q.isUpdate?"Updating":"Installing",V=Q.isUpdate?"Updated":"Installed";console.log(`
  \uD83D\uDCE6 Found agents.json (${$} v${B})`);let W=jz(),K=0,H=0,G=[];for(let[J,U]of Object.entries(Z.agents)){if(!W.includes(J))continue;console.log(`
  ${q} ${J} agents:`);for(let _ of U){let w=UQ(z,J,_),b=OQ(J,_),y=[],A=[];for(let c of b){let E=gX(w,c);if(E.installed)y.push(c);else A.push(E.error)}if(y.length>0)console.log(`    ✓ ${_}`),G.push({platformKey:J,agentName:_,sourcePath:w,destPaths:y}),K++;else console.log(`    ✗ ${_}: ${A.join("; ")}`),H++}}if(G.length>0){let J=S(!0);if(!J.installedAgents)J.installedAgents={};if(!J.installedAgents[$])J.installedAgents[$]={version:B,agents:{}};J.installedAgents[$].version=B;for(let{platformKey:U,agentName:_,sourcePath:w,destPaths:b}of G){if(!J.installedAgents[$].agents[U])J.installedAgents[$].agents[U]={};J.installedAgents[$].agents[U][_]={source:w,destination:b[0],destinations:b,installedAt:new Date().toISOString()}}m(J,!0)}if(K>0)console.log(`
  ${V} ${K} agent(s)${H>0?` (${H} failed)`:""}`)}var TQ=(z,Q)=>{if(Q.type==="local"){let Z=z;for(let Y=0;Y<10;Y++){if(F(M(Z,"agents.json")))return Z;let $=Gz(Z);if($===Z)break;Z=$}return z}let X=M(".agents","tmp");if(z.includes(X)){let Z=z.indexOf(X),$=z.substring(Z+X.length+1).split(dX)[0];return M(z.substring(0,Z),".agents","tmp",$)}return z},wQ=(z,Q)=>{let X=M(u(),".agents","repos"),Z=Q.replace(/^@/,"").replace(/[^a-zA-Z0-9_-]/g,"_"),Y=M(X,Z);try{if(mX(X,{recursive:!0}),F(Y))yz(Y,{recursive:!0,force:!0});return uX(z,Y,{recursive:!0}),Y}catch($){return console.log(`  Warning: Could not persist repository for agents: ${$.message}`),null}},Sz=(z)=>{let Q=z.match(/^https:\/\/github\.com\/([^/]+)\/([^/]+)\/tree\/([^/]+)\/(.+)$/);if(Q){let[,Y,$,B,q]=Q;return{type:"git-tree",repoUrl:`https://github.com/${Y}/${$}.git`,branch:B,path:q,original:z}}let X=z.match(/^git@github\.com:([^/]+)\/([^/]+?)(?:\.git)?$/);if(X){let[,Y,$]=X;return{type:"git-repo",repoUrl:`git@github.com:${Y}/${$}.git`,branch:null,path:null,original:z}}let Z=z.match(/^https:\/\/github\.com\/([^/]+)\/([^/]+?)(?:\.git)?$/);if(Z){let[,Y,$]=Z;return{type:"git-repo",repoUrl:`https://github.com/${Y}/${$}.git`,branch:null,path:null,original:z}}if(F(z))return{type:"local",path:z,original:z};return null},fz=(z)=>{let Q=z.includes("--global")||z.includes("-g"),X=z.includes("--project")||z.includes("-p");if(Q)return M(u(),".agents","skills");if(X)return M(process.cwd(),".agents","skills");let Z=M(process.cwd(),".agents","config.json"),Y=M(u(),".agents","config.json");for(let $ of[Z,Y])if(F($))try{if(JSON.parse(Vz($,"utf8")).installLocation==="project")return M(process.cwd(),".agents","skills")}catch(B){}return M(u(),".agents","skills")},kz=(z,Q)=>{let X=M(u(),".agents","tmp",`skill-install-${Date.now()}`);try{if(j(`mkdir -p "${X}"`,{stdio:"pipe"}),Q)j(`git clone --branch ${Q} --depth 1 "${z}" "${X}"`,{stdio:"pipe",timeout:30000});else j(`git clone --depth 1 "${z}" "${X}"`,{stdio:"pipe",timeout:30000});return X}catch(Z){try{j(`rm -rf "${X}"`,{stdio:"pipe"})}catch{}throw Error(`Failed to clone repository: ${Z.message}`)}},e=(z)=>{let Q=M(z,"skill.json");if(!F(Q))return null;try{let X=Vz(Q,"utf8");return JSON.parse(X)}catch(X){throw Error(`Failed to read skill.json: ${X.message}`)}},Kz=(z,Q,X,Z)=>{let Y=M(z,Q);if(!F(Y)){Z.errors.push(`Skill directory not found: ${Q}`);return}let $=e(Y);if(!$){Z.errors.push(`No skill.json found in: ${Q}`);return}let B=$.name||Q,q=B.split("/").pop()||B,V=M(X,q),W=Gz(V);try{j(`mkdir -p "${W}"`,{stdio:"pipe"})}catch(K){Z.errors.push(`Failed to create directory ${W}: ${K.message}`);return}try{if(F(V))j(`rm -rf "${V}"`,{stdio:"pipe"});j(`cp -R "${Y}" "${V}"`,{stdio:"pipe"}),Z.installed.push({name:q,path:V,title:$.title||$.description||q})}catch(K){Z.errors.push(`Failed to install ${Q}: ${K.message}`)}},MQ=(z,Q,X,Z,Y=null)=>{let $=S(Z);if(!$.installedSkills)$.installedSkills={};let B={source:z,installBase:Q,installedAt:new Date().toISOString(),skills:X.installed.map((q)=>({name:q.name,path:q.path}))};if($.installedSkills[z]=B,Y&&Y!==z)$.installedSkills[Y]=B;m($,Z)},NQ=()=>{let z=process.argv.slice(3),Q=z.find((H)=>!H.startsWith("-")),X=z.filter((H)=>H.startsWith("-"));if(!Q){console.log(`Usage: superpowers-agent add <url-or-path|@alias> [skill-path] [options]

Options:
  --global, -g   Install skills globally in ~/.agents/skills/ (default)
  --project, -p  Install skills in current project's .agents/skills/

Examples:
  superpowers-agent add https://github.com/example/skills
  superpowers-agent add https://github.com/example/repo/tree/main/skills
  superpowers-agent add ~/my-local-skills
  superpowers-agent add https://github.com/example/skills --project
  superpowers-agent add ~/my-local-skills --global
  superpowers-agent add @myrepo path/to/skill
  superpowers-agent add @myrepo path/to/skills --project

Description:
  Installs skill(s) from a Git repository, local directory, or repository alias.
  Supports repositories with single or multiple skills.
  Reads skill.json to determine skill names and installation paths.

  Repository aliases can be added using:
    superpowers-agent add-repository <git-url> [--as=@alias]`);return}console.log(`Installing skill(s)...
`);let Z=!X.includes("--project")&&!X.includes("-p"),Y=null,$=null,B=null;if(Q.startsWith("@")){let H=Q;B=H,$=z.find((_,w)=>w>0&&!_.startsWith("-")&&_!==H);let G=P(!1),J=P(!0);if(G[H])Y=G[H],console.log(`Using project repository alias: ${H}`);else if(J[H])Y=J[H],console.log(`Using global repository alias: ${H}`);else{console.log(`Error: Repository alias not found: ${H}`),console.log(`
Available aliases:`);let _={...J,...G};if(Object.keys(_).length===0)console.log("  (none)"),console.log(`
Add a repository using:`),console.log("  superpowers-agent add-repository <git-url>");else for(let[w,b]of Object.entries(_))console.log(`  ${w} -> ${b}`);return}let U=F(Y);if($)if(U)Q=M(Y,$),console.log(`Repository path: ${Y}`),console.log(`Skill path: ${$}
`);else Q=Y,console.log(`Repository URL: ${Y}`),console.log(`Skill path: ${$}
`);else if(Q=Y,U)console.log(`Repository path: ${Y}
`);else console.log(`Repository URL: ${Y}
`)}let q=Sz(Q);if(!q){console.log(`Error: Invalid URL or path not found: ${Q}`);return}if($&&q.type==="git-repo"&&!q.path)q.path=$;let V=fz(X);console.log(`Install location: ${V}
`);let W,K=!1;try{if(q.type==="git-repo"||q.type==="git-tree"){if(console.log(`Cloning repository: ${q.repoUrl}`),q.branch)console.log(`Branch: ${q.branch}`);if(W=kz(q.repoUrl,q.branch),K=!0,q.path){if(W=M(W,q.path),!F(W))throw Error(`Path not found in repository: ${q.path}`)}console.log("")}else W=q.path;let H=e(W);if(!H)throw Error("No skill.json found in source directory");let G={installed:[],errors:[],source:q.original};if(H.skills&&Array.isArray(H.skills)){console.log(`Found ${H.skills.length} skill(s) to install
`);for(let _ of H.skills)Kz(W,_,V,G)}else{let _=H.name||Xz(W).base;Kz(Gz(W),Xz(W).base,V,G)}let J=TQ(W,q),U=M(J,"agents.json");if(F(U)){let _=J;if(K){let w;try{w=JSON.parse(Vz(U,"utf8"))}catch{}let b=w?.repository||q.original,y=wQ(J,b);if(y)_=y}xz(_,{isUpdate:!1})}if(K&&W)try{let _=W.split("/.agents/tmp/")[0]+"/.agents/tmp/"+W.split("/.agents/tmp/")[1].split("/")[0];j(`rm -rf "${_}"`,{stdio:"pipe"})}catch{}if(console.log(`
**Successfully installed skills:**`),console.log(`- Source: ${G.source}`),G.installed.length>0)for(let _ of G.installed)console.log(`  - Installed: ${_.name} at ${_.path}`),console.log(`    ${_.title}`);if(G.errors.length>0){console.log(`
**Errors:**`);for(let _ of G.errors)console.log(`  - ${_}`)}if(G.installed.length===0&&G.errors.length===0)console.log("  No skills were installed");if(G.installed.length>0)MQ(q.original,V,G,Z,B)}catch(H){if(K&&W)try{let G=W.split("/.agents/tmp/")[0]+"/.agents/tmp/"+W.split("/.agents/tmp/")[1].split("/")[0];j(`rm -rf "${G}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${H.message}`)}},vQ=()=>{let z=process.argv.slice(3),Q=z.find((H)=>!H.startsWith("-")),X=z.filter((H)=>H.startsWith("-"));if(!Q){console.log(`Usage: superpowers-agent pull <url-or-path|@alias> [skill-path] [options]

Options:
  --global, -g   Update skills globally in ~/.agents/skills/ (default)
  --project, -p  Update skills in current project's .agents/skills/

Examples:
  superpowers-agent pull https://github.com/example/skills
  superpowers-agent pull https://github.com/example/repo/tree/main/skills
  superpowers-agent pull ~/my-local-skills
  superpowers-agent pull https://github.com/example/skills --project
  superpowers-agent pull ~/my-local-skills --global
  superpowers-agent pull @myrepo
  superpowers-agent pull @myrepo path/to/skill
  superpowers-agent pull @myrepo path/to/skills --project

Description:
  Updates or adds skill(s) from a Git repository, local directory, or repository alias.
  If skills already exist, they will be updated (replaced).
  If skills don't exist, they will be added.
  Supports repositories with single or multiple skills.
  Reads skill.json to determine skill names and installation paths.
  
  Repository aliases can be added using:
    superpowers-agent add-repository <git-url> [--as=@alias]`);return}console.log(`Updating skill(s)...
`);let Z=!X.includes("--project")&&!X.includes("-p"),Y=null,$=null,B=null;if(Q.startsWith("@")){let H=Q;B=H,$=z.find((_,w)=>w>0&&!_.startsWith("-")&&_!==H);let G=P(!1),J=P(!0);if(G[H])Y=G[H],console.log(`Using project repository alias: ${H}`);else if(J[H])Y=J[H],console.log(`Using global repository alias: ${H}`);else{console.log(`Error: Repository alias not found: ${H}`),console.log(`
Available aliases:`);let _={...J,...G};if(Object.keys(_).length===0)console.log("  (none)"),console.log(`
Add a repository using:`),console.log("  superpowers-agent add-repository <git-url>");else for(let[w,b]of Object.entries(_))console.log(`  ${w} -> ${b}`);return}let U=F(Y);if($)if(U)Q=M(Y,$),console.log(`Repository path: ${Y}`),console.log(`Skill path: ${$}
`);else Q=Y,console.log(`Repository URL: ${Y}`),console.log(`Skill path: ${$}
`);else if(Q=Y,U)console.log(`Repository path: ${Y}
`);else console.log(`Repository URL: ${Y}
`)}let q=Sz(Q);if(!q){console.log(`Error: Invalid URL or path not found: ${Q}`);return}if($&&q.type==="git-repo"&&!q.path)q.path=$;let V=fz(X);console.log(`Install location: ${V}
`);let W,K=!1;try{if(q.type==="git-repo"||q.type==="git-tree"){if(console.log(`Cloning repository: ${q.repoUrl}`),q.branch)console.log(`Branch: ${q.branch}`);if(W=kz(q.repoUrl,q.branch),K=!0,q.path){if(W=M(W,q.path),!F(W))throw Error(`Path not found in repository: ${q.path}`)}console.log("")}else W=q.path;let H=e(W);if(!H)throw Error("No skill.json found in source directory");let G={installed:[],errors:[],source:q.original};if(H.skills&&Array.isArray(H.skills)){console.log(`Found ${H.skills.length} skill(s) to update
`);for(let _ of H.skills)Kz(W,_,V,G)}else{let _=H.name||Xz(W).base;Kz(Gz(W),Xz(W).base,V,G)}let J=TQ(W,q),U=M(J,"agents.json");if(F(U)){let _=J;if(K){let w;try{w=JSON.parse(Vz(U,"utf8"))}catch{}let b=w?.repository||q.original,y=wQ(J,b);if(y)_=y}xz(_,{isUpdate:!0})}if(K&&W)try{let _=W.split("/.agents/tmp/")[0]+"/.agents/tmp/"+W.split("/.agents/tmp/")[1].split("/")[0];j(`rm -rf "${_}"`,{stdio:"pipe"})}catch{}if(console.log(`
**Successfully updated skills:**`),console.log(`- Source: ${G.source}`),G.installed.length>0)for(let _ of G.installed)console.log(`  - Updated: ${_.name} at ${_.path}`),console.log(`    ${_.title}`);if(G.errors.length>0){console.log(`
**Errors:**`);for(let _ of G.errors)console.log(`  - ${_}`)}if(G.installed.length===0&&G.errors.length===0)console.log("  No skills were updated");if(G.installed.length>0)MQ(q.original,V,G,Z,B)}catch(H){if(K&&W)try{let G=W.split("/.agents/tmp/")[0]+"/.agents/tmp/"+W.split("/.agents/tmp/")[1].split("/")[0];j(`rm -rf "${G}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${H.message}`)}},cX=(z)=>new Promise((Q)=>{let X=pX({input:process.stdin,output:process.stdout});X.question(z,(Z)=>{X.close(),Q(Z.trim())})}),Rz=(z)=>{try{return hX(z).isSymbolicLink()}catch{return!1}},AQ=async()=>{let z=process.argv.slice(3),Q=z.find((E)=>!E.startsWith("-")),X=z.filter((E)=>E.startsWith("-"));if(!Q){console.log(`Usage: superpowers-agent rm <url-or-path|@alias> [options]

Options:
  --global, -g   Remove globally installed skills (default)
  --project, -p  Remove project-installed skills

Examples:
  superpowers-agent rm https://github.com/example/skills
  superpowers-agent rm @myrepo
  superpowers-agent rm ~/my-local-skills`);return}let Z=!X.includes("--project")&&!X.includes("-p"),Y=Q,$=null;if(Q.startsWith("@")){$=Q;let E=P(!1),L=P(!0);if(E[Q])Y=E[Q];else if(L[Q])Y=L[Q];else{console.log(`Error: Repository alias not found: ${Q}`);return}}let B=cz(Z),q=B[Q]||B[Y],V=q?.installBase||fz(X),W=[];if(q)W=q.skills;else{console.log(`No tracking data found. Attempting to re-resolve skills from source...
`);let E=Sz(Y);if(!E){console.log(`Error: Invalid URL or path not found: ${Y}`);return}let L,C=!1;try{if(E.type==="git-repo"||E.type==="git-tree"){if(console.log(`Cloning repository: ${E.repoUrl}`),L=kz(E.repoUrl,E.branch),C=!0,E.path){if(L=M(L,E.path),!F(L))throw Error(`Path not found in repository: ${E.path}`)}}else L=E.path;let D=e(L);if(!D)throw Error("No skill.json found in source");let k=D.skills&&Array.isArray(D.skills)?D.skills:[D.name||Xz(L).base];for(let O of k){let h=O;if(D.skills){let Yz=M(L,O);if(F(Yz)){let Pz=e(Yz);if(Pz?.name)h=Pz.name}}W.push({name:h,path:M(V,h)})}if(C)try{let O=L.split("/.agents/tmp/")[0]+"/.agents/tmp/"+L.split("/.agents/tmp/")[1].split("/")[0];j(`rm -rf "${O}"`,{stdio:"pipe"})}catch{}}catch(D){if(C&&L)try{let k=L.split("/.agents/tmp/")[0]+"/.agents/tmp/"+L.split("/.agents/tmp/")[1].split("/")[0];j(`rm -rf "${k}"`,{stdio:"pipe"})}catch{}console.log(`Error resolving source: ${D.message}`);return}}let K=W.filter((E)=>F(E.path)||Rz(E.path)),H=[];for(let E of W)for(let L of Bz){let C=M(L.skillsDir(),E.name);if(F(C)||Rz(C))H.push({platform:L.name,path:C})}let G=nz(),J=$||Y,U=[];if(G[J])for(let[,E]of Object.entries(G[J].agents||{}))for(let[,L]of Object.entries(E)){let C=L.destinations||(L.destination?[L.destination]:[]);for(let D of C)if(D&&(F(D)||Rz(D)))U.push({path:D})}let _=J.replace(/^@/,"").replace(/[^a-zA-Z0-9_-]/g,"_"),w=M(u(),".agents","repos",_),b=F(w),y=u(),A=(E)=>E.replace(y,"~");if(K.length===0&&H.length===0&&U.length===0&&!b){console.log("Nothing to remove.");return}if(console.log("Will remove:"),K.length>0){console.log("  Skills:");for(let E of K)console.log(`    ${A(E.path)}`)}if(H.length>0){console.log("  Platform symlinks:");for(let E of H)console.log(`    ${A(E.path)}`)}if(U.length>0){console.log("  Agents:");for(let E of U)console.log(`    ${A(E.path)}`)}if(b)console.log("  Persisted repo:"),console.log(`    ${A(w)}`);if((await cX(`
Proceed? [y/N] `)).toLowerCase()!=="y"){console.log("Aborted.");return}for(let E of H){let L=Az(E.path);if(L.removed)console.log(`Removed symlink: ${A(E.path)}`),BQ(E.platform,E.path,"skills");else if(L.error==="Symlink does not exist")console.log(`Already removed: ${A(E.path)}`);else console.log(`Warning: ${A(E.path)}: ${L.error}`)}for(let E of U){let L=Az(E.path);if(L.removed)console.log(`Removed agent: ${A(E.path)}`);else if(L.error==="Symlink does not exist")console.log(`Already removed: ${A(E.path)}`);else console.log(`Warning: ${A(E.path)}: ${L.error}`)}if(b)try{yz(w,{recursive:!0,force:!0}),console.log(`Removed persisted repo: ${A(w)}`)}catch(E){console.log(`Warning: Failed to remove persisted repo: ${E.message}`)}for(let E of K){if(!F(E.path)){console.log(`Already removed: ${A(E.path)}`);continue}try{yz(E.path,{recursive:!0,force:!0}),console.log(`Removed skill: ${A(E.path)}`)}catch(L){console.log(`Warning: Failed to remove ${A(E.path)}: ${L.message}`)}}if(Iz(Q,Z),Y!==Q)Iz(Y,Z);if(G[J]){let E=S(!0);if(E.installedAgents)delete E.installedAgents[J],m(E,!0)}console.log(`
Done.`)},FQ=()=>{let z=process.argv.slice(3),Q=z.find((W)=>!W.startsWith("-")),X=z.filter((W)=>W.startsWith("-"));if(!Q){console.log(`Usage: superpowers-agent add-repository <git-url> [options]

Options:
  --global, -g        Add repository globally in ~/.agents/config.json (default)
  --project, -p       Add repository in current project's .agents/config.json
  --as=<alias>        Specify custom alias for the repository

Examples:
  superpowers-agent add-repository https://github.com/example/skills.git
  superpowers-agent add-repository https://github.com/example/skills.git --as=@myskills
  superpowers-agent add-repository https://github.com/example/skills.git --project
  superpowers-agent add-repository https://github.com/example/skills.git --as=@custom --global

Description:
  Adds a skill repository alias to your configuration.
  The repository's skill.json will be read to determine the default alias.
  Use --as to specify a custom alias.
  After adding, you can install skills using: superpowers-agent add @alias path/to/skill`);return}console.log(`Adding repository...
`);let Z=X.some((W)=>W==="--global"||W==="-g"),Y=X.some((W)=>W==="--project"||W==="-p"),$=X.find((W)=>W.startsWith("--as=")),B=$?$.split("=")[1]:null,q=Y?!1:!0,V;try{console.log(`Cloning repository: ${Q}`),V=M(u(),".agents","tmp",`repo-add-${Date.now()}`),j(`mkdir -p "${V}"`,{stdio:"pipe"}),j(`git clone --depth 1 "${Q}" "${V}"`,{stdio:"pipe",timeout:30000}),console.log("");let W=e(V);if(!W)throw Error("No skill.json found in repository");let K;if(B)K=B;else if(W.repository)K=W.repository;else{let J=Q.match(/\/([^/]+?)(?:\.git)?$/);if(J)K="@"+J[1];else throw Error("Could not determine repository alias. Use --as=@alias to specify manually.")}pz(K,Q,q),j(`rm -rf "${V}"`,{stdio:"pipe"});let H=q?"globally":"in project",G=q?M(u(),".agents","config.json"):M(process.cwd(),".agents","config.json");console.log(`✓ Repository added ${H}`),console.log(`  Alias: ${K}`),console.log(`  URL: ${Q}`),console.log(`  Config: ${G}`),console.log(`
You can now install skills using:`),console.log(`  superpowers-agent add ${K} <path-to-skill>`)}catch(W){if(V)try{j(`rm -rf "${V}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${W.message}`)}};var nX=()=>`<EXTREMELY_IMPORTANT>
${"---\n\n**YOU MUST read and use the `leveraging-cli-tools` skill** the moment a task involves using BASH, searching code, parsing JSON/YAML, finding files, structural refactors, data wrangling, or any command that floods your context with verbose output (logs, CI logs, test runs). Load it with your skill tool BEFORE you reach for grep/find/sed or before you read large command output. It routes you to high-performance tools (rg, jq, fd, yq, ast-grep, gh, sd) and reduces output to just the signal — cutting token cost and latency 5-50x. Skipping it wastes tokens and time on work the right tool does instantly."}
</EXTREMELY_IMPORTANT>`,lX=(z="raw")=>{let Q=nX();if(z==="claude")return JSON.stringify({hookSpecificOutput:{hookEventName:"SessionStart",additionalContext:Q}});if(z==="copilot")return JSON.stringify({additionalContext:Q});return Q},bQ=()=>{let z=process.argv.find((X)=>X.startsWith("--format=")),Q=z?z.split("=")[1]:"raw";process.stdout.write(lX(Q)+`
`)};var CQ={bootstrap:LQ,version:tz,"check-updates":()=>{sz().catch((z)=>{console.error(z.message),process.exit(1)})},update:()=>{let z=process.argv.includes("--no-reinstall");Oz({skipReinstall:z})},"config-get":iz,"config-set":az,"setup-skills":IQ,"session-context":bQ,add:NQ,"add-repository":FQ,"list-repositories":oz,pull:vQ,rm:AQ,"install-aliases":Fz,default:()=>{console.log(`Superpowers for Agents
Usage:
  superpowers-agent bootstrap [--no-update] [--force]               # Run complete bootstrap
  superpowers-agent version                                         # Show current version
  superpowers-agent check-updates                                   # Check for updates
  superpowers-agent update [--no-reinstall]                         # Update to latest version
  superpowers-agent config-get                                      # Show configuration
  superpowers-agent config-set <key> <value>                        # Update configuration
  superpowers-agent setup-skills                                    # Initialize project skills
  superpowers-agent session-context [--format=claude|copilot|raw]   # Print session-start context (used by hooks)
  superpowers-agent add <url-or-path|@alias> [path] [options]      # Install skills
  superpowers-agent add-repository <git-url> [--as=@alias] [opts]  # Add repository alias
  superpowers-agent list-repositories                              # List configured repository aliases
  superpowers-agent pull <url-or-path|@alias> [path] [options]     # Update skills
  superpowers-agent rm <url-or-path|@alias> [options]               # Remove skills
  superpowers-agent install-aliases                                 # Install universal aliases (superpowers, superpowers-agent)

Documentation: https://github.com/complexthings/superpowers
`)}},iX=process.argv[2]||"default",aX=CQ[iX]||CQ.default;try{let z=aX();if(z&&typeof z.then==="function")z.catch((Q)=>{console.error(`Error: ${Q.message}`),process.exit(1)})}catch(z){console.error(`Error: ${z.message}`),process.exit(1)}
