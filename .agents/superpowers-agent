#!/bin/sh
// 2>/dev/null; exec "$(command -v bun 2>/dev/null || echo node)" "$0" "$@"
import{relative as AQ,dirname as IQ,join as TX}from"path";import{existsSync as pq}from"fs";import{homedir as XQ,platform as $X}from"os";import{join as U,parse as KX,dirname as YQ}from"path";import{existsSync as $q}from"fs";import{fileURLToPath as HX}from"url";var WX=HX(import.meta.url),Zq=YQ(WX),zX=()=>{let q=XQ();switch($X()){case"darwin":return U(q,"Library","Application Support","Code","User");case"win32":return U(q,"AppData","Roaming","Code","User");case"linux":return U(q,".config","Code","User");default:return U(q,".config","Code","User")}},EX=()=>{let q=process.cwd(),Q=KX(q).root;while(q!==Q){if($q(U(q,".agents")))return q;q=YQ(q)}return process.cwd()},K={home:XQ(),vscodeUserDir:zX(),projectRoot:EX(),get projectClaudeSkills(){return U(this.projectRoot,".claude","skills")},get projectAgentsSkills(){return U(this.projectRoot,".agents","skills")},get projectCopilotSkills(){return U(this.projectRoot,".copilot","skills")},get projectOpencodeSkills(){return U(this.projectRoot,".opencode","skill")},get projectCursorSkills(){return U(this.projectRoot,".cursor","skills")},get projectGeminiSkills(){return U(this.projectRoot,".gemini","skills")},get projectCodexSkills(){return U(this.projectRoot,".codex","skills")},get projectSkillsDir(){return U(this.projectRoot,"skills")},get homePersonalSkills(){return U(this.home,".agents","skills")},get homeClaudeSkills(){return U(this.home,".claude","skills")},get homeCopilotSkills(){return U(this.home,".copilot","skills")},get homeOpencodeSkills(){return U(this.home,".config","opencode","skill")},get homeCursorSkills(){return U(this.home,".cursor","skills")},get homeGeminiSkills(){return U(this.home,".gemini","skills")},get homeCodexSkills(){return U(this.home,".codex","skills")},get bootstrap(){return U(this.projectRoot,".agents","superpowers-bootstrap.md")},get superpowersRepo(){let q=U(Zq,"..","..","..","skills");if($q(q)&&$q(U(Zq,"..","..","..",".github","prompts")))return U(Zq,"..","..","..");return U(this.home,".agents","superpowers")},get homeSuperpowersSkills(){return U(this.superpowersRepo,"skills")},get isSuperpowersRepo(){let q=U(Zq,"..","..","..","skills");return $q(q)&&$q(U(Zq,"..","..","..",".github","prompts"))}};import{readFileSync as ZQ}from"fs";var Kq=(q)=>{try{let X=ZQ(q,"utf8").split(`
`),$={name:"",description:"",whenToUse:""},Y=!1;for(let Z of X){if(Z.trim()==="---"){if(Y)break;Y=!0;continue}if(Y){let H=Z.match(/^(\w+):\s*(.*)$/);if(H){let[,W,B]=H,z={name:"name",description:"description",when_to_use:"whenToUse"};if(z[W])$[z[W]]=B.trim()}}}return $}catch{return{name:"",description:"",whenToUse:""}}},$Q=(q)=>{try{let Q=ZQ(q,"utf8"),X=Kq(q),$=Q.split(`
`),Y=!1,Z=!1,H=[];for(let W of $){if(W.trim()==="---"){if(Y){Z=!0;continue}Y=!0;continue}if(Z||!Y)H.push(W)}return{content:H.join(`
`).trim(),frontmatter:X}}catch(Q){throw Error(`Error reading skill file: ${Q.message}`)}};import{existsSync as kq,readdirSync as WQ,statSync as zQ}from"fs";import{join as uq,relative as JY}from"path";import{readFileSync as BX,existsSync as VX}from"fs";import{join as KQ}from"path";var Hq=(q)=>{let Q=KQ(q,"skill.json");if(!VX(Q))return null;try{let X=BX(Q,"utf8");return JSON.parse(X)}catch(X){return null}};var Sq=(q)=>{if(q.startsWith("superpowers:"))return{type:"superpowers",path:q.substring(12).replace(/^skills\//,"")};if(q.startsWith("claude:"))return{type:"claude",path:q.substring(7).replace(/^skills\//,"")};if(q.startsWith("copilot:"))return{type:"copilot",path:q.substring(8).replace(/^skills\//,"")};if(q.startsWith("opencode:"))return{type:"opencode",path:q.substring(9).replace(/^skills\//,"")};if(q.startsWith("cursor:"))return{type:"cursor",path:q.substring(7).replace(/^skills\//,"")};if(q.startsWith("gemini:"))return{type:"gemini",path:q.substring(7).replace(/^skills\//,"")};return{type:null,path:q.replace(/^skills\//,"")}},HQ=(q,Q)=>{let X=Hq(q);if(!X||!X.helpers||!Array.isArray(X.helpers))return null;let $=X.helpers,Y=Q.toLowerCase(),Z=null,H=0;for(let W of $){let B=W.toLowerCase();if(B===Y){Z=W;break}if(B.includes(Y)){let E=Y.length/B.length;if(E>H)H=E,Z=W}let z=W.split("/").pop().split(".")[0].toLowerCase();if(Y.includes(z)){let E=z.length/Y.length;if(E>H)H=E,Z=W}}return Z?KQ(q,Z):null},Wq={project:{dir:"projectAgentsSkills",prefix:""},claude:{dir:"projectClaudeSkills",prefix:"claude:"},copilot:{dir:"projectCopilotSkills",prefix:"copilot:"},opencode:{dir:"projectOpencodeSkills",prefix:"opencode:"},cursor:{dir:"projectCursorSkills",prefix:"cursor:"},gemini:{dir:"projectGeminiSkills",prefix:"gemini:"},personal:{dir:"homePersonalSkills",prefix:""},personalClaude:{dir:"homeClaudeSkills",prefix:"claude:"},personalCopilot:{dir:"homeCopilotSkills",prefix:"copilot:"},personalOpencode:{dir:"homeOpencodeSkills",prefix:"opencode:"},personalCursor:{dir:"homeCursorSkills",prefix:"cursor:"},personalGemini:{dir:"homeGeminiSkills",prefix:"gemini:"},superpowers:{dir:"homeSuperpowersSkills",prefix:"superpowers:"}};var Iq=(q,Q,X=null)=>{let $=[];if(!kq(q))return $;let Y=(Z,H)=>{if(X!==null&&H>X)return;try{let W=WQ(Z,{withFileTypes:!0});for(let B of W){if(B.name==="node_modules"||B.name===".git"||B.name.startsWith("."))continue;let z=uq(Z,B.name),E=B.isDirectory();if(B.isSymbolicLink())try{E=zQ(z).isDirectory()}catch{continue}if(!E)continue;let V=uq(z,"SKILL.md");if(kq(V))$.push(z);if(X===null||H<X)Y(z,H+1)}}catch{}};return Y(q,0),$};var EQ=(q,Q)=>{if(!kq(q))return[];let X=[],$=Q.toLowerCase().replace(/\\/g,"/"),Y=(Z,H="")=>{try{let W=WQ(Z,{withFileTypes:!0});for(let B of W){let z=uq(Z,B.name),E=H?`${H}/${B.name}`:B.name,V=B.isDirectory();if(B.isSymbolicLink())try{V=zQ(z).isDirectory()}catch{continue}if(V&&(B.name==="node_modules"||B.name===".git"||B.name.startsWith(".")))continue;if(V)Y(z,E);else if(B.name==="SKILL.md"){let v=H.toLowerCase().replace(/\\/g,"/");if(v===$||v.endsWith("/"+$)||v.endsWith($))X.push({file:z,path:H})}}}catch(W){}};return Y(q),X},BQ=(q,Q,X)=>{let $={project:"project skills (.agents/skills/)",claude:"claude skills (.claude/skills/)",copilot:"copilot skills (.copilot/skills/)",opencode:"opencode skills (.opencode/skill/)",cursor:"cursor skills (.cursor/skills/)",gemini:"gemini skills (.gemini/skills/)",personal:"personal skills (~/.agents/skills/)",personalClaude:"personal claude skills (~/.claude/skills/)",personalCopilot:"personal copilot skills (~/.copilot/skills/)",personalOpencode:"personal opencode skills (~/.config/opencode/skill/)",personalCursor:"personal cursor skills (~/.cursor/skills/)",personalGemini:"personal gemini skills (~/.gemini/skills/)",superpowers:"superpowers skills (~/.agents/superpowers/skills/)"}[X];console.log(`Error: Multiple skills match "${q}" in ${$}:
`);for(let Y of Q){let H=Wq[X].prefix+Y.path;try{let W=Kq(Y.file),B=W.description||W.whenToUse||"(no description)";console.log(`  ${H}`),console.log(`    ${B}
`)}catch(W){console.log(`  ${H}`),console.log(`    (unable to read description)
`)}}console.log("Please be more specific. Examples:");for(let Y of Q.slice(0,2)){let Z=Wq[X].prefix;console.log(`  superpowers-agent execute ${Z}${Y.path}`)}};import{existsSync as dq}from"fs";import{join as GX,relative as _X}from"path";var vX=(q)=>{let{type:Q,path:X}=Sq(q),$={superpowers:[K.homeSuperpowersSkills],claude:[K.projectClaudeSkills,K.homeClaudeSkills],copilot:[K.projectCopilotSkills,K.homeCopilotSkills],opencode:[K.projectOpencodeSkills,K.homeOpencodeSkills],gemini:[K.projectGeminiSkills,K.homeGeminiSkills],project:[K.projectAgentsSkills],personal:[K.homePersonalSkills]},Y;if(Q)Y=($[Q]||[]).map((H)=>({type:Q,dir:H}));else if(K.isSuperpowersRepo&&dq(K.projectSkillsDir))Y=[{type:"project",dir:K.projectSkillsDir},{type:"project",dir:K.projectAgentsSkills},{type:"claude",dir:K.projectClaudeSkills},{type:"copilot",dir:K.projectCopilotSkills},{type:"opencode",dir:K.projectOpencodeSkills},{type:"gemini",dir:K.projectGeminiSkills},{type:"personal",dir:K.homePersonalSkills},{type:"personalClaude",dir:K.homeClaudeSkills},{type:"personalCopilot",dir:K.homeCopilotSkills},{type:"personalOpencode",dir:K.homeOpencodeSkills},{type:"personalGemini",dir:K.homeGeminiSkills},{type:"superpowers",dir:K.homeSuperpowersSkills}];else Y=[{type:"project",dir:K.projectAgentsSkills},{type:"claude",dir:K.projectClaudeSkills},{type:"copilot",dir:K.projectCopilotSkills},{type:"opencode",dir:K.projectOpencodeSkills},{type:"gemini",dir:K.projectGeminiSkills},{type:"personal",dir:K.homePersonalSkills},{type:"personalClaude",dir:K.homeClaudeSkills},{type:"personalCopilot",dir:K.homeCopilotSkills},{type:"personalOpencode",dir:K.homeOpencodeSkills},{type:"personalGemini",dir:K.homeGeminiSkills},{type:"superpowers",dir:K.homeSuperpowersSkills}];for(let{type:Z,dir:H}of Y){let W=EQ(H,X);if(W.length===1)return{skillFile:W[0].file,sourceType:Z,actualSkillPath:W[0].path};if(W.length>1)BQ(X,W,Z)}return null},S=(q)=>{let Q=vX(q);if(Q)return Q;let{type:X,path:$}=Sq(q),Y={superpowers:[K.homeSuperpowersSkills],claude:[K.projectClaudeSkills,K.homeClaudeSkills],copilot:[K.projectCopilotSkills,K.homeCopilotSkills],opencode:[K.projectOpencodeSkills,K.homeOpencodeSkills],gemini:[K.projectGeminiSkills,K.homeGeminiSkills],project:[K.projectAgentsSkills],personal:[K.homePersonalSkills]},Z;if(X)Z=(Y[X]||[]).map((W)=>({type:X,dir:W}));else if(K.isSuperpowersRepo&&dq(K.projectSkillsDir))Z=[{type:"project",dir:K.projectSkillsDir},{type:"project",dir:K.projectAgentsSkills},{type:"claude",dir:K.projectClaudeSkills},{type:"copilot",dir:K.projectCopilotSkills},{type:"opencode",dir:K.projectOpencodeSkills},{type:"gemini",dir:K.projectGeminiSkills},{type:"personal",dir:K.homePersonalSkills},{type:"personalClaude",dir:K.homeClaudeSkills},{type:"personalCopilot",dir:K.homeCopilotSkills},{type:"personalOpencode",dir:K.homeOpencodeSkills},{type:"personalGemini",dir:K.homeGeminiSkills},{type:"superpowers",dir:K.homeSuperpowersSkills}];else Z=[{type:"project",dir:K.projectAgentsSkills},{type:"claude",dir:K.projectClaudeSkills},{type:"copilot",dir:K.projectCopilotSkills},{type:"opencode",dir:K.projectOpencodeSkills},{type:"gemini",dir:K.projectGeminiSkills},{type:"personal",dir:K.homePersonalSkills},{type:"personalClaude",dir:K.homeClaudeSkills},{type:"personalCopilot",dir:K.homeCopilotSkills},{type:"personalOpencode",dir:K.homeOpencodeSkills},{type:"personalGemini",dir:K.homeGeminiSkills},{type:"superpowers",dir:K.homeSuperpowersSkills}];for(let{type:H,dir:W}of Z){if(!dq(W))continue;let B=Iq(W,H,null);for(let z of B){let E=Hq(z);if(E&&E.aliases&&Array.isArray(E.aliases)){let V=$.toLowerCase();for(let v of E.aliases)if(v.toLowerCase()===V){let _=GX(z,"SKILL.md"),w=_X(W,z);return{skillFile:_,sourceType:H,actualSkillPath:w}}}}}return null};import{readFileSync as VQ,writeFileSync as GQ,existsSync as mq}from"fs";import{join as k}from"path";import{execSync as LX}from"child_process";var hq=()=>({auto_update:!0,last_update_check:null,last_updated_commit:null}),zq=()=>{let q=k(K.superpowersRepo,".config.json");try{if(!mq(q))return hq();let Q=VQ(q,"utf8");return{...hq(),...JSON.parse(Q)}}catch{return hq()}},Oq=(q)=>{let Q=k(K.superpowersRepo,".config.json"),$={...zq(),...q};try{GQ(Q,JSON.stringify($,null,2))}catch(Y){console.log(`Warning: couldn't save config: ${Y.message}`)}},Eq=(q)=>{let Q=q?k(K.home,".agents","config.json"):k(process.cwd(),".agents","config.json");if(!mq(Q))return{};try{let X=VQ(Q,"utf8");return JSON.parse(X)}catch(X){return{}}},wq=(q,Q)=>{let X=Q?k(K.home,".agents"):k(process.cwd(),".agents"),$=k(X,"config.json");try{if(!mq(X))LX(`mkdir -p "${X}"`,{stdio:"pipe"});return GQ($,JSON.stringify(q,null,2),"utf8"),!0}catch(Y){throw Error(`Failed to write config: ${Y.message}`)}},y=(q=!1)=>{return Eq(q).repositories||{}},_Q=(q,Q,X)=>{let $=Eq(X);if(!$.repositories)$.repositories={};$.repositories[q]=Q,wq($,X)};import{readFileSync as UX}from"fs";import{join as vQ,dirname as JX}from"path";import{fileURLToPath as AX}from"url";import{existsSync as IX}from"fs";import{execSync as OX}from"child_process";var wX=AX(import.meta.url),LQ=JX(wX),g=()=>{try{let q=vQ(LQ,"package.json");if(!IX(q))q=vQ(LQ,"..","..","package.json");return JSON.parse(UX(q,"utf8")).version||"5.4.0"}catch(q){return"5.4.0"}},C=()=>{let q=g();console.log(`^^SAV:${q}^^`)},UQ=async()=>{try{let Q=OX('curl -sS "https://raw.githubusercontent.com/complexthings/superpowers/main/.agents/package.json"',{encoding:"utf8",timeout:1e4}),X=JSON.parse(Q);if(!X.version)return g();return X.version}catch(q){throw Error(`Failed to fetch remote version: ${q.message}`)}},JQ=(q,Q)=>{let X=q.split(".").map(Number),$=Q.split(".").map(Number);for(let Y=0;Y<3;Y++){let Z=X[Y]||0,H=$[Y]||0;if(Z>H)return!0;if(Z<H)return!1}return!1};var bX={project:{dir:"projectAgentsSkills",prefix:""},claude:{dir:"projectClaudeSkills",prefix:"claude:"},copilot:{dir:"projectCopilotSkills",prefix:"copilot:"},opencode:{dir:"projectOpencodeSkills",prefix:"opencode:"},gemini:{dir:"projectGeminiSkills",prefix:"gemini:"},personal:{dir:"homePersonalSkills",prefix:""},personalClaude:{dir:"homeClaudeSkills",prefix:"claude:"},personalCopilot:{dir:"homeCopilotSkills",prefix:"copilot:"},personalOpencode:{dir:"homeOpencodeSkills",prefix:"opencode:"},personalGemini:{dir:"homeGeminiSkills",prefix:"gemini:"},superpowers:{dir:"homeSuperpowersSkills",prefix:"superpowers:"}},MX=(q,Q)=>{let X=TX(q,"SKILL.md"),{dir:$,prefix:Y}=bX[Q],Z=AQ(K[$],q).replace(/\\/g,"/");console.log(`${Y}${Z}`);let{description:H,whenToUse:W}=Kq(X);if(H)console.log(`  ${H}`);if(W)console.log(`  When to use: ${W}`);console.log("")},c=()=>{C();let q=new Set,Q=[{type:"project",dir:K.projectAgentsSkills,maxDepth:null},{type:"claude",dir:K.projectClaudeSkills,maxDepth:null},{type:"copilot",dir:K.projectCopilotSkills,maxDepth:null},{type:"opencode",dir:K.projectOpencodeSkills,maxDepth:null},{type:"gemini",dir:K.projectGeminiSkills,maxDepth:null},{type:"personal",dir:K.homePersonalSkills,maxDepth:null},{type:"personalClaude",dir:K.homeClaudeSkills,maxDepth:null},{type:"personalCopilot",dir:K.homeCopilotSkills,maxDepth:null},{type:"personalOpencode",dir:K.homeOpencodeSkills,maxDepth:null},{type:"personalGemini",dir:K.homeGeminiSkills,maxDepth:null},{type:"superpowers",dir:K.homeSuperpowersSkills,maxDepth:null}];for(let{type:X,dir:$,maxDepth:Y}of Q){let Z=Iq($,X,Y);for(let H of Z){let W=AQ($,H);if(!q.has(W))q.add(W),MX(H,X)}}console.log(`Usage:
  superpowers-agent execute <skill-name> # Load a specific skill`)},OQ=()=>{C();let q=process.argv[3];if(!q){console.log(`Usage: superpowers-agent execute <skill-name-or-alias>

Examples:
  superpowers-agent execute brainstorming
  superpowers-agent execute superpowers:collaboration/brainstorming
  superpowers-agent execute aem/block-collection-and-party
  superpowers-agent execute block-collection  # Using alias

Description:
  Instructs the agent to execute the specified skill.
  Supports skill aliases defined in skill.json files.`);return}let Q=S(q);if(!Q){console.log(`Error: Skill not found: ${q}

Available skills:`),c();return}let{skillFile:X,sourceType:$}=Q,Y=process.cwd(),Z=X.startsWith(Y),H=pq(K.projectAgentsSkills)||pq(K.projectClaudeSkills);if(Z)console.log(`Open this file with your Read Tool: ${X}

<IMPORTANT>
USE YOUR Read Tool to process this file. DO NOT use \`cat\` or print commands.
</IMPORTANT>`);else if(H&&$!=="project")console.log(`Open this file with your Read Tool: ${X}

<IMPORTANT>
1. Try using your Read Tool first
2. If your Read tool fails (file outside workspace), use: cat "${X}"
3. Consider using workspace-local skills for better tool compatibility
   Run: superpowers-agent setup-skills
</IMPORTANT>`);else console.log(`Open this file with your Read Tool: ${X}

<IMPORTANT>
1. Try using your Read Tool first
2. If your Read tool fails (file outside workspace), use: cat "${X}"
3. NEVER skip loading the skill content
</IMPORTANT>`)},wQ=()=>{let q=process.argv[3];if(!q){console.log(`Usage: superpowers-agent path <skill-name-or-alias>

Examples:
  superpowers-agent path brainstorming
  superpowers-agent path superpowers:collaboration/brainstorming
  superpowers-agent path aem/block-collection-and-party
  superpowers-agent path block-collection  # Using alias

Description:
  Outputs the file system path of the specified SKILL.md file.
  Supports skill aliases defined in skill.json files.`);return}let Q=S(q);if(!Q){console.log(`Error: Skill not found: ${q}

Available skills:`),c();return}let{skillFile:X}=Q;console.log(X)},TQ=()=>{let q=process.argv[3];if(!q){console.log(`Usage: superpowers-agent dir <skill-name>

Examples:
  superpowers-agent dir brainstorming
  superpowers-agent dir aem/block-collection-and-party
  superpowers-agent dir block-collection  # Using alias
  superpowers-agent dir superpowers:testing/test-driven-development

Description:
  Returns the directory path where the specified skill is located.
  Supports skill aliases defined in skill.json files.`);return}let Q=S(q);if(!Q){console.log(`Error: Skill not found: ${q}

Available skills:`),c();return}let{skillFile:X}=Q,$=IQ(X);console.log($)},bQ=()=>{C();let q=process.argv[3],Q=process.argv[4];if(!q||!Q){console.log(`Usage: superpowers-agent get-helpers <skill-name> <helper-search-term>

Examples:
  superpowers-agent get-helpers block-collection search-block
  superpowers-agent get-helpers aem/block-collection-and-party get-block-structure
  superpowers-agent get-helpers superpowers:testing/test-driven-development example

Description:
  Searches for helper files within a skill's skill.json configuration.
  Returns the full path to the best matching helper file.
  
  The command uses the skill's aliases if available, so you can use
  short names like "block-collection" instead of the full path.`);return}let X=S(q);if(!X){console.log(`Error: Skill not found: ${q}

Available skills:`),c();return}let{skillFile:$}=X,Y=IQ($),Z=Hq(Y);if(!Z){console.log(`Error: No skill.json found for skill: ${q}
  Location: ${Y}`);return}if(!Z.helpers||!Array.isArray(Z.helpers)||Z.helpers.length===0){console.log(`Error: No helpers defined in skill.json for skill: ${q}
  Location: ${Y}`);return}let H=HQ(Y,Q);if(!H){console.log(`Error: No helper found matching "${Q}" in skill: ${q}`),console.log(`
Available helpers:`);for(let W of Z.helpers)console.log(`  - ${W}`);return}if(!pq(H)){console.log(`Error: Helper file not found: ${H}
  Defined in skill.json but missing from filesystem`);return}console.log(H)},MQ=()=>{let q=zq();console.log("Current configuration:"),console.log(JSON.stringify(q,null,2))},NQ=()=>{let q=process.argv[3],Q=process.argv[4];if(!q||Q===void 0){console.log(`Usage: .agents/superpowers-agent config-set <key> <value>

Available keys:
  auto_update (true/false) - Enable/disable automatic updates during bootstrap

Examples:
  .agents/superpowers-agent config-set auto_update false
  .agents/superpowers-agent config-set auto_update true`);return}let X=Q;if(Q==="true")X=!0;else if(Q==="false")X=!1;let $={};$[q]=X,Oq($),console.log(`✓ Set ${q} = ${X}`)},FQ=()=>{C();let q=y(!0),Q=y(!1),X=[];for(let[Z,H]of Object.entries(q))X.push({alias:Z,url:H,source:"global"});for(let[Z,H]of Object.entries(Q)){let W=X.findIndex((B)=>B.alias===Z);if(W>=0)X[W]={alias:Z,url:H,source:"project"};else X.push({alias:Z,url:H,source:"project"})}if(X.length===0){console.log(`No repository aliases configured.

Add a repository using:
  superpowers-agent add-repository <git-url> [--as=@alias]`);return}let $=Math.max(5,...X.map((Z)=>Z.alias.length)),Y=Math.max(3,...X.map((Z)=>Z.url.length));console.log(`Repositories:
`),console.log(`${"Alias".padEnd($)}  ${"URL".padEnd(Y)}  Source`);for(let Z of X)console.log(`${Z.alias.padEnd($)}  ${Z.url.padEnd(Y)}  (${Z.source})`);console.log(`
Total: ${X.length} repository alias(es)`)};import{execSync as oX}from"child_process";import{execSync as u}from"child_process";var NX=()=>{try{return u("git status --porcelain --untracked-files=no",{cwd:K.superpowersRepo,encoding:"utf8",stdio:"pipe",timeout:3000}).trim().length===0}catch{return!1}},Bq=()=>{try{return u("git rev-parse --abbrev-ref HEAD",{cwd:K.superpowersRepo,encoding:"utf8",stdio:"pipe",timeout:3000}).trim()==="main"}catch{return!1}},Tq=()=>{try{u("git fetch origin",{cwd:K.superpowersRepo,timeout:5000,stdio:"pipe"});let q=!NX(),Q=u("git rev-parse HEAD",{cwd:K.superpowersRepo,encoding:"utf8",stdio:"pipe"}).trim(),X=u("git rev-parse origin/main",{cwd:K.superpowersRepo,encoding:"utf8",stdio:"pipe"}).trim();if(Q===X)return{hasUpdates:!1,hasLocalChanges:q,currentCommit:Q,latestCommit:X,commitsBehind:0,changedFiles:[]};let Y=parseInt(u("git rev-list --count HEAD..origin/main",{cwd:K.superpowersRepo,encoding:"utf8",stdio:"pipe"}).trim(),10),H=u("git diff --name-only HEAD origin/main",{cwd:K.superpowersRepo,encoding:"utf8",stdio:"pipe"}).trim().split(`
`).filter((W)=>W.length>0);return{hasUpdates:!0,hasLocalChanges:q,currentCommit:Q,latestCommit:X,commitsBehind:Y,changedFiles:H}}catch{return{hasUpdates:!1,hasLocalChanges:!1,currentCommit:"",latestCommit:"",commitsBehind:0,changedFiles:[],error:!0}}},CQ=(q)=>{let Q={".github/prompts/":"copilot-prompts",".github/copilot-instructions.md":"copilot-instructions","skills/meta/using-superpowers/":"copilot-instructions",".cursor/commands/":"cursor-commands","hooks/cursor/":"cursor-hooks",".codex/prompts/":"codex-prompts",".gemini/commands/":"gemini-commands","commands/":"claude-commands",".opencode/command/":"opencode-commands",".opencode/plugins/":"opencode-plugin"},X=new Set;for(let $ of q)for(let[Y,Z]of Object.entries(Q))if($.startsWith(Y))X.add(Z);return Array.from(X)};import{existsSync as n,readdirSync as FX,readFileSync as cq,writeFileSync as nq}from"fs";import{join as d,dirname as CX}from"path";import{execSync as bq}from"child_process";var a=()=>{let q=d(K.superpowersRepo,".github","prompts"),Q=d(K.vscodeUserDir,"prompts");if(!n(q)){console.log("⚠️  No Copilot prompts to install (source directory not found).");return}try{if(!n(Q))bq(`mkdir -p "${Q}"`,{stdio:"pipe"})}catch(Y){console.log(`Error creating prompts directory: ${Y.message}`);return}let X;try{X=FX(q).filter((Y)=>Y.endsWith(".prompt.md"))}catch(Y){console.log(`Error reading prompts directory: ${Y.message}`);return}if(X.length===0){console.log("⚠️  No prompt files found to install.");return}console.log("Installing GitHub Copilot prompts...");let $=0;for(let Y of X)try{let Z=d(q,Y),H=d(Q,Y);bq(`cp "${Z}" "${H}"`,{stdio:"pipe"}),console.log(`  ✓ Installed ${Y}`),$++}catch(Z){console.log(`  ✗ Failed to install ${Y}: ${Z.message}`)}if($>0)console.log(`
✓ Installed ${$} prompt(s) to ${Q}
  Use slash commands in GitHub Copilot:
    /brainstorm-with-superpowers - Refine ideas into designs
    /write-a-skill - Create new skills with TDD
    /skills - Discover available skills
    /use-skill - Load and apply a specific skill`)},l=()=>{let q=d(K.superpowersRepo,".github","copilot-instructions.md"),Q=d(K.superpowersRepo,"skills","meta","using-superpowers","SKILL.md"),X=d(K.home,".github","copilot-instructions.md"),$="<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->",Y="<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->";if(!n(q)){console.log("⚠️  No Copilot instructions to install (source template not found).");return}if(!n(Q)){console.log("⚠️  No Copilot instructions to install (using-superpowers SKILL.md not found).");return}let Z,H;try{Z=cq(q,"utf8"),H=cq(Q,"utf8")}catch(z){console.log(`✗ Failed to read source files: ${z.message}`);return}let W=Z.replace("${content}",H);if(!W.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->")||!W.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->")){console.log("⚠️  Template is missing required markers, cannot install.");return}let B=CX(X);try{if(!n(B))bq(`mkdir -p "${B}"`,{stdio:"pipe"})}catch(z){console.log(`✗ Error creating .github directory: ${z.message}`);return}if(n(X)){let z;try{z=cq(X,"utf8")}catch(v){console.log(`✗ Failed to read existing instructions: ${v.message}`);return}let E=new Date().toISOString().replace(/[:.]/g,"-"),V=`${X}.backup-${E}`;try{bq(`cp "${X}" "${V}"`,{stdio:"pipe"}),console.log(`✓ Backed up existing instructions to ${V}`)}catch(v){console.log(`✗ Failed to backup existing instructions: ${v.message}`);return}if(z.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->")&&z.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->")){let v=new RegExp("<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->[\\s\\S]*?<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->","g"),_=z.replace(v,W.trim());try{nq(X,_,"utf8"),console.log(`✓ Updated GitHub Copilot universal instructions (in-place)
  Location: ${X}`)}catch(w){console.log(`✗ Failed to update instructions: ${w.message}`)}}else{let v=z.trimEnd()+`

`+W.trim()+`
`;try{nq(X,v,"utf8"),console.log(`✓ Appended GitHub Copilot universal instructions to existing file
  Location: ${X}`)}catch(_){console.log(`✗ Failed to append instructions: ${_.message}`)}}}else try{nq(X,W,"utf8"),console.log(`✓ Installed GitHub Copilot universal instructions
  Location: ${X}
  GitHub Copilot will now use Superpowers skills universally in all workspaces`)}catch(z){console.log(`✗ Failed to install instructions: ${z.message}`)}};import{existsSync as Mq,readdirSync as RQ}from"fs";import{join as R}from"path";import{execSync as o}from"child_process";var i=()=>{let q=R(K.superpowersRepo,".cursor","commands"),Q=R(K.home,".cursor","commands");if(!Mq(q)){console.log("⚠️  No Cursor commands to install (source directory not found).");return}try{if(!Mq(Q))o(`mkdir -p "${Q}"`,{stdio:"pipe"})}catch(Y){console.log(`Error creating Cursor commands directory: ${Y.message}`);return}let X;try{X=RQ(q).filter((Y)=>Y.endsWith(".md"))}catch(Y){console.log(`Error reading Cursor commands directory: ${Y.message}`);return}if(X.length===0){console.log("⚠️  No command files found to install.");return}console.log("Installing Cursor commands...");let $=0;for(let Y of X)try{let Z=R(q,Y),H=R(Q,Y);o(`cp "${Z}" "${H}"`,{stdio:"pipe"}),console.log(`  ✓ Installed ${Y}`),$++}catch(Z){console.log(`  ✗ Failed to install ${Y}: ${Z.message}`)}if($>0)console.log(`
✓ Installed ${$} command(s) to ${Q}
  Use slash commands in Cursor:
    /brainstorm-with-superpowers - Refine ideas into designs
    /write-a-skill - Create new skills with TDD
    /skills - Discover available skills
    /use-skill - Load and apply a specific skill`)},r=()=>{let q=R(K.superpowersRepo,"hooks","cursor"),Q=R(K.home,".cursor","hooks"),X=R(q,"hooks.json"),$=R(K.home,".cursor","hooks.json");if(!Mq(q)){console.log("⚠️  No Cursor hooks to install (source directory not found).");return}try{if(!Mq(Q))o(`mkdir -p "${Q}"`,{stdio:"pipe"})}catch(H){console.log(`Error creating Cursor hooks directory: ${H.message}`);return}try{o(`cp "${X}" "${$}"`,{stdio:"pipe"}),console.log("  ✓ Installed hooks.json")}catch(H){console.log(`  ✗ Failed to install hooks.json: ${H.message}`);return}let Y;try{Y=RQ(q).filter((H)=>H.endsWith(".sh"))}catch(H){console.log(`Error reading hooks directory: ${H.message}`);return}if(Y.length===0){console.log("⚠️  No hook scripts found to install.");return}console.log("Installing Cursor hooks...");let Z=0;for(let H of Y)try{let W=R(q,H),B=R(Q,H);o(`cp "${W}" "${B}"`,{stdio:"pipe"}),o(`chmod +x "${B}"`,{stdio:"pipe"}),console.log(`  ✓ Installed ${H}`),Z++}catch(W){console.log(`  ✗ Failed to install ${H}: ${W.message}`)}if(Z>0)console.log(`
✓ Installed ${Z} hook(s) to ${Q}
  Cursor will now check for skills before each prompt submission
  Restart Cursor for hooks to take effect`)};import{existsSync as xQ,readdirSync as RX}from"fs";import{join as Nq}from"path";import{execSync as DQ}from"child_process";var s=()=>{let q=Nq(K.superpowersRepo,".codex","prompts"),Q=Nq(K.home,".codex","prompts");if(!xQ(q)){console.log("⚠️  No Codex prompts to install (source directory not found).");return}try{if(!xQ(Q))DQ(`mkdir -p "${Q}"`,{stdio:"pipe"})}catch(Y){console.log(`Error creating Codex prompts directory: ${Y.message}`);return}let X;try{X=RX(q).filter((Y)=>Y.endsWith(".md"))}catch(Y){console.log(`Error reading Codex prompts directory: ${Y.message}`);return}if(X.length===0){console.log("⚠️  No prompt files found to install.");return}console.log("Installing OpenAI Codex prompts...");let $=0;for(let Y of X)try{let Z=Nq(q,Y),H=Nq(Q,Y);DQ(`cp "${Z}" "${H}"`,{stdio:"pipe"}),console.log(`  ✓ Installed ${Y}`),$++}catch(Z){console.log(`  ✗ Failed to install ${Y}: ${Z.message}`)}if($>0)console.log(`
✓ Installed ${$} prompt(s) to ${Q}
  Use slash commands in OpenAI Codex:
    /prompts:brainstorm - Refine ideas into designs
    /prompts:write-skill - Create new skills with TDD
    /prompts:skills - Discover available skills
    /prompts:use-skill - Load and apply a specific skill
  Note: Restart Codex or open a new session to reload prompts`)};import{existsSync as jQ,readdirSync as xX}from"fs";import{join as Fq}from"path";import{execSync as PQ}from"child_process";var t=()=>{let q=Fq(K.superpowersRepo,".gemini","commands"),Q=Fq(K.home,".gemini","commands");if(!jQ(q)){console.log("⚠️  No Gemini commands to install (source directory not found).");return}try{if(!jQ(Q))PQ(`mkdir -p "${Q}"`,{stdio:"pipe"})}catch(Y){console.log(`Error creating Gemini commands directory: ${Y.message}`);return}let X;try{X=xX(q).filter((Y)=>Y.endsWith(".toml"))}catch(Y){console.log(`Error reading Gemini commands directory: ${Y.message}`);return}if(X.length===0){console.log("⚠️  No command files found to install.");return}console.log("Installing Gemini commands...");let $=0;for(let Y of X)try{let Z=Fq(q,Y),H=Fq(Q,Y);PQ(`cp "${Z}" "${H}"`,{stdio:"pipe"}),console.log(`  ✓ Installed ${Y}`),$++}catch(Z){console.log(`  ✗ Failed to install ${Y}: ${Z.message}`)}if($>0)console.log(`
✓ Installed ${$} command(s) to ${Q}
  Use slash commands in Gemini:
    /brainstorm-with-superpowers - Refine ideas into designs
    /write-a-skill - Create new skills with TDD
    /skills - Discover available skills
    /use-skill - Load and apply a specific skill`)};import{existsSync as fQ,readdirSync as DX}from"fs";import{join as Cq}from"path";import{execSync as yQ}from"child_process";var e=()=>{let q=Cq(K.superpowersRepo,"commands"),Q=Cq(K.home,".claude","commands");if(!fQ(q)){console.log("⚠️  No Claude commands to install (source directory not found).");return}try{if(!fQ(Q))yQ(`mkdir -p "${Q}"`,{stdio:"pipe"})}catch(Y){console.log(`Error creating Claude commands directory: ${Y.message}`);return}let X;try{X=DX(q).filter((Y)=>Y.endsWith(".md"))}catch(Y){console.log(`Error reading Claude commands directory: ${Y.message}`);return}if(X.length===0){console.log("⚠️  No command files found to install.");return}console.log("Installing Claude Code commands...");let $=0;for(let Y of X)try{let Z=Cq(q,Y),H=Cq(Q,Y);yQ(`cp "${Z}" "${H}"`,{stdio:"pipe"}),console.log(`  ✓ Installed ${Y}`),$++}catch(Z){console.log(`  ✗ Failed to install ${Y}: ${Z.message}`)}if($>0)console.log(`
✓ Installed ${$} command(s) to ${Q}
  Use slash commands in Claude Code:
    /brainstorm - Refine ideas into designs
    /execute-plan - Execute plans in batches
    /write-plan - Create implementation plans
    /skills - Discover available skills
    /use-skill - Load and apply a specific skill`)};import{existsSync as Vq,readdirSync as jX,lstatSync as PX,symlinkSync as gQ,unlinkSync as fX,mkdirSync as yX,readlinkSync as gX}from"fs";import{join as h,dirname as NZ}from"path";import{execSync as SQ}from"child_process";import{platform as SX}from"os";var qq=()=>{let q=h(K.superpowersRepo,".opencode","command"),Q=h(K.home,".config","opencode","command");if(!Vq(q)){console.log("⚠️  No opencode commands to install (source directory not found).");return}try{if(!Vq(Q))SQ(`mkdir -p "${Q}"`,{stdio:"pipe"})}catch(Y){console.log(`Error creating opencode commands directory: ${Y.message}`);return}let X;try{X=jX(q).filter((Y)=>Y.endsWith(".md"))}catch(Y){console.log(`Error reading opencode commands directory: ${Y.message}`);return}if(X.length===0){console.log("⚠️  No command files found to install.");return}console.log("Installing opencode commands...");let $=0;for(let Y of X)try{let Z=h(q,Y),H=h(Q,Y);SQ(`cp "${Z}" "${H}"`,{stdio:"pipe"}),console.log(`  ✓ Installed ${Y}`),$++}catch(Z){console.log(`  ✗ Failed to install ${Y}: ${Z.message}`)}if($>0)console.log(`
✓ Installed ${$} command(s) to ${Q}
  Use slash commands in opencode:
    /brainstorm - Refine ideas into designs
    /execute-plan - Execute plans in batches
    /write-plan - Create implementation plans
    /write-skill - Create new skills with TDD
    /skills - Discover available skills
    /use-skill - Load and apply a specific skill`)},aq=(q)=>{try{return PX(q).isSymbolicLink()}catch{return!1}},kX=(q,Q)=>{try{if(!aq(q))return!1;return gX(q)===Q}catch{return!1}},Gq=()=>{let q=h(K.superpowersRepo,".opencode","plugins","superpowers-agent.js"),Q=h(K.home,".config","opencode","plugins"),X=h(Q,"superpowers-agent.js");if(console.log("Installing OpenCode plugin symlink..."),!Vq(q))return console.log("⚠️  Source plugin not found, skipping symlink creation"),console.log(`   Expected at: ${q}`),{created:!1,error:"Source plugin not found"};if(kX(X,q))return console.log("✓ Plugin symlink already exists and is correct"),{created:!1,existed:!0};if(Vq(X)&&!aq(X))return console.log("⚠️  Warning: A file already exists at the destination path"),console.log(`   Path: ${X}`),console.log("   Skipping symlink creation to avoid overwriting existing file"),console.log("   To use superpowers-agent plugin, manually remove or rename the existing file"),{created:!1,error:"File already exists at destination"};if(!Vq(Q))try{yX(Q,{recursive:!0}),console.log(`✓ Created ${Q.replace(K.home,"~")}`)}catch(Y){return console.log(`⚠️  Failed to create plugins directory: ${Y.message}`),{created:!1,error:`Failed to create directory: ${Y.message}`}}if(aq(X))try{fX(X)}catch(Y){return console.log(`⚠️  Failed to remove existing symlink: ${Y.message}`),{created:!1,error:`Failed to remove existing symlink: ${Y.message}`}}let $=SX();try{if($==="win32")gQ(q,X,"file");else gQ(q,X);let Y=q.replace(K.home,"~"),Z=X.replace(K.home,"~");return console.log(`✓ Created symlink: ${Z}`),console.log(`  -> ${Y}`),{created:!0}}catch(Y){if($==="win32"&&Y.code==="EPERM")return console.log("⚠️  Windows requires Developer Mode or admin privileges for symlinks"),console.log("   Enable Developer Mode: Settings > Update & Security > For developers"),{created:!1,error:"Windows symlink permission denied"};return console.log(`⚠️  Failed to create symlink: ${Y.message}`),{created:!1,error:`Failed to create symlink: ${Y.message}`}}};import{existsSync as I,readdirSync as uX,lstatSync as dX,symlinkSync as kQ,unlinkSync as hX,mkdirSync as _q,readlinkSync as mX}from"fs";import{join as G,dirname as pX}from"path";import{homedir as T,platform as cX}from"os";var uQ=[{name:"claude",parentDir:()=>G(T(),".claude"),skillsDir:()=>G(T(),".claude","skills"),superpowersTarget:"superpowers"},{name:"copilot",parentDir:()=>G(T(),".copilot"),skillsDir:()=>G(T(),".copilot","skills"),superpowersTarget:"superpowers"},{name:"opencode",parentDir:()=>G(T(),".config","opencode"),skillsDir:()=>G(T(),".config","opencode","skill"),superpowersTarget:"superpowers"},{name:"cursor",parentDir:()=>G(T(),".cursor"),skillsDir:()=>G(T(),".cursor","skills"),superpowersTarget:"superpowers"},{name:"gemini",parentDir:()=>G(T(),".gemini"),skillsDir:()=>G(T(),".gemini","skills"),superpowersTarget:"superpowers"},{name:"codex",parentDir:()=>G(T(),".codex"),skillsDir:()=>G(T(),".codex","skills"),superpowersTarget:"superpowers"}],nX=[{name:"claude",agentDir:(q)=>G(q,".claude"),skillsDir:(q)=>G(q,".claude","skills"),detect:(q)=>I(G(q,".claude"))},{name:"copilot",agentDir:(q)=>G(q,".github"),skillsDir:(q)=>G(q,".github","skills"),detect:(q)=>{let Q=I(G(q,".github")),X=I(G(q,"AGENTS.md"))||I(G(q,".agents","AGENTS.md"));return Q&&X}},{name:"opencode",agentDir:(q)=>G(q,".opencode"),skillsDir:(q)=>G(q,".opencode","skill"),detect:(q)=>{let Q=I(G(q,".opencode")),X=I(G(q,"AGENTS.md"))||I(G(q,".agents","AGENTS.md"))||I(G(q,".opencode","AGENTS.md"));return Q&&X}},{name:"cursor",agentDir:(q)=>G(q,".cursor"),skillsDir:(q)=>G(q,".cursor","skills"),detect:(q)=>I(G(q,".cursor"))},{name:"gemini",agentDir:(q)=>G(q,".gemini"),skillsDir:(q)=>G(q,".gemini","skills"),detect:(q)=>{let Q=I(G(q,".gemini")),X=I(G(q,"GEMINI.md"))||I(G(q,".agents","GEMINI.md"));return Q&&X}},{name:"codex",agentDir:(q)=>G(q,".codex"),skillsDir:(q)=>G(q,".codex","skills"),detect:(q)=>{let Q=I(G(q,".codex")),X=I(G(q,"AGENTS.md"))||I(G(q,".agents","AGENTS.md"));return Q&&X}}],lq=(q)=>{try{return dX(q).isSymbolicLink()}catch{return!1}},aX=(q,Q)=>{try{if(!lq(q))return!1;return mX(q)===Q}catch{return!1}},oq=(q,Q)=>{if(!I(q))return{created:!1,existed:!1,error:`Source does not exist: ${q}`};if(aX(Q,q))return{created:!1,existed:!0};if(I(Q)||lq(Q))if(lq(Q))try{hX(Q)}catch(Y){return{created:!1,existed:!1,error:`Failed to remove existing symlink: ${Y.message}`}}else return{created:!1,existed:!1,error:`Target exists and is not a symlink: ${Q}`};let X=pX(Q);if(!I(X))try{_q(X,{recursive:!0})}catch(Y){return{created:!1,existed:!1,error:`Failed to create parent directory: ${Y.message}`}}let $=cX();try{if($==="win32")kQ(q,Q,"junction");else kQ(q,Q,"dir");return{created:!0,existed:!1}}catch(Y){if($==="win32"&&Y.code==="EPERM")return{created:!1,existed:!1,error:"Windows requires Developer Mode or admin privileges for symlinks. Enable Developer Mode: Settings > Update & Security > For developers"};return{created:!1,existed:!1,error:`Failed to create symlink: ${Y.message}`}}};var dQ=(q,Q,X="skills")=>{let $=Eq(!0);if(!$.symlinks)$.symlinks={};if(!$.symlinks[q])$.symlinks[q]={superpowers:null,skills:[]};if(X==="superpowers")$.symlinks[q].superpowers=Q;else{let Y=$.symlinks[q].skills||[];if(!Y.includes(Q))Y.push(Q),$.symlinks[q].skills=Y}wq($,!0)};var lX=(q,Q={})=>{let X=K.homeSuperpowersSkills,$=G(q.skillsDir(),q.superpowersTarget),Y=oq(X,$);if(Y.created){dQ(q.name,$,"superpowers");let Z=X.replace(T(),"~"),H=$.replace(T(),"~");return console.log(`  ✓ Created ${H} -> ${Z}`),{created:!0}}else if(Y.existed)return{created:!1,existed:!0};else if(Y.error)return console.log(`  ⚠️  ${q.name} superpowers: ${Y.error}`),{created:!1,error:Y.error};return{created:!1}},hQ=(q,Q={})=>{let X=K.homePersonalSkills;if(!I(X))return{created:0,existed:0};let $;try{$=uX(X,{withFileTypes:!0}).filter((Z)=>Z.isDirectory()&&!Z.name.startsWith(".")).map((Z)=>Z.name)}catch{return{created:0,existed:0}}if($.length===0)return{created:0,existed:0};let Y={created:0,existed:0,names:[]};for(let Z of $){let H=G(X,Z),W=G(q.skillsDir(),Z),B=oq(H,W);if(B.created)dQ(q.name,W,"skills"),Y.created++,Y.names.push(Z);else if(B.existed)Y.existed++;else if(B.error)console.log(`  ⚠️  ${Z}: ${B.error}`)}if(Y.created>0)console.log(`  ✓ Synced ${Y.created} personal skill(s): ${Y.names.join(", ")}`);return Y},Rq=(q={})=>{let{force:Q=!1}=q;for(let X of uQ){let $=X.parentDir(),Y=X.skillsDir();if(!I($))if(Q)try{_q($,{recursive:!0}),console.log(`✓ Created ${$.replace(T(),"~")}`)}catch(Z){console.log(`⚠️  Failed to create ${$.replace(T(),"~")}: ${Z.message}`);continue}else{console.log(`⚠️  Skipping ${X.name} (${$.replace(T(),"~")} not found)`),console.log("   Use --force to create directory");continue}if(!I(Y))try{_q(Y,{recursive:!0})}catch(Z){console.log(`⚠️  Failed to create skills directory for ${X.name}: ${Z.message}`);continue}console.log(`${X.name}:`),lX(X,q),hQ(X,q)}},iq=(q={})=>{let{force:Q=!1}=q;for(let X of uQ){let $=X.parentDir(),Y=X.skillsDir();if(!I($)){if(!Q)continue;try{_q($,{recursive:!0})}catch{continue}}if(!I(Y))try{_q(Y,{recursive:!0})}catch{continue}hQ(X,q)}};var mQ=(q={})=>{let Q=q.projectRoot||process.cwd(),X=G(Q,".agents","skills");if(!I(X))return{created:0,existed:0,skipped:0,errors:[]};let $={created:0,existed:0,skipped:0,errors:[]};for(let Y of nX){if(!Y.detect(Q)){$.skipped++;continue}let Z=Y.skillsDir(Q),H=Y.agentDir(Q);if(!I(H)){$.skipped++;continue}let W=oq(X,Z);if(W.created){let B=Z.replace(Q,".");console.log(`  ✓ Created ${B} -> .agents/skills`),$.created++}else if(W.existed)$.existed++;else if(W.error)console.log(`  ⚠️  ${Y.name}: ${W.error}`),$.errors.push({platform:Y.name,error:W.error})}return $};var iX=(q)=>{let X={"copilot-prompts":a,"copilot-instructions":l,"cursor-commands":i,"cursor-hooks":r,"codex-prompts":s,"gemini-commands":t,"claude-commands":e,"opencode-commands":qq,"opencode-plugin":Gq}[q];if(X)try{return X(),{success:!0,integration:q}}catch($){return{success:!1,integration:q,error:$.message}}return{success:!1,integration:q,error:"Unknown integration"}},rX=()=>{console.log("Installing aliases...")},xq=(q={})=>{let Q=q.skipReinstall||!1;console.log(`# Checking for Superpowers updates...
`);let X=Tq();if(X.error){console.log("⚠️  Could not check for updates (network issue)");return}if(!X.hasUpdates){console.log("✓ Already up to date");return}if(X.hasLocalChanges){console.log(`⚠️  Cannot auto-update: local changes detected
   Commit or stash your changes first, then run update again
   Or manually update: cd ${K.superpowersRepo} && git pull`);return}if(!Bq()){console.log(`⚠️  Not on main branch, skipping auto-update
   Switch to main branch first: cd ${K.superpowersRepo} && git checkout main`);return}console.log(`\uD83D\uDCE6 Updating from ${X.currentCommit.substring(0,7)} to ${X.latestCommit.substring(0,7)}
   (${X.commitsBehind} new commit${X.commitsBehind>1?"s":""})
`);try{oX("git pull origin main",{cwd:K.superpowersRepo,stdio:"pipe",timeout:1e4}),console.log("✓ Updated superpowers repository")}catch(H){console.log(`✗ Git pull failed: ${H.message}
   Please resolve manually and try again`);return}if(console.log(""),Oq({last_updated_commit:X.latestCommit,last_update_check:new Date().toISOString()}),Q){console.log(`ℹ️  Skipping integration reinstall (--no-reinstall flag)

✓ Update complete!`);return}let $=CQ(X.changedFiles);if($.length===0){console.log(`ℹ️  No integration files changed, skipping reinstalls

✓ Update complete!`);return}console.log(`\uD83D\uDD04 Reinstalling updated integrations:
`);let Y=[];for(let H of $){let W=iX(H);if(Y.push(W),W.success)console.log(`  ✓ ${H}`);else console.log(`  ✗ ${H} (${W.error})`)}console.log(""),console.log(`
---
`),rX(),console.log(`
---
`),console.log(`## Syncing Skill Symlinks
`),Rq(),console.log(`
---
`),console.log(`## OpenCode Plugin Symlink
`),Gq();let Z=Y.filter((H)=>!H.success);if(Z.length>0){console.log("⚠️  Update completed with errors:");for(let H of Z){console.log(`  - ${H.integration} failed to install`);let W=`install-${H.integration}`;console.log(`    Run manually: superpowers-agent ${W}`)}}else console.log("✓ Update complete!")},pQ=async()=>{C();let q=g();console.log(`Current version: ${q}`);try{let Q=await UQ();if(console.log(`Latest version: ${Q}`),JQ(Q,q))console.log("Update available: Yes"),process.exit(1);else console.log("You are up to date"),process.exit(0)}catch(Q){console.log(`Error checking for updates: ${Q.message}`),process.exit(1)}},cQ=()=>{let q=g();console.log(q)};import{existsSync as J,readFileSync as Jq,writeFileSync as rq}from"fs";import{join as O,dirname as tX,parse as Lq}from"path";import{execSync as F}from"child_process";import{platform as eX}from"os";import{execSync as sX}from"child_process";var vq=(q)=>{try{return sX(`which ${q}`,{stdio:"pipe",timeout:2000}),!0}catch{return!1}};var L={opencode:{check:()=>vq("opencode"),cli:!0,name:"OpenCode",installUrl:"https://opencode.ai/docs/installation",bootstrapCommand:"install-opencode-commands"},claude:{check:()=>vq("claude"),cli:!0,name:"Claude Code",installUrl:"https://code.claude.com/docs/en/installation",bootstrapCommand:"install-claude-commands"},gemini:{check:()=>vq("gemini"),cli:!0,name:"Gemini",installUrl:"https://cloud.google.com/gemini/docs/cli/install",bootstrapCommand:"install-gemini-commands"},codex:{check:()=>vq("codex"),cli:!0,name:"Codex",installUrl:"https://developers.openai.com/codex/docs/installation",bootstrapCommand:"install-codex-prompts"},cursor:{check:()=>!0,cli:!1,name:"Cursor",bootstrapCommand:"install-cursor-commands"},copilot:{check:()=>!0,cli:!1,name:"GitHub Copilot",bootstrapCommand:"install-copilot-prompts"}},nQ=()=>{let q=[];if(L.copilot.check())q.push("github-copilot");if(L.cursor.check())q.push("cursor");if(L.claude.check())q.push("claude-code");if(L.opencode.check())q.push("opencode");if(L.gemini.check())q.push("gemini");if(L.codex.check())q.push("codex");return q};var qY=()=>{let q=O(K.superpowersRepo,".agents","templates","TOOLS.md.template");if(J(q))try{return Jq(q,"utf8").trim()}catch(Q){return"### Using Tools\n\nUse your native skill tool. Fallback: `superpowers-agent find-skills`"}return""},Uq=(q,Q,X,$=!0)=>{let Y=J(q);if(!Y&&!$)return{updated:!1,created:!1,skipped:!0};let Z=qY(),H=Q.replace(/\{\{TOOL_MAPPINGS\}\}/g,Z),W=new Date().toISOString().split("T")[0];H=H.replace(/\{\{DATE\}\}/g,W),H=H.replace(/\{\{SUPERPOWERS_PATH\}\}/g,K.superpowersRepo);let B=`<!-- SUPERPOWERS_SKILLS_START -->
${H}
<!-- SUPERPOWERS_SKILLS_END -->`;if(Y){let z=new Date().toISOString().split("T")[0],E=`${q}.backup-${z}`;try{F(`cp "${q}" "${E}"`,{stdio:"pipe"})}catch(M){return{updated:!1,error:!0,message:`Failed to backup: ${M.message}`}}let V;try{V=Jq(q,"utf8")}catch(M){return{updated:!1,error:!0,message:`Failed to read: ${M.message}`}}let v="<!-- SUPERPOWERS_SKILLS_START -->",_="<!-- SUPERPOWERS_SKILLS_END -->",w;if(V.includes(v)&&V.includes(_)){let M=new RegExp(`${v}[\\s\\S]*?${_}`,"g");w=V.replace(M,B)}else{let M=V.match(/^#\s+.+$/m);if(M){let D=V.indexOf(`
`,M.index)+1;w=V.slice(0,D)+`
`+B+`
`+V.slice(D)}else w=B+`

`+V}try{return rq(q,w,"utf8"),{updated:!0,created:!1,backup:E}}catch(M){return{updated:!1,error:!0,message:`Failed to write: ${M.message}`}}}else try{let z=tX(q);if(!J(z))F(`mkdir -p "${z}"`,{stdio:"pipe"});let v=`# ${Lq(q).base.toUpperCase()}

`+B;return rq(q,v,"utf8"),{updated:!1,created:!0}}catch(z){return{updated:!1,created:!1,error:!0,message:`Failed to create: ${z.message}`}}},QY=()=>{console.log("Installing Unix aliases...");let q=O(K.superpowersRepo,".agents","superpowers-agent"),Q=O(K.home,".local","bin"),X=O(Q,"superpowers-agent"),$=O(Q,"superpowers");if(!J(q)){console.log("⚠️  superpowers-agent executable not found");return}if(!J(Q))try{F(`mkdir -p "${Q}"`,{stdio:"pipe"}),console.log(`✓ Created ${Q}`)}catch(Z){console.log(`⚠️  Failed to create ${Q}: ${Z.message}`);return}try{if(J(X))F(`rm "${X}"`,{stdio:"pipe"});F(`ln -s "${q}" "${X}"`,{stdio:"pipe"}),console.log(`✓ Created symlink: superpowers-agent -> ${q}`)}catch(Z){console.log(`⚠️  Failed to create superpowers-agent symlink: ${Z.message}`)}try{if(J($))F(`rm "${$}"`,{stdio:"pipe"});F(`ln -s "${q}" "${$}"`,{stdio:"pipe"}),console.log(`✓ Created symlink: superpowers -> ${q}`)}catch(Z){console.log(`⚠️  Failed to create superpowers symlink: ${Z.message}`)}if(!(process.env.PATH||"").includes(Q))console.log(`
⚠️  Warning: ${Q} is not in your PATH`),console.log("   Add this to your shell profile (~/.bashrc, ~/.zshrc, etc.):"),console.log(`   export PATH="$HOME/.local/bin:$PATH"
`)},XY=()=>{console.log("Installing Windows aliases..."),console.log("✓ Windows alias installation (implementation pending)")},sq=()=>{let q=eX();if(console.log(`## Installing Universal Aliases
`),q==="win32")return XY();else return QY()},aQ=()=>{C(),console.log(`# Setting up Superpowers skills for this project
`);let q=process.cwd(),Q=O(q,".agents"),X=O(Q,"skills"),$=O(q,"AGENTS.md"),Y=O(Q,"AGENTS.md"),Z=J($)?$:Y,H=O(K.superpowersRepo,".agents","templates","AGENTS.md.template");if(!J(H)){console.log(`✗ Error: AGENTS.md template not found
  Expected at: ${H}
  Please update your Superpowers installation`);return}if(!J(Q))try{F(`mkdir -p "${Q}"`,{stdio:"pipe"}),console.log("✓ Created .agents/ directory")}catch(A){console.log(`✗ Failed to create .agents/ directory: ${A.message}`);return}else console.log("✓ .agents/ directory exists");if(!J(X))try{F(`mkdir -p "${X}"`,{stdio:"pipe"}),F(`touch "${O(X,".gitkeep")}"`,{stdio:"pipe"}),console.log("✓ Created .agents/skills/ directory")}catch(A){console.log(`✗ Failed to create skills directory: ${A.message}`);return}else console.log("✓ .agents/skills/ directory exists");let W=O(Q,"docs"),B=O(W,"SUPERPOWERS.md"),z=O(K.superpowersRepo,".agents","templates","SUPERPOWERS.md.template");if(!J(W))try{F(`mkdir -p "${W}"`,{stdio:"pipe"}),console.log("✓ Created .agents/docs/ directory")}catch(A){console.log(`⚠️  Failed to create docs directory: ${A.message}`)}else console.log("✓ .agents/docs/ directory exists");let E=J(Z);if(E){let A=new Date().toISOString().split("T")[0],Aq=`${Z}.backup-${A}`;try{F(`cp "${Z}" "${Aq}"`,{stdio:"pipe"}),console.log(`✓ Backed up existing AGENTS.md to ${Lq(Aq).base}`)}catch(ZX){console.log(`✗ Failed to backup AGENTS.md: ${ZX.message}`);return}}let V;try{V=Jq(H,"utf8")}catch(A){console.log(`✗ Failed to read template: ${A.message}`);return}let v=g();if(V=V.replace(/\{\{VERSION\}\}/g,v),J(z))try{let A=Jq(z,"utf8"),Aq=new Date().toISOString().split("T")[0];A=A.replace(/\{\{VERSION\}\}/g,v),A=A.replace(/\{\{DATE\}\}/g,Aq),rq(B,A,"utf8"),console.log("✓ Created .agents/docs/SUPERPOWERS.md")}catch(A){console.log(`⚠️  Failed to create SUPERPOWERS.md: ${A.message}`)}else console.log("⚠️  SUPERPOWERS.md.template not found");let _=[],w=O(q,".github","copilot-instructions.md"),M=O(K.home,".github","copilot-instructions.md");if(J(w)||J(M)||L.copilot.check())_.push("github-copilot");let D=O(q,"CLAUDE.md"),eq=O(Q,"CLAUDE.md");if(J(D)||J(eq)||L.claude.check())_.push("claude-code");let Xq=O(q,"GEMINI.md"),qQ=O(Q,"GEMINI.md");if(J(Xq)||J(qQ)||L.gemini.check())_.push("gemini");if(L.cursor.check())_.push("cursor");if(L.opencode.check())_.push("opencode");if(L.codex.check())_.push("codex");console.log(`
Detected platforms for project: ${_.join(", ")||"none"}
`);let YX=_.filter((A)=>["github-copilot","cursor","opencode","codex"].includes(A)),Yq=Uq(Z,V,YX,!E);if(Yq.created)console.log(`✓ Created AGENTS.md with platform tool mappings (${Z===$?"root":".agents/"})`);else if(Yq.updated)console.log(`✓ Updated AGENTS.md with platform tool mappings (${Z===$?"root":".agents/"})`);else if(Yq.error)console.log("⚠️  Failed to update AGENTS.md");let fq=J(D)?D:eq,j=Uq(fq,V,["claude-code"],!1);if(j.created)console.log(`✓ Created CLAUDE.md with Claude Code tool mappings (${fq===D?"root":".agents/"})`);else if(j.updated){if(console.log(`✓ Updated CLAUDE.md with Claude Code tool mappings (${fq===D?"root":".agents/"})`),j.backup)console.log(`  Backed up to ${Lq(j.backup).base}`)}else if(j.skipped)console.log("ℹ️  Skipped CLAUDE.md (does not exist)");else if(j.error)console.log("⚠️  Failed to update CLAUDE.md");let yq=J(Xq)?Xq:qQ,P=Uq(yq,V,["gemini"],!1);if(P.created)console.log(`✓ Created GEMINI.md with Gemini tool mappings (${yq===Xq?"root":".agents/"})`);else if(P.updated){if(console.log(`✓ Updated GEMINI.md with Gemini tool mappings (${yq===Xq?"root":".agents/"})`),P.backup)console.log(`  Backed up to ${Lq(P.backup).base}`)}else if(P.skipped)console.log("ℹ️  Skipped GEMINI.md (does not exist)");else if(P.error)console.log("⚠️  Failed to update GEMINI.md");let gq=O(q,".github","copilot-instructions.md"),QQ=!J($)&&!J(Y),m={skipped:!0};if(J(gq)&&QQ){if(m=Uq(gq,V,["github-copilot"],!1),m.updated){if(console.log("✓ Updated .github/copilot-instructions.md with GitHub Copilot tool mappings"),m.backup)console.log(`  Backed up to ${Lq(m.backup).base}`)}else if(m.error)console.log("⚠️  Failed to update .github/copilot-instructions.md")}else if(J(gq)&&!QQ)console.log("ℹ️  Skipped .github/copilot-instructions.md (AGENTS.md exists, using that instead)");else console.log("ℹ️  Skipped .github/copilot-instructions.md (does not exist)");console.log(`
## Syncing Project Skill Symlinks
`);let p=mQ({projectRoot:q});if(p.created>0)console.log(`
✓ Created ${p.created} project skill symlink(s)`);else if(p.existed>0)console.log("ℹ️  Project skill symlinks already exist");else if(p.errors.length>0)console.log("⚠️  Some symlinks could not be created");else console.log("ℹ️  No agent directories detected for symlinking");let f=`
# Setup complete!

Your project now has:
  - .agents/ directory structure`;if(Yq.updated||Yq.created)f+=`
  - AGENTS.md with universal skills instructions`;if(j.updated||j.created)f+=`
  - CLAUDE.md with Claude Code skills instructions`;if(P.updated||P.created)f+=`
  - GEMINI.md with Gemini skills instructions`;if(m.updated)f+=`
  - .github/copilot-instructions.md with GitHub Copilot skills instructions`;if(p.created>0)f+=`
  - ${p.created} project skill symlink(s) to agent directories`;f+=`
  - .agents/skills/ ready for project-specific skills`,f+=`
  - .agents/docs/SUPERPOWERS.md for detailed reference
`,console.log(f)},lQ=()=>{if(C(),console.log(`# Superpowers Bootstrap for Agents
# ==================================
`),!process.argv.includes("--no-update")){let z=zq(),E=Tq();if(E.error)console.log(`## Update Check

⚠️  Could not check for updates (network issue)

---
`);else if(E.hasUpdates)if(z.auto_update&&!E.hasLocalChanges&&Bq())console.log(`## Auto-Update
`),xq(),console.log(`
---
`);else{if(console.log(`## Update Available
`),E.hasLocalChanges)console.log("⚠️  Your superpowers installation is behind the latest version.\n    Cannot auto-update: local changes detected\n    To update, commit/stash changes then run: `superpowers-agent update`");else if(!Bq())console.log("⚠️  Your superpowers installation is behind the latest version.\n    Cannot auto-update: not on main branch\n    To update, switch to main then run: `superpowers-agent update`");else console.log("⚠️  Your superpowers installation is behind the latest version.\n    To update, run: `superpowers-agent update`\n    Or enable auto-update: `superpowers-agent config-set auto_update true`");console.log(`
---
`)}}if(sq(),console.log(`---
`),console.log(`## GitHub Copilot Integration
`),a(),console.log(""),l(),console.log(`
---
`),console.log(`## Cursor Integration
`),i(),console.log(""),r(),console.log(`
---
`),console.log(`## OpenAI Codex Integration
`),L.codex.check())s();else console.log(`⚠️  Skipped (${L.codex.name} CLI not detected)
\uD83D\uDCA1 To enable Codex integration:
   1. Install Codex: ${L.codex.installUrl}
   2. Run: superpowers-agent ${L.codex.bootstrapCommand}`);if(console.log(`
---
`),console.log(`## Gemini Integration
`),L.gemini.check())t();else console.log(`⚠️  Skipped (${L.gemini.name} CLI not detected)
\uD83D\uDCA1 To enable Gemini integration:
   1. Install Gemini: ${L.gemini.installUrl}
   2. Run: superpowers-agent ${L.gemini.bootstrapCommand}`);if(console.log(`
---
`),console.log(`## Claude Code Integration
`),L.claude.check())e();else console.log(`⚠️  Skipped (${L.claude.name} CLI not detected)
\uD83D\uDCA1 To enable Claude Code integration:
   1. Install Claude Code: ${L.claude.installUrl}
   2. Run: superpowers-agent ${L.claude.bootstrapCommand}`);if(console.log(`
---
`),console.log(`## OpenCode Integration
`),L.opencode.check())qq(),console.log(""),Gq();else console.log(`⚠️  Skipped (${L.opencode.name} CLI not detected)
\uD83D\uDCA1 To enable OpenCode integration:
   1. Install OpenCode: ${L.opencode.installUrl}
   2. Run: superpowers-agent ${L.opencode.bootstrapCommand}`);console.log(`
---
`),console.log(`## Generating Platform-Specific Files
`);let Z=nQ();console.log(`Detected platforms: ${Z.join(", ")||"none"}
`);let H=O(K.superpowersRepo,".agents","templates","AGENTS.md.template"),W="";try{W=Jq(H,"utf8")}catch(z){console.log(`⚠️  Could not read AGENTS.md template: ${z.message}
`)}if(W){let z=g();W=W.replace(/\{\{VERSION\}\}/g,z);let E=O(K.home,".agents","AGENTS.md"),V=Uq(E,W,Z,!0);if(V.created)console.log(`✓ Created ${E}`);else if(V.updated)console.log(`✓ Updated ${E}`)}console.log(`
---
`),console.log(`## Syncing Skill Symlinks
`);let B=process.argv.includes("--force");Rq({force:B}),console.log(`
---
`),console.log(`# Bootstrap Complete!
`),console.log("✓ All integrations installed"),console.log("✓ Skills system ready"),console.log(`✓ Skill symlinks synced
`),console.log("Next steps:"),console.log("  - Run `superpowers-agent find-skills` to see available skills"),console.log("  - Run `superpowers-agent setup-skills` in your project directory")};import{existsSync as x,readFileSync as oQ}from"fs";import{join as b,dirname as tq,parse as Dq}from"path";import{execSync as N}from"child_process";import{homedir as Qq}from"os";var iQ=(q)=>{let Q=q.match(/^https:\/\/github\.com\/([^/]+)\/([^/]+)\/tree\/([^/]+)\/(.+)$/);if(Q){let[,Y,Z,H,W]=Q;return{type:"git-tree",repoUrl:`https://github.com/${Y}/${Z}.git`,branch:H,path:W,original:q}}let X=q.match(/^git@github\.com:([^/]+)\/([^/]+?)(?:\.git)?$/);if(X){let[,Y,Z]=X;return{type:"git-repo",repoUrl:`git@github.com:${Y}/${Z}.git`,branch:null,path:null,original:q}}let $=q.match(/^https:\/\/github\.com\/([^/]+)\/([^/]+)(?:\.git)?$/);if($){let[,Y,Z]=$;return{type:"git-repo",repoUrl:`https://github.com/${Y}/${Z}.git`,branch:null,path:null,original:q}}if(x(q))return{type:"local",path:q,original:q};return null},rQ=(q)=>{let Q=q.includes("--global")||q.includes("-g"),X=q.includes("--project")||q.includes("-p");if(Q)return b(Qq(),".agents","skills");if(X)return b(process.cwd(),".agents","skills");let $=b(process.cwd(),".agents","config.json"),Y=b(Qq(),".agents","config.json");for(let Z of[$,Y])if(x(Z))try{if(JSON.parse(oQ(Z,"utf8")).installLocation==="project")return b(process.cwd(),".agents","skills")}catch(H){}return b(Qq(),".agents","skills")},sQ=(q,Q)=>{let X=b(Qq(),".agents","tmp",`skill-install-${Date.now()}`);try{if(N(`mkdir -p "${X}"`,{stdio:"pipe"}),Q)N(`git clone --branch ${Q} --depth 1 "${q}" "${X}"`,{stdio:"pipe",timeout:30000});else N(`git clone --depth 1 "${q}" "${X}"`,{stdio:"pipe",timeout:30000});return X}catch($){try{N(`rm -rf "${X}"`,{stdio:"pipe"})}catch{}throw Error(`Failed to clone repository: ${$.message}`)}},Pq=(q)=>{let Q=b(q,"skill.json");if(!x(Q))return null;try{let X=oQ(Q,"utf8");return JSON.parse(X)}catch(X){throw Error(`Failed to read skill.json: ${X.message}`)}},jq=(q,Q,X,$)=>{let Y=b(q,Q);if(!x(Y)){$.errors.push(`Skill directory not found: ${Q}`);return}let Z=Pq(Y);if(!Z){$.errors.push(`No skill.json found in: ${Q}`);return}let H=Z.name||Q,W=b(X,H),B=tq(W);try{N(`mkdir -p "${B}"`,{stdio:"pipe"})}catch(z){$.errors.push(`Failed to create directory ${B}: ${z.message}`);return}try{if(x(W))N(`rm -rf "${W}"`,{stdio:"pipe"});N(`cp -R "${Y}" "${W}"`,{stdio:"pipe"}),$.installed.push({name:H,path:W,title:Z.title||Z.description||H})}catch(z){$.errors.push(`Failed to install ${Q}: ${z.message}`)}},tQ=()=>{let q=process.argv.slice(3),Q=q.find((z)=>!z.startsWith("-")),X=q.filter((z)=>z.startsWith("-"));if(!Q){console.log(`Usage: superpowers-agent add <url-or-path|@alias> [skill-path] [options]

Options:
  --global, -g   Install skills globally in ~/.agents/skills/ (default)
  --project, -p  Install skills in current project's .agents/skills/

Examples:
  superpowers-agent add https://github.com/example/skills
  superpowers-agent add https://github.com/example/repo/tree/main/skills
  superpowers-agent add ~/my-local-skills
  superpowers-agent add https://github.com/example/skills --project
  superpowers-agent add ~/my-local-skills --global
  superpowers-agent add @myrepo path/to/skill
  superpowers-agent add @myrepo path/to/skills --project

Description:
  Installs skill(s) from a Git repository, local directory, or repository alias.
  Supports repositories with single or multiple skills.
  Reads skill.json to determine skill names and installation paths.
  
  Repository aliases can be added using:
    superpowers-agent add-repository <git-url> [--as=@alias]`);return}console.log(`Installing skill(s)...
`);let $=null,Y=null;if(Q.startsWith("@")){let z=Q;Y=q.find((_,w)=>w>0&&!_.startsWith("-")&&_!==z);let E=y(!1),V=y(!0);if(E[z])$=E[z],console.log(`Using project repository alias: ${z}`);else if(V[z])$=V[z],console.log(`Using global repository alias: ${z}`);else{console.log(`Error: Repository alias not found: ${z}`),console.log(`
Available aliases:`);let _={...V,...E};if(Object.keys(_).length===0)console.log("  (none)"),console.log(`
Add a repository using:`),console.log("  superpowers-agent add-repository <git-url>");else for(let[w,M]of Object.entries(_))console.log(`  ${w} -> ${M}`);return}let v=x($);if(Y)if(v)Q=b($,Y),console.log(`Repository path: ${$}`),console.log(`Skill path: ${Y}
`);else Q=`${$}/tree/main/${Y}`,console.log(`Repository URL: ${$}`),console.log(`Skill path: ${Y}
`);else if(Q=$,v)console.log(`Repository path: ${$}
`);else console.log(`Repository URL: ${$}
`)}let Z=iQ(Q);if(!Z){console.log(`Error: Invalid URL or path not found: ${Q}`);return}let H=rQ(X);console.log(`Install location: ${H}
`);let W,B=!1;try{if(Z.type==="git-repo"||Z.type==="git-tree"){if(console.log(`Cloning repository: ${Z.repoUrl}`),Z.branch)console.log(`Branch: ${Z.branch}`);if(W=sQ(Z.repoUrl,Z.branch),B=!0,Z.path){if(W=b(W,Z.path),!x(W))throw Error(`Path not found in repository: ${Z.path}`)}console.log("")}else W=Z.path;let z=Pq(W);if(!z)throw Error("No skill.json found in source directory");let E={installed:[],errors:[],source:Z.original};if(z.skills&&Array.isArray(z.skills)){console.log(`Found ${z.skills.length} skill(s) to install
`);for(let V of z.skills)jq(W,V,H,E)}else{let V=z.name||Dq(W).base;jq(tq(W),Dq(W).base,H,E)}if(B&&W)try{let V=W.split("/.agents/tmp/")[0]+"/.agents/tmp/"+W.split("/.agents/tmp/")[1].split("/")[0];N(`rm -rf "${V}"`,{stdio:"pipe"})}catch{}if(console.log(`
**Successfully installed skills:**`),console.log(`- Source: ${E.source}`),E.installed.length>0)for(let V of E.installed)console.log(`  - Installed: ${V.name} at ${V.path}`),console.log(`    ${V.title}`);if(E.errors.length>0){console.log(`
**Errors:**`);for(let V of E.errors)console.log(`  - ${V}`)}if(E.installed.length===0&&E.errors.length===0)console.log("  No skills were installed");if(E.installed.length>0)console.log(`
**Syncing skill symlinks...**`),iq()}catch(z){if(B&&W)try{let E=W.split("/.agents/tmp/")[0]+"/.agents/tmp/"+W.split("/.agents/tmp/")[1].split("/")[0];N(`rm -rf "${E}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${z.message}`)}},eQ=()=>{let q=process.argv.slice(3),Q=q.find((z)=>!z.startsWith("-")),X=q.filter((z)=>z.startsWith("-"));if(!Q){console.log(`Usage: superpowers-agent pull <url-or-path|@alias> [skill-path] [options]

Options:
  --global, -g   Update skills globally in ~/.agents/skills/ (default)
  --project, -p  Update skills in current project's .agents/skills/

Examples:
  superpowers-agent pull https://github.com/example/skills
  superpowers-agent pull https://github.com/example/repo/tree/main/skills
  superpowers-agent pull ~/my-local-skills
  superpowers-agent pull https://github.com/example/skills --project
  superpowers-agent pull ~/my-local-skills --global
  superpowers-agent pull @myrepo
  superpowers-agent pull @myrepo path/to/skill
  superpowers-agent pull @myrepo path/to/skills --project

Description:
  Updates or adds skill(s) from a Git repository, local directory, or repository alias.
  If skills already exist, they will be updated (replaced).
  If skills don't exist, they will be added.
  Supports repositories with single or multiple skills.
  Reads skill.json to determine skill names and installation paths.
  
  Repository aliases can be added using:
    superpowers-agent add-repository <git-url> [--as=@alias]`);return}console.log(`Updating skill(s)...
`);let $=null,Y=null;if(Q.startsWith("@")){let z=Q;Y=q.find((_,w)=>w>0&&!_.startsWith("-")&&_!==z);let E=y(!1),V=y(!0);if(E[z])$=E[z],console.log(`Using project repository alias: ${z}`);else if(V[z])$=V[z],console.log(`Using global repository alias: ${z}`);else{console.log(`Error: Repository alias not found: ${z}`),console.log(`
Available aliases:`);let _={...V,...E};if(Object.keys(_).length===0)console.log("  (none)"),console.log(`
Add a repository using:`),console.log("  superpowers-agent add-repository <git-url>");else for(let[w,M]of Object.entries(_))console.log(`  ${w} -> ${M}`);return}let v=x($);if(Y)if(v)Q=b($,Y),console.log(`Repository path: ${$}`),console.log(`Skill path: ${Y}
`);else Q=`${$}/tree/main/${Y}`,console.log(`Repository URL: ${$}`),console.log(`Skill path: ${Y}
`);else if(Q=$,v)console.log(`Repository path: ${$}
`);else console.log(`Repository URL: ${$}
`)}let Z=iQ(Q);if(!Z){console.log(`Error: Invalid URL or path not found: ${Q}`);return}let H=rQ(X);console.log(`Install location: ${H}
`);let W,B=!1;try{if(Z.type==="git-repo"||Z.type==="git-tree"){if(console.log(`Cloning repository: ${Z.repoUrl}`),Z.branch)console.log(`Branch: ${Z.branch}`);if(W=sQ(Z.repoUrl,Z.branch),B=!0,Z.path){if(W=b(W,Z.path),!x(W))throw Error(`Path not found in repository: ${Z.path}`)}console.log("")}else W=Z.path;let z=Pq(W);if(!z)throw Error("No skill.json found in source directory");let E={installed:[],errors:[],source:Z.original};if(z.skills&&Array.isArray(z.skills)){console.log(`Found ${z.skills.length} skill(s) to update
`);for(let V of z.skills)jq(W,V,H,E)}else{let V=z.name||Dq(W).base;jq(tq(W),Dq(W).base,H,E)}if(B&&W)try{let V=W.split("/.agents/tmp/")[0]+"/.agents/tmp/"+W.split("/.agents/tmp/")[1].split("/")[0];N(`rm -rf "${V}"`,{stdio:"pipe"})}catch{}if(console.log(`
**Successfully updated skills:**`),console.log(`- Source: ${E.source}`),E.installed.length>0)for(let V of E.installed)console.log(`  - Updated: ${V.name} at ${V.path}`),console.log(`    ${V.title}`);if(E.errors.length>0){console.log(`
**Errors:**`);for(let V of E.errors)console.log(`  - ${V}`)}if(E.installed.length===0&&E.errors.length===0)console.log("  No skills were updated");if(E.installed.length>0)console.log(`
**Syncing skill symlinks...**`),iq()}catch(z){if(B&&W)try{let E=W.split("/.agents/tmp/")[0]+"/.agents/tmp/"+W.split("/.agents/tmp/")[1].split("/")[0];N(`rm -rf "${E}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${z.message}`)}},qX=()=>{let q=process.argv.slice(3),Q=q.find((z)=>!z.startsWith("-")),X=q.filter((z)=>z.startsWith("-"));if(!Q){console.log(`Usage: superpowers-agent add-repository <git-url> [options]

Options:
  --global, -g        Add repository globally in ~/.agents/config.json (default)
  --project, -p       Add repository in current project's .agents/config.json
  --as=<alias>        Specify custom alias for the repository

Examples:
  superpowers-agent add-repository https://github.com/example/skills.git
  superpowers-agent add-repository https://github.com/example/skills.git --as=@myskills
  superpowers-agent add-repository https://github.com/example/skills.git --project
  superpowers-agent add-repository https://github.com/example/skills.git --as=@custom --global

Description:
  Adds a skill repository alias to your configuration.
  The repository's skill.json will be read to determine the default alias.
  Use --as to specify a custom alias.
  After adding, you can install skills using: superpowers-agent add @alias path/to/skill`);return}console.log(`Adding repository...
`);let $=X.some((z)=>z==="--global"||z==="-g"),Y=X.some((z)=>z==="--project"||z==="-p"),Z=X.find((z)=>z.startsWith("--as=")),H=Z?Z.split("=")[1]:null,W=Y?!1:!0,B;try{console.log(`Cloning repository: ${Q}`),B=b(Qq(),".agents","tmp",`repo-add-${Date.now()}`),N(`mkdir -p "${B}"`,{stdio:"pipe"}),N(`git clone --depth 1 "${Q}" "${B}"`,{stdio:"pipe",timeout:30000}),console.log("");let z=Pq(B);if(!z)throw Error("No skill.json found in repository");let E;if(H)E=H;else if(z.repository)E=z.repository;else{let _=Q.match(/\/([^/]+?)(?:\.git)?$/);if(_)E="@"+_[1];else throw Error("Could not determine repository alias. Use --as=@alias to specify manually.")}_Q(E,Q,W),N(`rm -rf "${B}"`,{stdio:"pipe"});let V=W?"globally":"in project",v=W?b(Qq(),".agents","config.json"):b(process.cwd(),".agents","config.json");console.log(`✓ Repository added ${V}`),console.log(`  Alias: ${E}`),console.log(`  URL: ${Q}`),console.log(`  Config: ${v}`),console.log(`
You can now install skills using:`),console.log(`  superpowers-agent add ${E} <path-to-skill>`)}catch(z){if(B)try{N(`rm -rf "${B}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${z.message}`)}};import{dirname as YY}from"path";var QX=(q)=>{if(!q){console.log(`Usage: superpowers-agent use-skill <skill-name>

Examples (smart matching):
  superpowers-agent use-skill brainstorming              # Matches superpowers:collaboration/brainstorming
  superpowers-agent use-skill collaboration/brainstorming # More specific suffix
  superpowers-agent use-skill test-driven-development    # Matches superpowers:testing/test-driven-development

Examples (explicit paths):
  superpowers-agent use-skill superpowers:collaboration/brainstorming  # Full superpowers skill path
  superpowers-agent use-skill claude:email-assistant                   # Full claude skill path
  superpowers-agent use-skill my-custom-skill                          # Project skill

Smart matching:
  - Type just the skill name or any suffix of the path
  - Priority: .agents/skills/ > .claude/skills/ > ~/.agents/skills/ > ~/.agents/superpowers/skills/
  - If multiple skills match at same priority, you'll see them with descriptions`);return}let Q=S(q);if(!Q){console.log(`Error: Skill not found: ${q}

Available skills:`),console.log("Run: superpowers-agent find-skills");return}let{skillFile:X,sourceType:$,actualSkillPath:Y}=Q;try{let{content:Z,frontmatter:H}=$Q(X),W=Wq[$].prefix+Y,B=YY(X),z=`# ${H.name||W}`;if(H.description)z+=`
# ${H.description}`;if(H.whenToUse)z+=`
# When to use: ${H.whenToUse}`;z+=`
# Supporting tools and docs are in ${B}
# ============================================

${Z}`,console.log(z)}catch(Z){console.log(Z.message)}};var XX={bootstrap:lQ,version:cQ,"check-updates":()=>{pQ().catch((q)=>{console.error(q.message),process.exit(1)})},update:()=>{let q=process.argv.includes("--no-reinstall");xq({skipReinstall:q})},"config-get":MQ,"config-set":NQ,"setup-skills":aQ,"use-skill":()=>QX(process.argv[3]),execute:OQ,"find-skills":c,add:tQ,"add-repository":qX,"list-repositories":FQ,pull:eQ,dir:TQ,path:wQ,"get-helpers":bQ,"install-copilot-prompts":a,"install-copilot-instructions":l,"install-cursor-commands":i,"install-cursor-hooks":r,"install-codex-prompts":s,"install-gemini-commands":t,"install-claude-commands":e,"install-opencode-commands":qq,"install-aliases":sq,default:()=>{console.log(`Superpowers for Agents
Usage:
  superpowers-agent bootstrap [--no-update] [--force]               # Run complete bootstrap
  superpowers-agent version                                         # Show current version
  superpowers-agent check-updates                                   # Check for updates
  superpowers-agent update [--no-reinstall]                         # Update to latest version
  superpowers-agent config-get                                      # Show configuration
  superpowers-agent config-set <key> <value>                        # Update configuration
  superpowers-agent setup-skills                                    # Initialize project skills
  superpowers-agent use-skill <skill-name>                          # Load a skill
  superpowers-agent find-skills                                     # List available skills
  superpowers-agent add <url-or-path|@alias> [path] [options]      # Install skills
  superpowers-agent add-repository <git-url> [--as=@alias] [opts]  # Add repository alias
  superpowers-agent list-repositories                              # List configured repository aliases
  superpowers-agent pull <url-or-path|@alias> [path] [options]     # Update skills
  superpowers-agent dir <skill-name>                                # Get skill directory
  superpowers-agent path <skill-name>                               # Get skill file path
  superpowers-agent execute <skill-name>                            # Execute a skill
  superpowers-agent get-helpers <skill> <search-term>               # Get helper file path
  superpowers-agent install-copilot-prompts                         # Install GitHub Copilot prompts
  superpowers-agent install-copilot-instructions                    # Install universal instructions
  superpowers-agent install-cursor-commands                         # Install Cursor commands
  superpowers-agent install-cursor-hooks                            # Install Cursor hooks
  superpowers-agent install-codex-prompts                           # Install Codex prompts
  superpowers-agent install-gemini-commands                         # Install Gemini commands
  superpowers-agent install-claude-commands                         # Install Claude commands
  superpowers-agent install-opencode-commands                       # Install OpenCode commands
  superpowers-agent install-aliases                                 # Install universal aliases (superpowers, superpowers-agent)

Documentation: https://github.com/complexthings/superpowers
`)}},ZY=process.argv[2]||"default",$Y=XX[ZY]||XX.default;try{$Y()}catch(q){console.error(`Error: ${q.message}`),process.exit(1)}
