#!/bin/sh
// 2>/dev/null; exec "$(command -v bun 2>/dev/null || echo node)" "$0" "$@"
import{relative as _1,dirname as V1,join as U2}from"path";import{existsSync as j$}from"fs";import{homedir as r$,platform as e1}from"os";import{join as A,parse as $2,dirname as s$}from"path";import{existsSync as c}from"fs";import{fileURLToPath as Q2}from"url";var X2=Q2(import.meta.url),K$=s$(X2),Y2=()=>{let $=r$();switch(e1()){case"darwin":return A($,"Library","Application Support","Code","User");case"win32":return A($,"AppData","Roaming","Code","User");case"linux":return A($,".config","Code","User");default:return A($,".config","Code","User")}},Z2=()=>{let $=process.cwd(),Q=$2($).root;while($!==Q){if(c(A($,".agents")))return $;$=s$($)}return process.cwd()},z={home:r$(),vscodeUserDir:Y2(),projectRoot:Z2(),get projectClaudeSkills(){return A(this.projectRoot,".claude","skills")},get projectAgentsSkills(){return A(this.projectRoot,".agents","skills")},get projectCopilotSkills(){return A(this.projectRoot,".copilot","skills")},get projectOpencodeSkills(){return A(this.projectRoot,".opencode","skill")},get projectSkillsDir(){return A(this.projectRoot,"skills")},get homePersonalSkills(){return A(this.home,".agents","skills")},get homeClaudeSkills(){return A(this.home,".claude","skills")},get homeCopilotSkills(){return A(this.home,".copilot","skills")},get homeOpencodeSkills(){return A(this.home,".config","opencode","skill")},get bootstrap(){return A(this.projectRoot,".agents","superpowers-bootstrap.md")},get superpowersRepo(){let $=A(K$,"..","..","..");if(c(A($,"skills"))&&c(A($,".agents","superpowers-agent")))return $;let Q=A(K$,"..");if(c(A(Q,"skills"))&&c(A(Q,".agents","superpowers-agent")))return Q;return A(this.home,".agents","superpowers")},get homeSuperpowersSkills(){return A(this.superpowersRepo,"skills")},get isSuperpowersRepo(){let $=A(K$,"..","..","..");if(c(A($,"skills"))&&c(A($,".agents","superpowers-agent")))return!0;let Q=A(K$,"..");return c(A(Q,"skills"))&&c(A(Q,".agents","superpowers-agent"))}};import{readFileSync as t$}from"fs";var X$=($)=>{try{let X=t$($,"utf8").split(`
`),Z={name:"",description:"",whenToUse:""},Y=!1;for(let H of X){if(H.trim()==="---"){if(Y)break;Y=!0;continue}if(Y){let W=H.match(/^(\w+):\s*(.*)$/);if(W){let[,q,B]=W,E={name:"name",description:"description",when_to_use:"whenToUse"};if(E[q])Z[E[q]]=B.trim()}}}return Z}catch{return{name:"",description:"",whenToUse:""}}},e$=($)=>{try{let Q=t$($,"utf8"),X=X$($),Z=Q.split(`
`),Y=!1,H=!1,W=[];for(let q of Z){if(q.trim()==="---"){if(Y){H=!0;continue}Y=!0;continue}if(H||!Y)W.push(q)}return{content:W.join(`
`).trim(),frontmatter:X}}catch(Q){throw Error(`Error reading skill file: ${Q.message}`)}};import{existsSync as v$,readdirSync as X1,statSync as Y1}from"fs";import{join as T$,relative as w3}from"path";import{readFileSync as H2,existsSync as q2}from"fs";import{join as $1}from"path";var Y$=($)=>{let Q=$1($,"skill.json");if(!q2(Q))return null;try{let X=H2(Q,"utf8");return JSON.parse(X)}catch(X){return null}};var M$=($)=>{if($.startsWith("superpowers:"))return{type:"superpowers",path:$.substring(12).replace(/^skills\//,"")};if($.startsWith("claude:"))return{type:"claude",path:$.substring(7).replace(/^skills\//,"")};if($.startsWith("copilot:"))return{type:"copilot",path:$.substring(8).replace(/^skills\//,"")};if($.startsWith("opencode:"))return{type:"opencode",path:$.substring(9).replace(/^skills\//,"")};return{type:null,path:$.replace(/^skills\//,"")}},Q1=($,Q)=>{let X=Y$($);if(!X||!X.helpers||!Array.isArray(X.helpers))return null;let Z=X.helpers,Y=Q.toLowerCase(),H=null,W=0;for(let q of Z){let B=q.toLowerCase();if(B===Y){H=q;break}if(B.includes(Y)){let G=Y.length/B.length;if(G>W)W=G,H=q}let E=q.split("/").pop().split(".")[0].toLowerCase();if(Y.includes(E)){let G=E.length/Y.length;if(G>W)W=G,H=q}}return H?$1($,H):null},Z$={project:{dir:"projectAgentsSkills",prefix:""},claude:{dir:"projectClaudeSkills",prefix:"claude:"},copilot:{dir:"projectCopilotSkills",prefix:"copilot:"},opencode:{dir:"projectOpencodeSkills",prefix:"opencode:"},personal:{dir:"homePersonalSkills",prefix:""},personalClaude:{dir:"homeClaudeSkills",prefix:"claude:"},personalCopilot:{dir:"homeCopilotSkills",prefix:"copilot:"},personalOpencode:{dir:"homeOpencodeSkills",prefix:"opencode:"},superpowers:{dir:"homeSuperpowersSkills",prefix:"superpowers:"}};var G$=($,Q,X=null)=>{let Z=[];if(!v$($))return Z;let Y=(H,W)=>{if(X!==null&&W>X)return;try{let q=X1(H,{withFileTypes:!0});for(let B of q){if(B.name==="node_modules"||B.name===".git"||B.name.startsWith("."))continue;let E=T$(H,B.name),G=B.isDirectory();if(B.isSymbolicLink())try{G=Y1(E).isDirectory()}catch{continue}if(!G)continue;let K=T$(E,"SKILL.md");if(v$(K))Z.push(E);if(X===null||W<X)Y(E,W+1)}}catch{}};return Y($,0),Z};var Z1=($,Q)=>{if(!v$($))return[];let X=[],Z=Q.toLowerCase().replace(/\\/g,"/"),Y=(H,W="")=>{try{let q=X1(H,{withFileTypes:!0});for(let B of q){let E=T$(H,B.name),G=W?`${W}/${B.name}`:B.name,K=B.isDirectory();if(B.isSymbolicLink())try{K=Y1(E).isDirectory()}catch{continue}if(K&&(B.name==="node_modules"||B.name===".git"||B.name.startsWith(".")))continue;if(K)Y(E,G);else if(B.name==="SKILL.md"){let _=W.toLowerCase().replace(/\\/g,"/");if(_===Z||_.endsWith("/"+Z)||_.endsWith(Z))X.push({file:E,path:W})}}}catch(q){}};return Y($),X},H1=($,Q,X)=>{let Z={project:"project skills (.agents/skills/)",claude:"claude skills (.claude/skills/)",copilot:"copilot skills (.copilot/skills/)",opencode:"opencode skills (.opencode/skill/)",personal:"personal skills (~/.agents/skills/)",personalClaude:"personal claude skills (~/.claude/skills/)",personalCopilot:"personal copilot skills (~/.copilot/skills/)",personalOpencode:"personal opencode skills (~/.config/opencode/skill/)",superpowers:"superpowers skills (~/.agents/superpowers/skills/)"}[X];console.log(`Error: Multiple skills match "${$}" in ${Z}:
`);for(let Y of Q){let W=Z$[X].prefix+Y.path;try{let q=X$(Y.file),B=q.description||q.whenToUse||"(no description)";console.log(`  ${W}`),console.log(`    ${B}
`)}catch(q){console.log(`  ${W}`),console.log(`    (unable to read description)
`)}}console.log("Please be more specific. Examples:");for(let Y of Q.slice(0,2)){let H=Z$[X].prefix;console.log(`  superpowers-agent execute ${H}${Y.path}`)}};import{existsSync as C$}from"fs";import{join as W2,relative as z2}from"path";var E2=($)=>{let{type:Q,path:X}=M$($),Z={superpowers:[z.homeSuperpowersSkills],claude:[z.projectClaudeSkills,z.homeClaudeSkills],copilot:[z.projectCopilotSkills,z.homeCopilotSkills],opencode:[z.projectOpencodeSkills,z.homeOpencodeSkills],project:[z.projectAgentsSkills],personal:[z.homePersonalSkills]},Y;if(Q)Y=(Z[Q]||[]).map((W)=>({type:Q,dir:W}));else if(z.isSuperpowersRepo&&C$(z.projectSkillsDir))Y=[{type:"project",dir:z.projectSkillsDir},{type:"project",dir:z.projectAgentsSkills},{type:"claude",dir:z.projectClaudeSkills},{type:"copilot",dir:z.projectCopilotSkills},{type:"opencode",dir:z.projectOpencodeSkills},{type:"personal",dir:z.homePersonalSkills},{type:"personalClaude",dir:z.homeClaudeSkills},{type:"personalCopilot",dir:z.homeCopilotSkills},{type:"personalOpencode",dir:z.homeOpencodeSkills},{type:"superpowers",dir:z.homeSuperpowersSkills}];else Y=[{type:"project",dir:z.projectAgentsSkills},{type:"claude",dir:z.projectClaudeSkills},{type:"copilot",dir:z.projectCopilotSkills},{type:"opencode",dir:z.projectOpencodeSkills},{type:"personal",dir:z.homePersonalSkills},{type:"personalClaude",dir:z.homeClaudeSkills},{type:"personalCopilot",dir:z.homeCopilotSkills},{type:"personalOpencode",dir:z.homeOpencodeSkills},{type:"superpowers",dir:z.homeSuperpowersSkills}];for(let{type:H,dir:W}of Y){let q=Z1(W,X);if(q.length===1)return{skillFile:q[0].file,sourceType:H,actualSkillPath:q[0].path};if(q.length>1)H1(X,q,H)}return null},a=($)=>{let Q=E2($);if(Q)return Q;let{type:X,path:Z}=M$($),Y={superpowers:[z.homeSuperpowersSkills],claude:[z.projectClaudeSkills,z.homeClaudeSkills],copilot:[z.projectCopilotSkills,z.homeCopilotSkills],opencode:[z.projectOpencodeSkills,z.homeOpencodeSkills],project:[z.projectAgentsSkills],personal:[z.homePersonalSkills]},H;if(X)H=(Y[X]||[]).map((q)=>({type:X,dir:q}));else if(z.isSuperpowersRepo&&C$(z.projectSkillsDir))H=[{type:"project",dir:z.projectSkillsDir},{type:"project",dir:z.projectAgentsSkills},{type:"claude",dir:z.projectClaudeSkills},{type:"copilot",dir:z.projectCopilotSkills},{type:"opencode",dir:z.projectOpencodeSkills},{type:"personal",dir:z.homePersonalSkills},{type:"personalClaude",dir:z.homeClaudeSkills},{type:"personalCopilot",dir:z.homeCopilotSkills},{type:"personalOpencode",dir:z.homeOpencodeSkills},{type:"superpowers",dir:z.homeSuperpowersSkills}];else H=[{type:"project",dir:z.projectAgentsSkills},{type:"claude",dir:z.projectClaudeSkills},{type:"copilot",dir:z.projectCopilotSkills},{type:"opencode",dir:z.projectOpencodeSkills},{type:"personal",dir:z.homePersonalSkills},{type:"personalClaude",dir:z.homeClaudeSkills},{type:"personalCopilot",dir:z.homeCopilotSkills},{type:"personalOpencode",dir:z.homeOpencodeSkills},{type:"superpowers",dir:z.homeSuperpowersSkills}];for(let{type:W,dir:q}of H){if(!C$(q))continue;let B=G$(q,W,null);for(let E of B){let G=Y$(E);if(G&&G.aliases&&Array.isArray(G.aliases)){let K=Z.toLowerCase();for(let _ of G.aliases)if(_.toLowerCase()===K){let V=W2(E,"SKILL.md"),L=z2(q,E);return{skillFile:V,sourceType:W,actualSkillPath:L}}}}}return null};import{readFileSync as q1,writeFileSync as W1,existsSync as N$}from"fs";import{join as l}from"path";import{execSync as B2}from"child_process";var b$=()=>({auto_update:!0,last_update_check:null,last_updated_commit:null}),H$=()=>{let $=l(z.superpowersRepo,".config.json");try{if(!N$($))return b$();let Q=q1($,"utf8");return{...b$(),...JSON.parse(Q)}}catch{return b$()}},z1=($)=>{let Q=l(z.superpowersRepo,".config.json"),Z={...H$(),...$};try{W1(Q,JSON.stringify(Z,null,2))}catch(Y){console.log(`Warning: couldn't save config: ${Y.message}`)}},y=($)=>{let Q=$?l(z.home,".agents","config.json"):l(process.cwd(),".agents","config.json");if(!N$(Q))return{};try{let X=q1(Q,"utf8");return JSON.parse(X)}catch(X){return{}}},m=($,Q)=>{let X=Q?l(z.home,".agents"):l(process.cwd(),".agents"),Z=l(X,"config.json");try{if(!N$(X))B2(`mkdir -p "${X}"`,{stdio:"pipe"});return W1(Z,JSON.stringify($,null,2),"utf8"),!0}catch(Y){throw Error(`Failed to write config: ${Y.message}`)}},h=($=!1)=>{return y($).repositories||{}},E1=($,Q,X)=>{let Z=y(X);if(!Z.repositories)Z.repositories={};Z.repositories[$]=Q,m(Z,X)};function B1($=!0){return y($).installedSkills||{}}function F$($,Q=!0){let X=y(Q);if(X.installedSkills)delete X.installedSkills[$];m(X,Q)}function K1(){return y(!0).installedAgents||{}}import{readFileSync as K2}from"fs";import{join as x$,dirname as G2}from"path";import{fileURLToPath as _2}from"url";import{existsSync as G1}from"fs";import{execSync as V2}from"child_process";var w2=_2(import.meta.url),D$=G2(w2),u=()=>{try{let $=x$(D$,"package.json");if(!G1($))$=x$(D$,"..","package.json");if(!G1($))$=x$(D$,"..","..","package.json");return JSON.parse(K2($,"utf8")).version||"0.0.0"}catch{return"0.0.0"}},p=()=>{let $=u();console.log(`^^SAV:${$}^^`)},_$=async()=>{try{let Q=V2('curl -sS "https://registry.npmjs.org/@complexthings/superpowers-agent/latest"',{encoding:"utf8",timeout:1e4}),X=JSON.parse(Q);if(!X.version)return u();return X.version}catch($){throw Error(`Failed to fetch remote version: ${$.message}`)}},V$=($,Q)=>{let X=$.split(".").map(Number),Z=Q.split(".").map(Number);for(let Y=0;Y<3;Y++){let H=X[Y]||0,W=Z[Y]||0;if(H>W)return!0;if(H<W)return!1}return!1};var I2={project:{dir:"projectAgentsSkills",prefix:""},claude:{dir:"projectClaudeSkills",prefix:"claude:"},copilot:{dir:"projectCopilotSkills",prefix:"copilot:"},opencode:{dir:"projectOpencodeSkills",prefix:"opencode:"},personal:{dir:"homePersonalSkills",prefix:""},personalClaude:{dir:"homeClaudeSkills",prefix:"claude:"},personalCopilot:{dir:"homeCopilotSkills",prefix:"copilot:"},personalOpencode:{dir:"homeOpencodeSkills",prefix:"opencode:"},superpowers:{dir:"homeSuperpowersSkills",prefix:"superpowers:"}},L2=($,Q)=>{let X=U2($,"SKILL.md"),{dir:Z,prefix:Y}=I2[Q],H=_1(z[Z],$).replace(/\\/g,"/");console.log(`${Y}${H}`);let{description:W,whenToUse:q}=X$(X);if(W)console.log(`  ${W}`);if(q)console.log(`  When to use: ${q}`);console.log("")},r=()=>{p();let $=new Set,Q=[{type:"project",dir:z.projectAgentsSkills,maxDepth:null},{type:"claude",dir:z.projectClaudeSkills,maxDepth:null},{type:"copilot",dir:z.projectCopilotSkills,maxDepth:null},{type:"opencode",dir:z.projectOpencodeSkills,maxDepth:null},{type:"personal",dir:z.homePersonalSkills,maxDepth:null},{type:"personalClaude",dir:z.homeClaudeSkills,maxDepth:null},{type:"personalCopilot",dir:z.homeCopilotSkills,maxDepth:null},{type:"personalOpencode",dir:z.homeOpencodeSkills,maxDepth:null},{type:"superpowers",dir:z.homeSuperpowersSkills,maxDepth:null}];for(let{type:X,dir:Z,maxDepth:Y}of Q){let H=G$(Z,X,Y);for(let W of H){let q=_1(Z,W);if(!$.has(q))$.add(q),L2(W,X)}}console.log(`Usage:
  superpowers-agent execute <skill-name> # Load a specific skill`)},w1=()=>{p();let $=process.argv[3];if(!$){console.log(`Usage: superpowers-agent execute <skill-name-or-alias>

Examples:
  superpowers-agent execute brainstorming
  superpowers-agent execute superpowers:collaboration/brainstorming
  superpowers-agent execute aem/block-collection-and-party
  superpowers-agent execute block-collection  # Using alias

Description:
  Instructs the agent to execute the specified skill.
  Supports skill aliases defined in skill.json files.`);return}let Q=a($);if(!Q){console.log(`Error: Skill not found: ${$}

Available skills:`),r();return}let{skillFile:X,sourceType:Z}=Q,Y=process.cwd(),H=X.startsWith(Y),W=j$(z.projectAgentsSkills)||j$(z.projectClaudeSkills);if(H)console.log(`Open this file with your Read Tool: ${X}

<IMPORTANT>
USE YOUR Read Tool to process this file. DO NOT use \`cat\` or print commands.
</IMPORTANT>`);else if(W&&Z!=="project")console.log(`Open this file with your Read Tool: ${X}

<IMPORTANT>
1. Try using your Read Tool first
2. If your Read tool fails (file outside workspace), use: cat "${X}"
3. Consider using workspace-local skills for better tool compatibility
   Run: superpowers-agent setup-skills
</IMPORTANT>`);else console.log(`Open this file with your Read Tool: ${X}

<IMPORTANT>
1. Try using your Read Tool first
2. If your Read tool fails (file outside workspace), use: cat "${X}"
3. NEVER skip loading the skill content
</IMPORTANT>`)},U1=()=>{let $=process.argv[3];if(!$){console.log(`Usage: superpowers-agent path <skill-name-or-alias>

Examples:
  superpowers-agent path brainstorming
  superpowers-agent path superpowers:collaboration/brainstorming
  superpowers-agent path aem/block-collection-and-party
  superpowers-agent path block-collection  # Using alias

Description:
  Outputs the file system path of the specified SKILL.md file.
  Supports skill aliases defined in skill.json files.`);return}let Q=a($);if(!Q){console.log(`Error: Skill not found: ${$}

Available skills:`),r();return}let{skillFile:X}=Q;console.log(X)},I1=()=>{let $=process.argv[3];if(!$){console.log(`Usage: superpowers-agent dir <skill-name>

Examples:
  superpowers-agent dir brainstorming
  superpowers-agent dir aem/block-collection-and-party
  superpowers-agent dir block-collection  # Using alias
  superpowers-agent dir superpowers:testing/test-driven-development

Description:
  Returns the directory path where the specified skill is located.
  Supports skill aliases defined in skill.json files.`);return}let Q=a($);if(!Q){console.log(`Error: Skill not found: ${$}

Available skills:`),r();return}let{skillFile:X}=Q,Z=V1(X);console.log(Z)},L1=()=>{p();let $=process.argv[3],Q=process.argv[4];if(!$||!Q){console.log(`Usage: superpowers-agent get-helpers <skill-name> <helper-search-term>

Examples:
  superpowers-agent get-helpers block-collection search-block
  superpowers-agent get-helpers aem/block-collection-and-party get-block-structure
  superpowers-agent get-helpers superpowers:testing/test-driven-development example

Description:
  Searches for helper files within a skill's skill.json configuration.
  Returns the full path to the best matching helper file.
  
  The command uses the skill's aliases if available, so you can use
  short names like "block-collection" instead of the full path.`);return}let X=a($);if(!X){console.log(`Error: Skill not found: ${$}

Available skills:`),r();return}let{skillFile:Z}=X,Y=V1(Z),H=Y$(Y);if(!H){console.log(`Error: No skill.json found for skill: ${$}
  Location: ${Y}`);return}if(!H.helpers||!Array.isArray(H.helpers)||H.helpers.length===0){console.log(`Error: No helpers defined in skill.json for skill: ${$}
  Location: ${Y}`);return}let W=Q1(Y,Q);if(!W){console.log(`Error: No helper found matching "${Q}" in skill: ${$}`),console.log(`
Available helpers:`);for(let q of H.helpers)console.log(`  - ${q}`);return}if(!j$(W)){console.log(`Error: Helper file not found: ${W}
  Defined in skill.json but missing from filesystem`);return}console.log(W)},O1=()=>{let $=H$();console.log("Current configuration:"),console.log(JSON.stringify($,null,2))},J1=()=>{let $=process.argv[3],Q=process.argv[4];if(!$||Q===void 0){console.log(`Usage: .agents/superpowers-agent config-set <key> <value>

Available keys:
  auto_update (true/false) - Enable/disable automatic updates during bootstrap

Examples:
  .agents/superpowers-agent config-set auto_update false
  .agents/superpowers-agent config-set auto_update true`);return}let X=Q;if(Q==="true")X=!0;else if(Q==="false")X=!1;let Z={};Z[$]=X,z1(Z),console.log(`✓ Set ${$} = ${X}`)},A1=()=>{p();let $=h(!0),Q=h(!1),X=[];for(let[H,W]of Object.entries($))X.push({alias:H,url:W,source:"global"});for(let[H,W]of Object.entries(Q)){let q=X.findIndex((B)=>B.alias===H);if(q>=0)X[q]={alias:H,url:W,source:"project"};else X.push({alias:H,url:W,source:"project"})}if(X.length===0){console.log(`No repository aliases configured.

Add a repository using:
  superpowers-agent add-repository <git-url> [--as=@alias]`);return}let Z=Math.max(5,...X.map((H)=>H.alias.length)),Y=Math.max(3,...X.map((H)=>H.url.length));console.log(`Repositories:
`),console.log(`${"Alias".padEnd(Z)}  ${"URL".padEnd(Y)}  Source`);for(let H of X)console.log(`${H.alias.padEnd(Z)}  ${H.url.padEnd(Y)}  (${H.source})`);console.log(`
Total: ${X.length} repository alias(es)`)};var w$=async()=>{try{let $=u(),Q=await _$();return{hasUpdates:V$(Q,$),localVersion:$,remoteVersion:Q}}catch{return{hasUpdates:!1,localVersion:u(),remoteVersion:"",error:!0}}};import{execSync as M1}from"child_process";var R$=async({skipReinstall:$=!1}={})=>{console.log(`# Checking for Superpowers updates...
`);let Q=await w$();if(Q.error){console.log("⚠️  Could not check for updates (network issue)");return}if(!Q.hasUpdates){console.log(`✓ Already up to date (v${Q.localVersion})`);return}if(console.log(`\uD83D\uDCE6 Update available: v${Q.localVersion} → v${Q.remoteVersion}
`),$){console.log("   Skipping install (--no-reinstall flag set)"),console.log(`   Run manually: npm install -g @complexthings/superpowers-agent
`);return}console.log(`   Installing update...
`);try{M1("npm install -g @complexthings/superpowers-agent",{stdio:"inherit"}),console.log(`
✓ Update complete!`)}catch(X){console.log(`
✗ Update failed: ${X.message}`),console.log("   Try running manually: npm install -g @complexthings/superpowers-agent"),process.exit(1)}console.log(`
   Running bootstrap to complete setup...
`);try{M1("superpowers-agent bootstrap --no-update",{stdio:"inherit"}),console.log(`
✓ Bootstrap complete!`)}catch(X){console.log(`
⚠️  Bootstrap step failed: ${X.message}`),console.log("   The update installed successfully. Run manually to finish setup:"),console.log("   superpowers-agent bootstrap")}},v1=async()=>{p();let $=u();console.log(`Current version: ${$}`);try{let Q=await _$();if(console.log(`Latest version: ${Q}`),V$(Q,$))console.log("Update available: Yes"),process.exit(1);else console.log("You are up to date"),process.exit(0)}catch(Q){console.log(`Error checking for updates: ${Q.message}`),process.exit(1)}},T1=()=>{let $=u();console.log($)};import{existsSync as M,readFileSync as o,writeFileSync as $$,rmSync as P1,lstatSync as R2}from"fs";import{join as T,dirname as u1,parse as q$}from"path";import{execSync as S}from"child_process";import{platform as S2}from"os";import{execSync as O2}from"child_process";var S$=($)=>{try{return O2(`which ${$}`,{stdio:"pipe",timeout:2000}),!0}catch{return!1}};var R={opencode:{check:()=>S$("opencode"),cli:!0,name:"OpenCode",installUrl:"https://opencode.ai/docs/installation",bootstrapCommand:"install-opencode-commands"},claude:{check:()=>S$("claude"),cli:!0,name:"Claude Code",installUrl:"https://code.claude.com/docs/en/installation",bootstrapCommand:"install-claude-commands"},copilot:{check:()=>!0,cli:!1,name:"GitHub Copilot",bootstrapCommand:"install-copilot-prompts"}},C1=()=>{let $=[];if(R.copilot.check())$.push("github-copilot");if(R.claude.check())$.push("claude-code");if(R.opencode.check())$.push("opencode");return $};import{existsSync as y$,readdirSync as ZQ,lstatSync as J2,symlinkSync as b1,unlinkSync as A2,mkdirSync as M2,readlinkSync as v2}from"fs";import{join as f$,dirname as qQ}from"path";import{platform as T2}from"os";var g$=($)=>{try{return J2($).isSymbolicLink()}catch{return!1}},C2=($,Q)=>{try{if(!g$($))return!1;return v2($)===Q}catch{return!1}},N1=()=>{let $=f$(z.superpowersRepo,".opencode","plugins","superpowers-agent.js"),Q=f$(z.home,".config","opencode","plugins"),X=f$(Q,"superpowers-agent.js");if(console.log("Installing OpenCode plugin symlink..."),!y$($))return console.log("⚠️  Source plugin not found, skipping symlink creation"),console.log(`   Expected at: ${$}`),{created:!1,error:"Source plugin not found"};if(C2(X,$))return console.log("✓ Plugin symlink already exists and is correct"),{created:!1,existed:!0};if(y$(X)&&!g$(X))return console.log("⚠️  Warning: A file already exists at the destination path"),console.log(`   Path: ${X}`),console.log("   Skipping symlink creation to avoid overwriting existing file"),console.log("   To use superpowers-agent plugin, manually remove or rename the existing file"),{created:!1,error:"File already exists at destination"};if(!y$(Q))try{M2(Q,{recursive:!0}),console.log(`✓ Created ${Q.replace(z.home,"~")}`)}catch(Y){return console.log(`⚠️  Failed to create plugins directory: ${Y.message}`),{created:!1,error:`Failed to create directory: ${Y.message}`}}if(g$(X))try{A2(X)}catch(Y){return console.log(`⚠️  Failed to remove existing symlink: ${Y.message}`),{created:!1,error:`Failed to remove existing symlink: ${Y.message}`}}let Z=T2();try{if(Z==="win32")b1($,X,"file");else b1($,X);let Y=$.replace(z.home,"~"),H=X.replace(z.home,"~");return console.log(`✓ Created symlink: ${H}`),console.log(`  -> ${Y}`),{created:!0}}catch(Y){if(Z==="win32"&&Y.code==="EPERM")return console.log("⚠️  Windows requires Developer Mode or admin privileges for symlinks"),console.log("   Enable Developer Mode: Settings > Update & Security > For developers"),{created:!1,error:"Windows symlink permission denied"};return console.log(`⚠️  Failed to create symlink: ${Y.message}`),{created:!1,error:`Failed to create symlink: ${Y.message}`}}};import{existsSync as b,readdirSync as F1,lstatSync as b2,symlinkSync as U$,unlinkSync as P$,mkdirSync as s,readlinkSync as N2}from"fs";import{join as C,dirname as F2,basename as x2}from"path";import{homedir as k,platform as x1}from"os";var e=[{name:"claude",parentDir:()=>C(k(),".claude"),skillsDir:()=>C(k(),".claude","skills")},{name:"copilot",parentDir:()=>C(k(),".copilot"),skillsDir:()=>C(k(),".copilot","skills")},{name:"opencode",parentDir:()=>C(k(),".config","opencode"),skillsDir:()=>C(k(),".config","opencode","skill")}],D2=[{name:"claude",agentDir:($)=>C($,".claude"),skillsDir:($)=>C($,".claude","skills"),detect:($)=>b(C($,".claude"))},{name:"copilot",agentDir:($)=>C($,".github"),skillsDir:($)=>C($,".github","skills"),detect:($)=>{let Q=b(C($,".github")),X=b(C($,"AGENTS.md"))||b(C($,".agents","AGENTS.md"));return Q&&X}},{name:"opencode",agentDir:($)=>C($,".opencode"),skillsDir:($)=>C($,".opencode","skill"),detect:($)=>{let Q=b(C($,".opencode")),X=b(C($,"AGENTS.md"))||b(C($,".agents","AGENTS.md"))||b(C($,".opencode","AGENTS.md"));return Q&&X}}],t=($)=>{try{return b2($).isSymbolicLink()}catch{return!1}},D1=($,Q)=>{try{if(!t($))return!1;return N2($)===Q}catch{return!1}},I$=($,Q)=>{if(!b($))return{created:!1,existed:!1,error:`Source does not exist: ${$}`};if(D1(Q,$))return{created:!1,existed:!0};if(b(Q)||t(Q))if(t(Q))try{P$(Q)}catch(Y){return{created:!1,existed:!1,error:`Failed to remove existing symlink: ${Y.message}`}}else return{created:!1,existed:!1,error:`Target exists and is not a symlink: ${Q}`};let X=F2(Q);if(!b(X))try{s(X,{recursive:!0})}catch(Y){return{created:!1,existed:!1,error:`Failed to create parent directory: ${Y.message}`}}let Z=x1();try{if(Z==="win32")U$($,Q,"junction");else U$($,Q,"dir");return{created:!0,existed:!1}}catch(Y){if(Z==="win32"&&Y.code==="EPERM")return{created:!1,existed:!1,error:"Windows requires Developer Mode or admin privileges for symlinks. Enable Developer Mode: Settings > Update & Security > For developers"};return{created:!1,existed:!1,error:`Failed to create symlink: ${Y.message}`}}},u$=($)=>{if(!b($)&&!t($))return{removed:!1,error:"Symlink does not exist"};if(!t($))return{removed:!1,error:"Target is not a symlink"};try{return P$($),{removed:!0}}catch(Q){return{removed:!1,error:`Failed to remove symlink: ${Q.message}`}}},j2=($,Q,X="skills")=>{let Z=y(!0);if(!Z.symlinks)Z.symlinks={};if(!Z.symlinks[$])Z.symlinks[$]={superpowers:null,skills:[]};if(X==="superpowers")Z.symlinks[$].superpowers=Q;else{let Y=Z.symlinks[$].skills||[];if(!Y.includes(Q))Y.push(Q),Z.symlinks[$].skills=Y}m(Z,!0)},j1=($,Q,X="skills")=>{let Z=y(!0);if(!Z.symlinks||!Z.symlinks[$])return;if(X==="superpowers")Z.symlinks[$].superpowers=null;else{let Y=Z.symlinks[$].skills||[];Z.symlinks[$].skills=Y.filter((H)=>H!==Q)}m(Z,!0)};var R1=($,Q={})=>{let X=z.homePersonalSkills;if(!b(X))return{created:0,existed:0};let Z;try{Z=F1(X,{withFileTypes:!0}).filter((H)=>(H.isDirectory()||H.isSymbolicLink())&&!H.name.startsWith(".")).map((H)=>H.name)}catch{return{created:0,existed:0}}if(Z.length===0)return{created:0,existed:0};let Y={created:0,existed:0,names:[]};for(let H of Z){let W=C(X,H),q=C($.skillsDir(),H),B=I$(W,q);if(B.created)j2($.name,q,"skills"),Y.created++,Y.names.push(H);else if(B.existed)Y.existed++;else if(B.error)console.log(`  ⚠️  ${H}: ${B.error}`)}if(Y.created>0)console.log(`  ✓ Synced ${Y.created} personal skill(s): ${Y.names.join(", ")}`);return Y},S1=($={})=>{let{force:Q=!1,forceAgents:X=new Set}=$;for(let Z of e){let Y=Q||X.has(Z.name),H=Z.parentDir(),W=Z.skillsDir();if(!b(H))if(Y)try{s(H,{recursive:!0}),console.log(`✓ Created ${H.replace(k(),"~")}`)}catch(q){console.log(`⚠️  Failed to create ${H.replace(k(),"~")}: ${q.message}`);continue}else{console.log(`⚠️  Skipping ${Z.name} (${H.replace(k(),"~")} not found)`),console.log("   Use --force or --force-<agent> to create directory");continue}if(!b(W))try{s(W,{recursive:!0})}catch(q){console.log(`⚠️  Failed to create skills directory for ${Z.name}: ${q.message}`);continue}console.log(`${Z.name}:`),R1(Z,$)}},k$=($={})=>{let{force:Q=!1}=$;for(let X of e){let Z=X.parentDir(),Y=X.skillsDir();if(!b(Z)){if(!Q)continue;try{s(Z,{recursive:!0})}catch{continue}}if(!b(Y))try{s(Y,{recursive:!0})}catch{continue}R1(X,$)}};var y1=($,Q=[])=>{let X;try{X=F1($,{withFileTypes:!0})}catch{return Q}let Z=!1,Y=[];for(let H of X){if(!H.isDirectory()&&H.name==="SKILL.md")Z=!0;if(H.isDirectory()&&!H.name.startsWith("."))Y.push(C($,H.name))}if(Z)Q.push($);for(let H of Y)y1(H,Q);return Q},f1=()=>{let $=z.homeSuperpowersSkills,Q=z.homePersonalSkills;if(!b($))return{created:0,existed:0,updated:0,errors:[`Skills directory not found: ${$}`]};if(!b(Q))try{s(Q,{recursive:!0})}catch(Y){return{created:0,existed:0,updated:0,errors:[`Failed to create ${Q}: ${Y.message}`]}}let X=y1($),Z={created:0,existed:0,updated:0,errors:[]};for(let Y of X){let H=x2(Y),W=C(Q,H);if(D1(W,Y)){Z.existed++;continue}if(t(W))try{P$(W),Z.updated++}catch(B){Z.errors.push(`Failed to remove stale symlink ${W}: ${B.message}`);continue}else if(b(W)){Z.errors.push(`Skipped ${W}: path exists and is not a symlink`);continue}let q=x1();try{if(q==="win32")U$(Y,W,"junction");else U$(Y,W,"dir");let B=W.replace(k(),"~"),E=Y.replace(k(),"~");console.log(`  ✓ ${B} -> ${E}`),Z.created++}catch(B){Z.errors.push(`Failed to create symlink ${W}: ${B.message}`)}}return Z},g1=($={})=>{let Q=$.projectRoot||process.cwd(),X=C(Q,".agents","skills");if(!b(X))return{created:0,existed:0,skipped:0,errors:[]};let Z={created:0,existed:0,skipped:0,errors:[]};for(let Y of D2){if(!Y.detect(Q)){Z.skipped++;continue}let H=Y.skillsDir(Q),W=Y.agentDir(Q);if(!b(W)){Z.skipped++;continue}let q=I$(X,H);if(q.created){let B=H.replace(Q,".");console.log(`  ✓ Created ${B} -> .agents/skills`),Z.created++}else if(q.existed)Z.existed++;else if(q.error)console.log(`  ⚠️  ${Y.name}: ${q.error}`),Z.errors.push({platform:Y.name,error:q.error})}return Z};var y2=()=>{let $=T(z.superpowersRepo,".agents","templates","TOOLS.md.template");if(M($))try{return o($,"utf8").trim()}catch(Q){return"### Using Tools\n\nUse your native skill tool. Fallback: `superpowers-agent find-skills`"}return""},L$=($,Q,X,Z=!0)=>{let Y=M($);if(!Y&&!Z)return{updated:!1,created:!1,skipped:!0};let H=y2(),W=Q.replace(/\{\{TOOL_MAPPINGS\}\}/g,H),q=new Date().toISOString().split("T")[0];W=W.replace(/\{\{DATE\}\}/g,q),W=W.replace(/\{\{SUPERPOWERS_PATH\}\}/g,z.superpowersRepo);let B=`<!-- SUPERPOWERS_SKILLS_START -->
${W}
<!-- SUPERPOWERS_SKILLS_END -->`;if(Y){let E=new Date().toISOString().split("T")[0],G=`${$}.backup-${E}`;try{S(`cp "${$}" "${G}"`,{stdio:"pipe"})}catch(U){return{updated:!1,error:!0,message:`Failed to backup: ${U.message}`}}let K;try{K=o($,"utf8")}catch(U){return{updated:!1,error:!0,message:`Failed to read: ${U.message}`}}let _="<!-- SUPERPOWERS_SKILLS_START -->",V="<!-- SUPERPOWERS_SKILLS_END -->",L;if(K.includes(_)&&K.includes(V)){let U=new RegExp(`${_}[\\s\\S]*?${V}`,"g");L=K.replace(U,B)}else{let U=K.match(/^#\s+.+$/m);if(U){let O=K.indexOf(`
`,U.index)+1;L=K.slice(0,O)+`
`+B+`
`+K.slice(O)}else L=B+`

`+K}try{return $$($,L,"utf8"),{updated:!0,created:!1,backup:G}}catch(U){return{updated:!1,error:!0,message:`Failed to write: ${U.message}`}}}else try{let E=u1($);if(!M(E))S(`mkdir -p "${E}"`,{stdio:"pipe"});let _=`# ${q$($).base.toUpperCase()}

`+B;return $$($,_,"utf8"),{updated:!1,created:!0}}catch(E){return{updated:!1,created:!1,error:!0,message:`Failed to create: ${E.message}`}}},f2=()=>{console.log("Installing Unix aliases...");let $=T(z.superpowersRepo,".agents","superpowers-agent"),Q=T(z.home,".local","bin"),X=T(Q,"superpowers-agent"),Z=T(Q,"superpowers");if(!M($)){console.log("⚠️  superpowers-agent executable not found");return}if(!M(Q))try{S(`mkdir -p "${Q}"`,{stdio:"pipe"}),console.log(`✓ Created ${Q}`)}catch(H){console.log(`⚠️  Failed to create ${Q}: ${H.message}`);return}try{if(M(X))S(`rm "${X}"`,{stdio:"pipe"});S(`ln -s "${$}" "${X}"`,{stdio:"pipe"}),console.log(`✓ Created symlink: superpowers-agent -> ${$}`)}catch(H){console.log(`⚠️  Failed to create superpowers-agent symlink: ${H.message}`)}try{if(M(Z))S(`rm "${Z}"`,{stdio:"pipe"});S(`ln -s "${$}" "${Z}"`,{stdio:"pipe"}),console.log(`✓ Created symlink: superpowers -> ${$}`)}catch(H){console.log(`⚠️  Failed to create superpowers symlink: ${H.message}`)}if(!(process.env.PATH||"").includes(Q))console.log(`
⚠️  Warning: ${Q} is not in your PATH`),console.log("   Add this to your shell profile (~/.bashrc, ~/.zshrc, etc.):"),console.log(`   export PATH="$HOME/.local/bin:$PATH"
`)},g2=()=>{console.log("Installing Windows aliases..."),console.log("✓ Windows alias installation (implementation pending)")},d$=()=>{let $=S2();if(console.log(`## Installing Universal Aliases
`),$==="win32")return g2();else return f2()},P2=($)=>{let Q=T(z.superpowersRepo,".github","copilot-instructions.md"),X=T(z.superpowersRepo,"skills","meta","using-superpowers","SKILL.md"),Z=T($,".github","copilot-instructions.md"),Y="<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->",H="<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->";if(!M(Q))return{error:!0,message:"Source template not found"};if(!M(X))return{error:!0,message:"using-superpowers SKILL.md not found"};let W,q;try{W=o(Q,"utf8"),q=o(X,"utf8")}catch(G){return{error:!0,message:`Failed to read source files: ${G.message}`}}let B=W.replace("${content}",q);if(!B.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->")||!B.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->"))return{error:!0,message:"Template is missing required markers"};let E=u1(Z);try{if(!M(E))S(`mkdir -p "${E}"`,{stdio:"pipe"})}catch(G){return{error:!0,message:`Failed to create .github directory: ${G.message}`}}if(M(Z)){let G;try{G=o(Z,"utf8")}catch(V){return{error:!0,message:`Failed to read existing file: ${V.message}`}}let K=new Date().toISOString().replace(/[:.]/g,"-"),_=`${Z}.backup-${K}`;try{S(`cp "${Z}" "${_}"`,{stdio:"pipe"})}catch(V){return{error:!0,message:`Failed to backup: ${V.message}`}}if(G.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->")&&G.includes("<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->")){let V=new RegExp("<!-- SUPERPOWERS_-_INSTRUCTIONS_START -->[\\s\\S]*?<!-- SUPERPOWERS_-_INSTRUCTIONS_END -->","g"),L=G.replace(V,B.trim());try{return $$(Z,L,"utf8"),{updated:!0,backup:_}}catch(U){return{error:!0,message:`Failed to update: ${U.message}`}}}else{let V=G.trimEnd()+`

`+B.trim()+`
`;try{return $$(Z,V,"utf8"),{updated:!0,backup:_}}catch(L){return{error:!0,message:`Failed to append: ${L.message}`}}}}else try{return $$(Z,B,"utf8"),{created:!0}}catch(G){return{error:!0,message:`Failed to create: ${G.message}`}}},k1=()=>{p(),console.log(`# Setting up Superpowers skills for this project
`);let $=process.cwd(),Q=T($,".agents"),X=T(Q,"skills"),Z=T($,"AGENTS.md"),Y=T(Q,"AGENTS.md"),H=M(Z)?Z:Y,W=T(z.superpowersRepo,".agents","templates","AGENTS.md.template");if(!M(W)){console.log(`✗ Error: AGENTS.md template not found
  Expected at: ${W}
  Please update your Superpowers installation`);return}if(!M(Q))try{S(`mkdir -p "${Q}"`,{stdio:"pipe"}),console.log("✓ Created .agents/ directory")}catch(J){console.log(`✗ Failed to create .agents/ directory: ${J.message}`);return}else console.log("✓ .agents/ directory exists");if(!M(X))try{S(`mkdir -p "${X}"`,{stdio:"pipe"}),S(`touch "${T(X,".gitkeep")}"`,{stdio:"pipe"}),console.log("✓ Created .agents/skills/ directory")}catch(J){console.log(`✗ Failed to create skills directory: ${J.message}`);return}else console.log("✓ .agents/skills/ directory exists");let q=T(Q,"docs"),B=T(q,"SUPERPOWERS.md"),E=T(z.superpowersRepo,".agents","templates","SUPERPOWERS.md.template");if(!M(q))try{S(`mkdir -p "${q}"`,{stdio:"pipe"}),console.log("✓ Created .agents/docs/ directory")}catch(J){console.log(`⚠️  Failed to create docs directory: ${J.message}`)}else console.log("✓ .agents/docs/ directory exists");let G=M(H);if(G){let J=new Date().toISOString().split("T")[0],i=`${H}.backup-${J}`;try{S(`cp "${H}" "${i}"`,{stdio:"pipe"}),console.log(`✓ Backed up existing AGENTS.md to ${q$(i).base}`)}catch(B$){console.log(`✗ Failed to backup AGENTS.md: ${B$.message}`);return}}let K;try{K=o(W,"utf8")}catch(J){console.log(`✗ Failed to read template: ${J.message}`);return}let _=u();if(K=K.replace(/\{\{VERSION\}\}/g,_),M(E))try{let J=o(E,"utf8"),i=new Date().toISOString().split("T")[0];J=J.replace(/\{\{VERSION\}\}/g,_),J=J.replace(/\{\{DATE\}\}/g,i),$$(B,J,"utf8"),console.log("✓ Created .agents/docs/SUPERPOWERS.md")}catch(J){console.log(`⚠️  Failed to create SUPERPOWERS.md: ${J.message}`)}else console.log("⚠️  SUPERPOWERS.md.template not found");let V=[],L=T($,".github","copilot-instructions.md"),U=T(z.home,".github","copilot-instructions.md");if(M(L)||M(U)||R.copilot.check())V.push("github-copilot");let O=T($,"CLAUDE.md"),x=T(Q,"CLAUDE.md");if(M(O)||M(x)||R.claude.check())V.push("claude-code");if(R.opencode.check())V.push("opencode");console.log(`
Detected platforms for project: ${V.join(", ")||"none"}
`);let f=V.filter((J)=>["github-copilot","opencode"].includes(J)),N=L$(H,K,f,!G);if(N.created)console.log(`✓ Created AGENTS.md with platform tool mappings (${H===Z?"root":".agents/"})`);else if(N.updated)console.log(`✓ Updated AGENTS.md with platform tool mappings (${H===Z?"root":".agents/"})`);else if(N.error)console.log("⚠️  Failed to update AGENTS.md");let E$=M(O)?O:x,w=L$(E$,K,["claude-code"],!1);if(w.created)console.log(`✓ Created CLAUDE.md with Claude Code tool mappings (${E$===O?"root":".agents/"})`);else if(w.updated){if(console.log(`✓ Updated CLAUDE.md with Claude Code tool mappings (${E$===O?"root":".agents/"})`),w.backup)console.log(`  Backed up to ${q$(w.backup).base}`)}else if(w.skipped)console.log("ℹ️  Skipped CLAUDE.md (does not exist)");else if(w.error)console.log("⚠️  Failed to update CLAUDE.md");let I=T($,".github","copilot-instructions.md"),D={skipped:!0};if(V.includes("github-copilot")){if(D=P2($),D.created)console.log("✓ Created .github/copilot-instructions.md with Superpowers instructions");else if(D.updated){if(console.log("✓ Updated .github/copilot-instructions.md with Superpowers instructions"),D.backup)console.log(`  Backed up to ${q$(D.backup).base}`)}else if(D.error)console.log(`⚠️  Failed to update .github/copilot-instructions.md: ${D.message||""}`)}else console.log("ℹ️  Skipped .github/copilot-instructions.md (GitHub Copilot not detected)");let P=!M(Z)&&!M(Y);if(M(I)&&P){let J=L$(I,K,["github-copilot"],!1);if(J.updated){if(console.log("✓ Updated .github/copilot-instructions.md with GitHub Copilot tool mappings"),J.backup)console.log(`  Backed up to ${q$(J.backup).base}`)}else if(J.error)console.log("⚠️  Failed to update .github/copilot-instructions.md with tool mappings")}else if(M(I)&&!P)console.log("ℹ️  Skipped .github/copilot-instructions.md tool mappings (AGENTS.md exists, using that instead)");else if(!M(I)&&!V.includes("github-copilot"))console.log("ℹ️  Skipped .github/copilot-instructions.md tool mappings (does not exist)");console.log(`
## Syncing Project Skill Symlinks
`);let d=g1({projectRoot:$});if(d.created>0)console.log(`
✓ Created ${d.created} project skill symlink(s)`);else if(d.existed>0)console.log("ℹ️  Project skill symlinks already exist");else if(d.errors.length>0)console.log("⚠️  Some symlinks could not be created");else console.log("ℹ️  No agent directories detected for symlinking");let g=`
# Setup complete!

Your project now has:
  - .agents/ directory structure`;if(N.updated||N.created)g+=`
  - AGENTS.md with universal skills instructions`;if(w.updated||w.created)g+=`
  - CLAUDE.md with Claude Code skills instructions`;if(D.updated||D.created)g+=`
  - .github/copilot-instructions.md with Superpowers instructions`;if(d.created>0)g+=`
  - ${d.created} project skill symlink(s) to agent directories`;g+=`
  - .agents/skills/ ready for project-specific skills`,g+=`
  - .agents/docs/SUPERPOWERS.md for detailed reference
`,console.log(g)},u2=()=>{let $=[...["brainstorming.prompt.md","execute-plan.prompt.md","write-plan.prompt.md","setup-skills.prompt.md","create-meta-prompt.md","finding-skills.prompt.md","skills.prompt.md","use-skill.prompt.md","using-a-skill.prompt.md"].map((X)=>T(z.vscodeUserDir,"prompts",X)),...["brainstorm.md","create-meta-prompt.md","execute-plan.md","finding-skills.md","skills.md","use-skill.md","using-a-skill.md","write-plan.md","setup-skills.md"].map((X)=>T(z.home,".claude","commands",X)),...["brainstorm.md","create-meta-prompt.md","execute-plan.md","finding-skills.md","skills.md","use-skill.md","using-a-skill.md","write-plan.md","setup-skills.md"].map((X)=>T(z.home,".config","opencode","command",X))],Q=0;for(let X of $)if(M(X))try{P1(X),Q++}catch(Z){console.log(`  ⚠️  Could not remove ${X}: ${Z.message}`)}if(Q>0)console.log(`✓ Removed ${Q} legacy prompt/command file${Q!==1?"s":""}`);else console.log("✓ No legacy prompt/command files found")},k2=()=>{let $=0;for(let Q of e){let X=Q.skillsDir(),Z=T(X,"superpowers");try{if(R2(Z).isSymbolicLink())P1(Z),console.log(`✓ Removed legacy superpowers symlink from ${X.replace(z.home,"~")}`),$++}catch{}}if($===0)console.log("✓ No legacy superpowers directory symlinks found")},d1=async()=>{p(),console.log(`# Superpowers Bootstrap for Agents
# ==================================
`);let $=process.argv.includes("--no-update"),X=new Set(["copilot","claude","opencode"].filter((W)=>process.argv.includes(`--force-${W}`))),Z=X.size>0;if(!$){let W=H$(),q=await w$();if(q.error)console.log(`## Update Check

⚠️  Could not check for updates (network issue)

---
`);else if(q.hasUpdates)console.log(`## Update Available
`),console.log(`⚠️  Update available: v${q.localVersion} → v${q.remoteVersion}
    To update, run: \`npm install -g @complexthings/superpowers-agent\``),console.log(`
---
`)}if(!Z)d$(),console.log(`---
`);if(!Z)console.log(`## Cleaning Up Legacy Files
`),u2(),console.log(`
---
`);if(!Z||X.has("copilot"))console.log(`## GitHub Copilot Integration
`),console.log("✓ Skill symlinks handled in sync step below"),console.log(`
---
`);if(!Z||X.has("claude")){if(console.log(`## Claude Code Integration
`),!R.claude.check())console.log(`⚠️  Skipped (${R.claude.name} CLI not detected)
\uD83D\uDCA1 To enable Claude Code integration:
   1. Install Claude Code: ${R.claude.installUrl}
   2. Run: superpowers-agent ${R.claude.bootstrapCommand}`);else console.log("✓ Skill symlinks handled in sync step below");console.log(`
---
`)}if(!Z||X.has("opencode")){if(console.log(`## OpenCode Integration
`),R.opencode.check())N1();else console.log(`⚠️  Skipped (${R.opencode.name} CLI not detected)
\uD83D\uDCA1 To enable OpenCode integration:
   1. Install OpenCode: ${R.opencode.installUrl}
   2. Run: superpowers-agent ${R.opencode.bootstrapCommand}`);console.log(`
---
`)}if(!Z){console.log(`## Generating Platform-Specific Files
`);let W=C1();console.log(`Detected platforms: ${W.join(", ")||"none"}
`);let q=T(z.superpowersRepo,".agents","templates","AGENTS.md.template"),B="";try{B=o(q,"utf8")}catch(E){console.log(`⚠️  Could not read AGENTS.md template: ${E.message}
`)}if(B){let E=u();B=B.replace(/\{\{VERSION\}\}/g,E);let G=T(z.home,".agents","AGENTS.md"),K=L$(G,B,W,!0);if(K.created)console.log(`✓ Created ${G}`);else if(K.updated)console.log(`✓ Updated ${G}`)}console.log(`
---
`)}console.log(`## Cleaning Up Legacy Superpowers Dir Symlinks
`),k2(),console.log(`
---
`),console.log(`## Syncing Repo Skills -> ~/.agents/skills/
`);let Y=f1();if(Y.created>0||Y.updated>0)console.log(`  ✓ ${Y.created} created, ${Y.updated} updated, ${Y.existed} already current`);else if(Y.errors.length>0)for(let W of Y.errors)console.log(`  ⚠️  ${W}`);else console.log(`  ✓ ${Y.existed} skill symlinks already up to date`);console.log(`
---
`),console.log(`## Syncing Skill Symlinks
`);let H=process.argv.includes("--force");S1({force:H,forceAgents:X}),console.log(`# Bootstrap Complete!
`),console.log("✓ All integrations installed"),console.log("✓ Skills system ready"),console.log(`✓ Skill symlinks synced
`),console.log("Next steps:"),console.log("  - Run `superpowers-agent find-skills` to see available skills"),console.log("  - Run `superpowers-agent setup-skills` in your project directory")};import{existsSync as F,readFileSync as J$,mkdirSync as a2,rmSync as o$,cpSync as l2,lstatSync as r2}from"fs";import{join as v,dirname as A$,parse as z$,sep as s2}from"path";import{execSync as j}from"child_process";import{homedir as n}from"os";import{createInterface as t2}from"readline";import{readFileSync as d2,existsSync as p$,unlinkSync as m2,lstatSync as h2,mkdirSync as p2}from"fs";import{join as c2,dirname as n2}from"path";import{join as W$}from"path";import{homedir as m1}from"os";var m$={github:{name:"github",sourceDir:".github/agents",sourceExt:".agent.md",getDestDir:()=>W$(z.vscodeUserDir,"prompts"),destExt:".agent.md"},opencode:{name:"opencode",sourceDir:".opencode/agents",sourceExt:".md",getDestDir:()=>W$(m1(),".config","opencode","agents"),destExt:".md"},claude:{name:"claude",sourceDir:".claude/agents",sourceExt:".md",getDestDir:()=>W$(m1(),".claude","agents"),destExt:".md"}};function h1($,Q,X){let Z=m$[Q];if(!Z)return null;return W$($,Z.sourceDir,`${X}${Z.sourceExt}`)}function p1($,Q){let X=m$[$];if(!X)return null;return W$(X.getDestDir(),`${Q}${X.destExt}`)}function h$(){return Object.keys(m$)}function o2($){if(!$||typeof $!=="object")return{valid:!1,error:"agents.json must be a JSON object"};if(!$.version||typeof $.version!=="string")return{valid:!1,error:'agents.json must have a "version" string field'};if(!$.agents||typeof $.agents!=="object")return{valid:!1,error:'agents.json must have an "agents" object field'};let Q=h$(),X=[];for(let[Z,Y]of Object.entries($.agents)){if(!Q.includes(Z)){X.push(Z);continue}if(!Array.isArray(Y))return{valid:!1,error:`agents.${Z} must be an array of agent names`};for(let H of Y)if(typeof H!=="string")return{valid:!1,error:`agents.${Z} must contain only string agent names`}}return{valid:!0,data:$,skippedPlatforms:X}}function i2($,Q){if(!p$($))return{installed:!1,error:`Source file not found: ${$}`};if(p$(Q))try{if(!h2(Q).isSymbolicLink())m2(Q)}catch(Z){}p2(n2(Q),{recursive:!0});let X=I$($,Q);if(X.error)return{installed:!1,error:X.error};return{installed:!0}}function c$($,Q={}){let X=c2($,"agents.json");if(!p$(X))return;let Z;try{Z=JSON.parse(d2(X,"utf8"))}catch(V){console.log(`
  ⚠ Could not parse agents.json: ${V.message}`);return}let Y=o2(Z);if(!Y.valid){console.log(`
  ⚠ Invalid agents.json: ${Y.error}`);return}for(let V of Y.skippedPlatforms)console.log(`  ⚠ Skipping unknown platform: ${V}`);let H=Z.repository||"unknown",W=Z.version,q=Q.isUpdate?"Updating":"Installing",B=Q.isUpdate?"Updated":"Installed";console.log(`
  \uD83D\uDCE6 Found agents.json (${H} v${W})`);let E=h$(),G=0,K=0,_=[];for(let[V,L]of Object.entries(Z.agents)){if(!E.includes(V))continue;console.log(`
  ${q} ${V} agents:`);for(let U of L){let O=h1($,V,U),x=p1(V,U),f=i2(O,x);if(f.installed)console.log(`    ✓ ${U}`),_.push({platformKey:V,agentName:U,sourcePath:O,destPath:x}),G++;else console.log(`    ✗ ${U}: ${f.error}`),K++}}if(_.length>0){let V=y(!0);if(!V.installedAgents)V.installedAgents={};if(!V.installedAgents[H])V.installedAgents[H]={version:W,agents:{}};V.installedAgents[H].version=W;for(let{platformKey:L,agentName:U,sourcePath:O,destPath:x}of _){if(!V.installedAgents[H].agents[L])V.installedAgents[H].agents[L]={};V.installedAgents[H].agents[L][U]={source:O,destination:x,installedAt:new Date().toISOString()}}m(V,!0)}if(G>0)console.log(`
  ${B} ${G} agent(s)${K>0?` (${K} failed)`:""}`)}var c1=($,Q)=>{if(Q.type==="local"){let Z=$;for(let Y=0;Y<10;Y++){if(F(v(Z,"agents.json")))return Z;let H=A$(Z);if(H===Z)break;Z=H}return $}let X=v(".agents","tmp");if($.includes(X)){let Z=$.indexOf(X),H=$.substring(Z+X.length+1).split(s2)[0];return v($.substring(0,Z),".agents","tmp",H)}return $},n1=($,Q)=>{let X=v(n(),".agents","repos"),Z=Q.replace(/^@/,"").replace(/[^a-zA-Z0-9_-]/g,"_"),Y=v(X,Z);try{if(a2(X,{recursive:!0}),F(Y))o$(Y,{recursive:!0,force:!0});return l2($,Y,{recursive:!0}),Y}catch(H){return console.log(`  Warning: Could not persist repository for agents: ${H.message}`),null}},i$=($)=>{let Q=$.match(/^https:\/\/github\.com\/([^/]+)\/([^/]+)\/tree\/([^/]+)\/(.+)$/);if(Q){let[,Y,H,W,q]=Q;return{type:"git-tree",repoUrl:`https://github.com/${Y}/${H}.git`,branch:W,path:q,original:$}}let X=$.match(/^git@github\.com:([^/]+)\/([^/]+?)(?:\.git)?$/);if(X){let[,Y,H]=X;return{type:"git-repo",repoUrl:`git@github.com:${Y}/${H}.git`,branch:null,path:null,original:$}}let Z=$.match(/^https:\/\/github\.com\/([^/]+)\/([^/]+?)(?:\.git)?$/);if(Z){let[,Y,H]=Z;return{type:"git-repo",repoUrl:`https://github.com/${Y}/${H}.git`,branch:null,path:null,original:$}}if(F($))return{type:"local",path:$,original:$};return null},a$=($)=>{let Q=$.includes("--global")||$.includes("-g"),X=$.includes("--project")||$.includes("-p");if(Q)return v(n(),".agents","skills");if(X)return v(process.cwd(),".agents","skills");let Z=v(process.cwd(),".agents","config.json"),Y=v(n(),".agents","config.json");for(let H of[Z,Y])if(F(H))try{if(JSON.parse(J$(H,"utf8")).installLocation==="project")return v(process.cwd(),".agents","skills")}catch(W){}return v(n(),".agents","skills")},l$=($,Q)=>{let X=v(n(),".agents","tmp",`skill-install-${Date.now()}`);try{if(j(`mkdir -p "${X}"`,{stdio:"pipe"}),Q)j(`git clone --branch ${Q} --depth 1 "${$}" "${X}"`,{stdio:"pipe",timeout:30000});else j(`git clone --depth 1 "${$}" "${X}"`,{stdio:"pipe",timeout:30000});return X}catch(Z){try{j(`rm -rf "${X}"`,{stdio:"pipe"})}catch{}throw Error(`Failed to clone repository: ${Z.message}`)}},Q$=($)=>{let Q=v($,"skill.json");if(!F(Q))return null;try{let X=J$(Q,"utf8");return JSON.parse(X)}catch(X){throw Error(`Failed to read skill.json: ${X.message}`)}},O$=($,Q,X,Z)=>{let Y=v($,Q);if(!F(Y)){Z.errors.push(`Skill directory not found: ${Q}`);return}let H=Q$(Y);if(!H){Z.errors.push(`No skill.json found in: ${Q}`);return}let W=H.name||Q,q=W.split("/").pop()||W,B=v(X,q),E=A$(B);try{j(`mkdir -p "${E}"`,{stdio:"pipe"})}catch(G){Z.errors.push(`Failed to create directory ${E}: ${G.message}`);return}try{if(F(B))j(`rm -rf "${B}"`,{stdio:"pipe"});j(`cp -R "${Y}" "${B}"`,{stdio:"pipe"}),Z.installed.push({name:q,path:B,title:H.title||H.description||q})}catch(G){Z.errors.push(`Failed to install ${Q}: ${G.message}`)}},o1=($,Q,X,Z,Y=null)=>{let H=y(Z);if(!H.installedSkills)H.installedSkills={};let W={source:$,installBase:Q,installedAt:new Date().toISOString(),skills:X.installed.map((q)=>({name:q.name,path:q.path}))};if(H.installedSkills[$]=W,Y&&Y!==$)H.installedSkills[Y]=W;m(H,Z)},i1=()=>{let $=process.argv.slice(3),Q=$.find((K)=>!K.startsWith("-")),X=$.filter((K)=>K.startsWith("-"));if(!Q){console.log(`Usage: superpowers-agent add <url-or-path|@alias> [skill-path] [options]

Options:
  --global, -g   Install skills globally in ~/.agents/skills/ (default)
  --project, -p  Install skills in current project's .agents/skills/

Examples:
  superpowers-agent add https://github.com/example/skills
  superpowers-agent add https://github.com/example/repo/tree/main/skills
  superpowers-agent add ~/my-local-skills
  superpowers-agent add https://github.com/example/skills --project
  superpowers-agent add ~/my-local-skills --global
  superpowers-agent add @myrepo path/to/skill
  superpowers-agent add @myrepo path/to/skills --project

Description:
  Installs skill(s) from a Git repository, local directory, or repository alias.
  Supports repositories with single or multiple skills.
  Reads skill.json to determine skill names and installation paths.

  Repository aliases can be added using:
    superpowers-agent add-repository <git-url> [--as=@alias]`);return}console.log(`Installing skill(s)...
`);let Z=!X.includes("--project")&&!X.includes("-p"),Y=null,H=null,W=null;if(Q.startsWith("@")){let K=Q;W=K,H=$.find((U,O)=>O>0&&!U.startsWith("-")&&U!==K);let _=h(!1),V=h(!0);if(_[K])Y=_[K],console.log(`Using project repository alias: ${K}`);else if(V[K])Y=V[K],console.log(`Using global repository alias: ${K}`);else{console.log(`Error: Repository alias not found: ${K}`),console.log(`
Available aliases:`);let U={...V,..._};if(Object.keys(U).length===0)console.log("  (none)"),console.log(`
Add a repository using:`),console.log("  superpowers-agent add-repository <git-url>");else for(let[O,x]of Object.entries(U))console.log(`  ${O} -> ${x}`);return}let L=F(Y);if(H)if(L)Q=v(Y,H),console.log(`Repository path: ${Y}`),console.log(`Skill path: ${H}
`);else Q=Y,console.log(`Repository URL: ${Y}`),console.log(`Skill path: ${H}
`);else if(Q=Y,L)console.log(`Repository path: ${Y}
`);else console.log(`Repository URL: ${Y}
`)}let q=i$(Q);if(!q){console.log(`Error: Invalid URL or path not found: ${Q}`);return}if(H&&q.type==="git-repo"&&!q.path)q.path=H;let B=a$(X);console.log(`Install location: ${B}
`);let E,G=!1;try{if(q.type==="git-repo"||q.type==="git-tree"){if(console.log(`Cloning repository: ${q.repoUrl}`),q.branch)console.log(`Branch: ${q.branch}`);if(E=l$(q.repoUrl,q.branch),G=!0,q.path){if(E=v(E,q.path),!F(E))throw Error(`Path not found in repository: ${q.path}`)}console.log("")}else E=q.path;let K=Q$(E);if(!K)throw Error("No skill.json found in source directory");let _={installed:[],errors:[],source:q.original};if(K.skills&&Array.isArray(K.skills)){console.log(`Found ${K.skills.length} skill(s) to install
`);for(let U of K.skills)O$(E,U,B,_)}else{let U=K.name||z$(E).base;O$(A$(E),z$(E).base,B,_)}let V=c1(E,q),L=v(V,"agents.json");if(F(L)){let U=V;if(G){let O;try{O=JSON.parse(J$(L,"utf8"))}catch{}let x=O?.repository||q.original,f=n1(V,x);if(f)U=f}c$(U,{isUpdate:!1})}if(G&&E)try{let U=E.split("/.agents/tmp/")[0]+"/.agents/tmp/"+E.split("/.agents/tmp/")[1].split("/")[0];j(`rm -rf "${U}"`,{stdio:"pipe"})}catch{}if(console.log(`
**Successfully installed skills:**`),console.log(`- Source: ${_.source}`),_.installed.length>0)for(let U of _.installed)console.log(`  - Installed: ${U.name} at ${U.path}`),console.log(`    ${U.title}`);if(_.errors.length>0){console.log(`
**Errors:**`);for(let U of _.errors)console.log(`  - ${U}`)}if(_.installed.length===0&&_.errors.length===0)console.log("  No skills were installed");if(_.installed.length>0)console.log(`
**Syncing skill symlinks...**`),k$(),o1(q.original,B,_,Z,W)}catch(K){if(G&&E)try{let _=E.split("/.agents/tmp/")[0]+"/.agents/tmp/"+E.split("/.agents/tmp/")[1].split("/")[0];j(`rm -rf "${_}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${K.message}`)}},a1=()=>{let $=process.argv.slice(3),Q=$.find((K)=>!K.startsWith("-")),X=$.filter((K)=>K.startsWith("-"));if(!Q){console.log(`Usage: superpowers-agent pull <url-or-path|@alias> [skill-path] [options]

Options:
  --global, -g   Update skills globally in ~/.agents/skills/ (default)
  --project, -p  Update skills in current project's .agents/skills/

Examples:
  superpowers-agent pull https://github.com/example/skills
  superpowers-agent pull https://github.com/example/repo/tree/main/skills
  superpowers-agent pull ~/my-local-skills
  superpowers-agent pull https://github.com/example/skills --project
  superpowers-agent pull ~/my-local-skills --global
  superpowers-agent pull @myrepo
  superpowers-agent pull @myrepo path/to/skill
  superpowers-agent pull @myrepo path/to/skills --project

Description:
  Updates or adds skill(s) from a Git repository, local directory, or repository alias.
  If skills already exist, they will be updated (replaced).
  If skills don't exist, they will be added.
  Supports repositories with single or multiple skills.
  Reads skill.json to determine skill names and installation paths.
  
  Repository aliases can be added using:
    superpowers-agent add-repository <git-url> [--as=@alias]`);return}console.log(`Updating skill(s)...
`);let Z=!X.includes("--project")&&!X.includes("-p"),Y=null,H=null,W=null;if(Q.startsWith("@")){let K=Q;W=K,H=$.find((U,O)=>O>0&&!U.startsWith("-")&&U!==K);let _=h(!1),V=h(!0);if(_[K])Y=_[K],console.log(`Using project repository alias: ${K}`);else if(V[K])Y=V[K],console.log(`Using global repository alias: ${K}`);else{console.log(`Error: Repository alias not found: ${K}`),console.log(`
Available aliases:`);let U={...V,..._};if(Object.keys(U).length===0)console.log("  (none)"),console.log(`
Add a repository using:`),console.log("  superpowers-agent add-repository <git-url>");else for(let[O,x]of Object.entries(U))console.log(`  ${O} -> ${x}`);return}let L=F(Y);if(H)if(L)Q=v(Y,H),console.log(`Repository path: ${Y}`),console.log(`Skill path: ${H}
`);else Q=Y,console.log(`Repository URL: ${Y}`),console.log(`Skill path: ${H}
`);else if(Q=Y,L)console.log(`Repository path: ${Y}
`);else console.log(`Repository URL: ${Y}
`)}let q=i$(Q);if(!q){console.log(`Error: Invalid URL or path not found: ${Q}`);return}if(H&&q.type==="git-repo"&&!q.path)q.path=H;let B=a$(X);console.log(`Install location: ${B}
`);let E,G=!1;try{if(q.type==="git-repo"||q.type==="git-tree"){if(console.log(`Cloning repository: ${q.repoUrl}`),q.branch)console.log(`Branch: ${q.branch}`);if(E=l$(q.repoUrl,q.branch),G=!0,q.path){if(E=v(E,q.path),!F(E))throw Error(`Path not found in repository: ${q.path}`)}console.log("")}else E=q.path;let K=Q$(E);if(!K)throw Error("No skill.json found in source directory");let _={installed:[],errors:[],source:q.original};if(K.skills&&Array.isArray(K.skills)){console.log(`Found ${K.skills.length} skill(s) to update
`);for(let U of K.skills)O$(E,U,B,_)}else{let U=K.name||z$(E).base;O$(A$(E),z$(E).base,B,_)}let V=c1(E,q),L=v(V,"agents.json");if(F(L)){let U=V;if(G){let O;try{O=JSON.parse(J$(L,"utf8"))}catch{}let x=O?.repository||q.original,f=n1(V,x);if(f)U=f}c$(U,{isUpdate:!0})}if(G&&E)try{let U=E.split("/.agents/tmp/")[0]+"/.agents/tmp/"+E.split("/.agents/tmp/")[1].split("/")[0];j(`rm -rf "${U}"`,{stdio:"pipe"})}catch{}if(console.log(`
**Successfully updated skills:**`),console.log(`- Source: ${_.source}`),_.installed.length>0)for(let U of _.installed)console.log(`  - Updated: ${U.name} at ${U.path}`),console.log(`    ${U.title}`);if(_.errors.length>0){console.log(`
**Errors:**`);for(let U of _.errors)console.log(`  - ${U}`)}if(_.installed.length===0&&_.errors.length===0)console.log("  No skills were updated");if(_.installed.length>0)console.log(`
**Syncing skill symlinks...**`),k$(),o1(q.original,B,_,Z,W)}catch(K){if(G&&E)try{let _=E.split("/.agents/tmp/")[0]+"/.agents/tmp/"+E.split("/.agents/tmp/")[1].split("/")[0];j(`rm -rf "${_}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${K.message}`)}},e2=($)=>new Promise((Q)=>{let X=t2({input:process.stdin,output:process.stdout});X.question($,(Z)=>{X.close(),Q(Z.trim())})}),n$=($)=>{try{return r2($).isSymbolicLink()}catch{return!1}},l1=async()=>{let $=process.argv.slice(3),Q=$.find((w)=>!w.startsWith("-")),X=$.filter((w)=>w.startsWith("-"));if(!Q){console.log(`Usage: superpowers-agent rm <url-or-path|@alias> [options]

Options:
  --global, -g   Remove globally installed skills (default)
  --project, -p  Remove project-installed skills

Examples:
  superpowers-agent rm https://github.com/example/skills
  superpowers-agent rm @myrepo
  superpowers-agent rm ~/my-local-skills`);return}let Z=!X.includes("--project")&&!X.includes("-p"),Y=Q,H=null;if(Q.startsWith("@")){H=Q;let w=h(!1),I=h(!0);if(w[Q])Y=w[Q];else if(I[Q])Y=I[Q];else{console.log(`Error: Repository alias not found: ${Q}`);return}}let W=B1(Z),q=W[Q]||W[Y],B=q?.installBase||a$(X),E=[];if(q)E=q.skills;else{console.log(`No tracking data found. Attempting to re-resolve skills from source...
`);let w=i$(Y);if(!w){console.log(`Error: Invalid URL or path not found: ${Y}`);return}let I,D=!1;try{if(w.type==="git-repo"||w.type==="git-tree"){if(console.log(`Cloning repository: ${w.repoUrl}`),I=l$(w.repoUrl,w.branch),D=!0,w.path){if(I=v(I,w.path),!F(I))throw Error(`Path not found in repository: ${w.path}`)}}else I=w.path;let P=Q$(I);if(!P)throw Error("No skill.json found in source");let d=P.skills&&Array.isArray(P.skills)?P.skills:[P.name||z$(I).base];for(let g of d){let J=g;if(P.skills){let i=v(I,g);if(F(i)){let B$=Q$(i);if(B$?.name)J=B$.name}}E.push({name:J,path:v(B,J)})}if(D)try{let g=I.split("/.agents/tmp/")[0]+"/.agents/tmp/"+I.split("/.agents/tmp/")[1].split("/")[0];j(`rm -rf "${g}"`,{stdio:"pipe"})}catch{}}catch(P){if(D&&I)try{let d=I.split("/.agents/tmp/")[0]+"/.agents/tmp/"+I.split("/.agents/tmp/")[1].split("/")[0];j(`rm -rf "${d}"`,{stdio:"pipe"})}catch{}console.log(`Error resolving source: ${P.message}`);return}}let G=E.filter((w)=>F(w.path)||n$(w.path)),K=[];for(let w of E)for(let I of e){let D=v(I.skillsDir(),w.name);if(F(D)||n$(D))K.push({platform:I.name,path:D})}let _=K1(),V=H||Y,L=[];if(_[V]){for(let[,w]of Object.entries(_[V].agents||{}))for(let[,I]of Object.entries(w))if(I.destination&&(F(I.destination)||n$(I.destination)))L.push({path:I.destination})}let U=V.replace(/^@/,"").replace(/[^a-zA-Z0-9_-]/g,"_"),O=v(n(),".agents","repos",U),x=F(O),f=n(),N=(w)=>w.replace(f,"~");if(G.length===0&&K.length===0&&L.length===0&&!x){console.log("Nothing to remove.");return}if(console.log("Will remove:"),G.length>0){console.log("  Skills:");for(let w of G)console.log(`    ${N(w.path)}`)}if(K.length>0){console.log("  Platform symlinks:");for(let w of K)console.log(`    ${N(w.path)}`)}if(L.length>0){console.log("  Agents:");for(let w of L)console.log(`    ${N(w.path)}`)}if(x)console.log("  Persisted repo:"),console.log(`    ${N(O)}`);if((await e2(`
Proceed? [y/N] `)).toLowerCase()!=="y"){console.log("Aborted.");return}for(let w of K){let I=u$(w.path);if(I.removed)console.log(`Removed symlink: ${N(w.path)}`),j1(w.platform,w.path,"skills");else if(I.error==="Symlink does not exist")console.log(`Already removed: ${N(w.path)}`);else console.log(`Warning: ${N(w.path)}: ${I.error}`)}for(let w of L){let I=u$(w.path);if(I.removed)console.log(`Removed agent: ${N(w.path)}`);else if(I.error==="Symlink does not exist")console.log(`Already removed: ${N(w.path)}`);else console.log(`Warning: ${N(w.path)}: ${I.error}`)}if(x)try{o$(O,{recursive:!0,force:!0}),console.log(`Removed persisted repo: ${N(O)}`)}catch(w){console.log(`Warning: Failed to remove persisted repo: ${w.message}`)}for(let w of G){if(!F(w.path)){console.log(`Already removed: ${N(w.path)}`);continue}try{o$(w.path,{recursive:!0,force:!0}),console.log(`Removed skill: ${N(w.path)}`)}catch(I){console.log(`Warning: Failed to remove ${N(w.path)}: ${I.message}`)}}if(F$(Q,Z),Y!==Q)F$(Y,Z);if(_[V]){let w=y(!0);if(w.installedAgents)delete w.installedAgents[V],m(w,!0)}console.log(`
Done.`)},r1=()=>{let $=process.argv.slice(3),Q=$.find((E)=>!E.startsWith("-")),X=$.filter((E)=>E.startsWith("-"));if(!Q){console.log(`Usage: superpowers-agent add-repository <git-url> [options]

Options:
  --global, -g        Add repository globally in ~/.agents/config.json (default)
  --project, -p       Add repository in current project's .agents/config.json
  --as=<alias>        Specify custom alias for the repository

Examples:
  superpowers-agent add-repository https://github.com/example/skills.git
  superpowers-agent add-repository https://github.com/example/skills.git --as=@myskills
  superpowers-agent add-repository https://github.com/example/skills.git --project
  superpowers-agent add-repository https://github.com/example/skills.git --as=@custom --global

Description:
  Adds a skill repository alias to your configuration.
  The repository's skill.json will be read to determine the default alias.
  Use --as to specify a custom alias.
  After adding, you can install skills using: superpowers-agent add @alias path/to/skill`);return}console.log(`Adding repository...
`);let Z=X.some((E)=>E==="--global"||E==="-g"),Y=X.some((E)=>E==="--project"||E==="-p"),H=X.find((E)=>E.startsWith("--as=")),W=H?H.split("=")[1]:null,q=Y?!1:!0,B;try{console.log(`Cloning repository: ${Q}`),B=v(n(),".agents","tmp",`repo-add-${Date.now()}`),j(`mkdir -p "${B}"`,{stdio:"pipe"}),j(`git clone --depth 1 "${Q}" "${B}"`,{stdio:"pipe",timeout:30000}),console.log("");let E=Q$(B);if(!E)throw Error("No skill.json found in repository");let G;if(W)G=W;else if(E.repository)G=E.repository;else{let V=Q.match(/\/([^/]+?)(?:\.git)?$/);if(V)G="@"+V[1];else throw Error("Could not determine repository alias. Use --as=@alias to specify manually.")}E1(G,Q,q),j(`rm -rf "${B}"`,{stdio:"pipe"});let K=q?"globally":"in project",_=q?v(n(),".agents","config.json"):v(process.cwd(),".agents","config.json");console.log(`✓ Repository added ${K}`),console.log(`  Alias: ${G}`),console.log(`  URL: ${Q}`),console.log(`  Config: ${_}`),console.log(`
You can now install skills using:`),console.log(`  superpowers-agent add ${G} <path-to-skill>`)}catch(E){if(B)try{j(`rm -rf "${B}"`,{stdio:"pipe"})}catch{}console.log(`
Error: ${E.message}`)}};import{dirname as $3}from"path";var s1=($)=>{if(!$){console.log(`Usage: superpowers-agent use-skill <skill-name>

Examples (smart matching):
  superpowers-agent use-skill brainstorming              # Matches superpowers:collaboration/brainstorming
  superpowers-agent use-skill collaboration/brainstorming # More specific suffix
  superpowers-agent use-skill test-driven-development    # Matches superpowers:testing/test-driven-development

Examples (explicit paths):
  superpowers-agent use-skill superpowers:collaboration/brainstorming  # Full superpowers skill path
  superpowers-agent use-skill claude:email-assistant                   # Full claude skill path
  superpowers-agent use-skill my-custom-skill                          # Project skill

Smart matching:
  - Type just the skill name or any suffix of the path
  - Priority: .agents/skills/ > .claude/skills/ > ~/.agents/skills/ > ~/.agents/superpowers/skills/
  - If multiple skills match at same priority, you'll see them with descriptions`);return}let Q=a($);if(!Q){console.log(`Error: Skill not found: ${$}

Available skills:`),console.log("Run: superpowers-agent find-skills");return}let{skillFile:X,sourceType:Z,actualSkillPath:Y}=Q;try{let{content:H,frontmatter:W}=e$(X),q=Z$[Z].prefix+Y,B=$3(X),E=`# ${W.name||q}`;if(W.description)E+=`
# ${W.description}`;if(W.whenToUse)E+=`
# When to use: ${W.whenToUse}`;E+=`
# Supporting tools and docs are in ${B}
# ============================================

${H}`,console.log(E)}catch(H){console.log(H.message)}};var t1={bootstrap:d1,version:T1,"check-updates":()=>{v1().catch(($)=>{console.error($.message),process.exit(1)})},update:()=>{let $=process.argv.includes("--no-reinstall");R$({skipReinstall:$})},"config-get":O1,"config-set":J1,"setup-skills":k1,"use-skill":()=>s1(process.argv[3]),execute:w1,"find-skills":r,add:i1,"add-repository":r1,"list-repositories":A1,pull:a1,rm:l1,dir:I1,path:U1,"get-helpers":L1,"install-aliases":d$,default:()=>{console.log(`Superpowers for Agents
Usage:
  superpowers-agent bootstrap [--no-update] [--force]               # Run complete bootstrap
  superpowers-agent version                                         # Show current version
  superpowers-agent check-updates                                   # Check for updates
  superpowers-agent update [--no-reinstall]                         # Update to latest version
  superpowers-agent config-get                                      # Show configuration
  superpowers-agent config-set <key> <value>                        # Update configuration
  superpowers-agent setup-skills                                    # Initialize project skills
  superpowers-agent use-skill <skill-name>                          # Load a skill
  superpowers-agent find-skills                                     # List available skills
  superpowers-agent add <url-or-path|@alias> [path] [options]      # Install skills
  superpowers-agent add-repository <git-url> [--as=@alias] [opts]  # Add repository alias
  superpowers-agent list-repositories                              # List configured repository aliases
  superpowers-agent pull <url-or-path|@alias> [path] [options]     # Update skills
  superpowers-agent rm <url-or-path|@alias> [options]             # Remove skills
  superpowers-agent dir <skill-name>                                # Get skill directory
  superpowers-agent path <skill-name>                               # Get skill file path
  superpowers-agent execute <skill-name>                            # Execute a skill
  superpowers-agent get-helpers <skill> <search-term>               # Get helper file path
  superpowers-agent install-aliases                                 # Install universal aliases (superpowers, superpowers-agent)

Documentation: https://github.com/complexthings/superpowers
`)}},Q3=process.argv[2]||"default",X3=t1[Q3]||t1.default;try{let $=X3();if($&&typeof $.then==="function")$.catch((Q)=>{console.error(`Error: ${Q.message}`),process.exit(1)})}catch($){console.error(`Error: ${$.message}`),process.exit(1)}
