# Correct active test-runner guidance

`AGENTS.md` currently says the repository has no automated test runner, but `.agents` has a Bun test suite. Update the active guidance to identify `bun test` (run from `.agents`) as the available test runner; leave historical references unchanged.
