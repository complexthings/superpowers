# Remove duplicate verification skill bundles

`verification-before-completion` currently has physical bundled directories in both `skills/debugging` and `skills/testing`. Remove both directories; no bundled copy remains, so the skill is no longer distributed as part of Superpowers Agent.
