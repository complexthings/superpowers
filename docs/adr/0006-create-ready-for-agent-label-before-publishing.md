# Create `ready-for-agent` before publishing specs or tickets

Before publishing specs or tickets, create the canonical GitHub label `ready-for-agent`; do not publish unlabeled work or use `wontfix`, which means work will not be actioned.
